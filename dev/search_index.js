var documenterSearchIndex = {"docs":
[{"category":"section","location":"provided-elements/#Provided-integrators-and-caches","page":"Provided integrators and caches","text":"The integrators and element caches the package ships: composition over one domain, multi-domain routing, the automatic-differentiation decorator, the transfer prolongators, and the element-assembly decorator that gives any bilinear cache the ELEMENT storage level. Element authors implement the contracts on the Element API reference page; the types below are ready-made implementations of them.","title":"Provided integrators and caches"},{"category":"section","location":"provided-elements/#The-tensor-product-sum-factorization-core","page":"Provided integrators and caches","text":"The sum-factorization core a matrix-free tensor-product element is written against — the 1D reference operators, the lattice permutations and the two mapping pipelines — plus its two reference consumers ships as its own lib subpackage, FerriteOperatorsTensorProduct.","title":"The tensor-product sum-factorization core"},{"category":"type","location":"provided-elements/#FerriteOperators.BilinearCompositeIntegrator","page":"Provided integrators and caches","text":"NonlinearCompositeIntegrator(subintegrators::Tuple)\nBilinearCompositeIntegrator(subintegrators::Tuple)\nLinearCompositeIntegrator(subintegrators::Tuple)\n\nIntegrator stacking several sub-integrators over the SAME domain into one element: cache setup maps every sub-integrator over the subdomain and combines the results through compose_element_caches.\n\nScope bound: one domain, one evaluation context, one scatter target — as for CompositeVolumetricElementCache. Hence the single field: terms needing their own context, weight or domain are separate operator terms, not inners.\n\nNested composites are flattened at construction, which also rejects an empty tuple, a sub-integrator carrying condensed internal state, and cross-sink mixes. A bilinear sub-integrator inside a nonlinear composite is legitimate — the operator its form induces scatters into the same matrix and residual, its residual being the element matrix acting on the element vector — whereas an AbstractLinearIntegrator describes a load form, whose sink is a vector alone, and never composes into a nonlinear or bilinear operator.\n\nRouting and composition compose in one order: a *MultiDomainIntegrator whose values are composite integrators. A composite of routers is not supported.\n\n\n\n\n\n","title":"FerriteOperators.BilinearCompositeIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperators.CompositeFacetItemCache","page":"Provided integrators and caches","text":"Combines multiple facet-item caches over one subdomain. The subdomain declares ONE facet set — the union of the inners' facet_items — and the fan-out gates on the facets each inner declared, so an inner integrates exactly its own set. The per-inner sets are resolved once, at setup, and shared read-only during a sweep.\n\nSame scope bound as CompositeVolumetricElementCache: one domain, one context, one sink, no values objects shared by construction. Each inner is handed its own per-facet parameter view, produced by that inner's query_facet_parameters.\n\nA weighted sweep routes PER INNER — every covering inner is handed the same WeightedJacobianRequest and accumulates its own fused contribution, which is what puts a boundary spring (∂F/∂u only) next to a dashpot (∂F/∂v only) in one W. This route composes no per-slot Jacobians, so an inner without the fused weighted facet kernel is rejected by assert_facet_item_route rather than folded around.\n\n\n\n\n\n","title":"FerriteOperators.CompositeFacetItemCache"},{"category":"type","location":"provided-elements/#FerriteOperators.CompositeParameters","page":"Provided integrators and caches","text":"Per-inner parameter views produced by a composite cache's query_cell_parameters / query_facet_parameters. The fan-out re-seeds args.p per inner from this bundle; any other p reaches every inner unchanged, so a hand-built CellArgs/FacetArgs still works.\n\n\n\n\n\n","title":"FerriteOperators.CompositeParameters"},{"category":"type","location":"provided-elements/#FerriteOperators.CompositeVolumetricElementCache","page":"Provided integrators and caches","text":"Combines multiple element caches over the same volume.\n\nScope bound: the inners share ONE domain, ONE evaluation context and ONE scatter target. Terms evaluated at different contexts (generalized-α's two times) or scattered into different targets (term-split operators, IMEX row blocks) are separate sweeps over separate integrators, not inners of one composite.\n\nNo values objects are shared by construction — each inner sets up and reinitializes its own; deliberate sharing stays the sharing element's responsibility. Each inner is handed its OWN parameter view, produced by that inner's query_cell_parameters.\n\nThe composite is analytic for a kind iff every inner is; otherwise the whole composite falls back to AD over the fused residual (one Dual sweep, one geometry pass). That routing is all-or-nothing.\n\nInners carrying condensed internal state are rejected at setup — see validate_element_cache.\n\n\n\n\n\n","title":"FerriteOperators.CompositeVolumetricElementCache"},{"category":"type","location":"provided-elements/#FerriteOperators.LinearCompositeIntegrator","page":"Provided integrators and caches","text":"NonlinearCompositeIntegrator(subintegrators::Tuple)\nBilinearCompositeIntegrator(subintegrators::Tuple)\nLinearCompositeIntegrator(subintegrators::Tuple)\n\nIntegrator stacking several sub-integrators over the SAME domain into one element: cache setup maps every sub-integrator over the subdomain and combines the results through compose_element_caches.\n\nScope bound: one domain, one evaluation context, one scatter target — as for CompositeVolumetricElementCache. Hence the single field: terms needing their own context, weight or domain are separate operator terms, not inners.\n\nNested composites are flattened at construction, which also rejects an empty tuple, a sub-integrator carrying condensed internal state, and cross-sink mixes. A bilinear sub-integrator inside a nonlinear composite is legitimate — the operator its form induces scatters into the same matrix and residual, its residual being the element matrix acting on the element vector — whereas an AbstractLinearIntegrator describes a load form, whose sink is a vector alone, and never composes into a nonlinear or bilinear operator.\n\nRouting and composition compose in one order: a *MultiDomainIntegrator whose values are composite integrators. A composite of routers is not supported.\n\n\n\n\n\n","title":"FerriteOperators.LinearCompositeIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperators.NonlinearCompositeIntegrator","page":"Provided integrators and caches","text":"NonlinearCompositeIntegrator(subintegrators::Tuple)\nBilinearCompositeIntegrator(subintegrators::Tuple)\nLinearCompositeIntegrator(subintegrators::Tuple)\n\nIntegrator stacking several sub-integrators over the SAME domain into one element: cache setup maps every sub-integrator over the subdomain and combines the results through compose_element_caches.\n\nScope bound: one domain, one evaluation context, one scatter target — as for CompositeVolumetricElementCache. Hence the single field: terms needing their own context, weight or domain are separate operator terms, not inners.\n\nNested composites are flattened at construction, which also rejects an empty tuple, a sub-integrator carrying condensed internal state, and cross-sink mixes. A bilinear sub-integrator inside a nonlinear composite is legitimate — the operator its form induces scatters into the same matrix and residual, its residual being the element matrix acting on the element vector — whereas an AbstractLinearIntegrator describes a load form, whose sink is a vector alone, and never composes into a nonlinear or bilinear operator.\n\nRouting and composition compose in one order: a *MultiDomainIntegrator whose values are composite integrators. A composite of routers is not supported.\n\n\n\n\n\n","title":"FerriteOperators.NonlinearCompositeIntegrator"},{"category":"method","location":"provided-elements/#FerriteOperators.compose_element_caches-Tuple{Tuple}","page":"Provided integrators and caches","text":"compose_element_caches(caches::Tuple)\n\nCombine per-inner volumetric caches into the cache the engine sees. Empty caches carry no term and are dropped, so an all-empty composition collapses to EmptyVolumetricElementCache and a single survivor is returned unwrapped — the engine's empty-cache and single-cache fast paths survive composition.\n\n\n\n\n\n","title":"FerriteOperators.compose_element_caches"},{"category":"method","location":"provided-elements/#FerriteOperators.facet_items-Tuple{Union{BilinearCompositeIntegrator, LinearCompositeIntegrator, NonlinearCompositeIntegrator}, SubDofHandler}","page":"Provided integrators and caches","text":"facet_items(integrator::AnyCompositeIntegrator, sdh)\nsetup_facet_item_cache(integrator::AnyCompositeIntegrator, sdh)\n\nThe facet-item declaration of a composite: the sorted union of its sub-integrators' facet_items, served by ONE CompositeFacetItemCache that gates the fan-out on each inner's own set. Inners declaring nothing contribute nothing — to the declaration and to the cache — so an all-silent composite keeps the additive () default and a single declaring inner's cache is returned unwrapped, mirroring the compose_element_caches collapse rules.\n\nThe union is what makes overlap legal: two terms supported on the SAME facet are one item declared once and assembled by both inners, which the family's declared-twice rejection would otherwise refuse.\n\n\n\n\n\n","title":"FerriteOperators.facet_items"},{"category":"type","location":"provided-elements/#FerriteOperators.BilinearMultiDomainIntegrator","page":"Provided integrators and caches","text":"NonlinearMultiDomainIntegrator(subintegrators::Dict{String})\nLinearMultiDomainIntegrator(subintegrators::Dict{String})\nBilinearMultiDomainIntegrator(subintegrators::Dict{String})\n\nIntegrator carrying one sub-integrator per named subdomain, so a single operator hosts different physics per subdomain. The keys are names of volumetric cellsets in the grid's cellset registry.\n\nA name resolves everything the subdomain it claims declares — its element cache, its facet_items and their surface cache, its global dofs; facetset names play no part in the claim.\n\nEach subdomain must lie entirely within one declared cellset. Production mode assumes this holds and does not verify it — a subdomain's first cell alone decides its claim. FerriteOperators.debug_mode replaces the sample with an exhaustive check of every cell.\n\nResolution runs once per operator setup. ArgumentErrors in both modes:\n\na declared name that is not a cellset of the grid,\na subdomain whose first cell is owned by no declared name,\na subdomain whose first cell is owned by more than one declared name,\na declared name that claims no subdomain.\n\nDebug mode additionally rejects, naming the offending cell:\n\ntwo declared cellsets sharing a cell, whether or not a subdomain touches it,\na subdomain with any cell owned by no declared name,\na subdomain whose cells span several declared names.\n\nThere is deliberately no lenient fallback: an unclaimed subdomain, or a mistyped name silently contributing nothing, is what setup-time validation exists to catch.\n\nSub-integrators must share the operator's sink: a nonlinear router accepts nonlinear and bilinear sub-integrators — the operator a bilinear form induces has the same sink, its residual being the element matrix acting on the element vector — while the bilinear and linear routers accept only their own kind.\n\n\n\n\n\n","title":"FerriteOperators.BilinearMultiDomainIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperators.LinearMultiDomainIntegrator","page":"Provided integrators and caches","text":"NonlinearMultiDomainIntegrator(subintegrators::Dict{String})\nLinearMultiDomainIntegrator(subintegrators::Dict{String})\nBilinearMultiDomainIntegrator(subintegrators::Dict{String})\n\nIntegrator carrying one sub-integrator per named subdomain, so a single operator hosts different physics per subdomain. The keys are names of volumetric cellsets in the grid's cellset registry.\n\nA name resolves everything the subdomain it claims declares — its element cache, its facet_items and their surface cache, its global dofs; facetset names play no part in the claim.\n\nEach subdomain must lie entirely within one declared cellset. Production mode assumes this holds and does not verify it — a subdomain's first cell alone decides its claim. FerriteOperators.debug_mode replaces the sample with an exhaustive check of every cell.\n\nResolution runs once per operator setup. ArgumentErrors in both modes:\n\na declared name that is not a cellset of the grid,\na subdomain whose first cell is owned by no declared name,\na subdomain whose first cell is owned by more than one declared name,\na declared name that claims no subdomain.\n\nDebug mode additionally rejects, naming the offending cell:\n\ntwo declared cellsets sharing a cell, whether or not a subdomain touches it,\na subdomain with any cell owned by no declared name,\na subdomain whose cells span several declared names.\n\nThere is deliberately no lenient fallback: an unclaimed subdomain, or a mistyped name silently contributing nothing, is what setup-time validation exists to catch.\n\nSub-integrators must share the operator's sink: a nonlinear router accepts nonlinear and bilinear sub-integrators — the operator a bilinear form induces has the same sink, its residual being the element matrix acting on the element vector — while the bilinear and linear routers accept only their own kind.\n\n\n\n\n\n","title":"FerriteOperators.LinearMultiDomainIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperators.NonlinearMultiDomainIntegrator","page":"Provided integrators and caches","text":"NonlinearMultiDomainIntegrator(subintegrators::Dict{String})\nLinearMultiDomainIntegrator(subintegrators::Dict{String})\nBilinearMultiDomainIntegrator(subintegrators::Dict{String})\n\nIntegrator carrying one sub-integrator per named subdomain, so a single operator hosts different physics per subdomain. The keys are names of volumetric cellsets in the grid's cellset registry.\n\nA name resolves everything the subdomain it claims declares — its element cache, its facet_items and their surface cache, its global dofs; facetset names play no part in the claim.\n\nEach subdomain must lie entirely within one declared cellset. Production mode assumes this holds and does not verify it — a subdomain's first cell alone decides its claim. FerriteOperators.debug_mode replaces the sample with an exhaustive check of every cell.\n\nResolution runs once per operator setup. ArgumentErrors in both modes:\n\na declared name that is not a cellset of the grid,\na subdomain whose first cell is owned by no declared name,\na subdomain whose first cell is owned by more than one declared name,\na declared name that claims no subdomain.\n\nDebug mode additionally rejects, naming the offending cell:\n\ntwo declared cellsets sharing a cell, whether or not a subdomain touches it,\na subdomain with any cell owned by no declared name,\na subdomain whose cells span several declared names.\n\nThere is deliberately no lenient fallback: an unclaimed subdomain, or a mistyped name silently contributing nothing, is what setup-time validation exists to catch.\n\nSub-integrators must share the operator's sink: a nonlinear router accepts nonlinear and bilinear sub-integrators — the operator a bilinear form induces has the same sink, its residual being the element matrix acting on the element vector — while the bilinear and linear routers accept only their own kind.\n\n\n\n\n\n","title":"FerriteOperators.NonlinearMultiDomainIntegrator"},{"category":"method","location":"provided-elements/#FerriteOperators.algebraic_items-Tuple{Union{BilinearMultiDomainIntegrator, LinearMultiDomainIntegrator, NonlinearMultiDomainIntegrator}, Ferrite.AbstractDofHandler}","page":"Provided integrators and caches","text":"algebraic_items(integrator::AnyMultiDomainIntegrator, dh)\nsetup_algebraic_cache(integrator::AnyMultiDomainIntegrator, dh)\n\nA routed integrator's algebraic declaration is the declaration of the one sub-integrator that makes one. The algebraic family is declared over the WHOLE DofHandler, not per subdomain, so there is no subdomain claim to route on the way the cache and dof hooks above do; and the single-declaration model (algebraic_items) has no rule for merging two declarations, so several declaring sub-integrators are rejected rather than silently merged or silently dropped.\n\n\n\n\n\n","title":"FerriteOperators.algebraic_items"},{"category":"method","location":"provided-elements/#FerriteOperators.resolve_subdomain_claims-Tuple{Dict{String}, Ferrite.AbstractDofHandler}","page":"Provided integrators and caches","text":"resolve_subdomain_claims(subintegrators::Dict{String}, dh) -> Vector{String}\n\nThe declared name claiming each subdomain of dh, in dh.subdofhandlers order. Exhaustive under FerriteOperators.debug_mode, sampling otherwise; the flag is a compile-time constant, so the unused form is not reachable code.\n\n\n\n\n\n","title":"FerriteOperators.resolve_subdomain_claims"},{"category":"method","location":"provided-elements/#FerriteOperators.subintegrator_for_subdomain-Tuple{Dict{String}, SubDofHandler}","page":"Provided integrators and caches","text":"subintegrator_for_subdomain(subintegrators::Dict{String}, sdh::SubDofHandler)\n\nThe sub-integrator claiming sdh. Validates the whole DofHandler's subdomain mapping, so every setup hook reaches the same verdict.\n\n\n\n\n\n","title":"FerriteOperators.subintegrator_for_subdomain"},{"category":"method","location":"provided-elements/#FerriteOperators.subintegrators_per_subdomain-Tuple{Union{BilinearMultiDomainIntegrator, LinearMultiDomainIntegrator, NonlinearMultiDomainIntegrator}, Ferrite.AbstractDofHandler}","page":"Provided integrators and caches","text":"subintegrators_per_subdomain(integrator, dh) -> Vector\n\nThe sub-integrator claiming each subdomain of dh, in dh.subdofhandlers order.\n\n\n\n\n\n","title":"FerriteOperators.subintegrators_per_subdomain"},{"category":"type","location":"provided-elements/#FerriteOperators.ADElementBuffers","page":"Provided integrators and caches","text":"ADElementBuffers\n\nPer-worker seeds and ForwardDiff configs for ADElementCache. Outputs live on SensitivityBuffers instead: a request's destination is needed whether the resolved cache ends up analytic or decorated.\n\n\n\n\n\n","title":"FerriteOperators.ADElementBuffers"},{"category":"type","location":"provided-elements/#FerriteOperators.ADElementCache","page":"Provided integrators and caches","text":"ADElementCache{Inner, Backend, Buffers} <: AbstractVolumetricElementCache\nADElementCache(inner, sdh; backend = ForwardDiffAD(), n_global_dofs = 0, n_internal_dofs = 0)\nADElementCache(inner, ndofs::Int, ::Type{T} = Float64; backend = ForwardDiffAD())\n\nDecorates inner's mandatory residual kernel with automatic differentiation, serving per request whatever inner does not provide analytically (provides_analytic stays the inner's; serves_kind reports the union); the engine never forks between the two itself. The differentiated kernel follows from the args record — assemble_cell! for a CellArgs, assemble_algebraic! for an AlgebraicArgs — so one decorator serves both item families.\n\nAD-from-residual on a condensed inner (has_internal_state) computes the frozen-q PARTIAL, so the totals are completed generically: Consistent Jacobian/JacobianResidual as Jₑ = ∂F/∂ū|_q + ∂F/∂q · dq/dū with the block condensed_corrector supplies; the parameter and time kinds with dq/dθ/dq/dt derived from local_conditions! through the implicit function theorem. Lacking corrector or hook, admissibility rejects the kind, naming the missing correction.\n\nn_global_dofs pads the seeds and configs by the subdomain's global_dofs count, so the differentiated system is the FULL augmented one — setup_operator passes it. It is the CELL family's declaration: facet_item_global_dofs augments the facet items alone, which is what lets a condensed volumetric element sit on a subdomain carrying a tying term. The ndofs form sizes the buffers from a dof count alone, for an item family that has no SubDofHandler to allocate against.\n\nn_internal_dofs is the condensed internal-dof count of ONE cell and sizes the :q seeds and configs the generic routes above sweep with — the same get_number_of_internal_dofs_per_element declaration the InternalVariableHandler lays the q block out from, resolved by setup_operator (resolve_internal_dofs_per_element) so a config's seed length is exactly what the InternalSource gather hands the kernel. It is NOT getnquadpoints: an element carrying a tensor-valued internal variable owns several internal dofs per quadrature point. The default 0 builds no :q configuration at all, and a generic :q route then refuses by name rather than seeding a mis-sized sweep.\n\nsetup_operator wraps automatically (ad_backend = nothing opts out).\n\n\n\n\n\n","title":"FerriteOperators.ADElementCache"},{"category":"type","location":"provided-elements/#FerriteOperators.AbstractElementCacheDecorator","page":"Provided integrators and caches","text":"AbstractElementCacheDecorator{Inner} <: AbstractVolumetricElementCache\n\nSupertype of the caches this package wraps around a user's element cache (ADElementCache, FusedFromSplit). The wrapped cache sits in a field inner, and everything a decorator inherits is forwarded once here.\n\nA declaration about the wrapped ELEMENT is forwarded wholesale — the iteration seams, element_local_length, element_matrix_symmetry, element_action_row, and the matrix-free storage (with_action_storage, fill_quadrature_data!), which passes through and back via rewrap. A decorator with its own explicit method for a seam wins by ordinary dispatch.\n\nWhat a decorator answers about the REQUESTS it SERVES (provides_analytic, serves_kind) is NOT forwarded: one that forwards only some requests has no claim on the inner's for the rest.\n\nWhich subject a probe takes is the whole convention:\n\nAUTHOR-WRITTEN-METHOD probes (mandatory kernels, trait ↔ kernel, condensed_corrector/local_conditions!) run on the unwrap fixpoint — the forwarding methods below answer hasmethod for every inner, so probing the wrapper would pass a cache that implements nothing.\nSERVED-CAPABILITY probes (serves_kind, assert_sensitivity_admissible) run on the DECORATED type — that is the cache the engine calls, and a decorator serves kinds its inner does not.\n\n\n\n\n\n","title":"FerriteOperators.AbstractElementCacheDecorator"},{"category":"type","location":"provided-elements/#FerriteOperators.FerriteOperatorsADTag","page":"Provided integrators and caches","text":"Tag for the package-owned ForwardDiff configs, so per-worker configs outlive the per-cell closures.\n\n\n\n\n\n","title":"FerriteOperators.FerriteOperatorsADTag"},{"category":"type","location":"provided-elements/#FerriteOperators.ForwardDiffAD","page":"Provided integrators and caches","text":"ForwardDiffAD()\n\nThe default ADElementCache backend: ForwardDiff over the residual kernel. The AD backend is a hub seam — a downstream extension implements its own buffer struct and assemble_cell! methods for its own marker type.\n\n\n\n\n\n","title":"FerriteOperators.ForwardDiffAD"},{"category":"type","location":"provided-elements/#FerriteOperators.FusedFromSplit","page":"Provided integrators and caches","text":"FusedFromSplit(inner)\n\nMini-decorator for a cache that serves JacobianKind and the mandatory residual analytically but not the fused JacobianResidualKind: issues the two split analytic kernels back to back instead of falling back to AD. Chosen at construction (decorate_element_cache).\n\n\n\n\n\n","title":"FerriteOperators.FusedFromSplit"},{"category":"function","location":"provided-elements/#FerriteOperators.condensed_corrector","page":"Provided integrators and caches","text":"condensed_corrector(cache, args) -> AbstractMatrix\n\nThe completed nq × ndofs dq/dū block ADElementCache's generic Consistent combination reads to complete a condensed cache's AD partial into Jₑ = ∂F/∂ū|_q + ∂F/∂q · dq/dū. No default; this is the seam for THAT combination only.\n\nA cache serving Consistent kinds analytically never implements it — it reads and writes the same per-item corrector directly, through item_state/set_item_state! on its own ItemStates field, from inside its own assemble_cell! kernel. Implement condensed_corrector only where the generic AD combination is the route actually taken.\n\nTakes the item's CellArgs rather than an item id so that both CorrectorElections can be served through it: a Stored() cache reads its store keyed by cellid(args.cell), a Recompute one re-derives the block from args.states. The combination is the same either way.\n\n\n\n\n\n","title":"FerriteOperators.condensed_corrector"},{"category":"method","location":"provided-elements/#FerriteOperators.rewrap-Union{Tuple{D}, Tuple{D, Any}} where D<:AbstractElementCacheDecorator","page":"Provided integrators and caches","text":"rewrap(d::AbstractElementCacheDecorator, inner) -> decorator\n\nd around a REPLACEMENT inner, for a seam that transforms the wrapped cache rather than delegating to it (with_action_storage). The default calls the decorator type's constructor with the new inner; a decorator carrying state beside inner overloads this to carry it across.\n\n\n\n\n\n","title":"FerriteOperators.rewrap"},{"category":"method","location":"provided-elements/#FerriteOperators.unwrap-Tuple{Any}","page":"Provided integrators and caches","text":"unwrap(cache) -> cache\nunwrap(T::Type) -> Type\n\nThe cache an author wrote: cache itself, or the innermost cache a chain of AbstractElementCacheDecorators wraps. Defined on values and on types, since the probes come in both shapes.\n\n\n\n\n\n","title":"FerriteOperators.unwrap"},{"category":"method","location":"provided-elements/#FerriteOperators.weighted_seed_buffers!-Tuple{FerriteOperators.ADElementBuffers, Int64}","page":"Provided integrators and caches","text":"weighted_seed_buffers!(buf::ADElementBuffers, nslots) -> Vector of Dual buffers\n\nGrow the weighted sweep's Dual slot buffers to nslots entries, one per participating slot. The Dual type is fixed by the sweep's ∂F/∂u configuration.\n\n\n\n\n\n","title":"FerriteOperators.weighted_seed_buffers!"},{"category":"type","location":"provided-elements/#FerriteOperators.MassProlongatorElementCache","page":"Provided integrators and caches","text":"The cache associated with MassProlongatorIntegrator to assemble element Mass matrices.\n\n\n\n\n\n","title":"FerriteOperators.MassProlongatorElementCache"},{"category":"type","location":"provided-elements/#FerriteOperators.MassProlongatorIntegrator","page":"Provided integrators and caches","text":"MassProlongatorIntegrator\n\nPij = M^{-1}{e} ∫ ϕi(x) ⋅ ϕcj(x) dx (ϕc is the coarse basis function)\n\n\n\n\n\n","title":"FerriteOperators.MassProlongatorIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperators.NestedMassProlongatorElementCache","page":"Provided integrators and caches","text":"The cache associated with NestedMassProlongatorIntegrator.\n\n\n\n\n\n","title":"FerriteOperators.NestedMassProlongatorElementCache"},{"category":"type","location":"provided-elements/#FerriteOperators.NestedMassProlongatorIntegrator","page":"Provided integrators and caches","text":"NestedMassProlongatorIntegrator\n\nProlongation operator between a fine and a coarse grid that are hierarchically nested (geometric multigrid).\n\nUnlike MassProlongatorIntegrator (same grid, two DofHandlers), this variant evaluates the coarse basis functions at fine quadrature points through the reference-space affine map stored in NestedGridCellCache:\n\nxi_textcoarse = sum_i N_i^textgeofine(xi_textfine) hatx_i\n\nwhere hatx_i are the positions of the fine cell's nodes in the parent (coarse) reference element (child_ref_coords).\n\n\n\n\n\n","title":"FerriteOperators.NestedMassProlongatorIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperators.ElementAssemblyCache","page":"Provided integrators and caches","text":"ElementAssemblyCache <: AbstractElementCacheDecorator\n\nThe cache a storage = ElementAssembly() operator runs (MatrixFreeAction): the wrapped element cache, the subdomain's element matrices, and the cell → slot map that addresses them. It is element-AGNOSTIC, so it serves a cache with no matrix-free kernel at all.\n\nK's layout is the element_matrix_symmetry election, read once off the wrapped cache at construction and carried as symmetry:\n\nGeneralElementMatrix (the default) — K is (slot, i, j) dense.\nSymmetricElementMatrix — K is (slot, t), t the packed upper-triangle index (row-major, diagonal included). Kₑ = Kₑᵀ is TRUSTED, not checked: the action never reads the lower triangle.\n\nslot is leading and stride-1 either way, so the lanes of one (i, j) step read adjacent addresses. slots[cellid] is the cell's row, 0 outside this subdomain, keeping the store the size of the SUBDOMAIN rather than of the grid.\n\nlocal_size is Val(ndofs_per_cell) (element_local_length), so a device kernel's trip counts are compile-time constants and the element vectors stay in registers.\n\nK/slots are shared read-only by the action, so the device layout batches neither (adapt_shared) and recurses into the inner cache. scratch is the exception — a per-worker ndofs_per_cell² buffer the SYMMETRIC fill packs from, 0×0 and unbatched under GeneralElementMatrix.\n\nwarning: Experimental surface\nThis decorator and the elections that build it may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.ElementAssemblyCache"},{"category":"method","location":"provided-elements/#FerriteOperators.apply_element_action!-Tuple{Any, ElementAssemblyCache, Any, CellArgs}","page":"Provided integrators and caches","text":"apply_element_action!(yₑ, cache::ElementAssemblyCache, uₑ, args)\n\nThe ELEMENT-level action: the dense product yₑ += Kₑ·uₑ over the matrix this cell's slot holds. No quadrature point is visited — the element's own kernels are consumed by the FILL. The extents come from local_size, not from the store's dimensions.\n\n\n\n\n\n","title":"FerriteOperators.apply_element_action!"},{"category":"method","location":"provided-elements/#FerriteOperators.element_action_row-Tuple{ElementAssemblyCache, Any, CellArgs, Int64}","page":"Provided integrators and caches","text":"element_action_row(cache::ElementAssemblyCache, uₑ, args, i)\n\nThe ELEMENT level's row: the dot product of row i of this cell's stored matrix with uₑ, accumulated in ONE register and returned by value — what a LanesPerElement lane owns. Neither layout reads the lower triangle.\n\n\n\n\n\n","title":"FerriteOperators.element_action_row"},{"category":"method","location":"provided-elements/#FerriteOperators.element_matrix_fill_route-Union{Tuple{Type{C}}, Tuple{C}} where C","page":"Provided integrators and caches","text":"element_matrix_fill_route(::Type{C}) -> MatrixKernelFill() or ActionKernelFill()\n\nWhich route an ElementAssemblyCache over C fills its matrices through: the element's own element-matrix kernel where provides_analytic declares one, the action applied to the unit vectors where apply_element_action! exists, a rejection where neither does.\n\n\n\n\n\n","title":"FerriteOperators.element_matrix_fill_route"},{"category":"method","location":"provided-elements/#FerriteOperators.fill_quadrature_data!-Tuple{ElementAssemblyCache, CellArgs}","page":"Provided integrators and caches","text":"fill_quadrature_data!(cache::ElementAssemblyCache, args)\n\nFill this cell's element matrix — the ELEMENT level's answer to the same QuadratureDataKind sweep the PARTIAL level runs.\n\nThe inner cache is filled FIRST where it keeps a store of its own, so an element assembled through its action route reads factors as fresh as the sweep filling it.\n\n\n\n\n\n","title":"FerriteOperators.fill_quadrature_data!"},{"category":"module","location":"provided-elements/#FerriteOperatorsTensorProduct.FerriteOperatorsTensorProduct","page":"Provided integrators and caches","text":"The tensor-product sum-factorization core for FerriteOperators' matrix-free elements: the 1D reference operators, the lattice permutations, the contraction pipeline, and the two reference elements written against it — SumFactorizedDiffusionIntegrator and SumFactorizedMassIntegrator.\n\nAn element in this family is AbstractTensorProductElementCache plus a POINTWISE MAP (tensor_product_pointwise); the B/G blocks of the MFEM/libCEED decomposition and both execution mappings (WorkerPerElement/CooperativeElement) are this package's.\n\nScope: tensor-product Lagrange on RefQuadrilateral/RefHexahedron, the shapes Ferrite ships such a basis for.\n\nwarning: Experimental surface\nThis package's names and the element entry points they call may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.FerriteOperatorsTensorProduct"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.AbstractQuadratureQuantity","page":"Provided integrators and caches","text":"AbstractQuadratureQuantity\nQuadratureValue()\nQuadratureGradient()\n\nWhat the forward contraction produces at a quadrature point, and thereby the components the element's pointwise map (tensor_product_pointwise) receives: the single interpolated VALUE, or the dim components of the REFERENCE GRADIENT. It also fixes the 1D operator each component is contracted with along each lattice axis — B everywhere for a value, dB along the component's own axis for a gradient — and with it the scratch width.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.AbstractQuadratureQuantity"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.AbstractTensorProductElementCache","page":"Provided integrators and caches","text":"AbstractTensorProductElementCache <: AbstractVolumetricElementCache\n\nAn element evaluating its operator's ACTION by Deville–Fischer–Mund sum factorization over a tensor-product lattice, in O(p^{d+1}) per cell instead of forming Kₑ. The core supplies the contractions, the lattice bookkeeping and BOTH matrix-free entries — apply_element_action! and the cooperative_stage! pipeline — over one definition of the element math.\n\nWhat a concrete cache brings:\n\nthe fields values (a TensorProductValues) and scratch (the contraction box, allocate_tensor_product_scratch), or overrides of tensor_product_values/tensor_product_scratch\ntensor_product_quantity — what the forward contraction produces\ntensor_product_pointwise — THE POINTWISE MAP, the one place the form enters\noptionally fill_quadrature_data!, where that map reads factors a QuadratureDataKind sweep stored instead of re-deriving them\nthe device struct-of-arrays trio (setup_device_instances, device_worker_view, duplicate_for_device), which names the concrete type and is therefore not derivable here; scratch is the one per-worker field, everything else shared read-only\n\nThe cache has NO element-matrix kernel: assembling it under FullAssembly is refused where the matrix would be formed.\n\nwarning: Experimental surface\nThis supertype, its accessors and the pipeline it drives may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.AbstractTensorProductElementCache"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.QuadratureGradient","page":"Provided integrators and caches","text":"AbstractQuadratureQuantity\nQuadratureValue()\nQuadratureGradient()\n\nWhat the forward contraction produces at a quadrature point, and thereby the components the element's pointwise map (tensor_product_pointwise) receives: the single interpolated VALUE, or the dim components of the REFERENCE GRADIENT. It also fixes the 1D operator each component is contracted with along each lattice axis — B everywhere for a value, dB along the component's own axis for a gradient — and with it the scratch width.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.QuadratureGradient"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.QuadratureValue","page":"Provided integrators and caches","text":"AbstractQuadratureQuantity\nQuadratureValue()\nQuadratureGradient()\n\nWhat the forward contraction produces at a quadrature point, and thereby the components the element's pointwise map (tensor_product_pointwise) receives: the single interpolated VALUE, or the dim components of the REFERENCE GRADIENT. It also fixes the 1D operator each component is contracted with along each lattice axis — B everywhere for a value, dB along the component's own axis for a gradient — and with it the scratch width.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.QuadratureValue"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.SumFactorizedDiffusionElementCache","page":"Provided integrators and caches","text":"SumFactorizedDiffusionElementCache\n\nThe cache SumFactorizedDiffusionIntegrator sets up: the diffusion tensor in the reference frame, one TensorProductValues, the per-quadrature-point factor store the form's storage election allocated (nothing under Recompute()), and the contraction scratch.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.SumFactorizedDiffusionElementCache"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.SumFactorizedDiffusionIntegrator","page":"Provided integrators and caches","text":"SumFactorizedDiffusionIntegrator(D, qrc, field_name)\n\nThe same bilinear form as SimpleBilinearDiffusionIntegrator, a(uv) = int nabla v cdot D nabla u dx, written for the MATRIX-FREE level: its cache evaluates the ACTION yₑ = Kₑ·uₑ by sum factorization (Deville–Fischer–Mund) in O(p^{d+1}) per cell instead of forming Kₑ.\n\nD is a constant scalar (isotropic) or a SymmetricTensor{2, dim}. Set the operator up with form = MatrixFreeAction(); the cache serves both element mappings and both per-quadrature-point storage elections.\n\nThe element math is the pointwise map W_q = w_q det(J_q) J_q^-1 D J_q^-T; everything around it is AbstractTensorProductElementCache's, whose scope (tensor-product Lagrange on RefQuadrilateral/RefHexahedron, distorted cells included) is this integrator's.\n\nThe cache has no element-matrix kernel: FullAssembly over this integrator is refused where the matrix would be formed. Assemble SimpleBilinearDiffusionIntegrator for a matrix of the same form.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.SumFactorizedDiffusionIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.SumFactorizedMassElementCache","page":"Provided integrators and caches","text":"SumFactorizedMassElementCache\n\nThe cache SumFactorizedMassIntegrator sets up: the density, one TensorProductValues, the per-quadrature-point factor store the form's storage election allocated (nothing under Recompute()), and the contraction scratch.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.SumFactorizedMassElementCache"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.SumFactorizedMassIntegrator","page":"Provided integrators and caches","text":"SumFactorizedMassIntegrator(ρ, qrc, field_name)\n\nThe same bilinear form as SimpleBilinearMassIntegrator, a(uv) = int v  rho  u dx, written for the MATRIX-FREE level: its cache evaluates the ACTION yₑ = Mₑ·uₑ by sum factorization, reusing AbstractTensorProductElementCache whole. ρ is a constant scalar.\n\nThe whole element is its pointwise map rho  w_q det(J_q) — a SCALAR factor on the interpolated value, where SumFactorizedDiffusionIntegrator has a tensor on the reference gradient. Everything else is AbstractTensorProductElementCache's.\n\nSet the operator up with form = MatrixFreeAction(). The cache has no element-matrix kernel; assemble SimpleBilinearMassIntegrator for a matrix of the same form.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.SumFactorizedMassIntegrator"},{"category":"type","location":"provided-elements/#FerriteOperatorsTensorProduct.TensorProductValues","page":"Provided integrators and caches","text":"TensorProductValues(sdh::SubDofHandler, field_name::Symbol, qrc)\n\nThe values object of a tensor-product element: the 1D operators evaluated at the 1D quadrature points, and the two lattice permutations that turn Ferrite's local orderings into the lexicographic lattice sum factorization contracts over.\n\nEverything is isbits and every extent a type parameter, so the object crosses a device boundary unchanged and a cooperative kernel takes its group-local memory sizes from the element's type:\n\nB, dB — φₐ(ξ_q) and φ'ₐ(ξ_q) of the 1D Lagrange basis, Nq × Nb\nBg, dBg — the same for the 1D geometric basis, Nq × Nn\nw — the 1D quadrature weights\ndof_lattice, node_lattice — lexicographic lattice index → Ferrite's local dof / local node\n\nScope: tensor-product Lagrange on RefQuadrilateral and RefHexahedron (Ferrite ships orders 1–3 there), a tensor-product Gauss rule through qrc — whose order is its POINT COUNT per direction — and a tensor-product Lagrange geometry. Anything else is rejected here; rejecting a basis whose nodes are not that lattice is also what certifies that it factorizes into the 1D basis.\n\nThe per-cell storage lives on the element CACHE (AbstractTensorProductElementCache) rather than here: a mutable field is what a device layout has to batch and adapt.\n\nwarning: Experimental surface\nThe tensor-product core, its names and the element entry points it calls may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.TensorProductValues"},{"category":"method","location":"provided-elements/#FerriteOperators.apply_element_action!-Tuple{Any, AbstractTensorProductElementCache, Any, CellArgs}","page":"Provided integrators and caches","text":"apply_element_action!(yₑ, cache::AbstractTensorProductElementCache, uₑ, args)\n\nThe worker-per-element action: the cooperative pipeline run by a single lane over the worker's own scratch.\n\n\n\n\n\n","title":"FerriteOperators.apply_element_action!"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.allocate_tensor_product_scratch-Union{Tuple{T}, Tuple{Nn}, Tuple{Nq}, Tuple{Nb}, Tuple{dim}, Tuple{TensorProductValues{dim, Nb, Nq, Nn, T}, AbstractQuadratureQuantity}} where {dim, Nb, Nq, Nn, T}","page":"Provided integrators and caches","text":"allocate_tensor_product_scratch(values::TensorProductValues, quantity) -> Matrix\n\nThe per-cell contraction box both element mappings work in: 1 + 2·nc lattice boxes of max(Nb, Nq)^dim entries, nc being the component count quantity names. CooperativeElement stages the same boxes in group-local memory instead (tensor_product_scratch_prototype).\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.allocate_tensor_product_scratch"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.quadrature_lattice_index-Union{Tuple{Nq}, Tuple{Nb}, Tuple{dim}, Tuple{TensorProductValues{dim, Nb, Nq}, Int64}} where {dim, Nb, Nq}","page":"Provided integrators and caches","text":"quadrature_lattice_index(values::TensorProductValues, qp::Int) -> NTuple{dim, Int}\n\nThe per-axis 1D quadrature indices of the qp-th quadrature point, the inverse of the lexicographic (dimension 1 fastest) numbering the contraction lattice uses — and the numbering a per-quadrature-point store is laid out in.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.quadrature_lattice_index"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_contract!-Union{Tuple{dim}, Tuple{M}, Tuple{Any, Int64, Int64, Any, Int64, Int64, Int64, Val{M}, Val{dim}, Int64, Int64}} where {M, dim}","page":"Provided integrators and caches","text":"tensor_product_contract!(scratch, dcol, scol, Op, k, nout, nin, Val(M), Val(dim), lane, nlanes)\n\nOne contraction, dst[…, iₖ, …] = Σₐ Op[iₖ, a] · src[…, a, …] along axis k of the scratch box, over the spectator slabs lane owns. lane = nlanes = 1 walks every slab; a workgroup of nlanes splits them. dcol/scol must differ.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_contract!"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_jacobian-Union{Tuple{T}, Tuple{Nn}, Tuple{Nq}, Tuple{Nb}, Tuple{dim}, Tuple{TensorProductValues{dim, Nb, Nq, Nn, T}, Any, NTuple{dim, Int64}}} where {dim, Nb, Nq, Nn, T}","page":"Provided integrators and caches","text":"tensor_product_jacobian(values::TensorProductValues, coordinates, q::NTuple{dim, Int}) -> SMatrix{dim, dim}\n\nThe Jacobian ∂x/∂ξ of the isoparametric map at the quadrature point q names (the per-axis 1D indices quadrature_lattice_index returns), formed from the cell's node coordinates in Ferrite's own order. No affine map is assumed.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_jacobian"},{"category":"function","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_pointwise","page":"Provided integrators and caches","text":"tensor_product_pointwise(cache, args::CellArgs, q::NTuple{dim, Int}, qp::Int, v::SVector) -> SVector\n\nTHE POINTWISE MAP: the element's D block, applied to what the forward contraction produced at one quadrature point and returning what the backward contraction takes back to the dofs. v carries the components tensor_product_quantity names.\n\nq is the point's per-axis 1D index, which the geometry helpers tensor_product_jacobian/tensor_product_weight take; qp is its linear index in the cell's own quadrature numbering, which addresses a per-quadrature-point store (quadrature_lattice_index relates the two). A map that RE-DERIVES its geometry reads q, one that reads stored factors reads qp.\n\nCalled by the lane that produced the point, so nothing here synchronizes and nothing is written outside the point.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_pointwise"},{"category":"function","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_quantity","page":"Provided integrators and caches","text":"tensor_product_values(cache) -> TensorProductValues\ntensor_product_scratch(cache) -> AbstractMatrix\ntensor_product_quantity(cache) -> AbstractQuadratureQuantity\n\nThe three things the core reads off an AbstractTensorProductElementCache. The first two default to the values and scratch FIELDS; the quantity has no default, a wrong one evaluating a different form in silence.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_quantity"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_scratch-Tuple{AbstractTensorProductElementCache}","page":"Provided integrators and caches","text":"tensor_product_values(cache) -> TensorProductValues\ntensor_product_scratch(cache) -> AbstractMatrix\ntensor_product_quantity(cache) -> AbstractQuadratureQuantity\n\nThe three things the core reads off an AbstractTensorProductElementCache. The first two default to the values and scratch FIELDS; the quantity has no default, a wrong one evaluating a different form in silence.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_scratch"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_scratch_prototype-Tuple{Any, Any}","page":"Provided integrators and caches","text":"tensor_product_scratch_prototype(device, scratch) -> scratch\n\nThe scratch a cache's setup_device_instances hands the device to batch: the scratch itself, or an EMPTY one under CooperativeElement, whose kernel stages the boxes in group-local memory. A cache's device layout passes its scratch through this instead of testing the mapping itself.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_scratch_prototype"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_values-Tuple{AbstractTensorProductElementCache}","page":"Provided integrators and caches","text":"tensor_product_values(cache) -> TensorProductValues\ntensor_product_scratch(cache) -> AbstractMatrix\ntensor_product_quantity(cache) -> AbstractQuadratureQuantity\n\nThe three things the core reads off an AbstractTensorProductElementCache. The first two default to the values and scratch FIELDS; the quantity has no default, a wrong one evaluating a different form in silence.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_values"},{"category":"method","location":"provided-elements/#FerriteOperatorsTensorProduct.tensor_product_weight-Union{Tuple{T}, Tuple{Nn}, Tuple{Nq}, Tuple{Nb}, Tuple{dim}, Tuple{TensorProductValues{dim, Nb, Nq, Nn, T}, NTuple{dim, Int64}}} where {dim, Nb, Nq, Nn, T}","page":"Provided integrators and caches","text":"tensor_product_weight(values::TensorProductValues, q::NTuple{dim, Int}) -> Number\n\nThe tensor-product quadrature weight at the point q names — the companion of tensor_product_jacobian; the two together are the geometric factor a pointwise map scales by.\n\n\n\n\n\n","title":"FerriteOperatorsTensorProduct.tensor_product_weight"},{"category":"section","location":"operator-api/#Operator-API-reference","page":"Operator API reference","text":"","title":"Operator API reference"},{"category":"type","location":"operator-api/#FerriteOperators.AbstractBilinearOperator","page":"Operator API reference","text":"AbstractBilinearOperator <: AbstractNonlinearOperator\n\nThe operator a bilinear form induces: a state-independent matrix A with action F(u) = A·u. The matrix IS the Jacobian, so update_linearization! is update_operator! plus that action, and the family carries no differentiation machinery (see AbstractBilinearIntegrator).\n\n\n\n\n\n","title":"FerriteOperators.AbstractBilinearOperator"},{"category":"type","location":"operator-api/#FerriteOperators.AbstractLinearOperator","page":"Operator API reference","text":"AbstractLinearOperator\n\nSupertype for operators which only depend on the test space.\n\n\n\n\n\n","title":"FerriteOperators.AbstractLinearOperator"},{"category":"type","location":"operator-api/#FerriteOperators.AbstractNonlinearOperator","page":"Operator API reference","text":"AbstractNonlinearOperator\n\nModels of a nonlinear function F(u)v, where v is a test function.\n\nInterface:\n\nevaluate!(op, residual::AbstractVector, states::NamedTuple, p, ctx)\nevaluate!(op, residual::AbstractVector, u::AbstractVector, p)\nBase.eltype(op)\nBase.size(op[, axis])\n\n# linearization\nmul!(out::AbstractVector, op, in::AbstractVector)\nmul!(out::AbstractVector, op, in::AbstractVector, α, β)\nupdate_linearization!(op, states::NamedTuple, p, ctx)\nupdate_linearization!(op, residual::AbstractVector, states::NamedTuple, p, ctx)\n\nThe (states, p, ctx) forms are canonical: states carries one entry per declared slot, p the parameter object element kernels query, ctx the per-sweep evaluation context. The (u, p) forms are stationary conveniences evaluating at states = (u = u,) with no context.\n\n\n\n\n\n","title":"FerriteOperators.AbstractNonlinearOperator"},{"category":"type","location":"operator-api/#FerriteOperators.AlgebraicDomain","page":"Operator API reference","text":"AlgebraicDomain(element, items)\n\nDomain descriptor of an algebraic subdomain cache: the cache setup_algebraic_cache built (field named after AssemblyDomain's so the per-sweep validation helpers read both domains) and the resolved dof vectors.\n\n\n\n\n\n","title":"FerriteOperators.AlgebraicDomain"},{"category":"type","location":"operator-api/#FerriteOperators.AssemblyEngine","page":"Operator API reference","text":"AssemblyEngine\n\nThe assembly machinery shared by all operators: the execution strategy, the per-subdomain caches (workspaces + partitions), the dof handler the operator assembles against, the engine-scoped internal-variable handler, and the slot names setup_engine sized the per-worker buffers for. Operators are payload (matrices/vectors) plus an engine plus their integrator.\n\nThe declared request KINDS are not carried: they are consumed during setup, as the argument the cache validations run against, and a declaration restricts nothing afterwards — an undeclared kind runs the same checks at its call-time entry point.\n\n\n\n\n\n","title":"FerriteOperators.AssemblyEngine"},{"category":"type","location":"operator-api/#FerriteOperators.FacetItemDomain","page":"Operator API reference","text":"FacetItemDomain(sdh, element, items)\n\nDomain descriptor of a facet item subdomain cache: the SubDofHandler whose cells own the facets, the surface cache setup_facet_item_cache built (field named after AssemblyDomain's so the per-sweep validation helpers read every domain), and the resolved FacetItems.\n\n\n\n\n\n","title":"FerriteOperators.FacetItemDomain"},{"category":"type","location":"operator-api/#FerriteOperators.LinearNullOperator","page":"Operator API reference","text":"LinearNullOperator <: AbstractLinearOperator\n\nLiterally the null vector.\n\n\n\n\n\n","title":"FerriteOperators.LinearNullOperator"},{"category":"type","location":"operator-api/#FerriteOperators.NullOperator","page":"Operator API reference","text":"NullOperator <: AbstractBilinearOperator\n\nLiterally a \"null matrix\".\n\n\n\n\n\n","title":"FerriteOperators.NullOperator"},{"category":"type","location":"operator-api/#FerriteOperators.SubdomainCache","page":"Operator API reference","text":"SubdomainCache(domain, device_cache, partition)\n\nOne subdomain's traversal: its item-family domain descriptor (AssemblyDomain, FacetItemDomain, AlgebraicDomain), the per-worker device scratch and the partition of its items.\n\ncontributes is the structural verdict of whether an ASSEMBLY sweep can reach any kernel here, decided once from the caches' types (_domain_assembles) and skipped on by execute_on_subdomains!. Reductions decide separately and per kind, through _may_contribute.\n\n\n\n\n\n","title":"FerriteOperators.SubdomainCache"},{"category":"function","location":"operator-api/#FerriteOperators.evaluate!","page":"Operator API reference","text":"evaluate!(op, residual, u, p)\n\nEvaluate the residual F(u) into residual without updating the Jacobian.\n\n\n\n\n\n","title":"FerriteOperators.evaluate!"},{"category":"method","location":"operator-api/#FerriteOperators.execute_on_subdomains!-Tuple{Any, Any, Any}","page":"Operator API reference","text":"execute_on_subdomains!(task, engine)\n\nRun task over every subdomain that can contribute to an assembly sweep, skipping the ones whose caches make a contribution structurally impossible (SubdomainCache.contributes) rather than paying their geometry reinit, slot gather and zero scatter per item.\n\n\n\n\n\n","title":"FerriteOperators.execute_on_subdomains!"},{"category":"method","location":"operator-api/#FerriteOperators.get_dof_handler-Tuple{Any}","page":"Operator API reference","text":"get_dof_handler(op) -> AbstractDofHandler\n\nThe DofHandler op assembles against (op.engine.dh).\n\n\n\n\n\n","title":"FerriteOperators.get_dof_handler"},{"category":"method","location":"operator-api/#FerriteOperators.get_matrix-Tuple{Any}","page":"Operator API reference","text":"get_matrix(op)\n\nOptional accessor hook: the matrix an operator type chooses to expose to a consumer that needs the array itself rather than the operator's action. There is no default — the fallback errors — so an operator has this accessor only where its own type implements it, and an operator backed by no explicit matrix legitimately has none.\n\nFerriteOperators ships this for NullOperator (materializing its all-zero matrix on demand) and for the two operator kinds whose matrix field is unambiguous — BilinearFerriteOperator's A, LinearizedFerriteOperator's J — both of which also expose that array through operator_payload, which is what Base.eltype/Base.size read. A LinearFerriteOperator holds a load VECTOR and has no method here; a downstream operator type implements get_matrix as its own accessor.\n\n\n\n\n\n","title":"FerriteOperators.get_matrix"},{"category":"method","location":"operator-api/#FerriteOperators.get_strategy-Tuple{Any}","page":"Operator API reference","text":"get_strategy(op) -> AssemblyStrategy\n\nThe AssemblyStrategy op was set up with (op.engine.strategy).\n\n\n\n\n\n","title":"FerriteOperators.get_strategy"},{"category":"method","location":"operator-api/#FerriteOperators.get_subdomain_caches-Tuple{Any}","page":"Operator API reference","text":"get_subdomain_caches(op) -> Vector{SubdomainCache}\n\nThe per-subdomain caches op's engine assembles over (op.engine.subdomain_caches). Each entry's .domain names what the subdomain serves — a FacetItemDomain for tying facets, a cell/boundary or algebraic descriptor otherwise — so downstream code can filter subdomains by served domain (e.g. which chambers a coupler ties).\n\n\n\n\n\n","title":"FerriteOperators.get_subdomain_caches"},{"category":"function","location":"operator-api/#FerriteOperators.operator_payload","page":"Operator API reference","text":"operator_payload(op)\n\nThe assembled array an operator's Base.eltype and Base.size read: the matrix a bilinear, nonlinear or transfer operator holds, the load vector of a linear operator, one stage block of a StageBlockOperator.\n\nOperators whose shape lives in type parameters instead (NullOperator, LinearNullOperator) carry no payload and keep their own methods.\n\n\n\n\n\n","title":"FerriteOperators.operator_payload"},{"category":"method","location":"operator-api/#FerriteOperators.reduce_on_subdomains-Tuple{Any, Any, Any}","page":"Operator API reference","text":"reduce_on_subdomains(task, engine) -> value\n\nThe value-returning counterpart of execute_on_subdomains!: run task over every subdomain that can contribute to the task's kind and reduce the per-subdomain partials in subdomain order. Within a subdomain reduce_on_device reduces the per-worker partials in worker order, so the reduction order is fixed by the partition and the result is deterministic for a fixed worker count.\n\nA subdomain _may_contribute declines is skipped whole — a surface functional pays no reinit! over the mesh's cells. Only zero contributions are removed and the fold order among the contributing subdomains is untouched, so the value is the one an unskipped traversal computes.\n\n\n\n\n\n","title":"FerriteOperators.reduce_on_subdomains"},{"category":"method","location":"operator-api/#FerriteOperators.update_linearization!-Tuple{AbstractNonlinearOperator, AbstractVector, AbstractVector, Any}","page":"Operator API reference","text":"update_linearization!(op, residual, u, p)\n\nSetup the linearized operator Jᵤ(u) := dᵤF(u) in op and its residual F(u), in preparation to solve J(u) Δu = F(u) for the increment Δu.\n\n\n\n\n\n","title":"FerriteOperators.update_linearization!"},{"category":"method","location":"operator-api/#FerriteOperators.update_linearization!-Tuple{AbstractNonlinearOperator, AbstractVector, Any}","page":"Operator API reference","text":"update_linearization!(op, u, p)\n\nSetup the linearized operator Jᵤ(u) in op.\n\n\n\n\n\n","title":"FerriteOperators.update_linearization!"},{"category":"method","location":"operator-api/#FerriteOperators._needs_fused_from_split-Union{Tuple{Type{T}}, Tuple{T}} where T","page":"Operator API reference","text":"true iff T provides JacobianKind{:u} and the mandatory residual analytically but not the fused JacobianResidualKind — the FusedFromSplit case.\n\n\n\n\n\n","title":"FerriteOperators._needs_fused_from_split"},{"category":"function","location":"operator-api/#FerriteOperators.decorate_element_cache","page":"Operator API reference","text":"decorate_element_cache(cache, sdh, ad_backend, n_global_dofs = 0; n_internal_dofs = 0)\n\nResolve cache into the form the engine calls unconditionally: FusedFromSplit where it provides split analytic kernels but not the fused one, then ADElementCache where it still lacks analytic coverage of some AD-decorator kind. ad_backend === nothing opts out of the ADElementCache step only. A CompositeVolumetricElementCache wraps its non-analytic inners as ONE sub-composite (the maximal-sub-composite policy), since wrapping each individually costs a full seeding pass per inner.\n\nn_global_dofs is the subdomain's global_dofs count and pads the AD buffers, so an AD fallback differentiates the augmented local system rather than its field-space head. n_internal_dofs is one cell's condensed internal-dof count (resolve_internal_dofs_per_element) and sizes the :q seeds and configs. Both are resolved from the integrator's declarations before any cache is decorated, so the decorator itself stays integrator-free.\n\n\n\n\n\n","title":"FerriteOperators.decorate_element_cache"},{"category":"method","location":"operator-api/#FerriteOperators.fully_analytic-Union{Tuple{Type{T}}, Tuple{T}} where T","page":"Operator API reference","text":"true iff T has an analytic kernel for every AD-decorator-covered kind — no wrapping needed.\n\n\n\n\n\n","title":"FerriteOperators.fully_analytic"},{"category":"method","location":"operator-api/#FerriteOperators.needs_ad_decoration-Tuple{Any}","page":"Operator API reference","text":"needs_ad_decoration(integrator) -> Bool\n\nWhether an operator built for integrator may issue a kind ADElementCache covers — STRUCTURAL, so a bilinear or linear operator carries no AD/sensitivity machinery whatever an element cache implements analytically. true for AbstractNonlinearIntegrator.\n\n\n\n\n\n","title":"FerriteOperators.needs_ad_decoration"},{"category":"method","location":"operator-api/#FerriteOperators.allocate_operator_matrix-Union{Tuple{MT}, Tuple{FerriteOperators.AbstractDevice, Type{MT}, Any}} where MT","page":"Operator API reference","text":"allocate_operator_matrix(device, matrix_type, sp)\n\nThe operator's global matrix over the sparsity pattern sp create_system_matrix built.\n\nFerrite ships no allocate_matrix(::Type{<:device matrix}, ::SparsityPattern), so a GPU device allocates the host SparseMatrixCSC over that pattern and hands it to the device type's constructor. The type parameters come from that constructor (CUSPARSE fixes the index type at Cint), not from the request.\n\n\n\n\n\n","title":"FerriteOperators.allocate_operator_matrix"},{"category":"method","location":"operator-api/#FerriteOperators.assert_declaration_signatures-Tuple{Any, Ferrite.AbstractDofHandler}","page":"Operator API reference","text":"assert_declaration_signatures(integrator, dh)\n\nReject a declaration hook whose method was written against a signature the engine does not call. global_dofs, facet_items, facet_item_global_dofs and algebraic_items all default to an EMPTY declaration and item_families to the families those three imply, so a drifted signature never surfaces as a MethodError: the default answers instead, and the operator assembles a subset — a missing local-system tail, an unvisited boundary, absent algebraic rows, an unregistered family — without a word.\n\nThe check is type-level and runs once per setup_engine. For every integrator that answers these hooks — the outer one and, through the wrappers that forward them, their sub-integrators — a hook with ANY method specialized on that integrator's type must have one the engine's own call resolves to. An integrator declaring nothing has no specialized method and passes; a correct declarer's method is what the call resolves to and passes.\n\nThe iteration seams are keyed on the ELEMENT CACHE rather than the integrator, so they are checked separately and later, once the caches exist — assert_iteration_signatures.\n\n\n\n\n\n","title":"FerriteOperators.assert_declaration_signatures"},{"category":"method","location":"operator-api/#FerriteOperators.assert_iteration_signatures-Tuple{Any, Any, Ferrite.AbstractDofHandler}","page":"Operator API reference","text":"assert_iteration_signatures(kind, element_caches, dh)\n\nReject an ITERATION-seam declaration whose method was written against a signature the engine does not call. assembly_iterator and item_provider are keyed on the (kind, element cache, subdomain) triple — not on the integrator — so they are checked here rather than in assert_declaration_signatures, once the caches exist and the sweep kind is resolved.\n\nBoth default to the cell answer (CellCache, CellItems), so a drifted method is never reached and never a MethodError: the default answers and the sweep positions on CELL ids and visits CELLS — a silently wrong operator, hence the setup-time rejection.\n\nThe subject is the unwrap fixpoint of the element cache, not the possibly-decorated cache the engine calls: probing the decorated one would resolve to the decorator's forwarding method, which narrows the cache argument just enough to look like a declaration and would pass every drifted inner silently. A hook with ANY method narrowing that argument to a type this subdomain's UNWRAPPED cache conforms to must have one the engine's own call resolves to. A method narrowing only the KIND is not treated as drift.\n\nA method that is simply ABSENT no check can see; only the item COUNT a sweep visits reveals it.\n\n\n\n\n\n","title":"FerriteOperators.assert_iteration_signatures"},{"category":"method","location":"operator-api/#FerriteOperators.create_system_matrix-Tuple{Any, Any}","page":"Operator API reference","text":"create_system_matrix(strategy, dh)\n\nAllocate the operator's global matrix over the sparsity pattern the strategy's operator specification declares (StandardOperatorSpecification → SparsityPattern, BlockedOperatorSpecification → BlockSparsityPattern), with entries added by Ferrite's add_sparsity_entries! from the specification's algebraic_couplings and constraint_handler, then by the specification's sparsity_entries callable — the coupling an item family introduces and the handler's cell pattern does not carry — allocated as matrix_type(strategy).\n\nThe constraint handler contributes SPARSITY ENTRIES only; applying the constraints to the assembled system stays the caller's, through Ferrite's apply!/apply_assemble!.\n\n\n\n\n\n","title":"FerriteOperators.create_system_matrix"},{"category":"method","location":"operator-api/#FerriteOperators.init_nested_transfer_sparsity_pattern-Tuple{DofHandler, DofHandler, AbstractVector{Int64}}","page":"Operator API reference","text":"init_nested_transfer_sparsity_pattern(dh_fine, dh_coarse, fine2coarse)\n\nBuild a Ferrite.SparsityPattern of size (ndofs(dh_fine) × ndofs(dh_coarse)) for a nested-grid transfer operator: entry (rdof, cdof) is added whenever rdof belongs to fine cell i and cdof to its parent coarse cell fine2coarse[i].\n\n\n\n\n\n","title":"FerriteOperators.init_nested_transfer_sparsity_pattern"},{"category":"method","location":"operator-api/#FerriteOperators.init_transfer_sparsity_pattern-Tuple{DofHandler, DofHandler}","page":"Operator API reference","text":"init_transfer_sparsity_pattern(dh_row::DofHandler, dh_col::DofHandler)\n\nBuild a Ferrite.SparsityPattern of size (ndofs(dh_row) × ndofs(dh_col)) covering all DoF pairs (rdof, cdof) that share a cell. Both DofHandlers must live on the same grid and have the same number of subdomains.\n\n\n\n\n\n","title":"FerriteOperators.init_transfer_sparsity_pattern"},{"category":"method","location":"operator-api/#FerriteOperators.resolve_internal_dofs_per_element-Tuple{Any, Any, Any}","page":"Operator API reference","text":"resolve_internal_dofs_per_element(integrator, cache, sdh) -> Int\n\nThe condensed internal-dof count of ONE cell of sdh, from the same get_number_of_internal_dofs_per_element declaration the InternalVariableHandler lays the q block out from. This is what sizes ADElementCache's :q seeds and configurations, so the length a generic route seeds is the length the InternalSource gather hands the kernel — a tensor-valued internal variable owns several internal dofs per quadrature point, which is why the quadrature-point count does not answer this.\n\n0 where the cache declares no real internal block, matching the placeholder block setup_internal_variable_handler builds for the same caches; the decorator then builds no :q configuration and a generic :q route refuses by name.\n\nOne configuration serves every cell of the subdomain, so a count that varies between them is refused here rather than surfacing as a DimensionMismatch inside the first generic sweep.\n\n\n\n\n\n","title":"FerriteOperators.resolve_internal_dofs_per_element"},{"category":"method","location":"operator-api/#FerriteOperators.setup_engine-Tuple{AbstractAssemblyStrategy, Any, Ferrite.AbstractDofHandler}","page":"Operator API reference","text":"setup_engine(strategy, integrator, dh; slots = (:u,), requests = (), ad_backend = ForwardDiffAD())\n\nBuild the AssemblyEngine shared by all operator kinds from the setup-time declarations: slots names the state slots a sweep may carry, one per-worker buffer each, and requests the request kinds whose trait ↔ kernel and internal-state admissibility checks run here instead of at first use. Declaring a kind builds no per-worker state; it moves that kind's checks forward. Kinds are normalized to their UnionAll bases, so an instance or a payload-parameterized type declares the same kind as its bare name. The engine carries slots — the per-worker buffers are sized for them — and not the kinds, which are consumed here and restrict nothing afterwards.\n\nElement caches lacking analytic coverage of some AD-decorator kind are wrapped in ADElementCache at construction, for every kind the integrator might issue and not only the declared ones, decided STRUCTURALLY by needs_ad_decoration. ad_backend = nothing opts out of wrapping.\n\nThe declaration hooks are signature-checked first (assert_declaration_signatures), since each defaults to an empty declaration and a drifted method would otherwise assemble a silent subset.\n\nOnce the caches exist the ITERATION seams are signature-checked against them (assert_iteration_signatures), being keyed on the element cache and defaulting rather than erroring. Each subdomain's validate_element_cache call then probes reinit_values! against that subdomain's RESOLVED host assembly_iterator type rather than against CellCache unconditionally, so an author-annotated reinit_values!(c, ::MyIterator) method is validated on the type it was written against.\n\nThe subdomain caches are then the concatenation of what each REGISTERED item family builds, in the order item_families returns them — cells, then facet items (facet_items), then algebraic items (algebraic_items) for the default declaration — every one of them through the same setup_family_caches dispatch. The algebraic domain is resolved BEFORE the InternalVariableHandler is built, since a condensed algebraic cache's item block sizes itself from the resolved items and cache, and decorated afterwards alongside the cell caches.\n\n\n\n\n\n","title":"FerriteOperators.setup_engine"},{"category":"method","location":"operator-api/#FerriteOperators.setup_evaluation_operator-Tuple{AbstractAssemblyStrategy, Any, Ferrite.AbstractDofHandler}","page":"Operator API reference","text":"setup_evaluation_operator(strategy, integrator, dh; slots = (:u,), requests = (), ad_backend = ForwardDiffAD())\n\nBuild the EvaluationFerriteOperator for integrator over dh: the same engine setup_operator builds — same element, boundary, facet-item and algebraic caches through the same family dispatch, same setup-time validation — and no matrix or vector.\n\nThis is the route for a term that is only ever EVALUATED: a functional reduced over the domain (evaluate_functional), a per-quadrature-point evaluation (evaluate_quadrature!). Any integrator family may take it; what the operator does not do is assemble, and the assembly entry points say so.\n\n\n\n\n\n","title":"FerriteOperators.setup_evaluation_operator"},{"category":"method","location":"operator-api/#FerriteOperators.setup_internal_variable_handler-NTuple{4, Any}","page":"Operator API reference","text":"setup_internal_variable_handler(integrator, element_caches, algebraic_domain, dh)\n\nBuild the InternalVariableHandler from what the resolved caches declare; algebraic_domain is resolve_algebraic_domain's result, nothing where the integrator declares no algebraic items.\n\nWhether a block is REAL (an offsets array) or the placeholder (nothing) is decided per block by the cache — has_internal_state AND an implemented dof-count hook (get_number_of_internal_dofs_per_element / get_number_of_internal_dofs_per_algebraic_item) — not by the integrator's type: the cell and item blocks are independent, and either or both can be real at once (the layout-collision case).\n\n\n\n\n\n","title":"FerriteOperators.setup_internal_variable_handler"},{"category":"method","location":"operator-api/#FerriteOperators.setup_nested_transfer_operator-Tuple{AbstractAssemblyStrategy, AbstractTransferIntegrator, DofHandler, DofHandler, AbstractVector{Int64}, AbstractVector}","page":"Operator API reference","text":"setup_nested_transfer_operator(strategy, integrator, dh_fine, dh_coarse, fine2coarse, child_ref_coords)\n\nSet up a TransferFerriteOperator assembling a rectangular sparse matrix of size (ndofs(dh_fine) × ndofs(dh_coarse)). dh_fine and dh_coarse must live on different grids where every fine cell is a child of exactly one coarse cell, as encoded by fine2coarse and child_ref_coords.\n\nwarning: Experimental surface\nThe transfer constructors and operator types may change in a minor release; the assembled matrix and its sparsity are not affected.\n\n\n\n\n\n","title":"FerriteOperators.setup_nested_transfer_operator"},{"category":"method","location":"operator-api/#FerriteOperators.setup_operator-Tuple{AbstractAssemblyStrategy, AbstractBilinearIntegrator, Ferrite.AbstractDofHandler}","page":"Operator API reference","text":"setup_operator(strategy, problem, dh; slots = (:u,), requests = (), ad_backend = ForwardDiffAD())\n\nBuild the operator for problem (an integrator) over dh from what the caller declares: slots the state slot names sweeps may carry — the engine sizes one per-worker buffer per name — and requests the request kinds whose setup-time validation runs here (setup_engine). ad_backend selects the ADElementCache backend wrapping caches that lack analytic coverage (nothing opts out).\n\nDeclaring stays a hint, not a capability restriction: an undeclared kind stays usable and runs its checks at the call-time entry points instead.\n\nTransfer operators keep their own constructor (setup_transfer_operator); patch sweeps run on an ordinary operator through foreach_patch.\n\n\n\n\n\n","title":"FerriteOperators.setup_operator"},{"category":"method","location":"operator-api/#FerriteOperators.setup_transfer_operator-Tuple{AbstractAssemblyStrategy, AbstractTransferIntegrator, DofHandler, DofHandler}","page":"Operator API reference","text":"setup_transfer_operator(strategy, integrator, dh_row, dh_col)\n\nSet up a TransferFerriteOperator assembling a rectangular sparse matrix of size (ndofs(dh_row) × ndofs(dh_col)). dh_row and dh_col must live on the same grid with 1-to-1 subdomain lists (same length, same cellsets at each index).\n\nintegrator must be an AbstractTransferIntegrator; its setup_transfer_element_cache(integrator, sdh_row, sdh_col) runs once per subdomain pair.\n\nwarning: Experimental surface\nThe transfer constructors and operator types may change in a minor release; the assembled matrix and its sparsity are not affected.\n\n\n\n\n\n","title":"FerriteOperators.setup_transfer_operator"},{"category":"type","location":"operator-api/#FerriteOperators.MatrixFreeFerriteOperator","page":"Operator API reference","text":"MatrixFreeFerriteOperator <: AbstractBilinearOperator\n\nThe operator setup_operator returns for an AbstractBilinearIntegrator under MatrixFreeAction: the AssemblyEngine and integrator alone. There is no global matrix — every mul! re-evaluates the action from the element kernels (apply_element_action!) and scatters it. Which of the MFEM ELEMENT/PARTIAL/NONE levels it runs at is the form's storage election.\n\nSurface: mul!(y, op, u) and the five-argument form, evaluate! (the same action with parameters and a context), size, eltype. There is no get_matrix and no operator_payload: an operator that stores nothing has neither.\n\ny and u must NOT alias. Both mul! forms sweep items in an arbitrary order, writing y as they go while still reading u, so mul!(y, op, y) silently returns a partly-updated mix rather than A·y. Pass a separate destination.\n\nNOT BITWISE REPRODUCIBLE where the scatter is atomic. Under SequentialScheduling the items of one chunk accumulate into y in whatever order the hardware runs them, so two evaluations of the same action can differ in the last bits. The sequential CPU device (one worker) and ColoredScheduling (no atomics) do repeat bit for bit. A Krylov method over this operator therefore sees an operator that is not a function of u to the last bit; treat run-to-run iteration counts accordingly, or elect a colouring.\n\nwarning: Experimental surface\nThis operator, its entry points and the element hooks it calls may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.MatrixFreeFerriteOperator"},{"category":"method","location":"operator-api/#FerriteOperators.evaluate!-Tuple{MatrixFreeFerriteOperator, AbstractVector, NamedTuple, Any, Any}","page":"Operator API reference","text":"evaluate!(op::MatrixFreeFerriteOperator, y, states, p, ctx)\nevaluate!(op::MatrixFreeFerriteOperator, y, u::AbstractVector, p)\n\nThe operator's action y = A·u, carrying the parameters and the sweep context an element kernel may read. y is zeroed first, so this OVERWRITES.\n\n\n\n\n\n","title":"FerriteOperators.evaluate!"},{"category":"method","location":"operator-api/#FerriteOperators.setup_operator-Tuple{AssemblyStrategy{<:MatrixFreeAction}, AbstractBilinearIntegrator, Ferrite.AbstractDofHandler}","page":"Operator API reference","text":"setup_operator(strategy::AssemblyStrategy{<:MatrixFreeAction}, integrator, dh; …)\n\nBuild the MatrixFreeFerriteOperator for a bilinear integrator: the same AssemblyEngine every other form builds, with no global matrix.\n\nThe form's AbstractElementMapping is resolved onto the device here (with_element_mapping); the storage election reaches the element caches through with_assembly_form, and what it keeps is FILLED here with p = nothing, so an operator is usable the moment it is set up.\n\nOnly the bilinear family takes this form: a nonlinear residual is not the action of a stored operator, and a linear form has no u to act on.\n\n\n\n\n\n","title":"FerriteOperators.setup_operator"},{"category":"function","location":"operator-api/#FerriteOperators.update_operator!","page":"Operator API reference","text":"update_operator!(op::MatrixFreeFerriteOperator, p, ctx = nothing)\n\nRefill what the operator's storage election keeps (MatrixFreeAction) — the elements' per-quadrature-point factors under Stored, the dense element matrices under ElementAssembly — with one QuadratureDataKind sweep carrying p and ctx to fill_quadrature_data!. Under Recompute nothing is kept and this does nothing.\n\nFRESHNESS IS THE CALLER'S, exactly as for an assembled operator: the store holds what the last such call put there (setup_operator makes one with p = nothing), and an action evaluated after p or the context time changed reads stale factors until this is called again.\n\n\n\n\n\n","title":"FerriteOperators.update_operator!"},{"category":"type","location":"operator-api/#FerriteOperators.ADSensitivity","page":"Operator API reference","text":"ADSensitivity()\n\nDefault derivative method: ForwardDiff seeding through the residual kernel.\n\n\n\n\n\n","title":"FerriteOperators.ADSensitivity"},{"category":"type","location":"operator-api/#FerriteOperators.FiniteDifferenceSensitivity","page":"Operator API reference","text":"FiniteDifferenceSensitivity(h = cbrt(eps(Float64)))\n\nCentral-difference derivative method, accuracy O(h²): (F(t+h) − F(t−h)) / 2h from PRIMAL residual evaluations at contexts carrying perturbed evaluation times, on a protected copy of u so trial write-back never leaks. No Dual propagation, hence admissible for condensed elements: it condenses AT each perturbed context and so yields the TOTAL t-derivative at fixed u, the element-local state's response included. The operator's corrector stores are left holding the last perturbed point's condensation.\n\nThe operator-level counterpart of the ADElementCache decorator, and the split is final: this is the only BOUNDARY-INCLUSIVE route (it differences evaluate!, facet terms included, rather than an element kernel) and the only Dual-free one, and being an operator-level OVERRIDE it bypasses analytic sensitivity kernels everywhere.\n\n\n\n\n\n","title":"FerriteOperators.FiniteDifferenceSensitivity"},{"category":"type","location":"operator-api/#FerriteOperators.LinearizedFerriteOperator","page":"Operator API reference","text":"LinearizedFerriteOperator(J, engine, integrator)\n\nA model for a function with its fully assembled linearization. Entry points route request kinds through the shared assembly engine; element kernels are the request-typed assemble_cell!/assemble_facet! methods.\n\n\n\n\n\n","title":"FerriteOperators.LinearizedFerriteOperator"},{"category":"method","location":"operator-api/#FerriteOperators.assemble_slot_jacobian!-Tuple{AbstractMatrix, FerriteOperators.LinearizedFerriteOperator, JacobianKind, NamedTuple, Any, Any}","page":"Operator API reference","text":"assemble_slot_jacobian!(J, op, kind::JacobianKind, states, p, ctx)\n\nAssemble ∂F/∂slot into J, where kind names the slot (JacobianKind). J is any matrix the operator's assembler accepts — in particular a member of an allocate_components bag, so the multi-slot linearization Σ wₛ ∂F/∂s is assembled slot by slot and folded with combine!. op.J is untouched unless passed as J.\n\n:q is not a slot here — ∂F/∂q is field × internal-shaped, not square, and has its own target and entry point (update_internal_jacobian!).\n\n\n\n\n\n","title":"FerriteOperators.assemble_slot_jacobian!"},{"category":"method","location":"operator-api/#FerriteOperators.assemble_weighted_jacobian!-Tuple{AbstractMatrix, FerriteOperators.LinearizedFerriteOperator, NamedTuple, NamedTuple, Any, Any}","page":"Operator API reference","text":"assemble_weighted_jacobian!(W, op, weights::NamedTuple, states, p, ctx)\n\nAssemble the weighted Jacobian W = Σₛ weights[s] · ∂F/∂s — the matrix a scheme solves with — over the slots weights names, at frozen values of every other slot. weights are chain-rule scalars ((u = 1.0, du = 1/(γΔt)) for SDIRK/backward Euler). W must share the operator's sparsity pattern: op.J, a member of allocate_components, or share_pattern (ComplexF64 for a complex combination).\n\nTwo routes give the same matrix to round-off (check_derivatives verifies); which one runs is a capability of the element caches, not a caller choice:\n\nfused — one WeightedJacobianKind sweep: the analytic WeightedJacobianRequest kernel where declared, else the residual kernel with every participating slot seeded by weight-scaled Duals.\ncomposed — one assemble_slot_jacobian! sweep per slot into components sharing W's pattern, folded by combine!. Runs for complex weights (the Dual machinery is real — transformed Radau needs this) and where condensed internal state makes the fused route inadmissible; its per-slot sweeps carry their own guards, so the weighted kind is servable exactly when every participating JacobianKind is.\n\nA facet_items term is its own traversal and takes neither route: it serves the weighted kind through its own fused assemble_facet!(::WeightedJacobianRequest, …) kernel or not at all (assert_facet_item_route).\n\n\n\n\n\n","title":"FerriteOperators.assemble_weighted_jacobian!"},{"category":"method","location":"operator-api/#FerriteOperators.parameter_vjp!-Tuple{AbstractVector, FerriteOperators.LinearizedFerriteOperator, AbstractVector, NamedTuple, Any, Any}","page":"Operator API reference","text":"parameter_vjp!(g, op, λ, u, p)\n\nAccumulate the adjoint pullback g = (∂F/∂θ)ᵀ λ (length nθ) at the trial state u without materializing ∂F/∂θ. Never writes back into u.\n\n\n\n\n\n","title":"FerriteOperators.parameter_vjp!"},{"category":"method","location":"operator-api/#FerriteOperators.residual_size-Tuple{FerriteOperators.LinearizedFerriteOperator}","page":"Operator API reference","text":"residual_size(op) -> Int\nunknown_size(op) -> Int\n\nThe two lengths an operator's entry points size their arguments by: residual_size is the number of FE dofs (rows of F, length of a residual or adjoint vector); unknown_size adds the condensed internal tail ([ū; q], length of a solution vector). They coincide exactly when the operator carries no condensed element.\n\n\n\n\n\n","title":"FerriteOperators.residual_size"},{"category":"method","location":"operator-api/#FerriteOperators.state_jvp!-Tuple{AbstractVector, FerriteOperators.LinearizedFerriteOperator, AbstractVector, NamedTuple, Any, Any}","page":"Operator API reference","text":"state_jvp!(Jv, op, v, states, p, ctx)\nstate_jvp!(Jv, op, v, u, p)\n\nMatrix-free action of the state Jacobian: Jv = (∂F/∂u)·v at the trial state, computed kernel-level (one directional-Dual sweep per cell for the AD fallback; analytic StateJVPRequest kernels win per cache). Never writes back into the caller's state. Requires unknown_size(op) == residual_size(op), i.e. no condensed unknowns; anything else is an ArgumentError.\n\n\n\n\n\n","title":"FerriteOperators.state_jvp!"},{"category":"method","location":"operator-api/#FerriteOperators.state_vjp!-Tuple{AbstractVector, FerriteOperators.LinearizedFerriteOperator, AbstractVector, NamedTuple, Any, Any}","page":"Operator API reference","text":"state_vjp!(g, op, λ, states, p, ctx)\nstate_vjp!(g, op, λ, u, p)\n\nMatrix-free pullback of the state Jacobian: g = (∂F/∂u)ᵀλ at the trial state — the action adjoint time stepping applies. Kernel-level (per-cell gradient of λₑ·rₑ for the AD fallback; analytic StateVJPRequest kernels win per cache). Never writes back into the caller's state, and requires no condensed unknowns, like state_jvp!.\n\n\n\n\n\n","title":"FerriteOperators.state_vjp!"},{"category":"method","location":"operator-api/#FerriteOperators.time_sensitivity!-Tuple{AbstractVector, FerriteOperators.LinearizedFerriteOperator, NamedTuple, Any, Any}","page":"Operator API reference","text":"time_sensitivity!(g, op, states, p, ctx; method = ADSensitivity())\n\nAssemble the time sensitivity ∂F/∂t into g (residual_size(op)) at the trial state, never writing back into the caller's state. The evaluation time is evaluation_time(ctx) — time reaches elements through the context channel only, so ctx is mandatory here.\n\nWith ADSensitivity (default) an analytic TimeSensitivityRequest kernel wins per cache, and every other cache falls back to ForwardDiff of its residual kernel over a Dual-timed context. FiniteDifferenceSensitivity is an operator-level override that BYPASSES analytic sensitivity kernels; prefer it only where AD is inadmissible (condensed internal state without analytic kernels or insensitivity declarations).\n\n\n\n\n\n","title":"FerriteOperators.time_sensitivity!"},{"category":"method","location":"operator-api/#FerriteOperators.unknown_size-Tuple{FerriteOperators.LinearizedFerriteOperator}","page":"Operator API reference","text":"residual_size(op) -> Int\nunknown_size(op) -> Int\n\nThe two lengths an operator's entry points size their arguments by: residual_size is the number of FE dofs (rows of F, length of a residual or adjoint vector); unknown_size adds the condensed internal tail ([ū; q], length of a solution vector). They coincide exactly when the operator carries no condensed element.\n\n\n\n\n\n","title":"FerriteOperators.unknown_size"},{"category":"method","location":"operator-api/#FerriteOperators.update_parameter_jacobian!-Tuple{AbstractMatrix, FerriteOperators.LinearizedFerriteOperator, NamedTuple, Any, Any}","page":"Operator API reference","text":"update_parameter_jacobian!(B, op, u, p)\n\nAssemble the parameter Jacobian ∂F/∂θ into B (residual_size(op) × nθ) at the trial state u, never writing back into u. θ is the flat parameter view defined by parameter_vector/rebuild_parameters; elements provide analytic ParameterJacobianRequest kernels or fall back to AD of their residual.\n\nnote: Note\nThe u-vector forms evaluate at states = (u = u,) with no time-integration context. Elements reading further slots (uprev, …) or args.ctx must use the states/ctx forms.\n\n\n\n\n\n","title":"FerriteOperators.update_parameter_jacobian!"},{"category":"method","location":"operator-api/#LinearAlgebra.mul!-Tuple{AbstractVector, FerriteOperators.LinearizedFerriteOperator, AbstractVector}","page":"Operator API reference","text":"mul!(out::AbstractVector, op::LinearizedFerriteOperator, in::AbstractVector)\nmul!(out::AbstractVector, op::LinearizedFerriteOperator, in::AbstractVector, α, β)\n\nApply the (scaled) action of the assembled linearization to the vector in.\n\n\n\n\n\n","title":"LinearAlgebra.mul!"},{"category":"type","location":"operator-api/#FerriteOperators.BilinearFerriteOperator","page":"Operator API reference","text":"BilinearFerriteOperator <: AbstractBilinearOperator\n\nThe operator setup_operator returns for an AbstractBilinearIntegrator: the assembled matrix op.A plus the AssemblyEngine and integrator that built it. The matrix IS the Jacobian (see AbstractBilinearOperator).\n\n\n\n\n\n","title":"FerriteOperators.BilinearFerriteOperator"},{"category":"method","location":"operator-api/#FerriteOperators.evaluate!-Tuple{BilinearFerriteOperator, AbstractVector, NamedTuple, Any, Any}","page":"Operator API reference","text":"evaluate!(op::BilinearFerriteOperator, residual, states, p, ctx)\nevaluate!(op::BilinearFerriteOperator, residual, u, p)\n\nAssemble residual = A·u from the element residual kernels — the action of the operator induced by the bilinear form, evaluated without touching op.A. The residual kernel is mandatory for every element cache (validated at setup), so this entry point exists for every bilinear operator.\n\n\n\n\n\n","title":"FerriteOperators.evaluate!"},{"category":"function","location":"operator-api/#FerriteOperators.update_operator!-2","page":"Operator API reference","text":"update_operator!(op::BilinearFerriteOperator, p, ctx = nothing)\n\nAssemble the operator's matrix op.A from the element kernels. ctx is the sweep's context, read by kernels through evaluation_time and friends: a coefficient like ρ(x, t) or D(x, t) needs one, a constant one does not.\n\n\n\n\n\n","title":"FerriteOperators.update_operator!"},{"category":"type","location":"operator-api/#FerriteOperators.LinearFerriteOperator","page":"Operator API reference","text":"LinearFerriteOperator <: AbstractLinearOperator\n\nThe operator setup_operator returns for an AbstractLinearIntegrator: the assembled load vector op.b plus the AssemblyEngine and integrator that built it.\n\n\n\n\n\n","title":"FerriteOperators.LinearFerriteOperator"},{"category":"function","location":"operator-api/#FerriteOperators.update_operator!-3","page":"Operator API reference","text":"update_operator!(op::LinearFerriteOperator, p, ctx = nothing)\n\nAssemble the operator's vector op.b from the element kernels. ctx is the sweep's context, read by kernels through evaluation_time and friends: a load like f(x, t) needs one, a constant one does not.\n\n\n\n\n\n","title":"FerriteOperators.update_operator!"},{"category":"type","location":"operator-api/#FerriteOperators.EvaluationFerriteOperator","page":"Operator API reference","text":"EvaluationFerriteOperator(engine, integrator)\n\nThe payload-free operator: an AssemblyEngine and the integrator that built it, with no matrix and no vector. Built by setup_evaluation_operator.\n\nIt serves every entry point that reads op.engine and writes nowhere the operator owns — evaluate_functional and evaluate_quadrature! in particular, whose sinks are the caller's value and the caller's QVector. The assembly entry points (update_operator!, update_linearization!, evaluate!) have no target here and say so instead of allocating one.\n\n\n\n\n\n","title":"FerriteOperators.EvaluationFerriteOperator"},{"category":"method","location":"operator-api/#FerriteOperators.allocate_components-Union{Tuple{N}, Tuple{Any, NTuple{N, Symbol}}} where N","page":"Operator API reference","text":"allocate_components(op, names::NTuple{N, Symbol}) -> NamedTuple of matrices\n\nAllocate N square system matrices for op that share ONE sparsity pattern (see share_pattern). Each is a plain system matrix — assemble into it through the ordinary entry points, fold the bag with combine!.\n\nThe shared pattern makes combine! a pure nzval operation, and is a contract: structural mutation of a component (dropzeros!, inserting an entry) breaks the whole bag. apply_zero!-style value mutation is fine.\n\ncomps = allocate_components(op, (:Ju, :Jdu))\nassemble_slot_jacobian!(comps.Ju,  op, JacobianKind{:u}(),  states, p, ctx)\nassemble_slot_jacobian!(comps.Jdu, op, JacobianKind{:du}(), states, p, ctx)\ncombine!(W, comps, (Jdu = 1 / Δt, Ju = 1.0))   # backward Euler Newton matrix\n\n\n\n\n\n","title":"FerriteOperators.allocate_components"},{"category":"method","location":"operator-api/#FerriteOperators.combine!-Tuple{SparseArrays.SparseMatrixCSC, NamedTuple, NamedTuple}","page":"Operator API reference","text":"combine!(W, comps::NamedTuple, weights::NamedTuple) -> W\n\nFold W = Σ weights[k] · comps[k] over the names in weights, a subset of comps. Values only: W and the components must share the sparsity pattern (checked), and W.nzval is overwritten. Real components with complex weights combine into a Complex W — what transformed (diagonalized) Radau needs per eigenvalue of the tableau inverse.\n\n\n\n\n\n","title":"FerriteOperators.combine!"},{"category":"method","location":"operator-api/#FerriteOperators.share_pattern-Union{Tuple{SparseArrays.SparseMatrixCSC}, Tuple{T}, Tuple{SparseArrays.SparseMatrixCSC, Type{T}}} where T","page":"Operator API reference","text":"share_pattern(A::SparseMatrixCSC, T = eltype(A)) -> SparseMatrixCSC\n\nA T-valued matrix aliasing A's colptr/rowval with a fresh zeroed nzval — \"same pattern group as A\". Pass T = ComplexF64 for the combination target of a transformed Radau stage.\n\n\n\n\n\n","title":"FerriteOperators.share_pattern"},{"category":"type","location":"operator-api/#FerriteOperators.StageBlockOperator","page":"Operator API reference","text":"StageBlockOperator(op, A, c, Δt)\n\nThe Newton block operator of an s-stage fully implicit Runge-Kutta scheme: stage i contributes the row F(t + cᵢΔt, zᵢ, kᵢ), zᵢ = uₙ + Δt Σⱼ aᵢⱼ kⱼ, so the block against the stage derivatives k is\n\nblock(i, j) = δᵢⱼ · Jdu⁽ⁱ⁾ + Δt · aᵢⱼ · Ju⁽ⁱ⁾ ,\n\nwith Ju⁽ⁱ⁾ = ∂F/∂u, Jdu⁽ⁱ⁾ = ∂F/∂du at stage i's state and time. All s² blocks come from those 2s components, which share one sparsity pattern (see allocate_components) — the tableau enters as coefficients, never as extra assembly. Fill them with assemble_stages!; mul! applies the block matrix over stage-stacked vectors of length s·n.\n\nnote: Transformed (simplified-Newton) Radau needs no dedicated machinery\nThe Hairer-Wanner transformation replaces the stage-dependent Jacobians by ONE pair (Ju, Jdu) at uₙ and diagonalizes A⁻¹, leaving one decoupled W_λ = Jdu + Δt·λ·Ju per eigenvalue, complex for the conjugate pairs. Build those from a component bag with share_pattern(Ju, ComplexF64) and a complex combine!, not with this operator.\n\n\n\n\n\n","title":"FerriteOperators.StageBlockOperator"},{"category":"method","location":"operator-api/#FerriteOperators.assemble_stages!-Tuple{StageBlockOperator, Any, AbstractVector{<:NamedTuple}, Any, AbstractVector}","page":"Operator API reference","text":"assemble_stages!(sbop, op, stage_states, p, ctxs) -> sbop\n\nAssemble the stage components of sbop: per stage i, one JacobianKind{:u}() sweep into Ju[i] and one JacobianKind{:du}() sweep into Jdu[i], both at stage_states[i] and ctxs[i]. The caller owns the stage arithmetic — stage_states[i] is (u = zᵢ, du = kᵢ, …) with both slots plain vectors, and ctxs[i] carries t + cᵢΔt and the stage's local interval.\n\n\n\n\n\n","title":"FerriteOperators.assemble_stages!"},{"category":"method","location":"operator-api/#FerriteOperators.nstages-Tuple{StageBlockOperator}","page":"Operator API reference","text":"Number of stages of the tableau.\n\n\n\n\n\n","title":"FerriteOperators.nstages"},{"category":"method","location":"operator-api/#LinearAlgebra.mul!-Tuple{AbstractVector, StageBlockOperator, AbstractVector}","page":"Operator API reference","text":"mul!(y, sbop::StageBlockOperator, x)\nmul!(y, sbop::StageBlockOperator, x, α, β)\n\nAction of the stage-block matrix on the stage-stacked vector x (s blocks of length n): yᵢ = Jdu⁽ⁱ⁾ xᵢ + Δt · Ju⁽ⁱ⁾ Σⱼ aᵢⱼ xⱼ.\n\n\n\n\n\n","title":"LinearAlgebra.mul!"},{"category":"function","location":"operator-api/#FerriteOperators.check_derivatives","page":"Operator API reference","text":"check_derivatives(op, states, p, ctx = nothing; h = cbrt(eps(Float64)),\n                  rtol = 1e-5, atol = 1e-8, nprobes = 3,\n                  weights = nothing) -> (passed, checks)\n\nCross-check the operator's derivative paths against central finite differences of its own residual, through the entry points solvers use — a wrong analytic kernel (provides_analytic) fails against the FD referee. Checks: Jacobian action along nprobes deterministic directions, fused-vs-split residual consistency, parameter Jacobian (per flat-θ column) and VJP, matrix-free state JVP/VJP, and — when ctx is given — the time sensitivity against FiniteDifferenceSensitivity, seeded at the context's evaluation time.\n\nweights (per-slot, e.g. (u = 1.0, du = 1/(γ*Δt))) adds two more: weighted_jacobian probes the fused Σₛ wₛ ∂F/∂s against per-slot finite differences, and weighted_jacobian_routes requires the fused and composed routes of assemble_weighted_jacobian! to agree — the caller cannot tell which route an operator takes, so the two must be interchangeable. Both skip without weights, with complex weights (the FD referee is real), or when a participating slot carries an AffineRate source.\n\nThe FD referee evaluates the FULL residual, boundary terms included. A facet_items term enters the sensitivity sweeps analytically or not at all, so a failing parameter, time, or state-product check on an operator carrying one is the diagnostic for a surface cache missing that kind's facet kernel.\n\nchecks holds one (passed, err, skipped) entry per check, with the reason recorded on a skip; passed is the conjunction of the non-skipped ones. The caller's vectors are never mutated. Condensed operators (unknown_size(op) > residual_size(op)) are probed along the field dofs only; the FD evaluations exercise the full local solves, so the check validates the consistent condensed tangent.\n\nSupports LinearizedFerriteOperator only.\n\n\n\n\n\n","title":"FerriteOperators.check_derivatives"},{"category":"type","location":"operator-api/#FerriteOperators.CondensationKind","page":"Operator API reference","text":"CondensationKind{T}(weights)\n\nThe kind condense_internal! rides: a FunctionalFamily kind whose kernel ALSO writes back — the one combination the three built-in driver bodies (primal_cell_sweep!, sensitivity_cell_sweep!, functional_cell_sweep) don't have. weights are condense_cell!'s per-slot scalars, or nothing for the residual-only election; T fixes the report's functional_value_type so a device reduction can preallocate typed partials.\n\n\n\n\n\n","title":"FerriteOperators.CondensationKind"},{"category":"type","location":"operator-api/#FerriteOperators.CondensationReport","page":"Operator API reference","text":"CondensationReport{T}\n\nPer-item summary of a condense_internal! sweep, isbits by construction so the same mechanism serves a device sweep. converged replaces a thrown non-convergence exception with data a caller can act on without crossing a device boundary.\n\nfield monoid op meaning\nconverged & did every local problem reach its tolerance\nsolves + local problems attempted\niterations + total inner iterations\nworst_iterations max worst per-quadrature-point iteration count\nworst_cell argmax carrier the cell (or, negated, the algebraic item) attaining worst_iterations\nworst_qp argmax carrier the quadrature point attaining worst_iterations; 0 for an algebraic item, which has no quadrature points\nworst_residual max largest local-residual magnitude at exit\ndt_factor min 1 means \"no reduction requested\"; < 1 is a request\n\nworst_cell is FAMILY-DISAMBIGUATED by sign, since condense_cell! and condense_algebraic! reports fold into the same total and a cellid and an item index are both small positive integers: a cell reports its cellid (≥ 1), condense_algebraic! reports -item.index (≤ -1), and 0 means neither — everything converged in zero iterations, which is what the type's zero and every closed-form local solve reports.\n\nReports combine with +: a commutative monoid except the argmax tie-break, which keeps the FIRST contribution in fold order — the determinism fold_items already states for a fixed worker count. The identity is zero(CondensationReport{T}).\n\n\n\n\n\n","title":"FerriteOperators.CondensationReport"},{"category":"method","location":"operator-api/#FerriteOperators.allocate_internal_jacobian-Tuple{Any}","page":"Operator API reference","text":"allocate_internal_jacobian(op) -> SparseMatrixCSC\n\nAllocate the rectangular ∂F/∂q target of a condensed operator, residual_size(op) × ndofs(op.engine.ivh), over the pattern init_internal_jacobian_sparsity_pattern builds. Pass the result to update_internal_jacobian!, or hand that entry point any matrix sharing the pattern.\n\n\n\n\n\n","title":"FerriteOperators.allocate_internal_jacobian"},{"category":"method","location":"operator-api/#FerriteOperators.commit_state!-Tuple{Any, AbstractVector, AbstractVector}","page":"Operator API reference","text":"commit_state!(op, u, committed)\n\nAccept a converged trial: copy u into the committed solution committed. Correctors stay valid — the committed point is the last condensed point, so no invalidation is needed.\n\n\n\n\n\n","title":"FerriteOperators.commit_state!"},{"category":"method","location":"operator-api/#FerriteOperators.condensation_cell_sweep!-Tuple{CondensationKind, Any, Any}","page":"Operator API reference","text":"condensation_cell_sweep!(kind::CondensationKind, task, ws) -> CondensationReport\n\nThe cell family's condensation driver body: value-returning WITH write-back. Gathers the declared slots, hands them to condense_cell!, and copies the element-local q buffer it filled into the item's slice of the :q slot's global vector, through the cell's internal_variable_range.\n\n\n\n\n\n","title":"FerriteOperators.condensation_cell_sweep!"},{"category":"function","location":"operator-api/#FerriteOperators.condense_algebraic!","page":"Operator API reference","text":"condense_algebraic!(cache, args::AlgebraicArgs, weights::NamedTuple) -> CondensationReport\n\nThe condense_cell! counterpart for the algebraic-item family: solve the item's local problem, write the trial state into args.states.q (never into args.states.u), and store the corrector the cache's own Consistent kernel reads — item-keyed (an ItemStates store indexed by args.item.index, the mechanism a condensed cell cache uses with a cellid), or nothing at all under a Recompute election. weights are the solver's chain-rule scalars, exactly as for condense_cell!.\n\nweights === nothing is the residual-only election, with the same meaning it has for condense_cell!: solve for q, form no corrector.\n\nReport the item in worst_cell as -args.item.index (see CondensationReport's family-disambiguation convention), never the raw index — a positive value there is read as a cellid.\n\nThere is no default; only a condensed algebraic cache implements it, and it is called only when the cache declares has_internal_state.\n\nAnalytic-first: a condensed algebraic cache admits Consistent sensitivity/Jacobian kinds only by serving them analytically or by declaring internal_state_insensitive. There is no generic AD Consistent bootstrap for this family (unlike ADElementCache's condensed_corrector combination for condensed cells): an algebraic item has no cellid to key a corrector store by.\n\n\n\n\n\n","title":"FerriteOperators.condense_algebraic!"},{"category":"function","location":"operator-api/#FerriteOperators.condense_cell!","page":"Operator API reference","text":"condense_cell!(cache, args::CellArgs, weights::NamedTuple) -> CondensationReport\ncondense_cell!(cache, args::CellArgs, ::Nothing) -> CondensationReport\n\nElement hook run once per item by condense_internal!: solve every quadrature point's local problem, write the trial state into args.states.q (never into args.states.u), and — under a Stored election — store the corrector: whatever compact per-quadrature-point quantity the cache's own Consistent kernel later reads (Tier 1), or the completed local block a generic combination would need (Tier 2). Under Recompute nothing is stored and the corrector is re-derived at every point of use. weights are the solver's chain-rule scalars for reconstructed slots participating in the local model (e.g. a rate slot under Newmark), chained into the corrector INSIDE the local inverse, where post-hoc weighting of separated partial Jacobians cannot put them; being per-sweep solver data rather than item data, a recomputing element retains them per cache.\n\nweights === nothing is the RESIDUAL-ONLY election: solve for q and form no corrector at all. It exists because a sweep that only evaluates the residual reads q and never a corrector, so forming one is wasted work — see condense_internal! for the staleness the election buys with it. The q this branch writes must be BIT-IDENTICAL to the one the weighted branch writes: the election governs what is formed after the local solve, never the solve. Implementing it is optional — an element without a ::Nothing method simply cannot be condensed residual-only, and says so as a MethodError.\n\nThis is the only hook allowed to EVOLVE a condensed element's state: assemble_cell! kernels are pure evaluations at the q this hook wrote and never write back. An inline local solve inside a kernel stays legitimate when it is history-free — a pure function of the gathered args, recomputable outside a sweep — but re-solving from committed history inside a kernel has nowhere to write the result and silently freezes that history instead of advancing it.\n\nThere is no default; only condensed elements implement it.\n\n\n\n\n\n","title":"FerriteOperators.condense_cell!"},{"category":"method","location":"operator-api/#FerriteOperators.condense_internal!-Tuple{Any, Union{Nothing, NamedTuple}, NamedTuple, Any, Any}","page":"Operator API reference","text":"condense_internal!(op, weights::Union{NamedTuple, Nothing}, states::NamedTuple, p, ctx) -> CondensationReport\ncondense_internal!(op, states, p, ctx)\n\nSolve every condensed element's local problem over the WHOLE domain, write the trial q into the [ū; q] tail, store each item's corrector (under a Stored election), and report what happened. This is the only writer of q, and it must run before any Consistent-mode sweep, whatever the election: a sweep is a pure evaluation at whatever q the tail currently holds.\n\nUnder Stored(), reading a never-condensed or invalidated (rollback_state!) item throws, naming the item, through item_state's freshness contract on the element's corrector store. That catches \"never condensed\" and \"invalidated since\", but NOT the same vector mutated in place without going through rollback_state! — see what the phase concedes. Under Recompute there is no store to stamp, so none of these are detected and the ordering requirement is the caller's alone.\n\nweights are the solver's chain-rule scalars passed through to condense_cell!; the 4-argument form defaults them to (u = 1.0,).\n\nweights === nothing elects a RESIDUAL-ONLY condensation: every local problem is still solved and q is still written — bit-identically, the election never touches the solve — but no corrector is formed. A residual sweep reads q and no corrector, so on that route the corrector is pure waste. What the election costs is the corrector's validity: every element's store is invalidated up front, so a Consistent sweep at this state throws through item_state instead of combining correctors belonging to some earlier trial point. Condensing again WITH weights restores them.\n\nA Recompute cache has no store for the invalidation to act on, so the election protects it only as far as it is genuinely stateless. An element that retains ANY per-sweep solver data to recompute from — the weights themselves, typically — holds corrector state under another name and should refuse the election rather than recompute from a weight no residual-only sweep supplied.\n\nValue-returning WITH write-back, riding FunctionalFamily through fold_items/reduce_on_device/reduce_on_subdomains, so the deterministic fold order and the _check_reduction_domain structural checks hold for it too. Always its own domain traversal, run before the evaluation sweeps it feeds.\n\n\n\n\n\n","title":"FerriteOperators.condense_internal!"},{"category":"method","location":"operator-api/#FerriteOperators.corrector_election-Tuple{Any}","page":"Operator API reference","text":"corrector_election(integrator) -> StorageElection\n\nWhere a condensed element's Jacobian correction comes from — the StorageElection vocabulary applied to the per-item corrector, and an operator-construction choice of the INTEGRATOR (Stored() by default; a condensed integrator overrides this to report its own constructor-elected value).\n\nStored keeps the corrector the condensation sweep computed, per quadrature point. Recompute keeps none and re-derives it from the item's current (u, q) wherever a Consistent-mode kernel needs one, trading the store's memory for one extra evaluation of the element's local slopes per Jacobian-shaped sweep — the right election for a memory-bound ASSEMBLED sweep at scale, the wrong one for a Krylov mul!/JVP sequence at a fixed state. ElementAssembly is not a corrector election and no condensed element implements it.\n\nRecomputation is EXACT, not approximate: the corrector is a closed-form function of the converged pair (u, q) — the implicit-function-theorem slopes of the element's local conditions — so it is the same quantity condense_cell! would have stored, at the same point. An element chaining weights into its corrector retains them per cache, which is O(1) and not the storage this election trades away.\n\nThe election is invisible to kernels: a Consistent kernel reads its corrector through ONE access point, which either reads the store or recomputes. condensed_corrector is that access point for the AD decorator's generic combination, which is why it receives the args record.\n\nFreshness under Recompute: nothing is stored, so nothing can go stale and rollback_state! has no correctors to drop (restoring u restores its q tail with it). The q-freshness contract is unchanged — q is written only by condense_internal!, so a Consistent sweep still requires a condensation at the current trial point. What is lost is the DETECTION of a missing one: Stored throws through item_state's freshness contract on a never-condensed or invalidated item, while a recomputing kernel silently derives a corrector from whatever q the tail currently holds.\n\n\n\n\n\n","title":"FerriteOperators.corrector_election"},{"category":"method","location":"operator-api/#FerriteOperators.corrector_election_error-Tuple{Any}","page":"Operator API reference","text":"corrector_election_error(election)\n\nThe loud, self-naming rejection a condensed integrator's constructor raises for a StorageElection it does not implement, so the seam exists without silently accepting a selection the element cannot honor.\n\n\n\n\n\n","title":"FerriteOperators.corrector_election_error"},{"category":"method","location":"operator-api/#FerriteOperators.init_internal_jacobian_sparsity_pattern-Tuple{Any}","page":"Operator API reference","text":"init_internal_jacobian_sparsity_pattern(engine) -> SparsityPattern\n\nThe ndofs(dh) × ndofs(ivh) pattern of ∂F/∂q: for every item carrying condensed internal state, the product of the dofs its local system spans with its own internal_variable_range. Cell subdomains and condensed algebraic items both contribute, their column blocks lying where the [ū | q_cells | q_items] layout puts them.\n\n\n\n\n\n","title":"FerriteOperators.init_internal_jacobian_sparsity_pattern"},{"category":"method","location":"operator-api/#FerriteOperators.internal_jacobian_cell_sweep!-Union{Tuple{C}, Tuple{JacobianKind{:q, C}, Any, Any}} where C","page":"Operator API reference","text":"internal_jacobian_cell_sweep!(kind::JacobianKind{:q}, task, ws)\n\nThe ∂F/∂q driver body of the cell family: primal_cell_sweep! over a RECTANGULAR local block instead of the square ws.Ke, scattered by the two-index assemble!(assembler, rowdofs, coldofs, Kqₑ). An item owning no internal dofs contributes nothing and never reaches a kernel, which is what lets a plain subdomain share an operator with a condensed one.\n\n\n\n\n\n","title":"FerriteOperators.internal_jacobian_cell_sweep!"},{"category":"method","location":"operator-api/#FerriteOperators.invalidate_correctors!-Tuple{Any}","page":"Operator API reference","text":"invalidate_correctors!(cache)\n\nDrop every item's condensation corrector, so the cache's Consistent kernel throws on its next read until condense_internal! runs again. The default is a no-op; only caches carrying a corrector store override it — a Recompute cache has none and keeps the default. Called by rollback_state!.\n\n\n\n\n\n","title":"FerriteOperators.invalidate_correctors!"},{"category":"function","location":"operator-api/#FerriteOperators.local_conditions!","page":"Operator API reference","text":"local_conditions!(L, cache, args) -> L\n\nThe element's LOCAL CONDITIONS L(u, q, θ, t) = 0 for the item args stands on: the implicit equations condense_cell! solves for q, EVALUATED (never solved) at the args' current state and written into L, whose length is the item's condensed internal dof count and whose ordering is the :q slot's.\n\nOptional, and the third acceptance branch of the internal-state admissibility rule (assert_sensitivity_admissible): without an analytic kernel or an internal_state_insensitive declaration, a condensed cache is refused ParameterJacobianKind/ParameterVJPKind/TimeSensitivityKind. Given this hook, ADElementCache completes the total generically — differentiating local_conditions! for ∂L/∂q (factorized once per item), ∂L/∂θ and ∂L/∂t, then closing the implicit function theorem against the ∂F/∂q block JacobianKind{:q} already gives:\n\ndq/dθ = −(∂L/∂q)⁻¹ ∂L/∂θ,      dF/dθ = ∂F/∂θ|_q + ∂F/∂q · dq/dθ\n\nand likewise with t in place of θ. An analytic kernel still wins where the cache declares one.\n\nContract: L is the residual form of exactly the equations condense_cell! converged, a PURE function of (args.states, args.p, args.ctx) with no solve and no write-back, and eltype-generic — it is what gets differentiated, so q, the parameters and the evaluation time all reach it Dual-valued. Cell caches only: the ∂F/∂q block the combination multiplies is sized from getnquadpoints, which the algebraic-item family has no counterpart for.\n\nwarning: Experimental surface\nThe local-model seam is a CANDIDATE contract, not a frozen one: this signature may change in a minor release. The assembled results of the kinds it admits are not affected.\n\n\n\n\n\n","title":"FerriteOperators.local_conditions!"},{"category":"method","location":"operator-api/#FerriteOperators.rollback_state!-Tuple{Any, AbstractVector, AbstractVector}","page":"Operator API reference","text":"rollback_state!(op, u, committed)\n\nDiscard a rejected trial: copy the committed solution back into u and invalidate every condensation corrector the operator's element caches carry — they were computed for the discarded trial q, which u no longer carries. The next Consistent sweep needs condense_internal! again. This is the one mutation FerriteOperators itself sees: the solver's own u .+= Δu happens outside the package and is invisible to it (see what the phase concedes).\n\nA Recompute cache carries no correctors, so the invalidation is a no-op: u and its q tail are restored together, and a corrector re-derived from that pair is the committed point's own.\n\n\n\n\n\n","title":"FerriteOperators.rollback_state!"},{"category":"method","location":"operator-api/#FerriteOperators.update_internal_jacobian!-Tuple{AbstractMatrix, Any, NamedTuple, Any, Any}","page":"Operator API reference","text":"update_internal_jacobian!(Kq, op, states, p, ctx)\n\nAssemble ∂F/∂q into Kq — the residual_size(op) × ndofs(op.engine.ivh) block coupling the residual to the CONDENSED internal state, evaluated at the trial state. Kq must share the pattern allocate_internal_jacobian builds. Never writes back into states.\n\nThis is the block a Schur-complement consumer wants, and the ∂F/∂q factor of the corrector combination ∂F/∂· |_q + ∂F/∂q · dq/d·. Elements serve it through the analytic assemble_cell!(::JacobianRequest{:q}, …) kernel or by ForwardDiff seeding of the :q slot — which needs no admissibility guard, q being the seed itself, so Consistent and FrozenQ coincide here.\n\nAn operator without condensed unknowns is rejected: ndofs(ivh) == 0 means there is no column space to assemble into.\n\n\n\n\n\n","title":"FerriteOperators.update_internal_jacobian!"},{"category":"type","location":"operator-api/#FerriteOperators.AbstractTransferElementCache","page":"Operator API reference","text":"AbstractTransferElementCache\n\nSupertype for element caches used in transfer-operator assembly.\n\nRequired:\n\nassemble_transfer_element!(Pe, tc, element_cache, p)\n\nwith tc a SameGridCellCache or NestedGridCellCache and Pe the pre-allocated (nrdofs_per_cell × ncdofs_per_cell) element matrix.\n\n\n\n\n\n","title":"FerriteOperators.AbstractTransferElementCache"},{"category":"type","location":"operator-api/#FerriteOperators.AbstractTransferIntegrator","page":"Operator API reference","text":"AbstractTransferIntegrator\n\nSupertype for integrators producing element-local rectangular matrices — contributions to a transfer (prolongation / restriction) operator between two DofHandlers.\n\nRequired: setup_transfer_element_cache(integrator, sdh_row::SubDofHandler, sdh_col::SubDofHandler), returning an AbstractTransferElementCache.\n\n\n\n\n\n","title":"FerriteOperators.AbstractTransferIntegrator"},{"category":"type","location":"operator-api/#FerriteOperators.TransferFerriteOperator","page":"Operator API reference","text":"TransferFerriteOperator\n\nA transfer (prolongation / restriction) operator assembled as a rectangular sparse matrix P of size (nrdofs × ncdofs). Update via update_operator!, apply with mul!(out, op, x[, α, β]).\n\nBoth transfer geometries build this one type: setup_transfer_operator for two DofHandlers on the SAME grid, setup_nested_transfer_operator for hierarchically nested grids (geometric multigrid). They differ in the cell cache that walks the pair — SameGridCellCache vs NestedGridCellCache — not in what is assembled, so dh_row/dh_col are the fine and coarse handlers in the nested case.\n\nwarning: Experimental surface\nThe transfer constructors and operator types may change in a minor release; the assembled matrix and its sparsity are not affected.\n\n\n\n\n\n","title":"FerriteOperators.TransferFerriteOperator"},{"category":"type","location":"operator-api/#FerriteOperators.TransferWorkspace","page":"Operator API reference","text":"TransferWorkspace\n\nPer-worker workspace for transfer assembly: element cache, pre-allocated rectangular element matrix, and transfer cell cache.\n\n\n\n\n\n","title":"FerriteOperators.TransferWorkspace"},{"category":"method","location":"operator-api/#FerriteOperators.allocate_transfer_element_matrix-Tuple{AbstractTransferElementCache, Any, Any}","page":"Operator API reference","text":"allocate_transfer_element_matrix(element_cache, sdh_row, sdh_col) -> AbstractMatrix\n\nThe per-worker Pₑ buffer assemble_transfer_element! fills, defaulting to a dense (ndofs_per_cell(sdh_row) × ndofs_per_cell(sdh_col)) matrix. A cache needing a different element type or layout specializes this.\n\n\n\n\n\n","title":"FerriteOperators.allocate_transfer_element_matrix"},{"category":"function","location":"operator-api/#FerriteOperators.assemble_transfer_element!","page":"Operator API reference","text":"assemble_transfer_element!(Pₑ, tc, element_cache, pₑ)\n\nFill the rectangular element matrix Pₑ with this cell pair's transfer contribution. tc is the transfer cell cache positioned on the pair (SameGridCellCache or NestedGridCellCache), pₑ the element-local parameter view from query_cell_parameters. Pₑ arrives zeroed and is scattered to (getrowdofs(tc), getcolumndofs(tc)).\n\n\n\n\n\n","title":"FerriteOperators.assemble_transfer_element!"},{"category":"function","location":"operator-api/#FerriteOperators.setup_transfer_element_cache","page":"Operator API reference","text":"setup_transfer_element_cache(integrator, sdh_row::SubDofHandler, sdh_col::SubDofHandler)\n\nBuild the AbstractTransferElementCache serving one (row, column) subdomain pair — the transfer analogue of setup_element_cache, run once per pair at setup.\n\n\n\n\n\n","title":"FerriteOperators.setup_transfer_element_cache"},{"category":"method","location":"operator-api/#FerriteOperators.update_operator!-Tuple{TransferFerriteOperator, Any}","page":"Operator API reference","text":"update_operator!(op::TransferFerriteOperator, p)\n\nReassemble the rectangular transfer matrix op.P from scratch.\n\n\n\n\n\n","title":"FerriteOperators.update_operator!"},{"category":"section","location":"elements/#Writing-elements","page":"Writing elements","text":"An element consists of an integrator (its setup-time description), a cache, and request-typed kernels. The residual kernel is mandatory (validated at setup); everything else is derived from it by ForwardDiff unless an analytic kernel is declared.\n\nstruct MyIntegrator <: AbstractNonlinearIntegrator\n    qrc::QuadratureRuleCollection\n    field_name::Symbol\nend\n\nstruct MyCache{CV <: CellValues} <: AbstractVolumetricElementCache\n    cv::CV\nend\n\nfunction FerriteOperators.setup_element_cache(m::MyIntegrator, sdh::SubDofHandler)\n    qr = getquadraturerule(m.qrc, sdh)\n    T  = element_value_type(m.qrc)\n    ip = Ferrite.getfieldinterpolation(sdh, m.field_name)\n    ip_geo = FerriteOperators.geometric_subdomain_interpolation(sdh)\n    return MyCache(CellValues(T, qr, ip, ip_geo))\nend\nFerriteOperators.element_value_type(c::MyCache) = element_value_type(c.cv)\nFerriteOperators.duplicate_for_device(device, c::MyCache) =\n    MyCache(FerriteOperators.duplicate_for_device(device, c.cv))\n\nFerriteOperators.reinit_values!(c::MyCache, cell) = reinit!(c.cv, cell)\n\nfunction FerriteOperators.assemble_cell!(req::ResidualRequest, cache::MyCache, args::CellArgs)\n    (; cv) = cache\n    uₑ = args.states.u\n    for qp in 1:getnquadpoints(cv)\n        dΩ = getdetJdV(cv, qp)\n        # ... accumulate into req.r ...\n    end\nend\n\nsetup_operator wraps a cache lacking analytic coverage of a kind in ADElementCache automatically, and the engine calls assemble_cell! on the resolved cache unconditionally — it never forks between an analytic kernel and the AD fallback itself.\n\nThe residual kernel must be eltype-generic in eltype(args.states.*), eltype(args.p), and the context time — that is the entire AD contract. Kernels never write global state; the geometry cache in args.cell is read-only.\n\nAn element is a scheme-agnostic integrand: it reads slot values and never encodes a time discretization. The layer contract states where each piece of information a kernel might want belongs.","title":"Writing elements"},{"category":"section","location":"elements/#Storage-classes-for-elements-with-local-problems","page":"Writing elements","text":"A cache carries data of three different lifetimes, and duplicate_for_device treats each one differently:\n\nImmutable problem structure — meshes, handlers, factorizable sparsity patterns — is built once in setup_element_cache from data the integrator carries. duplicate_for_device may deliberately ALIAS it across workers: sharing read-only state per worker is legal and intended, not an oversight.\nPer-worker mutable solve workspace (a scratch buffer, a nonlinear solver's iteration state) lives as ordinary cache fields and is duplicated — not aliased — per worker, so concurrent workers never race on it.\nPer-item/per-QP state that must persist across sweeps (a retained factorization, a converged local value used as the next sweep's guess) lives in an ItemStates cache field, which duplicate_for_device deliberately aliases too: entries are indexed by item, and the cell partition assigns each item to exactly one worker at a time, so the aliased slots a worker touches are disjoint from every other worker's.\n\nassemble_cell! kernels are pure evaluations at fixed internal state — they are phase two of the two-stage protocol. A local nonlinear solve that EVOLVES state — anything whose result must survive to the next sweep or the next step — belongs in condense_cell! only, never inline inside a kernel.\n\nAn inline local solve of a history-free (stateless) implicit relation stays legitimate inside a kernel: if its root is a pure function of the gathered args alone, it is recomputable from args at any later time, including from postprocessing (evaluate_quadrature! is the route to re-run it outside a sweep). Differentiating such an inline iteration by AD is then the kernel author's responsibility — accuracy is limited by the inner solve's own tolerance; the clean upgrade once that limit matters is an analytic tangent via the implicit function theorem, the same shape condense_cell! uses for a corrector.\n\nInline STATE EVOLUTION has no error to catch it: a kernel receives q as an already-gathered slot and never writes the global solution vector, so an element that re-solves from committed history inside its assemble_cell! kernel has nowhere to put the new state — it silently freezes the committed state across steps instead of advancing it. State that must survive a step goes through condense_cell!. State kept in a private cache field instead of an ItemStates store puts commit/rollback/freshness entirely on the element author: rollback_state!/ commit_state! and the freshness guard in item_state cannot see private storage.","title":"Storage classes for elements with local problems"},{"category":"section","location":"elements/#Analytic-kernels","page":"Writing elements","text":"Analytic kernels are an opt-in, declared through a compile-time trait so no hasmethod probe reaches the hot loop:\n\nFerriteOperators.provides_analytic(::Type{<:MyCache}, ::FerriteOperators.JacobianKind) = true\nfunction FerriteOperators.assemble_cell!(req::JacobianRequest{:u}, cache::MyCache, args::CellArgs)\n    # ... accumulate into req.K ...\nend\n\nThere is exactly one root method for provides_analytic, so a specialization is always strictly more specific and a blanket declaration cannot create an ambiguity. setup_operator checks trait against kernel per element cache: a kind the trait claims without a matching assemble_cell! method is a loud ArgumentError at setup, never a silent fallback to AD.\n\nA cache lacking analytic coverage of some kind is wrapped in ADElementCache at setup_operator time (ad_backend = ForwardDiffAD() by default; ad_backend = nothing opts out). Per request the decorator forwards to the inner's own kernel where declared and differentiates the residual kernel otherwise — this is a construction-time, per-cache resolution, not a per-cell branch: element files need not know decoration exists. A composite wraps its non-analytic inners as ONE sub-composite rather than each individually (naive per-inner wrapping costs one full seeding pass per wrapped inner, worse than not wrapping at all once two or more inners need it).\n\nprovides_analytic means exactly \"there is a hand-written kernel\", also on a wrapped cache. Whether the RESOLVED cache serves a kind at all (hand kernel, AD, or generic completion) is the internal FerriteOperators.serves_kind; element authors only ever declare provides_analytic.\n\nThe available request types are ResidualRequest, JacobianRequest, JacobianResidualRequest (the fused Newton path), WeightedJacobianRequest (a scheme's combined matrix — see weighted Jacobians), and the sensitivity requests ParameterJacobianRequest, ParameterVJPRequest, TimeSensitivityRequest, StateJVPRequest, StateVJPRequest.","title":"Analytic kernels"},{"category":"section","location":"elements/#The-args-bundle","page":"Writing elements","text":"args is a CellArgs for cell kernels and a FacetArgs for facet kernels — the same four fields, no supertype between the two. Annotating the parameter (args::CellArgs) is permitted; kernels select on the (request, cache) pair, never on args.\n\nargs.states — NamedTuple of element-local state buffers, one per slot declared at setup (setup_operator(...; slots = (:u, :uprev))). A slot's source decides how it gathers: a plain vector reads celldofs(cell), AffineRate reconstructs over that same field-space gather, and InternalSource restricts to a cell's condensed internal-dof range — the mechanism that makes a condensed element's internal state q an ordinary slot (see Condensed elements).\nargs.cell — the geometry cache of the current item, read-only.\nargs.p — the user parameter bag, produced by the overridable query_cell_parameters (facets get their own query_facet_parameters per facet). Configuration only: time lives in ctx, history in slots.\nargs.ctx — the per-sweep solver scalars, i.e. the TimeIntegrationContext (t, Δt, γ̃) read through evaluation_time(args.ctx) and stage_scaling(args.ctx), or nothing for stationary problems. This is the one open channel: a scheme with richer per-sweep scalars passes its own context type. γ̃ is the normalized local stage interval of the element-local internal-variable problem — see its docstring for the exact contract and for why it is not a rate slope.\n\nA derivative sweep rebuilds args with one field replaced instead of re-deriving it from scratch: with_states, with_parameters and with_context.","title":"The args bundle"},{"category":"section","location":"elements/#Values-and-reinitialization","page":"Writing elements","text":"Elements own their values objects (CellValues etc.) and implement reinit_values!: the mandatory two-arg method reinitializes all of them; specializing the kind-dispatched three-arg form reinitializes only what that request needs — an element may carry several values objects, and not every request needs all of them. The loop owns the geometry cache reinit only.\n\nKernels are pure evaluation: repeated kernel invocations within one sweep (AD chunk passes, split Jacobian-then-residual fallbacks) do not reinitialize again. Facet kernels reinitialize their own FacetValues per facet, since the local facet index is theirs.","title":"Values and reinitialization"},{"category":"section","location":"elements/#Evaluation-precision","page":"Writing elements","text":"The integrator decides the precision its elements evaluate in. The engine passes no scalar type into setup_element_cache; the election travels on the integrator's own data, conventionally its quadrature collection:\n\nintegrator = MyIntegrator(QuadratureRuleCollection(Float32, 2), :u)\n\ngetquadraturerule then returns a Float32 rule, and element_value_type reads the election back off the collection. Spelling T into the values object is REQUIRED: Ferrite's CellValues(qr, ip, ip_geo) is Float64 whatever the rule carries, so an element that elects a precision writes CellValues(T, qr, ip, ip_geo).\n\nElement-local buffers follow the element. The allocate_element_* defaults size themselves in element_value_type of the CACHE, Float64 unless the cache declares otherwise:\n\nFerriteOperators.element_value_type(c::MyCache) = element_value_type(c.cv)\n\nThe device and the operator specification govern the GLOBAL system, and nothing else: value_type(device) and the specification's matrix_type are the scalar of the system matrix and vector the operator assembles into (see GPU assembly).\n\nThe two sides being independent is what makes MIXED PRECISION expressible:\n\nTerms of one operator may evaluate at different precisions — each integrator carries its own collection — and a composite's local system takes their promotion.\nThe local precision need not be the global one; the scatter converts entry-wise either way.\nFloat32 values with Float64 accumulation is an element-level choice: declare the values object in Float32 and override the allocate_element_* hooks to return Float64 buffers.","title":"Evaluation precision"},{"category":"section","location":"elements/#Facets","page":"Writing elements","text":"A boundary term declares the facets it is supported on (facet_items) and gets its own traversal. The framework walks the declared facets, queries facet parameters per facet, and hands the sweep's request to the facet kernel. Facet items covers the declaration; this section is the kernel contract.\n\nfunction FerriteOperators.assemble_facet!(req::ResidualRequest, cache::MyFacetCache, args::FacetArgs, lfi::Int)\n    reinit!(cache.fv, args.cell, lfi)\n    # accumulate into req.r; args.p came from query_facet_parameters(cache, cell, lfi, p)\nend\n\nFacet contributions have no AD fallback in any sweep: a surface cache serves the sweep's request analytically or not at all. A cache serving a weighted sweep therefore implements assemble_facet! for WeightedJacobianRequest; per-slot facet kernels are never composed behind the kernel's back.","title":"Facets"},{"category":"section","location":"elements/#Elements-with-global-dofs","page":"Writing elements","text":"Some unknowns belong to no cell: the macroscopic strain of a stress-driven RVE, a lumped chamber pressure coupling a whole surface, a Lagrange multiplier enforcing an integral constraint. Ferrite gives them dofs through AlgebraicVariable, numbered after the spatial dofs and never appearing in celldofs. An element declares which of them its local system carries:\n\nFerriteOperators.global_dofs(m::MyIntegrator, sdh::SubDofHandler) = algebraic_dofs(sdh.dh, :εbar)\n\nThe declaration lives on the integrator, one per subdomain, and is resolved once at setup — before any cache is built. It augments the cell family: the subdomain's volumetric kernel. A term supported on a facet set declares its own tail through facet_item_global_dofs instead (see Facet items), which is what keeps this sweep in the field space. The local layout is then a contract:\n\n[ celldofs(cell) ; the declared global dofs, in declaration order ]\n\nso the tail occupies global_dof_range. The framework passes no extra channel and CellArgs/FacetArgs keep their four fields; an element cache resolves its own range at setup and stores it:\n\nstruct MyCache{CV, AV} <: AbstractVolumetricElementCache\n    cv::CV\n    av::AV                    # Ferrite's AlgebraicValues for the variable\n    range_u::UnitRange{Int}\n    range_ε::UnitRange{Int}   # where the global dofs sit in the local system\nend\n\nfunction FerriteOperators.setup_element_cache(m::MyIntegrator, sdh::SubDofHandler)\n    ip = Ferrite.getfieldinterpolation(sdh, m.field_name)\n    cv = CellValues(element_value_type(m.qrc), getquadraturerule(m.qrc, sdh), ip,\n                    FerriteOperators.geometric_subdomain_interpolation(sdh))\n    return MyCache(cv, AlgebraicValues(m.variable), dof_range(sdh, m.field_name), global_dof_range(m, sdh))\nend\n\nfunction FerriteOperators.assemble_cell!(req::ResidualRequest, c::MyCache, args::CellArgs)\n    uₑ = args.states.u                                   # length ndofs_per_cell + 3\n    ε̄  = sum(uₑ[J] * algebraic_basis_value(c.av, jε) for (jε, J) in pairs(c.range_ε))\n    for qp in 1:getnquadpoints(c.cv)\n        dΩ = getdetJdV(c.cv, qp)\n        σ  = c.E ⊡ (ε̄ + function_symmetric_gradient(c.cv, qp, uₑ, c.range_u))\n        for (iu, I) in pairs(c.range_u)\n            req.r[I] += (shape_symmetric_gradient(c.cv, qp, iu) ⊡ σ) * dΩ\n        end\n        for (iε, I) in pairs(c.range_ε)\n            Eᵢ = algebraic_basis_value(c.av, iε)\n            req.r[I] += (Eᵢ ⊡ σ - c.σ̄ ⊡ Eᵢ) * dΩ\n        end\n    end\nend\n\nEverything the engine sizes follows the declaration: Ke, re, the slot buffers, the sensitivity buffers, and the ForwardDiff seeds are padded by the declared count, so an AD fallback differentiates the FULL augmented system — the ε̄-ε̄ block included. allocate_element_matrix and friends keep meaning the FIELD space, so an element overriding them states ndofs_per_cell and never the augmented size.\n\nThe sparsity is the caller's declaration. FerriteOperators does not infer which entries the coupling creates from the dof declaration — whether every cell couples to the variable, or only one facet set, is a modelling statement only the caller can make. It travels as a Ferrite coupling descriptor on the operator specification:\n\ncoupling = CellCoupling(1:getncells(grid); algebraic_coupling = ((:u, :εbar), (:εbar, :εbar)))\nspec     = StandardOperatorSpecification(; algebraic_couplings = (coupling,))\nstrategy = AssemblyStrategy(FullAssembly(spec), SequentialScheduling(), SequentialCPUDevice())\n\nA missing descriptor is not silent: it surfaces as Ferrite's missing-sparsity-entry error on the first assembly.\n\nSetup raises the restrictions this layout implies. ColoredScheduling is rejected — coloring works by giving no two items of a color a shared dof, and a declared global dof is shared by every item of its subdomain, so no coloring isolates it; the parallel route is the atomic scatter of SequentialScheduling under a parallel device. A condensed element cache without an analytic Consistent Jacobian kernel is rejected as well — the generic combination ∂F/∂ū|_q + ∂F/∂q · dq/dū reads a corrector block spanning the field space while the AD partials span the augmented system. The declaration itself is validated too: in bounds, without duplicates, and — sampled on the subdomain's first cell — disjoint from celldofs, since a dof appearing in both head and tail would receive every contribution twice. foreach_patch raises its own rejection at the call: a patch's dof map is built from celldofs too, so the declared tail would have no patch-local number and be dropped.\n\nnote: Requires Ferrite's mesh-free algebraic variables\nSee the canonical capability note under Algebraic terms; the same Ferrite AlgebraicVariable vocabulary carries this declaration. global_dofs defaults to (), so an operator declaring none is unaffected.","title":"Elements with global dofs"},{"category":"section","location":"elements/#Facet-items","page":"Writing elements","text":"A boundary term declares the facets it lives on and gets its own traversal — the one route a surface term takes:\n\nFerriteOperators.facet_items(m::MyIntegrator, sdh::SubDofHandler) = getfacetset(get_grid(sdh.dh), \"endocardium\")\nFerriteOperators.setup_facet_item_cache(m::MyIntegrator, sdh::SubDofHandler) = MyFacetCache(...)\n\nTHE DECLARED SET IS THE TRAVERSAL. A term supported on a small fraction of the boundary — a tying constraint on an endocardial surface, a contact patch — costs a small fraction of the boundary: nothing rediscovers membership by walking every facet of every cell, so the cost is the declared facets and no more. A facet whose cell is not in sdh.cellset, a local facet index the cell does not have, and a facet declared twice are all setup errors.\n\nThe family carries its own scheduling (FacetItems partitions the owning cells) and its own local-system tail (facet_item_global_dofs), independent of the subdomain's cell sweep.\n\nOne item is one owning cell with all of its declared facets — never one facet. Two facets of a cell therefore share one local system, are assembled against one geometry cache and scatter once, and can never land on different workers. Both partitions follow from the owning cells: SequentialScheduling hands out one chunk and lets the atomic scatter resolve the dofs neighbouring cells' facets share; ColoredScheduling colors the owning cells with Ferrite's cell coloring restricted to that set, which makes the barrier promise hold for facet items exactly as it holds for cells.\n\nThe local system is owning-cell-shaped: Ke, re and the slot buffers are sized like the cell family's, padded by this family's own facet_item_global_dofs declaration. That is what makes the tying shape work — a facet term whose local system is [celldofs(cell); one lumped pressure dof] writes its coupling rows and columns through the augmented tail, and the engine scatters through the augmented dof vector:\n\nFerriteOperators.facet_item_global_dofs(m::MyTyingIntegrator, sdh::SubDofHandler) = algebraic_dofs(sdh.dh, :p)\n\nfunction FerriteOperators.assemble_facet!(req::JacobianResidualRequest, c::MyTyingCache, args::FacetArgs, lfi::Int)\n    reinit!(c.fv, args.cell, lfi)\n    P = first(c.range_p)                       # `facet_item_global_dof_range`, resolved at setup\n    p = args.states.u[P]\n    # ... req.r[I] += p * ∫Nᵢ ; req.K[I, P] += ∫Nᵢ ; req.K[P, I] += ∫Nᵢ ...\nend\n\nIts own declaration, not the cell family's. Each family sizes its local system from its own hook, so a tying term leaves the subdomain's cell sweep in the field space. Two consequences: the coupling sparsity is declarable over the tying facets alone (Ferrite's FacetCoupling) instead of every cell of the subdomain, and a condensed volumetric element may sit beside the tying term — the ADElementCache rejection of condensed elements applies to global_dofs.\n\nSensitivities. A facet-item term enters the sensitivity sweeps (see Sensitivities). The no-AD-fallback rule of Facets holds there too, so the cache must implement assemble_facet! for the sensitivity request itself. Declaring the kind (setup_operator(...; requests = (ParameterJacobianKind,))) makes setup demand that kernel loudly, instead of letting a sweep reach a missing method.\n\nSeveral terms on one subdomain. Facet-item terms compose through NonlinearCompositeIntegrator and its siblings: the composite declares the union of the inners' facets and gates the fan-out per inner, so terms on overlapping sets — a boundary spring and a dashpot on the same surface — share one item and one traversal (see Composition).\n\nOne thing a facet item deliberately does not do: it never calls reinit_values! — a facet kernel reinitializes its own FacetValues for the local facet index it was handed.\n\nFunctionals. A surface integral over the declared set is a reduction of the facet-item family, served by evaluate_facet_functional — the evaluate_cell_functional counterpart, with the local facet index appended.","title":"Facet items"},{"category":"section","location":"elements/#Algebraic-terms-(items-with-no-mesh-support)","page":"Writing elements","text":"A term whose rows belong to no cell at all — a 0D circulation model's own equations, a lumped balance, an AlgebraicCoupling-only block — is its own item family. An item of this family IS a set of global dofs and nothing else: no geometry cache, no values object, no quadrature. Two declarations on the integrator introduce it:\n\nFerriteOperators.algebraic_items(m::MyIntegrator, dh::DofHandler) =\n    [[only(algebraic_dofs(dh, :p1)), only(algebraic_dofs(dh, :p2))] for _ in 1:nchambers]\n\nFerriteOperators.setup_algebraic_cache(m::MyIntegrator, dh::DofHandler) = MyChamberCache(m.parameters)\n\nalgebraic_items lists one dof vector per item, in the order the local system uses; setup_algebraic_cache builds one cache serving them all — the analogue of one element cache per SubDofHandler serving all its cells. It has no silent fallback: declaring items without it is a setup error.\n\nnote: Requires Ferrite's mesh-free algebraic variables\nThe whole vocabulary these features are spelled in — AlgebraicVariable, algebraic_dofs, AlgebraicValues, AlgebraicCoupling, and the algebraic_coupling/algebraic_couplings keywords — is not in the registered Ferrite 1.6. There the declaration fails loudly at its own call site before any FerriteOperators surface is reached; every FO-side declaration defaults to (), so operators declaring none are unaffected.\n\nKernels dispatch through assemble_algebraic!, the family's own entry point next to assemble_cell! and assemble_facet!, and receive an AlgebraicArgs — the same four fields as CellArgs with the AlgebraicItem where the geometry cache would be:\n\nfunction FerriteOperators.assemble_algebraic!(req::ResidualRequest, c::MyChamberCache, args::AlgebraicArgs)\n    k = args.item.index                # which of the declared items this is\n    Δ = args.states.u[1] - args.states.u[2]\n    req.r[1] += c.conductances[k] * Δ - args.p * c.sources[k]\n    req.r[2] -= c.conductances[k] * Δ - args.p * c.sources[k]\nend\n\nargs.item carries index and dofs; the local buffers are n × n and n for an item of n dofs, and args.states gathers through the item's dofs like any other item's gather. The mandatory-residual rule and the analytic/AD resolution are the cell family's, so ∂F/∂θ of a 0D model comes out of the same ForwardDiff seeding.\n\nItems of one declaration must be uniformly sized, which is what keeps a worker's local buffers fixed-size; the check is a setup error naming the offending item. Items usually share dofs (several rows on one lumped unknown), and that is what fixes the scheduling: SequentialScheduling puts the whole family in one chunk and lets the atomic scatter resolve the collisions, while ColoredScheduling — whose promise is that no two items of a barrier share a dof — can only run one item per barrier. The partition is derived, not rejected.\n\nThe sparsity is the caller's here too. Entries between two algebraic variables travel as an AlgebraicCoupling; diagonal entries are always allocated, so only the off-diagonal ones need declaring:\n\nspec = StandardOperatorSpecification(;\n    algebraic_couplings = (CellCoupling(1:getncells(grid); algebraic_coupling = ((:u, :p1),)),\n                           AlgebraicCoupling(; algebraic_coupling = ((:p1, :p2),))))\n\nTwo consequences of an item having no cell. An InternalSource slot gathers the item's own internal_variable_range — EMPTY unless the algebraic cache is itself condensed (below), which is what lets a condensed cell element and a stateless algebraic term share one operator with the algebraic kernel seeing a zero-length buffer for that slot. And a reduction reaches the family but contributes nothing by default: a term with no mesh support carries no volume, so evaluate_functional keeps summing the cell contributions alone unless the cache implements evaluate_algebraic_functional.","title":"Algebraic terms (items with no mesh support)"},{"category":"section","location":"elements/#Condensed-internal-state-on-algebraic-items","page":"Writing elements","text":"An algebraic item can carry its own condensed internal state q — a circulation chamber's fast internal variables, eliminated item-locally exactly like a condensed cell's per-quadrature-point state. Two declarations, the algebraic-item analogues of get_number_of_internal_dofs_per_element and condense_cell!:\n\nFerriteOperators.get_number_of_internal_dofs_per_algebraic_item(m::MyIntegrator, cache::MyChamberCache, items) =\n    fill(1, length(items))   # uniform per provider, like the items' own dof count\n\nfunction FerriteOperators.condense_algebraic!(cache::MyChamberCache, args::AlgebraicArgs, weights::NamedTuple)\n    q = local_solve(args.states.u, args.states.qprev, stage_scaling(args.ctx))\n    args.states.q[1] = q\n    # ... store the corrector `dq/du` (and `dq/dθ`, if serving ∂F/∂θ) in an `ItemStates` field, keyed by `args.item.index` ...\n    return CondensationReport(true, 1, 0, 0, -args.item.index, 0, 0.0, 1.0)\nend\n\nBoth are queried/called only when has_internal_state holds for the algebraic cache — a stateless cache changes nothing, and every existing algebraic operator keeps behaving exactly as before. Internal dofs are numbered into the SAME tail a condensed cell's are, cell block first: [ū | q_cells | q_items] — cell ranges stay keyed by cellid (internal_variable_range(ivh, cellid::Int)), item ranges are a SEPARATE method keyed by AlgebraicItem (internal_variable_range(ivh, args.item)), deliberately not one Int-keyed method shared by both — a cellid and an item index are unrelated integers, and collapsing them onto one dispatch would silently return the wrong range whenever they coincide. condense_internal!'s domain sweep reaches both families unconditionally; condense_algebraic!'s report worst_cell convention documents how a folded report tells a cell from an item apart.\n\nAnalytic-first, no generic AD bootstrap. A condensed CELL cache lacking analytic coverage still gets generic AD paths (the condensed_corrector and local_conditions! combinations below). The algebraic-item family has none of them: an item's buffers are sized from a dof count, so the framework builds no :q differentiation configuration to combine against, and there is no cellid to key a corrector store by either. A condensed algebraic cache is therefore admissible for a Consistent sensitivity/Jacobian kind only by serving it analytically or by declaring internal_state_insensitive — setup_operator rejects anything else at setup, naming assemble_algebraic! and the remedy. That is this family's standing limitation.","title":"Condensed internal state on algebraic items"},{"category":"section","location":"elements/#The-three-item-families-side-by-side","page":"Writing elements","text":" cell items facet items (facet_items) algebraic items (algebraic_items)\nan item is one cell of the subdomain one owning cell with all of its declared facets one vector of global dofs, no mesh support\nlocal system [celldofs(cell); global dofs] (global_dofs) the same, owning-cell-shaped (facet_item_global_dofs) n × n over the item's own n dofs\nColoredScheduling Ferrite's cell coloring; rejected once global_dofs are declared the owning cells' coloring, restricted to the declared set; rejected once facet_item_global_dofs are declared derived as one item per barrier\nsensitivities analytic kernel or the ADElementCache fallback included, but analytic-only analytic kernel or the ADElementCache fallback\nfunctional hook evaluate_cell_functional evaluate_facet_functional, over the declared facets alone evaluate_algebraic_functional\ncondensation condense_cell! not supported: q belongs to the cell family's item for that same cell condense_algebraic!","title":"The three item families side by side"},{"category":"section","location":"elements/#Condensed-elements-(internal-variables)","page":"Writing elements","text":"Elements with per-quadrature-point internal state append their unknowns after the FE dofs (u = [ū; q], managed by the InternalVariableHandler) and own their local stage problem. q is gathered through an InternalSource slot like any other state — declared at setup (slots = (:u, :q, …)) and sourced per call (states = (u = u, q = InternalSource(u), …)).","title":"Condensed elements (internal variables)"},{"category":"section","location":"elements/#The-two-stage-protocol","page":"Writing elements","text":"Every operator carrying condensed internal state is driven in two phases, and the split is the CONTRACT rather than one way of arranging the work:\n\nreport = condense_internal!(op, weights, states, p, ctx)   # phase one: solve every local problem\nupdate_linearization!(op, r, states, p, ctx)                # phase two: evaluate at fixed local state\n\nPhase one solves every local problem, at every trial point. condense_internal! makes its own traversal of the domain and calls condense_cell! (cell items) or condense_algebraic! (algebraic items) once per item. Those hooks solve that item's local problem, write the trial q into the [ū; q] tail and — under the default Stored election — store the item's corrector in an element-allocated ItemStates cache field. They are the only hooks allowed to EVOLVE internal state, and condense_internal! is the only writer of q. \"Every trial point\" is literal: q is a function of the trial ū, not of the time step, so a Newton iterate, a line-search probe and a finite-difference perturbation each need their own phase one.\n\nPhase two is a pure evaluation at the local state phase one left behind. Every sweep afterwards — update_linearization!, evaluate!, assemble_weighted_jacobian!, the sensitivity sweeps, evaluate_functional — is a pure function of (ū, q, p, t) at whatever q the tail currently holds. assemble_cell! kernels receive q as an already-gathered slot and never write back, which is what makes a sweep repeatable: split Jacobian-then-residual passes, AD chunk passes and parallel devices all evaluate the same thing.\n\nA sweep that only evaluates the residual reads q and no corrector, so passing weights = nothing elects a residual-only phase one: the same local solves, the same q down to the bit, no corrector formed. It invalidates the corrector stores as it goes, so a Consistent sweep at that state throws instead of combining an earlier trial point's corrections; condensing again with weights restores them.\n\ncondense_internal!(op, nothing, states, p, ctx)            # solves every q, forms no corrector\nevaluate!(op, r, states, p, ctx)                           # the only sweep this state admits","title":"The two-stage protocol"},{"category":"section","location":"elements/#Freshness:-condense-before-assembling","page":"Writing elements","text":"The two phases are ordered, and keeping them in order is the caller's job:\n\nmutating the solution vector moves the trial point, so phase one must run again before the next sweep. The solver's own ū .+= Δu happens outside this package and is invisible to it;\nrollback_state! restores the committed solution and INVALIDATES every corrector the operator carries — they were formed for the discarded trial's q. commit_state! invalidates nothing: the committed point is the last condensed point;\na residual-only phase one invalidates the correctors up front, so a residual sweep is the only one that state admits.\n\nUnder Stored a Consistent sweep reading a never-condensed or invalidated corrector throws, naming the item (item_state); under Recompute there is no store to stamp, so nothing detects it. Neither guard covers the same vector mutated in place between a phase one and a sweep, which is the one hazard the framework cannot close structurally. Forgetting to re-condense is therefore silently wrong, not an error. In a solve it is benign in kind — the residual stays exact, so Newton stalls visibly rather than converging to the wrong answer — while a sensitivity taken at a stale q is simply a different derivative than the caller believes.","title":"Freshness: condense before assembling"},{"category":"section","location":"elements/#This-is-not-operator-splitting","page":"Writing elements","text":"\"Local, then global\" here is EXACT ELIMINATION inside one implicit solve: phase one solves the local conditions L(ū, q, θ, t) = 0 at the current trial ū, phase two evaluates the same coupled system at that q, and the pair is the monolithic system reorganized — no term of the residual is deferred and no error is introduced beyond the local solves' own tolerance. A reaction–diffusion-style operator split is a different object: a TIME-DISCRETIZATION scheme that advances different terms of one equation over the same interval in sequence, carrying a splitting error that is a property of the step and does not vanish as the local solves converge. Both are described as \"local then global\", and conflating them turns an exact reorganization into an approximation or an approximation into an exactness claim. Splitting is a solver design that lives above this package; the two-stage protocol lives inside a single implicit solve.","title":"This is not operator splitting"},{"category":"section","location":"elements/#The-corrector-election","page":"Writing elements","text":"Whether the corrector is stored at all is a construction-time election (CorrectorElection), because per-quadrature-point corrector storage is the phase's binding cost at scale — gigabytes at 10⁶ cells for a tensor slope:\n\nSimpleCondensedPowerLawRelaxation(mat, qrc, :u, :q; corrector = Recompute())\n\nStored (the default) keeps the corrector per item; Recompute keeps none and re-derives it from the converged (u, q) where a kernel needs it, which is exact rather than approximate — the corrector is a closed form in that pair. Elect Recompute() for memory-bound ASSEMBLED sweeps and Stored() for action-style use, where every operator application would otherwise re-derive the same corrector. Write the element so the election is invisible to its kernels: read the corrector through ONE access point that either reads the store or recomputes.\n\nWhat the election costs beyond memory is the freshness guard — with no store there is nothing to stamp, so the detection above is gone while the ordering contract is unchanged — and, possibly, the kernel's input requirements. A recomputing access point reads whatever the closed form needs: a corrector re-derived from (u, q) and the stage scaling makes stage_scaling(args.ctx) a requirement of every Jacobian sweep, where the stored election reads the retained block and needs no context at all — which is exactly what the shipped SimpleCondensedLinearViscoelasticity does (see the example element reference).\n\nA Jacobian-shaped kind's CorrectionMode (Consistent, the default, or FrozenQ) selects the total ∂F/∂·|_q + ∂F/∂q · dq/d· or the partial ∂F/∂·|_q alone; FrozenQ must always be spelled and is refused at construction for the sensitivity kinds (a wrong gradient, unlike a wrong iteration matrix, is never a legitimate election).\n\nDeclare has_internal_state for such caches — it governs the sensitivity admissibility rules in Sensitivities: a kind with no CorrectionMode is always the total, so a plain AD fallback — which differentiates a pure kernel and therefore computes only the frozen-q partial — is missing the correction unless the cache serves the kind analytically or declares it internal_state_insensitive.\n\nA condensed cache still gets wrapped in ADElementCache when it lacks some kind's analytic coverage, and the decorator's Consistent state Jacobian/JacobianResidual then has a GENERIC path: it AD-differentiates the pure residual seeding ū and q separately and combines them with the dq/dū block — Jₑ = ∂F/∂ū|_q + ∂F/∂q · dq/dū.\n\nThe corrector has one storage and one access seam. The storage is the element's own ItemStates cache field, written in condense_cell! and read back through item_state/set_item_state! — or, under Recompute, the closed form that stands in for it. What differs is who reads it:\n\nan element whose analytic Consistent kernel forms the combination itself reads its own store from inside that kernel, and implements nothing further;\nan element relying on the decorator's generic combination exposes the block through condensed_corrector, which is the seam that path calls. It receives the item's CellArgs, so one implementation serves either election — read the store, or re-derive the block from args.states.\n\nThe generic route is the getting-started path, and the block it asks for is bigger than the compact corrector most analytic kernels keep: the framework needs the completed nq × ndofs block, where a hand-written kernel contracts its own slope on the fly.\n\nThe parameter and time sensitivities have the same shape of generic path, out of the element's LOCAL CONDITIONS rather than a stored block:\n\nfunction FerriteOperators.local_conditions!(L, cache::MyCache, args)\n    # L .= the residual form of the equations condense_cell! solved for q\nend\n\nGiven local_conditions!, the decorator differentiates it for ∂L/∂q — factorized once per item — plus ∂L/∂θ and ∂L/∂t, and closes the implicit function theorem against the same ∂F/∂q block: dq/dθ = −(∂L/∂q)⁻¹ ∂L/∂θ, dF/dθ = ∂F/∂θ|_q + ∂F/∂q · dq/dθ. L is evaluated, never solved, and must be eltype-generic — it is what gets differentiated, so q, the parameters and the evaluation time all reach it Dual-valued (which also means a parameter bag whose rebuild_parameters cannot return a Dual-valued object rules the route out). Without the hook, and without an analytic kernel or an internal_state_insensitive declaration, those kinds keep the admissibility rejection.\n\nwarning: Experimental surface\nThe local-model seam is a CANDIDATE contract: local_conditions!'s signature may change in a minor release. The assembled results of the kinds it admits are not affected.\n\nThe ∂F/∂q block itself is available to a solver, not only to the decorator: allocate_internal_jacobian builds the rectangular residual_size(op) × ndofs(ivh) target and update_internal_jacobian! fills it — the block a Schur-complement consumer wants. Elements serve it through the analytic assemble_cell!(::JacobianRequest{:q}, …) kernel or by ForwardDiff seeding of the :q slot, which needs no admissibility guard: q is the seed itself, so Consistent and FrozenQ coincide.\n\nThe algebraic-item family (Algebraic terms) condenses through the same condense_internal! sweep and the same [ū | q_cells | q_items] tail, via its own hook (condense_algebraic!), and its items ride the same ∂F/∂q target, their column block sitting after the cell block. None of the generic routes of this section reach it, for the reason that section states, and JacobianKind{:q} is analytic-first there too.","title":"The corrector election"},{"category":"section","location":"elements/#Composition","page":"Writing elements","text":"NonlinearCompositeIntegrator and its bilinear/linear siblings stack several sub-integrators over one domain into a single element:\n\nsetup_operator(strategy, BilinearCompositeIntegrator(mass, diffusion), dh)\n\nThe request carries the buffers, so one generic fan-out serves every request type, and each inner receives its own query_cell_parameters view. Empty caches are dropped when the composition is built, so an all-empty composition collapses to the empty cache and a single surviving cache is returned unwrapped — the engine's empty-cache fast path survives composition. Composed inners must agree on their quadrature rule; a getnquadpoints query on a disagreeing composite throws.\n\nThe scope bound is same-(context, sink) multiphysics on one domain: terms evaluated at different contexts or scattered into different targets are separate sweeps over separate integrators, and the type carries exactly one field so there is nowhere to smuggle a per-inner context or weight. No values objects are shared by construction — deliberate sharing stays an element-side concern.\n\nConstruction is rejected loudly for an empty tuple, for a sub-integrator with condensed internal state (composing condensed elements is not supported), and for cross-sink mixes. A bilinear inner inside a nonlinear composite is legitimate — the operator a bilinear form induces scatters into the same matrix and residual — whereas an AbstractLinearIntegrator describes a load form, whose sink is a vector alone, and never composes into a nonlinear or bilinear operator. Nested composites are flattened at construction.\n\nThe inners share one local system, so they share its tail: a composite's global_dofs are what its inners declare. Silent inners (the default ()) read the tail a declaring inner puts there; two inners declaring different dofs is an ArgumentError at setup, there being no unambiguous tail for the composed local system to have. Each family answers from its own hook, by the same rule: a composite's facet_item_global_dofs is derived from the inners exactly as its global_dofs is, and neither declaration leaks into the other family's local system.\n\nFacet-item terms compose too: a composite declares the sorted union of its inners' facet_items, and the CompositeFacetItemCache it builds gates the fan-out on each inner's own set — an inner integrates exactly the facets it declared. Taking the union is what makes two terms supported on the same facet legal: they are one item, declared once and assembled by both. Inners declaring nothing contribute to neither the declaration nor the cache, so an all-silent composite keeps the additive () default and a single declaring inner's cache is returned unwrapped.\n\nCompositeVolumetricElementCache and CompositeFacetItemCache are the caches these integrators build, and remain available for hand-built compositions.\n\nRouting and composition compose in one order — a *MultiDomainIntegrator whose values are composite integrators. A composite of routers is not supported.\n\nNonlinearMultiDomainIntegrator and its bilinear/linear siblings map volumetric cellset names to integrators, so one operator can carry different physics per subdomain. A name claims the subdomain whose cells lie in that cellset, and it resolves everything that subdomain declares — its element cache, its facet items and their surface cache, its global dofs; facetset names take no part in routing. Resolution runs once per operator setup; an unclaimed subdomain, an ambiguous claim, or a declared name claiming nothing is an ArgumentError there, never a silently empty contribution. It samples each subdomain's first cell, so the requirement that a subdomain lie entirely within one declared cellset is an assumption in production and a checked, cell-exact rejection under FerriteOperators.debug_mode.","title":"Composition"},{"category":"section","location":"elements/#Functionals","page":"Writing elements","text":"FerriteOperators.evaluate_cell_functional(::FunctionalKind{:energy}, cache::MyCache, args) =\n    # return this cell's ∫ contribution (a Number or a Tensors tensor)\n\nFerriteOperators.evaluate_facet_functional(::FunctionalKind{:flux}, cache::MySurfaceCache, args, lfi) =\n    # return this facet's ∫ contribution, or `nothing`\n\nFerriteOperators.functional_value_type(::FunctionalKind{:energy}) = Float64\n\nΦ = evaluate_functional(op, FunctionalKind(:energy), states, p, ctx)\n\nGlobal reductions (energies for line searches, dissipation, chamber volumes, quantities of interest) are request kinds whose kernels return their item's contribution; the engine sums per worker and reduces in a fixed order, so results are deterministic for a fixed worker count. All three item families serve them: cell items through the hook above, facet items through evaluate_facet_functional — the facets of one item folding in declaration order — and algebraic items through evaluate_algebraic_functional.\n\nFerriteOperators.functional_value_type declares the type the reduction accumulates in. It is required under a parallel device — the per-worker partials are one typed array allocated before the batch runs, so an undeclared kind evaluated on a PolyesterDevice is an ArgumentError naming the trait. Sequentially it is optional: without it the first contributing cell fixes the accumulator's type.\n\nWith the declaration each worker's fold starts at zero(T) — the reduction's additive identity — and a kernel returning some other type fails loudly instead of widening the accumulator. That identity is also what makes an empty sum well-defined, which is why the two kinds of \"nothing came back\" resolve differently:\n\nsituation when it is decided result\nthe operator's partitions carry no items; every subdomain's cache is an EmptyVolumetricElementCache; or no subdomain belongs to an item family the kind names (reduction_families) structural, checked before any cell runs ArgumentError — misconfiguration, whatever the kind declares\nthe sweep runs and every kernel returns nothing data-dependent zero(T) when the value type is declared; ArgumentError when it is not","title":"Functionals"},{"category":"section","location":"elements/#Unit-testing-a-kernel","page":"Writing elements","text":"Kernels are pure evaluation, so they can be called directly on a single cell without an operator. Building the cell cache and the CellArgs by hand is the supported testing seam:\n\ncache = FerriteOperators.setup_element_cache(MyIntegrator(qrc, :u), sdh)\n\ncc = Ferrite.CellCache(dh)\nreinit!(cc, 1)                      # geometry for cell 1\nreinit_values!(cache, cc)           # the element's own values objects\n\nuₑ = rand(ndofs_per_cell(sdh))\nrₑ = zeros(ndofs_per_cell(sdh))\nargs = CellArgs((u = uₑ,), cc, p, nothing)\nassemble_cell!(ResidualRequest(rₑ), cache, args)\n\nCellArgs is constructed positionally as (states, cell, p, ctx); ctx is whatever the kernel reads (nothing when it reads none). Pass further slots as additional entries of the states NamedTuple.","title":"Unit-testing a kernel"},{"category":"section","location":"elements/#Example-elements","page":"Writing elements","text":"Worked implementations of everything above live in FerriteOperatorsExampleElements, a separate package under lib/FerriteOperatorsExampleElements — one element per feature of the contract: a bilinear form and its induced residual, a linear form, a nonlinear element with analytic tangent, and condensed elements with per-quadrature-point internal state — one with a linear local problem, one whose local problem is nonlinear and communicates with the outer solver through the context and element-cache channels, and a nested pair whose local problem is itself a condensed finite element problem, the two-stage protocol run one level down. They are FerriteOperators' own test fixtures and are meant to be read and copied. Add them to an environment with\n\nPkg.add(url = \"https://github.com/termi-official/FerriteOperators.jl\",\n        subdir = \"lib/FerriteOperatorsExampleElements\")\n\nTheir docstrings are collected in the example element reference.\n\nThe generic functions and types above are collected in the element API reference.","title":"Example elements"},{"category":"section","location":"devdocs/rationale/#Design-rationale","page":"Design rationale","text":"This page answers why is it this way. It is the one sanctioned home for reasoning in the documentation: the load-bearing reason behind each design decision, and the alternatives that are rejected and on what grounds. Entries are organized by design question.\n\nEverything else states the contract. The layer contract says which layer owns which piece of information — and which pieces the calling solver owns instead — and what the extension points are; Writing elements and Operators and entry points say what an author must write. If a statement here disagrees with those pages, those pages are right and this one is stale — report it.","title":"Design rationale"},{"category":"section","location":"devdocs/rationale/#Argument-bundles","page":"Design rationale","text":"","title":"Argument bundles"},{"category":"section","location":"devdocs/rationale/#Why-the-kernel-argument-bundle-is-a-closed,-annotatable-record","page":"Design rationale","text":"Question. Is the bundle a kernel receives an open structural interface that a downstream operator family re-implements, or one fixed type per item family?\n\nAnswer. One concrete argument type per item family — CellArgs for cell kernels, FacetArgs for facet kernels, AlgebraicArgs for algebraic items — with no common supertype, and element authors may annotate args::CellArgs.\n\nWhy. An open bundle is a second extension axis, and it is the redundant one. What a downstream family genuinely needs — an extra per-sweep quantity — is served by ctx, which is open by design and costs nothing to keep open. Openness in the bundle buys nothing beyond that and pays for it in validation surface: a per-family declaration of which argument type it builds threaded through setup, a type-parameterized validator (because hasmethod against an abstract queried type misses concretely annotated methods), a reflection warning whose only job is to make those unnecessary, and every record rebuild promoted to a public seam.\n\nClosing the type is what makes annotation legal, and it turns those rebuilds into ordinary private constructors: with_states, with_parameters and with_context are named record updates that keep the field order written once instead of at every AD seeding site.","title":"Why the kernel argument bundle is a closed, annotatable record"},{"category":"section","location":"devdocs/rationale/#Why-a-record-and-not-positional-arguments","page":"Design rationale","text":"Question. Why does a kernel take one record rather than a flattened argument list?\n\nWhy. Three reasons, in increasing order of weight.\n\nTransposition. states and p are opaque bags. Positional arguments of opaque type produce silent wrong-argument errors instead of MethodErrors — exactly the failure mode the request types exist to cure. Discriminating one sweep from another by the shape of an argument is dispatch-ambiguous on top of that, so a downstream package needing a differently-shaped kernel set has to invent new function names.\nFrozen signatures. A fixed positional signature promises the argument list never grows. When it must grow, the pressure goes into whichever argument is a bag — the \"smuggle it through p\" failure described below, arrived at from the other direction.\nSeeding arithmetic, which is what settles it. Every derivative sweep seeds through exactly three channels: states, parameters, context — and there are many more sweeps than channels. The framework therefore needs three rebuild methods per argument family, not one per sweep. That ratio is a property of the record. Under positional arguments the framework cannot express \"the same call with one argument replaced\" generically, and the count becomes (sweeps × signature shapes).","title":"Why a record and not positional arguments"},{"category":"section","location":"devdocs/rationale/#Where-information-lives","page":"Design rationale","text":"","title":"Where information lives"},{"category":"section","location":"devdocs/rationale/#The-principle:-decorate-capabilities,-pass-data","page":"Design rationale","text":"Structure is decorated; data is passed. Anything fixed before a sweep starts and constant across it belongs to a type — an operator, a cache, or a decorator around a cache. Anything that varies per evaluation is an argument.\n\nctx is an argument because a time is per evaluation. Weights are arguments because a step size changes. The element cache is structure. Whether a Jacobian is computed analytically or by differentiating the residual is structure — and that is the observation the AD decorator rests on.\n\nThe failure the principle rules out is decorating the wrong layer: a time discretization wrapped around the element interface. A scheme is data (per step: uprev, Δt) masquerading as structure, so every new scheme becomes a new copy of the wrapper — a parallel parameter type, a parallel Union encoding the fork in the type system, the element assembly methods duplicated per wrapper — and no copy composes with the derivative machinery. The AD decorator decorates a capability onto a cache, which is genuinely structure: whether this element can produce this derivative does not change between steps.","title":"The principle: decorate capabilities, pass data"},{"category":"section","location":"devdocs/rationale/#Why-ctx-is-an-argument-—-and-not-p,-and-not-operator-state","page":"Design rationale","text":"Answer. TimeIntegrationContext reaches kernels as a field of the args record. It is neither folded into the parameter bag nor stored on the operator.\n\nRejected: time inside p. It fails four ways at once. The framework must see t where a parameter bag hides it — the ∂F/∂t sweep seeds a Dual through it and the engine threads γ̃ to element-local stage problems — so every wrapper needs an unwrapping convention; a user's own query_cell_parameters override silently drops that wrapper; and ∂F/∂θ and ∂F/∂t become indistinguishable, because they seed the same channel. That last one is a silent-zero defect: a time sweep seeding bare time through the parameter channel hands an element reading its context per the documented contract a derivative of exactly zero, with no error anywhere.\n\nRejected: per-sweep scalars as operator state (the \"solver as a decorator\" shape). A number stored on a structure has to be invalidated, and invalidation is solver knowledge that no operator can infer. Staleness stays with the solver, which is also why every derived quantity here is written by an update call and never by a query. The witness for the alternative is a backward-Euler path guarded by Δt ≈ Δt_last: an isapprox predicate, so a step size drifting within tolerance silently reuses a stale matrix.\n\nPer-item contexts — the reservation for local time stepping — do not contradict this. A context source is structure and lives on the operator; the value it yields for an item is call data and reaches the kernel as the argument.\n\nElements read the context only through accessors (evaluation_time, stage_scaling), which is what makes a richer downstream context type substitutable. An \"open channel\" means nothing unless the channel has a reader: a quantity elements reach as a struct field cannot be decorated.","title":"Why ctx is an argument — and not p, and not operator state"},{"category":"section","location":"devdocs/rationale/#Why-there-is-no-structural-unknowns/data-split","page":"Design rationale","text":"Answer. states is one flat bundle of slots. There is no grouping of slots into \"unknowns\" versus \"data\".\n\nWhy. Roles are request-relative: a request kind seeds what it names, and everything else in states is data for that sweep. q is data in a residual sweep and an unknown in a ∂F/∂q sweep — the same slot, two roles, decided by the request. A structural grouping would force kernels to read one slot from different groups across phases, making the kernel phase-aware, and would re-create precisely the slot-tuple-carrier problem the flat bundle removes.\n\nThe two use cases usually cited for such a split are served elsewhere and better: IMEX is term-split operators issuing different request sets, and local time stepping is item restriction.","title":"Why there is no structural unknowns/data split"},{"category":"section","location":"devdocs/rationale/#Differentiation","page":"Design rationale","text":"","title":"Differentiation"},{"category":"section","location":"devdocs/rationale/#Why-AD-is-a-decorator-cache-rather-than-engine-machinery","page":"Design rationale","text":"Answer. ADElementCache wraps an element cache that does not serve some request analytically, implements the derivative requests by seeding the inner's residual kernel, and forwards everything else. Wrapping happens once at setup_operator time. The engine has no differentiation in it.\n\nWhy. The choice of route is structure, so under the principle above it belongs in a type. Making it one has four consequences, and together they are what pays for the type.\n\nThere is no per-cell fork. A fork living in the engine is a branch inside every kernel-dispatch method, consulted per cell and answered by a trait. Post-decoration every resolved cache is analytic from the engine's point of view, so one generic kernel-dispatch body serves every case.\nDifferentiation machinery is structurally absent when unused. An operator that never differentiates carries none, because no cache was wrapped — a property that has to be engineered and maintained when the machinery instead sits on the workspace behind a declaration.\nComposition is per-inner. Capability that is all-or-nothing across a fan-out forces a composite whose inners are partly analytic to differentiate the whole composite residual. Wrapping is per-cache, so analytic blocks come from their own kernels and only the non-analytic ones are differentiated; the Jacobian of a sum is the sum of the Jacobians, so this is exact. The constructor's policy is binding: wrap the non-analytic inners as one maximal sub-composite, never individually — naive per-inner wrapping costs one full seeding pass per wrapped inner and is worse than not wrapping at all once two or more inners need it.\nThe backend is an extension seam. ForwardDiffAD is the default, but the decorator's contract is what a backend implements, so a reverse-mode backend is a package extension rather than a fork of the engine.\n\nWhat deliberately does not live in the decorator: the admissibility rules and check_derivatives are framework-side. The decorator implements; the constructor enforces. Likewise the rejection of gather-time-reconstructed slots in a differentiated position is a per-call check, because a slot's source is call data.\n\nAD output buffers live per worker on the workspace as SensitivityBuffers rather than in a task-owned bag, because an AssemblyTask is reused across subdomains of differing ndofs_per_cell while an AssemblyWorkspace is correctly per-subdomain-per-worker; a task-owned output bag would be sized for the wrong subdomain.\n\nThe decorator does not broaden provides_analytic, because two questions need two predicates: provides_analytic is \"is there a hand-written kernel\" and forwards through the decorators unchanged, while FerriteOperators.serves_kind is \"does the resolved cache serve this kind, by kernel or by a decorator's generic route\". One trait answering both needs per-kind non-broadening overrides wherever a check wants the first.","title":"Why AD is a decorator cache rather than engine machinery"},{"category":"section","location":"devdocs/rationale/#Why-FusedFromSplit-is-required-rather-than-an-optimization","page":"Design rationale","text":"Question. An element with analytic split kernels but no fused kernel could fall back to AD for the fused request. Why the extra decorator?\n\nAnswer. FusedFromSplit serves a fused Jacobian+residual request by issuing the split kernels back to back. Without it, check_derivatives false-negatives on a wrong analytic Jacobian: AD would silently substitute the correct Jacobian into the fused path, and the check would pass.","title":"Why FusedFromSplit is required rather than an optimization"},{"category":"section","location":"devdocs/rationale/#Why-finite-differences-are-operator-level-and-AD-is-per-cache","page":"Design rationale","text":"Question. Both are derivative routes. Why is one a cache decorator and the other an operator entry point?\n\nAnswer. Operator-level FiniteDifferenceSensitivity differences evaluate!, so every term enters — including a facet term whose cache carries no analytic sensitivity kernel — and no kernel ever sees a Dual. The ADElementCache decorator is the per-cache route: analytic kernels win cache by cache, and it is volumetric only.\n\nThere is no cache-level finite-difference decorator, and that is the settled division rather than a gap. It would differentiate the volumetric kernel exactly like the AD one, and would therefore lose the single property that makes the operator-level method worth keeping. Its remaining niche — kernels that cannot carry Duals — has no consumer.","title":"Why finite differences are operator-level and AD is per cache"},{"category":"section","location":"devdocs/rationale/#Condensation-and-internal-variables","page":"Design rationale","text":"","title":"Condensation and internal variables"},{"category":"section","location":"devdocs/rationale/#Why-condensation-is-an-explicit-phase","page":"Design rationale","text":"Answer. condense_internal! is a traversal of its own. It solves every quadrature point's local problem, writes the trial q into the [ū; q] tail, and stores each item's corrector. Every assembly sweep afterwards is a pure evaluation at frozen q.\n\nWhy: model/solver decoupling, completed at the local level. The global level is decoupled by slots, weights and ctx between them, which let a solver drive BDF or Newmark without an element knowing which. A condensed element that owns both its local model and its local time discretization, and runs both inside every kernel, is not decoupled at the local level: a Jacobian sweep and a residual sweep over the same cells then count their local solves twice.\n\nSplitting the phase off buys three things that a fused local solve cannot have:\n\nDerivative machinery becomes generic. With the kernel pure, a plain AD fallback computes a well-defined ∂F/∂·|_q, and the correction is a stored matrix rather than an iteration to differentiate through. Condensed elements get parameter sensitivities without a hand-written kernel: the payoff case, dF/dθ = ∂F/∂θ|_q + ∂F/∂q·dq/dθ, matches a finite-difference referee to ~1e-10 on the shipped power-law element.\nSolver families become caller sequencing. Exact condensed Newton, multilevel Newton, frozen-q modified Newton, and line search without re-condensing are all orderings of two calls. None of them requires element cooperation.\nNon-convergence becomes data. CondensationReport is isbits and folds as a monoid, so it crosses a device boundary — which an exception cannot.\n\nThe weights argument of the phase is where the solver's chain-rule slopes enter. The corrector is defined as dq/dū — with respect to the primary unknown, with slot reconstructions pre-chained — so the weighted-Jacobian machinery is untouched, keeps folding frozen-q partials with the solver's scalars, and nothing double-counts. The slopes must reach the element during condensation, because that is where they are chained in.","title":"Why condensation is an explicit phase"},{"category":"section","location":"devdocs/rationale/#Why-partial-versus-total-is-explicit-vocabulary","page":"Design rationale","text":"Answer. Jacobian-shaped kinds carry a CorrectionMode. Consistent is the default and means the total; FrozenQ means the partial and must always be spelled.\n\nWhy. Purity is mechanical, not semantic. L(q; ū, θ, t) = 0 remains an implicit function, so plain AD on a pure kernel yields the partial at fixed q — a correct number, answering a question the caller usually did not ask. Every total is one pattern with three seeds:\n\ndF/dū = ∂F/∂ū|_q + ∂F/∂q · dq/dū\ndF/dθ = ∂F/∂θ|_q + ∂F/∂q · dq/dθ\ndF/dt = ∂F/∂t|_q + ∂F/∂q · dq/dt\n\nThe silent-wrong class this creates is the missing combination: nothing throws, nothing is NaN, the number is simply a different derivative than the caller believes. A vocabulary that leaves partial-versus-total to the reader hands that class to every user. Naming it in the request kind puts it where elements dispatch on it, which is also where a consistent tangent is naturally formed — at the quadrature point.\n\nDefaults fail in the safe direction. The unsafe direction here is the missing combination, so the default is the total.\n\nFrozenQ is refused for the sensitivity kinds — structurally. A frozen tangent is a legitimate and valuable election for an iteration matrix (modified Newton, multilevel-Newton outer loops), where a wrong tangent costs convergence rate and nothing else. It is never legitimate for a gradient, where the wrong number is the answer. The refusal is not a runtime check: the five sensitivity kinds simply carry no CorrectionMode parameter, so electing one is a TypeError. The correction-mode parameter also sits between the slot and buffer type parameters of the kind family, so bare annotations stay UnionAll and a non-condensed element needs no changes to accommodate it.","title":"Why partial versus total is explicit vocabulary"},{"category":"section","location":"devdocs/rationale/#Why-q-is-a-slot","page":"Design rationale","text":"Answer. A condensed element's internal state arrives as an ordinary slot whose source, InternalSource, carries the internal-dof range as its restriction. Global [ū; q] storage is unaffected; this is a statement about the gather level only.\n\nWhy. It collapses a family of dedicated element-side hooks and cache fields into one already-existing hook. It makes ∂F/∂q fall out for free as an ordinary per-slot Jacobian — load-bearing twice, as the block the generic corrector combination needs and as the block a Schur-complement consumer wants. And it makes an otherwise necessary caveat unexpressable: the condensed tail of a reconstructed slot cannot be misinterpreted by an element, because a reconstructed slot's source is the field space and structurally cannot touch q.\n\nThe obvious alternative — keeping internal degrees out of slots entirely, on the grounds that q is solved inside the kernel and so is not an independent variable of the residual — rests on a premise the condensation phase makes false. After the phase, q is an independent variable of a pure residual.","title":"Why q is a slot"},{"category":"section","location":"devdocs/rationale/#Why-kernels-are-pure","page":"Design rationale","text":"Answer. Condensation is unconditionally its own traversal. No election lets an operator fuse it into the residual sweep.\n\nWhy.\n\nKernel purity is an invariant rather than a convention. The admissibility question turns from \"did this kernel differentiate through an iteration\" into \"is a corrector present\", which is structurally checkable. The hazard where AD runs through a deliberately inexact local solve — wrong by a solver-controlled amount, unlike a converged loop which is asymptotically right — dies structurally rather than by rule.\nThere is exactly one writer of q.\nSolver families need zero element cooperation — frozen-q, multilevel Newton, and line-search policies are all caller sequencing.\nA fused route would be structurally primal-only, so purity costs no capability. What it would buy is a second driver body, dual freshness semantics and dual reporting, including a once-per-item problem: a fused sweep enters the element more than once per item under split-Jacobian-residual and AD chunk passes.\n\nWhat it costs. One extra item traversal per exact-Newton iterate. For a fused-analytic user that saves no local solves at all and pays gather, geometry reinit and values reinit for nothing; it hits cheap-local-solve elements hardest, while modified-Newton and multilevel-Newton callers can net win and a line search is neutral.\n\nThe documented boundary: stateful do-it-yourself inline elements fail silently. A kernel receives q as an already-gathered slot and never writes the global vector, so an element that re-solves from committed history inside assemble_cell! has nowhere to put the new state: it freezes the committed state across steps instead of advancing it, with no error to catch it. State that must survive a step goes through condense_cell!.\n\nA stateless inline implicit solve stays legitimate and is unaffected: if its root is a pure function of the gathered args, it is recomputable at any later time, and AD through it is approximately-total, limited by the inner tolerance. The clean upgrade, when that limit matters, is an analytic tangent via the implicit function theorem — the same shape a corrector has.","title":"Why kernels are pure"},{"category":"section","location":"devdocs/rationale/#What-the-phase-concedes","page":"Design rationale","text":"Recorded so they are not rediscovered as surprises.\n\nFreshness is not structurally closable, and the residual hazard is named. The solver writes u .+= Δu outside the package entirely, so no hook sees it. Three independent guards narrow the window — corrector stamping, rollback_state! invalidating where commit_state! does not, and check_derivatives re-condensing at every trial point — but what remains uncovered is the same vector mutated in place between condensation and a sensitivity sweep. For a solve that is benign in kind: the residual is still exact, so Newton stalls visibly rather than converging to a wrong answer. For a sensitivity it is silently wrong, and an optimizer will consume the gradient happily. The documentation names that case rather than implying the guards are complete.\n\nPer-quadrature-point corrector storage is the binding constraint at scale, and it is why Recompute is a first-class election rather than a fallback: a scalar corrector is cheap, but a 6×6 slope on a viscoelastic element is ~2.3 GB at 10⁶ cells, and the generic block is bigger still.\n\nThe election is exact rather than approximate — the corrector is a closed form in the converged (u, q), so recomputing re-runs the arithmetic the condensation already ran, at the same point — which is what makes it an election rather than a trade of accuracy. What it costs instead is the freshness guard (with no store there is no stamp) and, since a recomputing access point evaluates that closed form, whatever the form reads becomes an input requirement of every sweep that reads the corrector. Both consequences are documented with the election in Condensed elements.\n\nThe phase is a global barrier. It must complete over the whole domain before any evaluation sweep begins. On a shared-memory engine that is one join; on a future distributed backend it is a real barrier that forecloses overlapping condensation with anything asynchronous. This is intrinsic to the decoupling — the decoupling is \"solve everything, then evaluate\" — not an implementation artifact.\n\nThe generic bootstrap reaches θ and t only through a declared local model. The decorator's generic Consistent path handles ∂F/∂ū (and its fused sibling) out of the corrector alone. dq/dθ = −(∂L/∂q)⁻¹ ∂L/∂θ needs something the corrector does not carry: the local operator itself, against a different right-hand side. The framework cannot see an element's local equations, so the extension could only ever be an element declaration — which is what local_conditions! is. With it the decorator differentiates the hook for ∂L/∂q, ∂L/∂θ and ∂L/∂t, factorizes the local operator once per item, and completes the total against the ∂F/∂q block; without it those kinds keep the admissibility rejection, now naming the hook as the third remedy.\n\nThe seam is spelled against the package's own kernel convention — local_conditions!(L, cache, args), per ITEM, filling the item's stacked local residual — rather than per quadrature point. A per-quadrature-point argument list cannot be driven by the framework: it asks for the field value AT a quadrature point, which only the element's own CellValues can produce, and for a qprev the framework has no vocabulary for. The args form carries both without the framework interpreting either, and it is what makes one factorization per item — the whole cost argument for the route — well defined. It is marked experimental for that reason: the contract is validated, not frozen.\n\n∂F/∂q is a target, not a slot Jacobian. The block is field × internal shaped, so it fits neither the operator's square matrix nor the per-slot component bag. It gets a rectangular sparse target of its own (allocate_internal_jacobian/update_internal_jacobian!) whose pattern is the cell loop's celldofs × internal range, scattered through the two-index assemble! the transfer operators already use. Columns are disjoint between items by construction — an item owns its internal range alone — so the sweep needs no new assembler and no new race analysis. assemble_slot_jacobian! refuses :q outright rather than silently scattering a rectangular block into a square pattern.","title":"What the phase concedes"},{"category":"section","location":"devdocs/rationale/#The-operator-layer","page":"Design rationale","text":"","title":"The operator layer"},{"category":"section","location":"devdocs/rationale/#Why-request-kinds-are-the-extension-axis","page":"Design rationale","text":"Answer. Request kinds are public, and a downstream kind is defined entirely outside this package — see Extension points.\n\nWhy. The kind axis is open and proven by test: a downstream kind can be defined with zero source edits, validating at setup, allocating nothing, and constant-folding. Replacing it with records of functions would invert the extensibility claim — closing a proven-open axis in exchange for making an already-cheap axis marginally cheaper.\n\nreinit_values!(cache, cell, kind) is the second reason kinds cannot be removed: it exists precisely so an element can reinitialize only the values a given sweep shape needs, and that requires a name for the shape.\n\nState-independence is not a kind of its own, though: the operator induced by a bilinear form is assembled by a Jacobian sweep whose plan gathers nothing, and the load vector of a linear form is a residual sweep whose plan gathers nothing. They are the same computations with an empty slot set, so they need no separate names.","title":"Why request kinds are the extension axis"},{"category":"section","location":"devdocs/rationale/#Eager-by-default,-lazy-only-with-a-consumer","page":"Design rationale","text":"Answer. Materialization is a constructor choice, stated in the type, never inferred. The default is eager: components are assembled into matrices sharing one sparsity pattern and folded by an O(nnz) combination. Eager is the form that has no invalidation semantics to get wrong.\n\nOne lazy form exists because a downstream consumer has it: a StageBlockOperator that keeps its blocks separate and applies the weighted sum at application time, so that a step-size change costs a field write instead of a re-fold. That operator stores its scalar — a deliberate exception, confined to the lazy variant.\n\nThe prohibition that shapes this: the one downstream package living inside the SciML ecosystem carries a written rule against subtyping the ecosystem's lazy operator root, because the branch it would land in drops the matrix-reuse flags. Concrete-versus-lazy confusion is not a hypothetical cost.\n\nThe pattern group is checked, not assumed. Components of one operator share a sparsity pattern — for the shared-memory compressed-column backend, by aliasing the index arrays and giving each component private values. Two downstream packages and three separate code paths form a scheme matrix by combining nonzero arrays under an unstated assumption that the patterns match, one of them across two different storage formats. share_pattern and combine!'s pattern assertion are what turn that assumption into an invariant. Square components share the group; rectangular members are legal in a bag and are never combined.","title":"Eager by default, lazy only with a consumer"},{"category":"section","location":"devdocs/rationale/#Scope","page":"Design rationale","text":"","title":"Scope"},{"category":"section","location":"devdocs/rationale/#Why-the-hub-principle-bounds-what-the-package-owns","page":"Design rationale","text":"FerriteOperators is an interface package. It owns contracts and the seams that make them extensible; it does not own every implementation of them. This is why the AD backend is a seam rather than a fork of the engine, why a downstream request kind needs zero source edits here, and why a solver-shaped need is answered by asking which seam is missing rather than by moving the downstream code upstream.\n\nThe rule has teeth in both directions: an unused generality is not a contract and may be removed, while a used one is a contract and stays open. Where a direction does narrow a proven extension axis, the honest cost is recorded rather than argued away, and the seams involved are exported rather than left for downstream code to reach as qualified internal names.","title":"Why the hub principle bounds what the package owns"},{"category":"section","location":"devdocs/rationale/#Why-the-package-speaks-the-MFEM/libCEED-decomposition","page":"Design rationale","text":"The design follows the fundamental finite-element operator decomposition A = Pᵀ Gᵀ Bᵀ D B G P popularized by MFEM and libCEED, and says so deliberately: the vocabulary is what keeps how much of the operator is materialized separate from what the physics is.\n\nblock meaning where it lives here\nP local↔global including constraints constraint handling around the element contribution\nG element restriction (gather/scatter) slot loaders and element-assembly dof maps\nB basis evaluation at quadrature points element-owned CellValues\nD pointwise quadrature-point operation the quadrature-point kernel tier\n\nMFEM's assembly levels — FULL, ELEMENT, PARTIAL, NONE — name the strategy axis, orthogonal to the device axis. FullAssembly and MatrixFreeAction are the shipped FORMS, and the latter spans the other three levels through its storage election (StorageElection): the QVector is the qdata store a Stored() element precomputes its geometric factors into (PARTIAL), a Recompute() one keeps none and re-evaluates the Jacobian at the quadrature point that consumes it (NONE), and an ElementAssembly() one keeps the dense element matrices (ELEMENT). The first two are the ELEMENT's storage, not the framework's — the fill (fill_quadrature_data!) is the element's own kernel — because what is worth storing per quadrature point is a property of the pointwise map, and only the element knows it. The third is the framework's, because a dense Kₑ is not: ElementAssemblyCache wraps any bilinear cache and fills it from the element's matrix kernel or, where there is none, from ndofs_per_cell applications of its action.\n\nSplitting the axis in two is the one deliberate deviation from MFEM's flat one: FULL is a different OPERATOR (it holds a global matrix, answers get_matrix, and every global-storage wall applies to it), while ELEMENT, PARTIAL and NONE are the same operator keeping different amounts of the same evaluation. The form axis separates what an operator IS; the storage election separates what it KEEPS.\n\nThe matrix-free level is also where the SECOND mapping question appears, and the answer is strategy-side: AbstractElementMapping says whether one worker or one workgroup evaluates an element, the element implements both decompositions of the SAME math, and the term names neither.\n\nThe deliberate deviation from libCEED: an element may own the whole Bᵀ D B block, because condensed materials with element-level local solves do not decompose into a pointwise D. The quadrature-point tier is opt-in, not mandatory. The same frame explains why kernel-level AD is the right sensitivity granularity: ∂F/∂p acts on D, the small dense pointwise map.\n\nOne nuance the condensation phase adds: the evaluation sweeps of a condensed element are per-quadrature-point decomposable, so the pointwise tier is reachable for them. The condensation phase itself is not.","title":"Why the package speaks the MFEM/libCEED decomposition"},{"category":"section","location":"patches/#Patch-items","page":"Patch items","text":"warning: Experimental\nThe patch surface may still change. Everything else in this documentation is contract; this page is a preview of the local-BVP layer.\n\nA patch item is a set of cells with its own dof numbering — the unit of work for error estimators, localized-orthogonal-decomposition correctors, and other methods whose systems live on a neighbourhood rather than on one cell. The cell kernels are reused unchanged; only the scatter target is patch-local.\n\nFO drives the sweep and assembles; everything a local BVP does per patch — factorize, solve, emit — happens in a callback the caller writes:\n\nprovider = PatchItems(sdh, cellsets; prescribed_facets)\nsink     = PatchTripletSink()\nfacts    = ItemStates{typeof(lu(zeros(1, 1)))}(npatches(provider))\n\nforeach_patch(op, provider, (u = u,), p) do pws, pid\n    n, free = patch_ndofs(provider, pid), patch_free_dofs(provider, pid)\n    if !has_item_state(facts, pid)                # retained across sweeps\n        K = zeros(n, n)\n        assemble_patch_target!(K, (PatchTerm(WholePatch()),), pws, (u = u,), p)\n        set_item_state!(facts, pid, lu(K[free, free]))\n    end\n    F, rows = item_state(facts, pid), patch_dofs(provider, pid)[free]\n    rhs = zeros(n)\n    for j in 1:ncols\n        fill!(rhs, 0.0)                          # the primitive accumulates\n        assemble_patch_target!(rhs, (PatchTerm(WholePatch(), MyColumnData(j)),), pws, (u = u,), p)\n        emit_patch_column!(sink, rows, (pid - 1) * ncols + j, F \\ rhs[free])\n    end\nend\n\nW = sparse(sink, ndofs(dh), npatches(provider) * ncols)\n\nRows and columns of a patch-local target follow patch_dofs, the injection from patch-local into global numbering. Solving the local system is deliberately not part of the contract: the caller owns the solve. patch_free_dofs / patch_prescribed_dofs describe the interior/boundary split, augment_prescribed_dofs! pins whatever else the local problem needs to be well posed, and ItemStates is the item-lifetime storage a retained factorization belongs in — neither per-worker scratch nor operator-global frozen data.\n\nThe pieces and their contracts:\n\nforeach_patch calls f(pws, patchid) once per patch, in ascending item order, with the patch workspace already positioned on the patch. Its items argument names a subset (default: every patch); only those patches are visited, which is what a partial re-solve needs — FO writes nothing outside f, so what an untouched patch contributed to an earlier sweep stays as it was.\nassemble_patch_target! accumulates into a target it never zeroes, so the caller decides what a call adds to. Whether the element's Jacobian or residual kernels run follows the target: a matrix (or a Ferrite assembler over one) gets the Jacobian, a vector the residual.\nThe target of one call is one column of the result. N right-hand sides are N calls with per-call term data and N emit_patch_column! emissions. Duplicate (row, col) entries are summed by sparse, so overlapping patches contributing to a shared column need no coordination.\nItemStates is the retained-state slot: a factorization lives as long as the item, and FO never writes or invalidates it. Nothing about a patch is stored anywhere else.\n\nPatch sweeps are pure evaluation: condensed element unknowns are gathered but never written back.","title":"Patch items"},{"category":"section","location":"patches/#Terms,-the-patch-context-and-sinks","page":"Patch items","text":"assemble_patch_target! takes a tuple of PatchTerms. Every term of one call is evaluated in a single pass over the patch's cells and accumulates into the same element buffer in tuple order — the accumulation order is part of the contract. A term's restriction is WholePatch or CellGroup, and its data payload selects either the element's ordinary cell kernel (nothing, over an ordinary CellArgs) or an assemble_patch_cell! term kernel. Terms that must fuse per quadrature point rather than per cell belong in ONE term whose data carries both sources.\n\nA term kernel receives a PatchArgs: the four fields of CellArgs plus where in the patch the cell sits — the provider, the patch id, the cell's group tag, and ldofs, the cell's dofs in patch-local numbering. That last one is the window a payload indexes patch-local data through, so a term kernel needs no per-patch handle of its own:\n\nfunction FerriteOperators.assemble_patch_cell!(req::ResidualRequest, cache::MyCache,\n                                               args::PatchArgs, data::MySource)\n    d = @view data.patch_field[args.ldofs]       # patch-local data on this cell\n    parent = args.group                          # e.g. the parent coarse cell\n    ...\nend\n\nargs.ldofs is the driver's scratch, refilled per cell and scattered through after the kernels run — read it, never retain it past the call.\n\nA sink is where a finished patch quantity goes, once the caller has assembled and solved it. PatchTripletSink collects triplets for a rectangular assembly through emit_patch_column!, which names the column per call; PatchGlobalVectorSink accumulates a patch-local vector back into a global one through the injection (patch_emit!). A downstream scatter mode is a new sink type with its own patch_emit! method, not a fork of the per-patch body.","title":"Terms, the patch context and sinks"},{"category":"section","location":"patches/#Parallel-patch-sweeps","page":"Patch items","text":"Patches are independent work items, so a caller can process them in parallel — and it is the caller who does it. FO ships no threaded patch loop: what a patch emits is the caller's collector, and FO cannot duplicate what it does not own. What FO provides are the seams that make a parallel sweep produce exactly the triplet stream of a sequential one, bit for bit:\n\nchunks = patch_chunks(provider, nchunks)                     # contiguous, ascending\nsinks  = [PatchTripletSink() for _ in chunks]                # one collector per worker\nwss    = [patch_workspace(op, provider) for _ in chunks]     # one workspace per worker\n\n@sync for c in eachindex(chunks)\n    Threads.@spawn for pid in chunks[c]\n        reinit!(wss[c], pid)\n        # assemble / solve / emit into sinks[c], exactly as in the callback above\n    end\nend\n\nmerged = PatchTripletSink()\nforeach(s -> append!(merged, s), sinks)                      # chunk order ⇒ item order\n\nThe contract:\n\nChunks are contiguous and ascending. patch_chunks splits the items that way; the workers may finish in any order, but merging the collectors in chunk order restores the item order a sequential sweep emits in. It takes the same items argument as foreach_patch, so a subset sweep chunks and merges exactly like a full one.\nOne workspace per worker. patch_workspace builds an independent PatchAssemblyWorkspace per call, and duplicate_for_device copies an existing one. Two workspaces share only the provider, which patch sweeps read and never write — but a provider mutated at setup time (augment_prescribed_dofs!) must be finished before any worker starts.\nCollectors are the caller's. One PatchTripletSink per chunk. PatchGlobalVectorSink is not thread safe: overlapping patches hit the same entries of the destination with a non-atomic read-modify-write.\nItem state is disjoint by construction. ItemStates slots are indexed by item, so workers processing different patches touch different slots. Item lifetime is not worker lifetime — a slot must never be handed to a worker-lifetime cache.\nBit-identity requires workspace-independent per-patch numerics. It holds because a patch's result depends only on the patch. Anything that makes it depend on what the same workspace saw earlier — an accumulating scratch, a cache whose fill order changes the values — breaks the guarantee, and no amount of chunk ordering restores it.\nPreparation happens before spawning. Data that is not safe to fill lazily from several workers (a memoized geometric map, a table built on first touch) must be complete before the workers start; a sequential warm-up pass over the patches is the place for it.\n\nforeach_patch itself is sequential for the collector reason above, and rejects a parallel device loudly. Parallelism lives on the seams in this section, not behind a flag.","title":"Parallel patch sweeps"},{"category":"section","location":"patches/#Two-grid-data-and-foreign-space-evaluation","page":"Patch items","text":"A local BVP frequently needs data from a function space that is not the operator's: a coarse space whose basis the patch problem is driven by, an enrichment space, a field discretized on another grid. FO carries no values type for that, and none is needed — the pattern is a downstream-owned values object:\n\nEvaluate the foreign space with the public Ferrite reference API (reference_shape_value / reference_shape_gradient on the interpolation, at the reference points of interest) and store the result in whatever layout the term kernel wants.\nHand the object to the kernel as a PatchTerm data payload, where it reaches assemble_patch_cell! unchanged, or keep it inside a downstream element cache.\nGive the object a duplicate_for_device method. It then participates in per-worker duplication like every other cache, which is what makes it usable from patch_workspace-based parallel sweeps.\n\nThe reference points themselves are the two-grid question, and that is where FO's transfer machinery comes in: a NestedGridCellCache carries the fine → coarse cell map and get_child_ref_coords, the current fine cell's nodes expressed in the parent coarse reference element. Composing the fine cell's quadrature points through those coordinates gives the coarse reference points at which the coarse interpolation is evaluated — one reference_shape_value call per point and basis function, no geometric search.\n\nWhich coarse cell a fine cell belongs to is also what a CellGroup restriction selects on and what PatchArgs.group reports, so a term restricted to one parent cell needs no map of its own.","title":"Two-grid data and foreign-space evaluation"},{"category":"section","location":"patches/#Patch-API-reference","page":"Patch items","text":"","title":"Patch API reference"},{"category":"type","location":"patches/#FerriteOperators.AbstractPatchSink","page":"Patch items","text":"Supertype of the patch assembly sinks: where a patch's finished quantity goes once the caller has assembled (and solved) it — a whole patch-local vector through patch_emit!, or one named column at a time through emit_patch_column!. A downstream scatter mode is a new sink type, not a fork of the per-patch body.\n\n\n\n\n\n","title":"FerriteOperators.AbstractPatchSink"},{"category":"type","location":"patches/#FerriteOperators.CellGroup","page":"Patch items","text":"CellGroup(id)\n\nTerm restriction: integrate only over the patch cells carrying the group tag id (see the groups argument of PatchItems). Tested inside the single pass over the patch's cells, so a restricted term accumulates in the same ascending cell order as an unrestricted one.\n\n\n\n\n\n","title":"FerriteOperators.CellGroup"},{"category":"type","location":"patches/#FerriteOperators.PatchArgs","page":"Patch items","text":"PatchArgs(states, cell, p, ctx, provider, patch, group, ldofs)\n\nThe argument bundle a patch term kernel's third parameter receives — the four fields of CellArgs plus where in the patch the cell sits:\n\nfield meaning\nprovider the PatchItems the sweep runs over\npatch the item index of the patch being assembled\ngroup the current cell's group tag, what a CellGroup restriction selects on\nldofs the current cell's dofs in PATCH-LOCAL numbering, aligned with celldofs(args.cell)\n\nldofs is the driver's scratch, refilled per cell and scattered through after the kernels run: read it, never retain it past the call — the same lifetime args.cell has. It is the window a term payload indexes patch-local data through, so a kernel needs no per-patch handle of its own.\n\nClosed, like CellArgs and FacetArgs: the patch item family's one args record, not an extension channel.\n\n\n\n\n\n","title":"FerriteOperators.PatchArgs"},{"category":"type","location":"patches/#FerriteOperators.PatchAssemblyWorkspace","page":"Patch items","text":"PatchAssemblyWorkspace\n\nPer-worker workspace of a patch sweep: a cell workspace plus the provider the item — a patch index — is resolved through. Ferrite.reinit!(ws, patchid) positions it on a patch, assemble_patch_target! runs the element kernels on it.\n\nBuild one per worker with patch_workspace or copy one with duplicate_for_device; independent workspaces share only the provider, which patch sweeps read and never write.\n\ncurrent is a Ref{Int} because the item is resolved through the provider between reinit! and the kernels rather than handed to them. That makes it a CPU-scoped positioning mechanism — one reason foreach_patch is sequential CPU only.\n\n\n\n\n\n","title":"FerriteOperators.PatchAssemblyWorkspace"},{"category":"type","location":"patches/#FerriteOperators.PatchCallbackKind","page":"Patch items","text":"PatchCallbackKind(f)\n\nRequest kind of the per-patch callback route: f(ws, patchid) runs once per patch with the workspace positioned on it, and everything a local BVP does per patch happens inside f — FO only drives the sweep. foreach_patch is the entry point.\n\n\n\n\n\n","title":"FerriteOperators.PatchCallbackKind"},{"category":"type","location":"patches/#FerriteOperators.PatchDofPartition","page":"Patch items","text":"PatchDofPartition(prescribed, free)\n\nThe view-derived dof partition of one patch, in PATCH-LOCAL numbering: prescribed are the dofs the caller's local BVP constrains (homogeneously — this layer carries no inhomogeneities), free is their sorted complement.\n\n\n\n\n\n","title":"FerriteOperators.PatchDofPartition"},{"category":"type","location":"patches/#FerriteOperators.PatchGlobalVectorSink","page":"Patch items","text":"PatchGlobalVectorSink(dest)\n\nAdditive global-vector sink: patch i's patch-local vector is accumulated into dest through the injection, dest[patch_dofs(provider, i)[l]] += v[l].\n\nNot thread safe: overlapping patches read-modify-write the same entries of dest non-atomically. Parallel callers collect into one PatchTripletSink per chunk instead and reduce afterwards.\n\n\n\n\n\n","title":"FerriteOperators.PatchGlobalVectorSink"},{"category":"type","location":"patches/#FerriteOperators.PatchItems","page":"Patch items","text":"PatchItems(sdh, cellsets; groups, prescribed_facets, field)\n\nWork-item provider over patches: item i is the cell set cellsets[i] (cells of sdh) with its own contiguous patch-local dof numbering. The injection map back to global dofs is patch_dofs.\n\nKeyword arguments:\n\ngroups: per patch, a group tag per cell (aligned with the sorted cell list of that patch), what a CellGroup restriction selects on. Defaults to the cell ids, so CellGroup(cellid) restricts to one cell; a multiscale patch typically tags by parent coarse cell.\nprescribed_facets: per patch, an iterable of FacetIndex on the patch's cells that the local BVP prescribes. FO does NOT classify the boundary — which facets are cut boundary is caller geometry — it only resolves the given classification into the dof partition (patch_free_dofs / patch_prescribed_dofs). Defaults to no prescribed dofs.\nfield: the field the partition and patch_vertex_dofs resolve against. Defaults to the sdh's only field.\n\nExperimental: the patch item surface may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.PatchItems"},{"category":"type","location":"patches/#FerriteOperators.PatchTerm","page":"Patch items","text":"PatchTerm(restriction, data = nothing)\n\nOne additive contribution of a patch request. restriction is WholePatch or CellGroup; data is the element-specific term payload handed to assemble_patch_cell! (nothing selects the element's ordinary cell kernel).\n\nA request carries a tuple of terms, all evaluated in one pass over the patch's cells and accumulated into the same element buffer in tuple order — that order is contract. Terms that must fuse per quadrature point rather than per cell belong in ONE term whose data carries both sources.\n\n\n\n\n\n","title":"FerriteOperators.PatchTerm"},{"category":"type","location":"patches/#FerriteOperators.PatchTripletSink","page":"Patch items","text":"PatchTripletSink{Tv}()\nPatchTripletSink()\n\nOrdered COO sink for vector-valued patch results: every emit_patch_column! appends (rows[l], col, values[l]) in order, and sparse(sink, m, n) SUMS duplicate (row, col) entries — what patch-additive quantities (a corrector basis over overlapping patches) rely on. The column is named per emission, so one patch may carry as many columns as its local problem has right-hand sides.\n\nEmission follows the sweep's item order, so a sequential sweep is reproducible bit-for-bit; parallel callers collect one sink per chunk and append! them back in chunk order to keep that.\n\n\n\n\n\n","title":"FerriteOperators.PatchTripletSink"},{"category":"type","location":"patches/#FerriteOperators.WholePatch","page":"Patch items","text":"WholePatch()\n\nTerm restriction: integrate over every cell of the patch.\n\n\n\n\n\n","title":"FerriteOperators.WholePatch"},{"category":"method","location":"patches/#FerriteOperators.any_patch_term_active-Tuple{Tuple{}, Int64}","page":"Patch items","text":"any_patch_term_active(terms::Tuple, group::Int) -> Bool\n\nWhether any term of a patch request contributes on a cell tagged group — the gate that lets a patch sweep skip a cell no term touches.\n\n\n\n\n\n","title":"FerriteOperators.any_patch_term_active"},{"category":"function","location":"patches/#FerriteOperators.assemble_patch_cell!","page":"Patch items","text":"assemble_patch_cell!(req, cache, args::PatchArgs, data)\n\nElement kernel for one PatchTerm on one cell: accumulate the term's contribution into req's buffer, exactly as assemble_cell! does. data is the term payload; a term whose data is nothing bypasses this entry point and runs the element's ordinary cell kernel over CellArgs instead, so unrestricted patch assembly reuses existing elements unchanged.\n\nargs is a PatchArgs — CellArgs's four fields plus the patch, the cell's group tag and its patch-local dof window.\n\nTerm kernels are analytic — no AD fallback for a custom payload.\n\n\n\n\n\n","title":"FerriteOperators.assemble_patch_cell!"},{"category":"function","location":"patches/#FerriteOperators.assemble_patch_target!","page":"Patch items","text":"assemble_patch_target!(target, terms, ws::PatchAssemblyWorkspace, states, p, ctx = nothing)\n\nACCUMULATE the tuple of PatchTerms terms over the patch ws is positioned on into target — a patch-local matrix (the Jacobian kernels run), a patch-local vector (the residual kernels run), or a Ferrite assembler over a patch-local matrix. target is NOT zeroed here, so repeated calls accumulate.\n\nOne pass over the patch's cells in ascending order, the active terms of each cell accumulating into the element buffer before a single scatter. Callable directly: a local BVP assembles its matrix with one call and its N right-hand sides with N calls carrying per-column term data, on one workspace.\n\nTerm payloads reach assemble_patch_cell! over a PatchArgs carrying the cell's group tag and patch-local dof window.\n\nPosition ws with Ferrite.reinit!(ws, patchid) first; foreach_patch has already done that. Slot names are validated by the entry points once per sweep, not here.\n\n\n\n\n\n","title":"FerriteOperators.assemble_patch_target!"},{"category":"method","location":"patches/#FerriteOperators.augment_prescribed_dofs!-Tuple{PatchItems, Int64, Any}","page":"Patch items","text":"augment_prescribed_dofs!(provider, i, localdofs)\n\nAdd localdofs (patch-local) to patch i's prescribed set and recompute the free set — for dofs the local BVP needs pinned that no prescribed facet touches (e.g. floating components of a perforated patch). Setup-time mutation: call before any sweep reads the partition.\n\n\n\n\n\n","title":"FerriteOperators.augment_prescribed_dofs!"},{"category":"method","location":"patches/#FerriteOperators.current_patch-Tuple{PatchAssemblyWorkspace}","page":"Patch items","text":"The patch index ws is positioned on, set by Ferrite.reinit!(ws, patchid).\n\n\n\n\n\n","title":"FerriteOperators.current_patch"},{"category":"method","location":"patches/#FerriteOperators.emit_patch_column!-Tuple{PatchTripletSink, Any, Int64, Any}","page":"Patch items","text":"emit_patch_column!(sink::PatchTripletSink, rows, col, values)\n\nAppend (rows[l], col, values[l]) for every l, in order.\n\n\n\n\n\n","title":"FerriteOperators.emit_patch_column!"},{"category":"function","location":"patches/#FerriteOperators.foreach_patch","page":"Patch items","text":"foreach_patch(f, op, provider::PatchItems, states, p, ctx = nothing; items = 1:npatches(provider))\n\nCall f(ws, patchid) once per patch named by items (default: every patch of provider), in ascending item order, with ws a PatchAssemblyWorkspace already positioned on patchid. Inside f the caller owns the patch: assemble targets with assemble_patch_target! (passing states, p and ctx on), factorize, solve, retain the factorization in an ItemStates slot, and emit through a sink — emit_patch_column! writes one column per call, so an N-column local basis is N emissions.\n\nitems is what a partial re-solve names: only those patches are visited, and what the untouched ones contributed to a previous sweep is untouched too, since FO writes nothing outside f.\n\nSequential: FO cannot duplicate what f writes into, since the collectors and the retained state are the caller's — scheduling is the caller's too, through patch_workspace, patch_chunks and duplicate_for_device (see the parallel section of the patch documentation).\n\nExperimental: part of the patch item family.\n\n\n\n\n\n","title":"FerriteOperators.foreach_patch"},{"category":"method","location":"patches/#FerriteOperators.npatches-Tuple{PatchItems}","page":"Patch items","text":"Number of patches (items) of the provider.\n\n\n\n\n\n","title":"FerriteOperators.npatches"},{"category":"method","location":"patches/#FerriteOperators.patch_cell_groups-Tuple{PatchItems, Int64}","page":"Patch items","text":"Group tag of each cell of patch i, aligned with patch_cells.\n\n\n\n\n\n","title":"FerriteOperators.patch_cell_groups"},{"category":"method","location":"patches/#FerriteOperators.patch_cells-Tuple{PatchItems, Int64}","page":"Patch items","text":"Cells of patch i, ascending — the order every patch sweep visits them in.\n\n\n\n\n\n","title":"FerriteOperators.patch_cells"},{"category":"method","location":"patches/#FerriteOperators.patch_chunks-Tuple{PatchItems, Int64}","page":"Patch items","text":"patch_chunks(provider::PatchItems, nchunks::Int; items = 1:npatches(provider))\n\nSplit items (default: every patch) into at most nchunks CONTIGUOUS ascending pieces of nearly equal length. Contiguity and order are contract: merging per-chunk collectors in chunk order reproduces the item order of the sequential sweep over the same items exactly. Empty chunks are dropped, so fewer items than chunks yields fewer pieces.\n\nA chunk is a slice of the item list, so the default yields UnitRange{Int}s of patch indices and an explicit subset yields the ascending item ids themselves.\n\n\n\n\n\n","title":"FerriteOperators.patch_chunks"},{"category":"method","location":"patches/#FerriteOperators.patch_dofs-Tuple{PatchItems, Int64}","page":"Patch items","text":"Global dofs of patch i in patch-local order (the local→global injection).\n\n\n\n\n\n","title":"FerriteOperators.patch_dofs"},{"category":"function","location":"patches/#FerriteOperators.patch_emit!","page":"Patch items","text":"patch_emit!(sink, provider, pid, v)\n\nPublish patch pid's finished patch-local vector v into sink, mapped through the injection. emit_patch_column! is the column-naming form a many-columns-per-patch result emits through instead.\n\n\n\n\n\n","title":"FerriteOperators.patch_emit!"},{"category":"method","location":"patches/#FerriteOperators.patch_free_dofs-Tuple{PatchItems, Int64}","page":"Patch items","text":"Patch-local dofs of patch i the local BVP solves for (sorted).\n\n\n\n\n\n","title":"FerriteOperators.patch_free_dofs"},{"category":"method","location":"patches/#FerriteOperators.patch_ndofs-Tuple{PatchItems, Int64}","page":"Patch items","text":"Number of dofs of patch i.\n\n\n\n\n\n","title":"FerriteOperators.patch_ndofs"},{"category":"method","location":"patches/#FerriteOperators.patch_prescribed_dofs-Tuple{PatchItems, Int64}","page":"Patch items","text":"Patch-local dofs of patch i the local BVP prescribes (sorted).\n\n\n\n\n\n","title":"FerriteOperators.patch_prescribed_dofs"},{"category":"method","location":"patches/#FerriteOperators.patch_provider-Tuple{PatchAssemblyWorkspace}","page":"Patch items","text":"The PatchItems provider ws resolves its patch index against.\n\n\n\n\n\n","title":"FerriteOperators.patch_provider"},{"category":"method","location":"patches/#FerriteOperators.patch_term_active-Tuple{WholePatch, Int64}","page":"Patch items","text":"patch_term_active(restriction, group::Int) -> Bool\n\nWhether a term carrying restriction contributes on a patch cell tagged group: always for WholePatch, only on a matching tag for CellGroup. A downstream restriction type is a method here.\n\n\n\n\n\n","title":"FerriteOperators.patch_term_active"},{"category":"method","location":"patches/#FerriteOperators.patch_vertex_dofs-Tuple{PatchItems, Int64}","page":"Patch items","text":"patch_vertex_dofs(provider, i) -> Dict{Int, Vector{Int}}\n\nGrid vertex (node) id → the patch-local dofs of the provider's field at that vertex, for patch i. Computed per call, so concurrent calls are safe.\n\n\n\n\n\n","title":"FerriteOperators.patch_vertex_dofs"},{"category":"method","location":"patches/#FerriteOperators.patch_vertices-Tuple{PatchItems, Int64}","page":"Patch items","text":"patch_vertices(provider, i) -> Vector{Int}\n\nGrid vertex (node) ids of patch i, ascending. With patch_vertex_dofs, what a caller needs to restrict its own vertex-indexed data (e.g. a fine→coarse vertex map) to the patch.\n\n\n\n\n\n","title":"FerriteOperators.patch_vertices"},{"category":"method","location":"patches/#FerriteOperators.patch_workspace-Tuple{Any, PatchItems}","page":"Patch items","text":"patch_workspace(op, provider::PatchItems)\n\nAn INDEPENDENT PatchAssemblyWorkspace over provider, built from the operator's element caches for the provider's SubDofHandler. Every call duplicates those caches, so n calls give n workers sharing only the provider — the constructor a caller scheduling patches itself needs.\n\n\n\n\n\n","title":"FerriteOperators.patch_workspace"},{"category":"type","location":"patches/#FerriteOperators.ItemStates","page":"Patch items","text":"ItemStates{S}(nitems)\n\nPer-item state slots of element type S, indexed by any integer item index, not only patch ids. Two uses, one mechanism, differing only in the caller's invalidation policy: item-lifetime state that must survive across sweeps (a retained factorization, a reduction snapshot), and the solve→scatter payload channel within one sweep.\n\nFreshness is the caller's: FO never writes and never invalidates a slot. Store with set_item_state!, test with has_item_state, and drop stale content with invalidate_item_state! when whatever the slot was derived from changes. Slots are indexed by ITEM, so items processed by different workers touch disjoint slots — item lifetime is not worker lifetime, and a slot must never be handed to a worker-lifetime cache.\n\n\n\n\n\n","title":"FerriteOperators.ItemStates"},{"category":"method","location":"patches/#FerriteOperators.has_item_state-Tuple{ItemStates, Int64}","page":"Patch items","text":"Is item i's state slot filled and not invalidated?\n\n\n\n\n\n","title":"FerriteOperators.has_item_state"},{"category":"method","location":"patches/#FerriteOperators.invalidate_item_state!-Tuple{ItemStates, Int64}","page":"Patch items","text":"Drop item i's state (the caller's invalidation trigger fired).\n\n\n\n\n\n","title":"FerriteOperators.invalidate_item_state!"},{"category":"method","location":"patches/#FerriteOperators.invalidate_item_states!-Tuple{ItemStates}","page":"Patch items","text":"Drop every item's state.\n\n\n\n\n\n","title":"FerriteOperators.invalidate_item_states!"},{"category":"method","location":"patches/#FerriteOperators.item_state-Tuple{ItemStates, Int64}","page":"Patch items","text":"item_state(st, i)\n\nItem i's state. Throws when the slot is empty or invalidated — guard with has_item_state.\n\n\n\n\n\n","title":"FerriteOperators.item_state"},{"category":"method","location":"patches/#FerriteOperators.set_item_state!-Tuple{ItemStates, Int64, Any}","page":"Patch items","text":"Store s as item i's state and mark it valid.\n\n\n\n\n\n","title":"FerriteOperators.set_item_state!"},{"category":"section","location":"element-api/#Element-API-reference","page":"Element API reference","text":"","title":"Element API reference"},{"category":"type","location":"element-api/#FerriteOperators.AbstractBilinearIntegrator","page":"Element API reference","text":"AbstractBilinearIntegrator\n\nSupertype of integrators describing a bilinear form a(u, v): the element kernel fills a state-independent element matrix. The operator that form induces is linear, so its assembled matrix IS that operator's Jacobian and its residual is the action F(u) = A·u.\n\nA structural declaration, not a performance hint: such an operator issues no sensitivity kind, so it carries neither the ADElementCache decoration nor the per-worker SensitivityBuffers, whatever its element caches implement analytically (needs_ad_decoration).\n\n\n\n\n\n","title":"FerriteOperators.AbstractBilinearIntegrator"},{"category":"type","location":"element-api/#FerriteOperators.AbstractCondensedNonlinearIntegrator","page":"Element API reference","text":"AbstractCondensedNonlinearIntegrator <: AbstractNonlinearIntegrator\n\nSupertype of AbstractNonlinearIntegrators whose elements condense internal state (has_internal_state): the dispatch anchor a downstream integrator subtypes to opt into the condensation machinery (condense_internal!, the InternalVariableHandler block, the admissibility checks that reject an AD fallback over hidden state).\n\n\n\n\n\n","title":"FerriteOperators.AbstractCondensedNonlinearIntegrator"},{"category":"type","location":"element-api/#FerriteOperators.AbstractLinearIntegrator","page":"Element API reference","text":"AbstractLinearIntegrator\n\nSupertype of integrators describing a linear form l(v): the element kernel fills the form's load vector, and the operator holds that vector and no matrix. The form has no state to depend on, so this family too carries no AD or sensitivity machinery, and a BlockedOperatorSpecification on such an operator is rejected at setup_operator — there is no matrix to lay out.\n\n\n\n\n\n","title":"FerriteOperators.AbstractLinearIntegrator"},{"category":"type","location":"element-api/#FerriteOperators.AbstractNonlinearIntegrator","page":"Element API reference","text":"AbstractNonlinearIntegrator\n\nSupertype of integrators describing a state-dependent residual form — the general case, and the only family whose operators carry differentiation machinery. The residual kernel is mandatory; every other request is served analytically where provides_analytic declares it, otherwise by differentiating that kernel — what the ADElementCache decoration and the per-worker SensitivityBuffers exist for.\n\nA bilinear sub-integrator composes into this family (the operator its form induces scatters into the same matrix and residual); a linear one does not, its sink being a vector alone.\n\n\n\n\n\n","title":"FerriteOperators.AbstractNonlinearIntegrator"},{"category":"type","location":"element-api/#FerriteOperators.AbstractAssemblyRequest","page":"Element API reference","text":"AbstractAssemblyRequest\n\nWhat a kernel is asked to compute for one cell. Kernels dispatch on the request type, never on argument shapes, so kernel sets for different state slots can coexist without ambiguity.\n\n\n\n\n\n","title":"FerriteOperators.AbstractAssemblyRequest"},{"category":"type","location":"element-api/#FerriteOperators.AffineRate","page":"Element API reference","text":"AffineRate(slope, anchor)\n\nSlot source reconstructing a rate-like slot from the primary unknown: the slot's cell-local value is slope · (u − anchor), formed at gather time from the :u slot. Solvers pass it in place of a plain vector, e.g. backward Euler states = (u = u, du = AffineRate(1/Δt, uprev)) or Newmark states = (u = u, v = AffineRate(γ/(β*Δt), uᵥ)); the slot is declared like any other (setup_operator(...; slots = (:u, :du))).\n\nA :u slot must exist and PRECEDE the reconstructed slot in the states NamedTuple — the sweep throws otherwise. Kernels read the reconstructed values through args.states.<name> and nothing else, so an element never encodes a time-integration scheme.\n\nwarning: Reconstructed slots are frozen under AD\nReconstruction happens at gather time, before any Dual seeding, and the ∂F/∂u sweep seeds only the :u buffer. A kernel reading a reconstructed slot sees its primal value throughout, so the assembled Jacobian is ∂F/∂u at frozen slot values. The chain-rule term (slope · ∂F/∂slot) belongs to the solver, which contributes it through its per-slot weights.\n\nnote: Condensed elements\nThe reconstruction applies uniformly to ALL entries of the element buffer, because u and anchor are both [ū; q]-shaped gathers. Internal variables are never rate-reconstructed by contract: the condensed tail of a reconstructed slot is an artifact of the uniform formula and elements must not interpret it.\n\n\n\n\n\n","title":"FerriteOperators.AffineRate"},{"category":"type","location":"element-api/#FerriteOperators.AlgebraicArgs","page":"Element API reference","text":"AlgebraicArgs(states, item, p, ctx)\n\nThe argument bundle an algebraic kernel's third parameter receives — the CellArgs analogue (see its table for states and ctx) with the AlgebraicItem where the geometry cache sits:\n\nfield owner / lifetime\nitem the engine; the AlgebraicItem the sweep stands on — READ-ONLY for kernels\np the cache; the item-local parameter view from query_cell_parameters, queried on the item\n\nKernels select on the (request, cache) pair, so annotating the parameter (args::AlgebraicArgs) is permitted. This record shares no supertype with CellArgs/FacetArgs: a cell kernel and an algebraic kernel never meet at the same dispatch site. Hand-constructing an instance unit-tests a kernel without an operator:\n\nargs = AlgebraicArgs((u = uₑ,), AlgebraicItem(1, [17]), p, nothing)\nassemble_algebraic!(ResidualRequest(rₑ), cache, args)\n\n\n\n\n\n","title":"FerriteOperators.AlgebraicArgs"},{"category":"type","location":"element-api/#FerriteOperators.AlgebraicItem","page":"Element API reference","text":"AlgebraicItem(index, dofs)\n\nThe item an algebraic kernel is positioned on: its index into the algebraic_items declaration, and the global dofs its local system maps onto. This is the whole geometry an item of this family has — a cache serving several 0D rows selects its model by args.item.index.\n\n\n\n\n\n","title":"FerriteOperators.AlgebraicItem"},{"category":"type","location":"element-api/#FerriteOperators.CellArgs","page":"Element API reference","text":"CellArgs(states, cell, p, ctx)\n\nThe argument bundle a cell kernel's third parameter receives.\n\nfield owner / lifetime\nstates the engine; one cell-local slot buffer per slot declared at setup ((u = uₑ, uprev = uₑprev, …)), refreshed every sweep\ncell the engine; the geometry cache of the current item — READ-ONLY for kernels\np the element; the cell-local parameter view from query_cell_parameters — configuration only, never time or history\nctx the solver; the per-sweep scalars — a TimeIntegrationContext (t, Δt, γ̃), or nothing for stationary sweeps. The one open channel: a scheme with richer scalars passes its own context type, read through evaluation_time/with_time/stage_scaling instead of field access.\n\nKernels select on the (request, cache) pair, never on args, so annotating the parameter (args::CellArgs) is permitted. Hand-constructing an instance unit-tests a kernel without an operator:\n\nargs = CellArgs((u = uₑ,), cell_cache, p, nothing)\nassemble_cell!(ResidualRequest(rₑ), cache, args)\n\n\n\n\n\n","title":"FerriteOperators.CellArgs"},{"category":"type","location":"element-api/#FerriteOperators.Consistent","page":"Element API reference","text":"CorrectionMode\nConsistent <: CorrectionMode\nFrozenQ    <: CorrectionMode\n\nWhether a Jacobian-shaped request over a condensed element is the TOTAL derivative or the PARTIAL at frozen internal state q:\n\nConsistent:  ∂F/∂·|_q + ∂F/∂q · dq/d·     (the total — DEFAULT)\nFrozenQ:     ∂F/∂·|_q only                (the partial)\n\nConsistent is the default wherever the type parameter is left unspecified: the unsafe direction is a silently missing correction, so FrozenQ must be spelled explicitly. For a stateless element (no q) the two coincide. FrozenQ is a legitimate election for an iteration matrix (modified Newton, MLN outer loops), where a wrong tangent costs convergence rate and nothing else, and never for a gradient — the sensitivity request kinds therefore carry no CorrectionMode parameter at all.\n\n\n\n\n\n","title":"FerriteOperators.Consistent"},{"category":"type","location":"element-api/#FerriteOperators.CorrectionMode","page":"Element API reference","text":"CorrectionMode\nConsistent <: CorrectionMode\nFrozenQ    <: CorrectionMode\n\nWhether a Jacobian-shaped request over a condensed element is the TOTAL derivative or the PARTIAL at frozen internal state q:\n\nConsistent:  ∂F/∂·|_q + ∂F/∂q · dq/d·     (the total — DEFAULT)\nFrozenQ:     ∂F/∂·|_q only                (the partial)\n\nConsistent is the default wherever the type parameter is left unspecified: the unsafe direction is a silently missing correction, so FrozenQ must be spelled explicitly. For a stateless element (no q) the two coincide. FrozenQ is a legitimate election for an iteration matrix (modified Newton, MLN outer loops), where a wrong tangent costs convergence rate and nothing else, and never for a gradient — the sensitivity request kinds therefore carry no CorrectionMode parameter at all.\n\n\n\n\n\n","title":"FerriteOperators.CorrectionMode"},{"category":"type","location":"element-api/#FerriteOperators.FacetArgs","page":"Element API reference","text":"FacetArgs(states, cell, p, ctx)\n\nThe argument bundle a facet kernel's third parameter receives — the same four fields as CellArgs, built by the framework's facet driver. CellArgs and FacetArgs share no supertype: a cell kernel and a facet kernel never meet at the same dispatch site.\n\n\n\n\n\n","title":"FerriteOperators.FacetArgs"},{"category":"type","location":"element-api/#FerriteOperators.FrozenQ","page":"Element API reference","text":"CorrectionMode\nConsistent <: CorrectionMode\nFrozenQ    <: CorrectionMode\n\nWhether a Jacobian-shaped request over a condensed element is the TOTAL derivative or the PARTIAL at frozen internal state q:\n\nConsistent:  ∂F/∂·|_q + ∂F/∂q · dq/d·     (the total — DEFAULT)\nFrozenQ:     ∂F/∂·|_q only                (the partial)\n\nConsistent is the default wherever the type parameter is left unspecified: the unsafe direction is a silently missing correction, so FrozenQ must be spelled explicitly. For a stateless element (no q) the two coincide. FrozenQ is a legitimate election for an iteration matrix (modified Newton, MLN outer loops), where a wrong tangent costs convergence rate and nothing else, and never for a gradient — the sensitivity request kinds therefore carry no CorrectionMode parameter at all.\n\n\n\n\n\n","title":"FerriteOperators.FrozenQ"},{"category":"type","location":"element-api/#FerriteOperators.InternalSource","page":"Element API reference","text":"InternalSource(u::AbstractVector)\n\nSlot source restricting the gather to the condensed internal-variable block of u (the q tail of [ū; q], internal_variable_range) instead of celldofs(cell) — this is what makes q an ordinary slot (states = (u = u, q = InternalSource(u), …)), the source carrying its own restriction exactly as AffineRate carries reconstruction. Such a slot is sized per cell by the cell's internal dof count, not by ndofs_per_cell, so its buffer is resized on every gather.\n\ncondense_internal! is the only writer of the underlying vector; evaluation sweeps only read through it.\n\n\n\n\n\n","title":"FerriteOperators.InternalSource"},{"category":"type","location":"element-api/#FerriteOperators.JacobianRequest","page":"Element API reference","text":"JacobianRequest{slot, C <: CorrectionMode}(K)\n\nAccumulate ∂F/∂slot into K, slot ∈ (:u, :du, :v, :a, :q, …), under correction mode C (see CorrectionMode). JacobianRequest{slot}(K) defaults C to Consistent.\n\n\n\n\n\n","title":"FerriteOperators.JacobianRequest"},{"category":"type","location":"element-api/#FerriteOperators.JacobianResidualRequest","page":"Element API reference","text":"JacobianResidualRequest{C <: CorrectionMode}(K, r)\n\nAccumulate ∂F/∂u and the residual in one sweep (the Newton hot path), under correction mode C. JacobianResidualRequest(K, r) defaults C to Consistent.\n\n\n\n\n\n","title":"FerriteOperators.JacobianResidualRequest"},{"category":"type","location":"element-api/#FerriteOperators.ParameterJacobianRequest","page":"Element API reference","text":"Accumulate the dense local parameter Jacobian ∂Fₑ/∂θ into B (ndofsₑ × nθ). p is the GLOBAL parameter bag, not the element-local args.p: the AD fallback re-queries query_cell_parameters from a Dual-rebuilt p per seed direction, so wrappers and per-element views forward Duals transparently. An analytic kernel reads B only.\n\n\n\n\n\n","title":"FerriteOperators.ParameterJacobianRequest"},{"category":"type","location":"element-api/#FerriteOperators.ParameterVJPRequest","page":"Element API reference","text":"Accumulate the dense local parameter Jacobian ∂Fₑ/∂θ into B (ndofsₑ × nθ). p is the GLOBAL parameter bag, not the element-local args.p: the AD fallback re-queries query_cell_parameters from a Dual-rebuilt p per seed direction, so wrappers and per-element views forward Duals transparently. An analytic kernel reads B only.\n\n\n\n\n\n","title":"FerriteOperators.ParameterVJPRequest"},{"category":"type","location":"element-api/#FerriteOperators.ResidualRequest","page":"Element API reference","text":"Accumulate the local residual into r.\n\n\n\n\n\n","title":"FerriteOperators.ResidualRequest"},{"category":"type","location":"element-api/#FerriteOperators.StateJVPRequest","page":"Element API reference","text":"Accumulate the state Jacobian-vector product (∂Fₑ/∂u)·vₑ into Jv (matrix-free J action).\n\n\n\n\n\n","title":"FerriteOperators.StateJVPRequest"},{"category":"type","location":"element-api/#FerriteOperators.StateVJPRequest","page":"Element API reference","text":"Accumulate the state pullback (∂Fₑ/∂u)ᵀλₑ into g (matrix-free Jᵀ action).\n\n\n\n\n\n","title":"FerriteOperators.StateVJPRequest"},{"category":"type","location":"element-api/#FerriteOperators.TimeIntegrationContext","page":"Element API reference","text":"TimeIntegrationContext(t, Δt, γ̃)\n\nSolver-controlled scalars: evaluation time t (Dual-typed during ∂F/∂t sweeps), physical step size Δt, and the effective local stage interval γ̃ of the element-local internal-variable problem, normalized by the canonical form\n\nq = q_ref + γ̃ · g(·, q)\n\n— passing γ̃ means \"an implicit-Euler local integrator would solve exactly this\" (q_ref is dof-shaped and flows through a slot, solver-folded for multistep schemes). This normalizes the number, not the element's local rule: the element may realize the update with any consistent rule over γ̃ — implicit Euler, exact exponential (Rush-Larsen-type q = q∞ + (q_ref − q∞)·exp(−γ̃/τ)), or local substepping.\n\nwarning: Warning\nγ̃ is NOT the rate-reconstruction slope of any state slot. Under backward Euler the two are reciprocals (slope = 1/Δt, γ̃ = Δt), so 1/γ̃ as a rate slope is accidentally right under BE and silently wrong under every other scheme (Newmark: slope = γ/(βΔt) while γ̃ = Δt). Rate slopes belong to the slot that carries the reconstruction.\n\n\n\n\n\n","title":"FerriteOperators.TimeIntegrationContext"},{"category":"type","location":"element-api/#FerriteOperators.TimeSensitivityRequest","page":"Element API reference","text":"Accumulate the explicit time sensitivity ∂Fₑ/∂t into g.\n\n\n\n\n\n","title":"FerriteOperators.TimeSensitivityRequest"},{"category":"type","location":"element-api/#FerriteOperators.WeightedJacobianRequest","page":"Element API reference","text":"WeightedJacobianRequest(K, weights)\n\nAccumulate the weighted Jacobian Σₛ wₛ ∂F/∂s into K, over the slots named by the per-slot NamedTuple weights and at frozen values of every other slot. The scheme's chain-rule scalars ride as request payload, so a fused kernel and the composed per-slot fallback draw from one source (see WeightedJacobianKind).\n\nA hand-fused scheme matrix (SDIRK/backward Euler W = M/(γΔt) + K) is an analytic provider of THIS request, not of JacobianRequest: it computes the combination internally, which no single-slot Jacobian does.\n\n\n\n\n\n","title":"FerriteOperators.WeightedJacobianRequest"},{"category":"function","location":"element-api/#FerriteOperators.assemble_cell!","page":"Element API reference","text":"assemble_cell!(req::AbstractAssemblyRequest, cache, args)\n\nThe volumetric kernel entry point. Elements must provide at least the ResidualRequest method (validated at setup); every other request falls back to automatic differentiation of the residual kernel unless provides_analytic declares an analytic method. args is a CellArgs.\n\n\n\n\n\n","title":"FerriteOperators.assemble_cell!"},{"category":"method","location":"element-api/#FerriteOperators.assert_sensitivity_admissible-Union{Tuple{Args}, Tuple{Type, Any}, Tuple{Type, Any, Any}, Tuple{Type, Any, Any, Type{Args}}, Tuple{Type, Any, Any, Type{Args}, Tuple}} where Args","page":"Element API reference","text":"assert_sensitivity_admissible(T::Type, kind)\nassert_sensitivity_admissible(T::Type, kind, entry, ::Type{Args}, trailing = ())\n\nThe internal-state admissibility check: throws unless a has_internal_state cache T serves_kind or declares it internal_state_insensitive. entry/Args/trailing name the item family whose kernel entry point, args record and signature tail the error message should point authors at; the 2-arg form defaults to the volumetric assemble_cell!/CellArgs. validate_algebraic_cache passes assemble_algebraic!/AlgebraicArgs, whose remedies differ, and the facet-item family its trailing ::Int local facet index.\n\nT is a SERVED-CAPABILITY subject, so it is the cache the engine calls, decorated where the engine decorated it. That is what admits the generic routes ADElementCache builds from condensed_corrector and local_conditions!: serves_kind reports them as the coverage they are, and an undecorated cache has no such route.\n\n\n\n\n\n","title":"FerriteOperators.assert_sensitivity_admissible"},{"category":"method","location":"element-api/#FerriteOperators.evaluation_time-Tuple{TimeIntegrationContext}","page":"Element API reference","text":"Evaluation time of the sweep this context belongs to.\n\n\n\n\n\n","title":"FerriteOperators.evaluation_time"},{"category":"method","location":"element-api/#FerriteOperators.has_cell_request-Union{Tuple{Type{K}}, Tuple{K}} where K","page":"Element API reference","text":"has_cell_request(::Type{K}) -> Bool\n\nWhether kind K materializes an assemble_cell! request, i.e. whether request_type answers for it. Setup validates the trait ↔ kernel backing of declared kinds that do.\n\nKinds served by a different element hook declare false: FunctionalKind reaches the element through evaluate_cell_functional and returns its contribution instead of filling a request.\n\n\n\n\n\n","title":"FerriteOperators.has_cell_request"},{"category":"method","location":"element-api/#FerriteOperators.has_internal_state-Tuple{Type}","page":"Element API reference","text":"has_internal_state(::Type{CacheType}) -> Bool\n\ntrue iff the element cache carries condensed per-item internal state q with a corrector store (condense_internal!). Governs the sensitivity admissibility check: a kind with no CorrectionMode is always the total, so AD-from-residual through the pure kernel — which computes only the frozen-q partial — is missing the correction unless the cache serves_kind or declares it internal_state_insensitive; time sensitivities alone admit a finite-difference escape.\n\n\n\n\n\n","title":"FerriteOperators.has_internal_state"},{"category":"method","location":"element-api/#FerriteOperators.internal_state_insensitive-Tuple{Type, Any}","page":"Element API reference","text":"internal_state_insensitive(::Type{CacheType}, kind) -> Bool\n\nAuthor-asserted declaration that the element-local internal-state equations do NOT depend on the quantity the sensitivity kind seeds (∂L/∂seed ≡ 0), so dq/∂seed = 0 and the total collapses to the frozen-q partial plain AD-from-residual already computes. The framework CANNOT verify this claim; a wrong assertion produces a silently wrong sensitivity. Same trust model as provides_analytic.\n\n\n\n\n\n","title":"FerriteOperators.internal_state_insensitive"},{"category":"method","location":"element-api/#FerriteOperators.parameter_vector-Tuple{Real}","page":"Element API reference","text":"parameter_vector(p) -> AbstractVector\n\nFlat vector view θ of the differentiable parameters in p. With rebuild_parameters this is the seam through which parameter sensitivities are seeded; implement both for custom parameter types.\n\nθ need not cover all of p: entries not exposed here are held fixed by every sensitivity sweep, and all parameter-sensitivity costs (seed dimension, local Jacobian columns) scale with length(θ).\n\n\n\n\n\n","title":"FerriteOperators.parameter_vector"},{"category":"method","location":"element-api/#FerriteOperators.provides_analytic-Tuple{Type, Any}","page":"Element API reference","text":"provides_analytic(::Type{CacheType}, kind) -> Bool\n\ntrue iff the cache has an AUTHOR-WRITTEN analytic kernel for the given request kind singleton (JacobianKind(), ParameterJacobianKind(), …) — that question and no other; whether the cache the engine calls serves the kind at all is serves_kind. Everything except the mandatory residual defaults to false, i.e. AD-from-residual, and decorators forward the trait unchanged.\n\nThere is deliberately exactly ONE root method (the residual default is a runtime branch), so specializations are always strictly more specific: blanket declarations like provides_analytic(::Type{<:MyCache}, kind) = true cannot create ambiguities, and there is no request-type-parameter matching to get wrong. Kernel/trait consistency is validated once at operator setup (validate_element_cache).\n\n\n\n\n\n","title":"FerriteOperators.provides_analytic"},{"category":"method","location":"element-api/#FerriteOperators.query_cell_parameters-Tuple{Any, Any, Any}","page":"Element API reference","text":"query_cell_parameters(cache, cell, p)\n\nElement-overridable query producing the element-local parameter view pₑ handed to volumetric kernels; parameter fields gather their per-element views through this seam. Defaults to the parameter bag itself.\n\n\n\n\n\n","title":"FerriteOperators.query_cell_parameters"},{"category":"method","location":"element-api/#FerriteOperators.query_facet_parameters-NTuple{4, Any}","page":"Element API reference","text":"query_facet_parameters(cache, cell, local_facet_index, p)\n\nFacet analogue of query_cell_parameters — boundary caches get their own parameter query per facet instead of reusing the volumetric object.\n\n\n\n\n\n","title":"FerriteOperators.query_facet_parameters"},{"category":"method","location":"element-api/#FerriteOperators.rebuild_parameters-Tuple{Real, Any}","page":"Element API reference","text":"rebuild_parameters(p, θ) -> typeof-compatible parameters\n\nReconstruct a parameter object structurally equal to p with the differentiable entries replaced by θ (possibly Dual-valued).\n\n\n\n\n\n","title":"FerriteOperators.rebuild_parameters"},{"category":"method","location":"element-api/#FerriteOperators.requires_admissibility_check-Tuple{Any}","page":"Element API reference","text":"requires_admissibility_check(kind) -> Bool\n\nWhether declaring kind runs the internal-state admissibility rule (assert_sensitivity_admissible) at setup instead of on first use. Defaults to false, so a downstream kind opts in.\n\nTrue for the kinds whose AD fallback differentiates THROUGH an element's local solve. Time sensitivities and weighted Jacobians are exempt although they differentiate: their escape (finite differences, the composed route) is chosen per call, so setup cannot know whether the AD path will be taken.\n\n\n\n\n\n","title":"FerriteOperators.requires_admissibility_check"},{"category":"method","location":"element-api/#FerriteOperators.serves_kind-Tuple{Type, Any}","page":"Element API reference","text":"serves_kind(::Type{CacheType}, kind) -> Bool\n\ntrue iff the RESOLVED cache — the possibly decorated type the engine calls — answers kind with the quantity that kind names, whether from an author-written kernel or from a decorator's generic route. provides_analytic implies this; the converse does not, since ADElementCache serves kinds its inner has no kernel for.\n\nSame single-root shape as provides_analytic, so the literal returns constant fold. Deliberately NOT exported: the methods belong to the wrapping layer (ADElementCache, CompositeVolumetricElementCache), while an element author declares provides_analytic and this default composes.\n\n\n\n\n\n","title":"FerriteOperators.serves_kind"},{"category":"method","location":"element-api/#FerriteOperators.stage_scaling-Tuple{TimeIntegrationContext}","page":"Element API reference","text":"Effective local stage interval γ̃ (see TimeIntegrationContext); wrapper context types must forward it, same as evaluation_time.\n\n\n\n\n\n","title":"FerriteOperators.stage_scaling"},{"category":"function","location":"element-api/#FerriteOperators.validate_element_cache","page":"Element API reference","text":"validate_element_cache(cache, declared_requests = (); iterator_type = CellCache)\n\nSetup-time consistency check for element caches: a cache opting into the request protocol must implement the mandatory ResidualRequest kernel, and every request kind provides_analytic claims must have a matching kernel method. Runs once per subdomain at setup_operator time, so a typo'd port fails loudly instead of silently assembling through the wrong path.\n\niterator_type is the subdomain's RESOLVED host assembly_iterator type: reinit_values! is probed against it rather than against CellCache unconditionally, so a cache written for a custom iterator (reinit_values!(c, ::MyIterator)) is validated against the type it was actually annotated on.\n\nThe two halves take different subjects, per the AbstractElementCacheDecorator convention. The KERNEL half — the mandatory-method probes and the trait ↔ kernel check — runs on the unwrap fixpoint (a composite recurses into its inners from there), since a decorator's forwarding methods would answer those probes unconditionally. The ADMISSIBILITY half runs on cache as the engine calls it, decoration included, so a decorator's generic routes count as the coverage they are.\n\nThe trait ↔ kernel check always covers the primal kinds. Every kind requires_admissibility_check names additionally runs the internal-state admissibility check (has_internal_state) here instead of on first use — unconditionally for the primal kinds, and for any other kind only when declared via setup_operator(...; requests = (ParameterVJPKind, …)). Undeclared, non-primal kinds stay usable; their checks run at the call-time entry points.\n\n\n\n\n\n","title":"FerriteOperators.validate_element_cache"},{"category":"method","location":"element-api/#FerriteOperators.validation_instance-Union{Tuple{Type{K}}, Tuple{K}} where K","page":"Element API reference","text":"validation_instance(::Type{K}) -> kind instance\n\nThe placeholder instance setup-time validation queries K's traits on: declarations carry kind TYPES normalized to their UnionAll base, while request_type and provides_analytic are queried on instances.\n\nThe default calls K(). A kind whose payload is a type parameter overloads it with a placeholder — only the type is read, never the value:\n\nFerriteOperators.validation_instance(::Type{<:MyVJPKind}) = MyVJPKind(nothing)\n\nThere is no fallback that skips: a declared kind that cannot be instantiated raises at setup rather than silently missing its validation.\n\n\n\n\n\n","title":"FerriteOperators.validation_instance"},{"category":"method","location":"element-api/#FerriteOperators.with_context-Tuple{CellArgs, Any}","page":"Element API reference","text":"Rebuild args with the per-sweep context replaced — the ∂F/∂t seeding seam.\n\n\n\n\n\n","title":"FerriteOperators.with_context"},{"category":"method","location":"element-api/#FerriteOperators.with_parameters-Tuple{CellArgs, Any}","page":"Element API reference","text":"Rebuild args with the element-local parameter view replaced.\n\n\n\n\n\n","title":"FerriteOperators.with_parameters"},{"category":"method","location":"element-api/#FerriteOperators.with_states-Tuple{CellArgs, NamedTuple}","page":"Element API reference","text":"Rebuild args with the slot buffers replaced.\n\n\n\n\n\n","title":"FerriteOperators.with_states"},{"category":"method","location":"element-api/#FerriteOperators.with_time-Tuple{TimeIntegrationContext, Any}","page":"Element API reference","text":"Rebuild ctx with the evaluation time replaced by t̃ (Dual-typed in ∂F/∂t sweeps).\n\n\n\n\n\n","title":"FerriteOperators.with_time"},{"category":"type","location":"element-api/#FerriteOperators.AbstractSurfaceElementCache","page":"Element API reference","text":"Supertype for all caches to integrate over surfaces.\n\nFacet kernels are request-typed,\n\nassemble_facet!(req, cache, args, local_facet_index::Int)\n\nand run over the facets facet_items declares; facet parameters are queried separately per facet via query_facet_parameters. Setup happens through setup_facet_item_cache.\n\n\n\n\n\n","title":"FerriteOperators.AbstractSurfaceElementCache"},{"category":"type","location":"element-api/#FerriteOperators.AbstractVolumetricElementCache","page":"Element API reference","text":"Supertype for all caches to integrate over volumes.\n\nElements implement request-typed kernels\n\nassemble_cell!(req, cache, args)\n\nwith a mandatory ResidualRequest method (the AD basis) and optional analytic methods declared via provides_analytic. The cache owns reinit! of its values objects, selecting per request kind; the loop owns only the geometry cache. Setup happens through setup_element_cache(integrator, sdh).\n\nCondensed elements additionally implement get_number_of_internal_dofs_per_element(model, cache, sdh) and condense_cell!; q, their condensed internal state, is an ordinary slot sourced by InternalSource (see condense_internal!).\n\n\n\n\n\n","title":"FerriteOperators.AbstractVolumetricElementCache"},{"category":"type","location":"element-api/#FerriteOperators.EmptySurfaceElementCache","page":"Element API reference","text":"Utility to execute noop assembly.\n\n\n\n\n\n","title":"FerriteOperators.EmptySurfaceElementCache"},{"category":"type","location":"element-api/#FerriteOperators.EmptyVolumetricElementCache","page":"Element API reference","text":"Utility to execute noop assembly.\n\n\n\n\n\n","title":"FerriteOperators.EmptyVolumetricElementCache"},{"category":"method","location":"element-api/#FerriteOperators.algebraic_items-Tuple{Any, Any}","page":"Element API reference","text":"algebraic_items(integrator, dh) -> collection of dof vectors\n\nThe items of the algebraic family: one vector of GLOBAL dof indices of dh per term that lives on no cell — a 0D model's own rows, an AlgebraicCoupling-only block, a lumped balance equation. An item's local system is exactly those dofs in declaration order, so a circulation model contributing one row per chamber declares one single-dof item per chamber. Defaults to ().\n\nAll items of one declaration carry the SAME number of dofs — that is what keeps a worker's local buffers fixed-size — validated at setup, as are the dofs themselves (in bounds, and unique within an item). Dofs shared BETWEEN items are the normal case; AlgebraicItems derives the partition from that.\n\nThe sparsity entries are not inferred from this declaration: which dofs couple is the caller's Ferrite coupling descriptor (AlgebraicCoupling), passed through StandardOperatorSpecification/BlockedOperatorSpecification, exactly as for global_dofs.\n\n\n\n\n\n","title":"FerriteOperators.algebraic_items"},{"category":"method","location":"element-api/#FerriteOperators.allocate_element_matrix-Tuple{Any, Any}","page":"Element API reference","text":"allocate_element_matrix(element_cache, sdh)\nallocate_element_unknown_vector(element_cache, sdh)\nallocate_element_residual_vector(element_cache, sdh)\n\nThe element-local buffers of one item, sized in the FIELD SPACE — the ndofs_per_cell(sdh) dofs celldofs carries — and carrying the cache's element_value_type. Where the integrator declares global_dofs the engine PADS what these return, so an override states the field-space size and never the augmented one.\n\n\n\n\n\n","title":"FerriteOperators.allocate_element_matrix"},{"category":"method","location":"element-api/#FerriteOperators.allocate_element_residual_vector-Tuple{Any, Any}","page":"Element API reference","text":"allocate_element_matrix(element_cache, sdh)\nallocate_element_unknown_vector(element_cache, sdh)\nallocate_element_residual_vector(element_cache, sdh)\n\nThe element-local buffers of one item, sized in the FIELD SPACE — the ndofs_per_cell(sdh) dofs celldofs carries — and carrying the cache's element_value_type. Where the integrator declares global_dofs the engine PADS what these return, so an override states the field-space size and never the augmented one.\n\n\n\n\n\n","title":"FerriteOperators.allocate_element_residual_vector"},{"category":"method","location":"element-api/#FerriteOperators.allocate_element_unknown_vector-Tuple{Any, Any}","page":"Element API reference","text":"allocate_element_matrix(element_cache, sdh)\nallocate_element_unknown_vector(element_cache, sdh)\nallocate_element_residual_vector(element_cache, sdh)\n\nThe element-local buffers of one item, sized in the FIELD SPACE — the ndofs_per_cell(sdh) dofs celldofs carries — and carrying the cache's element_value_type. Where the integrator declares global_dofs the engine PADS what these return, so an override states the field-space size and never the augmented one.\n\n\n\n\n\n","title":"FerriteOperators.allocate_element_unknown_vector"},{"category":"function","location":"element-api/#FerriteOperators.apply_element_action!","page":"Element API reference","text":"apply_element_action!(yₑ, cache, uₑ, args::CellArgs)\n\nThe MATRIX-FREE element kernel: accumulate yₑ += Kₑ·uₑ for the current cell WITHOUT forming Kₑ. yₑ and uₑ are the cell's local vectors in celldofs order and args the ordinary CellArgs; yₑ arrives zeroed.\n\nImplementing it is what makes a cache usable under MatrixFreeAction. It is separate from the mandatory assemble_cell!(::ResidualRequest, …) because the two promise different things: the residual kernel evaluates the form, this one evaluates the action at the cost the matrix-free level exists for. An element whose action is Kₑ·uₑ at O(ndofs²) should not declare it.\n\nBoth arguments may be views into a shared device batch, so an implementation indexes them and neither resizes nor reallocates.\n\nwarning: Experimental surface\nThis entry point may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.apply_element_action!"},{"category":"function","location":"element-api/#FerriteOperators.assemble_algebraic!","page":"Element API reference","text":"assemble_algebraic!(req::AbstractAssemblyRequest, cache, args)\n\nThe kernel entry point of the algebraic item family — one per integration domain kind, alongside assemble_cell! and assemble_facet!. Accumulate the current item's contribution into req's buffers, which are sized by the item's dof count.\n\nThe ResidualRequest method is mandatory and validated at setup; every other request is served analytically where provides_analytic declares it and by ForwardDiff over the residual kernel otherwise, exactly as for a volumetric cache. args is an AlgebraicArgs.\n\n\n\n\n\n","title":"FerriteOperators.assemble_algebraic!"},{"category":"function","location":"element-api/#FerriteOperators.assemble_facet!","page":"Element API reference","text":"assemble_facet!(req, cache, args, local_facet_index::Int)\n\nThe volumetric-kernel analogue for facets: accumulate this facet's contribution to req's buffers. args is a FacetArgs. Facet kernels reinitialize their own FacetValues for local_facet_index and have no automatic-differentiation fallback — a surface cache serves the sweep's request analytically or not at all.\n\n\n\n\n\n","title":"FerriteOperators.assemble_facet!"},{"category":"function","location":"element-api/#FerriteOperators.cooperative_group_size","page":"Element API reference","text":"cooperative_lattice_dim(cache) -> Int\ncooperative_group_size(cache) -> Int\ncooperative_scratch_shape(cache) -> (Val(length), Val(count))\ncooperative_load!(scratch, cache, uₑ, lane::Int, nlanes::Int)\ncooperative_stage!(scratch, cache, args, stage::Int, lane::Int, nlanes::Int)\ncooperative_store!(yₑ, scratch, cache, lane::Int, nlanes::Int)\n\nThe CooperativeElement entries: one WORKGROUP evaluates one element's action, its nlanes workers splitting the element's lattice between them and staging everything in group-local memory.\n\nThe device kernel owns the barriers, a KernelAbstractions @synchronize being a lexical split of the kernel body that cannot live in a callee. So the element declares a FIXED PIPELINE and the kernel synchronizes between its steps:\n\ncooperative_load!            # gather uₑ into the scratch lattice\ncooperative_stage!(…, 1)     # …\n⋮                            # `2 * cooperative_lattice_dim(cache) - 1` stages\ncooperative_store!           # write the local result into yₑ\n\ncooperative_scratch_shape sizes the group-local scratch, a length × count array the kernel allocates and hands to every step. cooperative_group_size is the workgroup the kernel launches with, cooperative_lattice_dim the pipeline length. Every step is called by all nlanes workers with lane in 1:nlanes and writes only what that lane owns.\n\nThe pipeline's shape is a tensor-product gradient operator's: dim forward contractions with the pointwise map fused into the last, then dim backward contractions with the store fused into the last. An element whose pipeline has a different length, or a data-dependent one, is not supported.\n\nwarning: Experimental surface\nThese entry points may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.cooperative_group_size"},{"category":"function","location":"element-api/#FerriteOperators.cooperative_lattice_dim","page":"Element API reference","text":"cooperative_lattice_dim(cache) -> Int\ncooperative_group_size(cache) -> Int\ncooperative_scratch_shape(cache) -> (Val(length), Val(count))\ncooperative_load!(scratch, cache, uₑ, lane::Int, nlanes::Int)\ncooperative_stage!(scratch, cache, args, stage::Int, lane::Int, nlanes::Int)\ncooperative_store!(yₑ, scratch, cache, lane::Int, nlanes::Int)\n\nThe CooperativeElement entries: one WORKGROUP evaluates one element's action, its nlanes workers splitting the element's lattice between them and staging everything in group-local memory.\n\nThe device kernel owns the barriers, a KernelAbstractions @synchronize being a lexical split of the kernel body that cannot live in a callee. So the element declares a FIXED PIPELINE and the kernel synchronizes between its steps:\n\ncooperative_load!            # gather uₑ into the scratch lattice\ncooperative_stage!(…, 1)     # …\n⋮                            # `2 * cooperative_lattice_dim(cache) - 1` stages\ncooperative_store!           # write the local result into yₑ\n\ncooperative_scratch_shape sizes the group-local scratch, a length × count array the kernel allocates and hands to every step. cooperative_group_size is the workgroup the kernel launches with, cooperative_lattice_dim the pipeline length. Every step is called by all nlanes workers with lane in 1:nlanes and writes only what that lane owns.\n\nThe pipeline's shape is a tensor-product gradient operator's: dim forward contractions with the pointwise map fused into the last, then dim backward contractions with the store fused into the last. An element whose pipeline has a different length, or a data-dependent one, is not supported.\n\nwarning: Experimental surface\nThese entry points may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.cooperative_lattice_dim"},{"category":"function","location":"element-api/#FerriteOperators.cooperative_load!","page":"Element API reference","text":"cooperative_lattice_dim(cache) -> Int\ncooperative_group_size(cache) -> Int\ncooperative_scratch_shape(cache) -> (Val(length), Val(count))\ncooperative_load!(scratch, cache, uₑ, lane::Int, nlanes::Int)\ncooperative_stage!(scratch, cache, args, stage::Int, lane::Int, nlanes::Int)\ncooperative_store!(yₑ, scratch, cache, lane::Int, nlanes::Int)\n\nThe CooperativeElement entries: one WORKGROUP evaluates one element's action, its nlanes workers splitting the element's lattice between them and staging everything in group-local memory.\n\nThe device kernel owns the barriers, a KernelAbstractions @synchronize being a lexical split of the kernel body that cannot live in a callee. So the element declares a FIXED PIPELINE and the kernel synchronizes between its steps:\n\ncooperative_load!            # gather uₑ into the scratch lattice\ncooperative_stage!(…, 1)     # …\n⋮                            # `2 * cooperative_lattice_dim(cache) - 1` stages\ncooperative_store!           # write the local result into yₑ\n\ncooperative_scratch_shape sizes the group-local scratch, a length × count array the kernel allocates and hands to every step. cooperative_group_size is the workgroup the kernel launches with, cooperative_lattice_dim the pipeline length. Every step is called by all nlanes workers with lane in 1:nlanes and writes only what that lane owns.\n\nThe pipeline's shape is a tensor-product gradient operator's: dim forward contractions with the pointwise map fused into the last, then dim backward contractions with the store fused into the last. An element whose pipeline has a different length, or a data-dependent one, is not supported.\n\nwarning: Experimental surface\nThese entry points may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.cooperative_load!"},{"category":"function","location":"element-api/#FerriteOperators.cooperative_scratch_shape","page":"Element API reference","text":"cooperative_lattice_dim(cache) -> Int\ncooperative_group_size(cache) -> Int\ncooperative_scratch_shape(cache) -> (Val(length), Val(count))\ncooperative_load!(scratch, cache, uₑ, lane::Int, nlanes::Int)\ncooperative_stage!(scratch, cache, args, stage::Int, lane::Int, nlanes::Int)\ncooperative_store!(yₑ, scratch, cache, lane::Int, nlanes::Int)\n\nThe CooperativeElement entries: one WORKGROUP evaluates one element's action, its nlanes workers splitting the element's lattice between them and staging everything in group-local memory.\n\nThe device kernel owns the barriers, a KernelAbstractions @synchronize being a lexical split of the kernel body that cannot live in a callee. So the element declares a FIXED PIPELINE and the kernel synchronizes between its steps:\n\ncooperative_load!            # gather uₑ into the scratch lattice\ncooperative_stage!(…, 1)     # …\n⋮                            # `2 * cooperative_lattice_dim(cache) - 1` stages\ncooperative_store!           # write the local result into yₑ\n\ncooperative_scratch_shape sizes the group-local scratch, a length × count array the kernel allocates and hands to every step. cooperative_group_size is the workgroup the kernel launches with, cooperative_lattice_dim the pipeline length. Every step is called by all nlanes workers with lane in 1:nlanes and writes only what that lane owns.\n\nThe pipeline's shape is a tensor-product gradient operator's: dim forward contractions with the pointwise map fused into the last, then dim backward contractions with the store fused into the last. An element whose pipeline has a different length, or a data-dependent one, is not supported.\n\nwarning: Experimental surface\nThese entry points may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.cooperative_scratch_shape"},{"category":"function","location":"element-api/#FerriteOperators.cooperative_stage!","page":"Element API reference","text":"cooperative_lattice_dim(cache) -> Int\ncooperative_group_size(cache) -> Int\ncooperative_scratch_shape(cache) -> (Val(length), Val(count))\ncooperative_load!(scratch, cache, uₑ, lane::Int, nlanes::Int)\ncooperative_stage!(scratch, cache, args, stage::Int, lane::Int, nlanes::Int)\ncooperative_store!(yₑ, scratch, cache, lane::Int, nlanes::Int)\n\nThe CooperativeElement entries: one WORKGROUP evaluates one element's action, its nlanes workers splitting the element's lattice between them and staging everything in group-local memory.\n\nThe device kernel owns the barriers, a KernelAbstractions @synchronize being a lexical split of the kernel body that cannot live in a callee. So the element declares a FIXED PIPELINE and the kernel synchronizes between its steps:\n\ncooperative_load!            # gather uₑ into the scratch lattice\ncooperative_stage!(…, 1)     # …\n⋮                            # `2 * cooperative_lattice_dim(cache) - 1` stages\ncooperative_store!           # write the local result into yₑ\n\ncooperative_scratch_shape sizes the group-local scratch, a length × count array the kernel allocates and hands to every step. cooperative_group_size is the workgroup the kernel launches with, cooperative_lattice_dim the pipeline length. Every step is called by all nlanes workers with lane in 1:nlanes and writes only what that lane owns.\n\nThe pipeline's shape is a tensor-product gradient operator's: dim forward contractions with the pointwise map fused into the last, then dim backward contractions with the store fused into the last. An element whose pipeline has a different length, or a data-dependent one, is not supported.\n\nwarning: Experimental surface\nThese entry points may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.cooperative_stage!"},{"category":"function","location":"element-api/#FerriteOperators.cooperative_store!","page":"Element API reference","text":"cooperative_lattice_dim(cache) -> Int\ncooperative_group_size(cache) -> Int\ncooperative_scratch_shape(cache) -> (Val(length), Val(count))\ncooperative_load!(scratch, cache, uₑ, lane::Int, nlanes::Int)\ncooperative_stage!(scratch, cache, args, stage::Int, lane::Int, nlanes::Int)\ncooperative_store!(yₑ, scratch, cache, lane::Int, nlanes::Int)\n\nThe CooperativeElement entries: one WORKGROUP evaluates one element's action, its nlanes workers splitting the element's lattice between them and staging everything in group-local memory.\n\nThe device kernel owns the barriers, a KernelAbstractions @synchronize being a lexical split of the kernel body that cannot live in a callee. So the element declares a FIXED PIPELINE and the kernel synchronizes between its steps:\n\ncooperative_load!            # gather uₑ into the scratch lattice\ncooperative_stage!(…, 1)     # …\n⋮                            # `2 * cooperative_lattice_dim(cache) - 1` stages\ncooperative_store!           # write the local result into yₑ\n\ncooperative_scratch_shape sizes the group-local scratch, a length × count array the kernel allocates and hands to every step. cooperative_group_size is the workgroup the kernel launches with, cooperative_lattice_dim the pipeline length. Every step is called by all nlanes workers with lane in 1:nlanes and writes only what that lane owns.\n\nThe pipeline's shape is a tensor-product gradient operator's: dim forward contractions with the pointwise map fused into the last, then dim backward contractions with the store fused into the last. An element whose pipeline has a different length, or a data-dependent one, is not supported.\n\nwarning: Experimental surface\nThese entry points may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.cooperative_store!"},{"category":"function","location":"element-api/#FerriteOperators.element_action_row","page":"Element API reference","text":"element_action_row(element_cache, uₑ, args::CellArgs, i::Int) -> yᵢ\n\nONE row of the matrix-free element kernel: yᵢ = (Kₑ·uₑ)[i] for the current cell, returned BY VALUE — what a LanesPerElement lane owns.\n\nuₑ is indexable but not necessarily stored: under the lane mapping it is a WINDOW onto the global vector, so an implementation may read uₑ[j] and nothing else.\n\nDeclaring it is the element's promise that a row costs O(ndofs_per_cell), not a whole element action per row. A cache that cannot answer a single row serves WorkerPerElement instead.\n\nwarning: Experimental surface\nInternal to the matrix-free action; it may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.element_action_row"},{"category":"method","location":"element-api/#FerriteOperators.element_local_length-Tuple{Any}","page":"Element API reference","text":"element_local_length(element_cache) -> Val{N} or nothing\n\nThe length of element_cache's element-local unknown vector as a COMPILE-TIME constant, or nothing where the cache does not name one. It lets the matrix-free action gather uₑ into an immutable static vector — registers on a device — instead of into the per-worker buffer.\n\nDeclaring it is also a promise about the KERNELS: they may only read uₑ, an immutable vector having no setindex!. Return a literal Val.\n\nwarning: Experimental surface\nInternal to the matrix-free action; it may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.element_local_length"},{"category":"method","location":"element-api/#FerriteOperators.element_value_type-Tuple{Any}","page":"Element API reference","text":"element_value_type(x) -> Type\n\nThe scalar type an ELEMENT evaluates and stores in — the integrator's own election, independent of the value_type a device accumulates the GLOBAL system in. Defined on the quadrature collections (QuadratureRuleCollection, FacetQuadratureRuleCollection), on Ferrite's values objects, and on element caches, where it is the scalar the generic allocate_element_* buffers carry.\n\nThe cache method DEFAULTS to Float64:\n\nFerriteOperators.element_value_type(c::MyCache) = element_value_type(c.cv)\n\nOverriding the allocate_element_* hooks instead expresses a DELIBERATE mismatch — Float32 values accumulating into Float64 element buffers, say.\n\n\n\n\n\n","title":"FerriteOperators.element_value_type"},{"category":"method","location":"element-api/#FerriteOperators.evaluate_algebraic_functional-Tuple{Any, Any, Any}","page":"Element API reference","text":"evaluate_algebraic_functional(kind::FunctionalKind, cache, args) -> value\n\nThe evaluate_cell_functional counterpart of the algebraic family: this item's contribution to the functional named by kind, or nothing for no contribution. The default IS nothing — a term with no mesh support carries no volume, so it enters a domain integral only where its author says otherwise — which keeps evaluate_functional working on an operator that has algebraic items.\n\n\n\n\n\n","title":"FerriteOperators.evaluate_algebraic_functional"},{"category":"function","location":"element-api/#FerriteOperators.evaluate_cell_functional","page":"Element API reference","text":"evaluate_cell_functional(kind::FunctionalKind, cache, args) -> value\n\nElement kernel for functional (reduction) queries: returns this cell's contribution to the functional named by kind — a Number or a Tensors tensor, summed across cells — or nothing for no contribution.\n\n\n\n\n\n","title":"FerriteOperators.evaluate_cell_functional"},{"category":"function","location":"element-api/#FerriteOperators.evaluate_facet_functional","page":"Element API reference","text":"evaluate_facet_functional(kind::FunctionalKind, cache, args, local_facet_index) -> value\n\nThe evaluate_cell_functional counterpart of the facet-item family: returns this facet's contribution to the functional named by kind — a Number or a Tensors tensor, summed across facets — or nothing for no contribution. args is a FacetArgs, and the kernel reinitializes its own FacetValues for local_facet_index, exactly as assemble_facet! does.\n\n\n\n\n\n","title":"FerriteOperators.evaluate_facet_functional"},{"category":"method","location":"element-api/#FerriteOperators.facet_item_global_dof_range-Tuple{Any, Any}","page":"Element API reference","text":"facet_item_global_dof_range(integrator, sdh) -> UnitRange{Int}\n\nWhere facet_item_global_dofs sits in a facet item's local system, the global_dof_range counterpart: a facet item's local system is its owning cell's, so the tail starts after ndofs_per_cell(sdh) here as well.\n\n\n\n\n\n","title":"FerriteOperators.facet_item_global_dof_range"},{"category":"method","location":"element-api/#FerriteOperators.facet_item_global_dofs-Tuple{Any, Any}","page":"Element API reference","text":"facet_item_global_dofs(integrator, sdh) -> AbstractVector{Int}\n\nThe global_dofs counterpart of the facet item family: the GLOBAL dofs a FACET ITEM of sdh carries beyond celldofs of its owning cell — the lumped chamber pressure a tying surface couples to. Ordered; resolved once at setup; defaults to (). The local system is [celldofs(cell); the declared dofs], whose tail is facet_item_global_dof_range.\n\nEach family sizes its own local system from its OWN declaration, which is what this hook buys over declaring global_dofs: the facet items' Ke/re/slot buffers and their scatter are augmented, the subdomain's CELL sweep stays in the pure field space. The coupling sparsity can then be declared over the tying facets alone (Ferrite's FacetCoupling) instead of every cell of the subdomain, and a condensed volumetric element may sit beside the tying term — the rejection in ADElementCache applies to the cell declaration.\n\nThe restrictions of global_dofs hold here too: the declaration excludes ColoredScheduling, and the sparsity entries are the caller's coupling descriptor, never inferred.\n\n\n\n\n\n","title":"FerriteOperators.facet_item_global_dofs"},{"category":"method","location":"element-api/#FerriteOperators.facet_items-Tuple{Any, Any}","page":"Element API reference","text":"facet_items(integrator, sdh) -> iterable of FacetIndex\n\nThe facets of sdh a boundary term is supported on. They assemble as their own work items — one item per owning cell — which is the ONE route a surface term takes. Defaults to (); a non-empty declaration additionally needs setup_facet_item_cache.\n\nEvery declared facet's cell must lie in sdh.cellset — a facet item's local system is its owning cell's — and no facet may be declared twice; both are setup errors. Facets of ONE cell form a single item, assembled and scattered as one local system.\n\nTHE DECLARED SET IS THE TRAVERSAL: a cell whose facets are undeclared is never visited by this family, which is what makes a term supported on a small fraction of the boundary cost a small fraction of the boundary.\n\n\n\n\n\n","title":"FerriteOperators.facet_items"},{"category":"method","location":"element-api/#FerriteOperators.fill_quadrature_data!-Tuple{Any, CellArgs}","page":"Element API reference","text":"fill_quadrature_data!(cache, args::CellArgs)\n\nFill the per-quadrature-point store cache owns for the current cell — the PARTIAL-assembly half of a matrix-free element. The default is a no-op.\n\nCalled by a QuadratureDataKind sweep at setup and on every update_operator!, so a factor depending on p or on the context's time is refreshed there and nowhere else.\n\nThe store is the cache's own. The sweep visits every cell exactly once and cells write disjoint slices, so no synchronization is needed and none is provided.\n\nwarning: Experimental surface\nThis entry point may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.fill_quadrature_data!"},{"category":"function","location":"element-api/#FerriteOperators.get_number_of_internal_dofs_per_algebraic_item","page":"Element API reference","text":"get_number_of_internal_dofs_per_algebraic_item(integrator, cache, items) -> AbstractVector{Int}\n\nNumber of condensed internal dofs each item of items owns, in declaration order — the get_number_of_internal_dofs_per_element counterpart for the algebraic-item family. Queried once at setup, only when has_internal_state holds for cache, to build the item block of the InternalVariableHandler ([ū | q_cells | q_items]). Every entry must be equal: the uniform-item-size rule that keeps local buffers fixed-size (resolve_algebraic_items) extends to the internal-dof count, validated loudly at setup. There is no fallback, so only a condensed algebraic cache implements it.\n\n\n\n\n\n","title":"FerriteOperators.get_number_of_internal_dofs_per_algebraic_item"},{"category":"function","location":"element-api/#FerriteOperators.get_number_of_internal_dofs_per_element","page":"Element API reference","text":"get_number_of_internal_dofs_per_element(integrator, cache, sdh) -> AbstractVector{Int}\n\nNumber of condensed internal dofs each cell of sdh owns, in sdh.cellset order. Queried once at setup to build the InternalVariableHandler; there is no fallback, so only condensed elements implement it.\n\n\n\n\n\n","title":"FerriteOperators.get_number_of_internal_dofs_per_element"},{"category":"method","location":"element-api/#FerriteOperators.global_dof_range-Tuple{Any, Any}","page":"Element API reference","text":"global_dof_range(integrator, sdh) -> UnitRange{Int}\n\nWhere the dofs global_dofs declares sit in the element-local system: ndofs_per_cell(sdh) .+ (1:length(global_dofs(integrator, sdh))). The local layout is a contract, and this is the one place that spells it.\n\n\n\n\n\n","title":"FerriteOperators.global_dof_range"},{"category":"method","location":"element-api/#FerriteOperators.global_dofs-Tuple{Any, Any}","page":"Element API reference","text":"global_dofs(integrator, sdh) -> AbstractVector{Int}\n\nThe GLOBAL dofs a CELL of sdh carries in its local system beyond celldofs — dofs of sdh.dh that belong to no cell (Ferrite's algebraic_dofs(sdh.dh, :name)) and that every cell of the subdomain couples to (a stress-driven RVE's macroscopic strain). Ordered; resolved once at setup. Defaults to ().\n\nThe declaration lives on the INTEGRATOR, one per subdomain, and augments the CELL family alone: the subdomain's volumetric kernel and the boundary kernel riding its sweep. A term supported on a facet SET declares its own tail through facet_item_global_dofs, which leaves the cell sweep in the pure field space. The local system is then, by contract,\n\n[ celldofs(cell) ; the declared global dofs, in declaration order ]\n\nso the tail occupies global_dof_range, which an element cache resolves once in setup_element_cache and stores — the framework passes no extra channel, and CellArgs/FacetArgs stay at their four fields. The engine sizes Ke/re/the slot buffers to the augmented length and scatters through the augmented dof vector; the AD fallback differentiates the full augmented system.\n\nOne restriction, raised at setup and shared with facet_item_global_dofs: a declaration excludes ColoredScheduling — a dof shared by every item cannot be isolated by coloring, so the parallel route is atomic scatter under SequentialScheduling.\n\nThe sparsity entries for the resulting coupling are NOT inferred: which items couple to the dofs is the user's Ferrite coupling descriptor, passed through StandardOperatorSpecification/BlockedOperatorSpecification. A missing descriptor surfaces as Ferrite's missing-sparsity-entry error on the first assembly.\n\n\n\n\n\n","title":"FerriteOperators.global_dofs"},{"category":"function","location":"element-api/#FerriteOperators.reinit_values!","page":"Element API reference","text":"reinit_values!(cache, cell)\nreinit_values!(cache, cell, kind)\n\nReinitialize the values objects an element cache carries for the given cell. The two-arg method is mandatory (validated at setup) and reinitializes all of them. The engine calls the three-arg form once per cell and sweep, before any kernel of that sweep; specialize it on the request kind to reinitialize only what that kind needs. Kernels are pure evaluation — repeated invocations within one sweep (AD chunk passes, split Jacobian-then-residual fallbacks) do not reinitialize again.\n\n\n\n\n\n","title":"FerriteOperators.reinit_values!"},{"category":"method","location":"element-api/#FerriteOperators.setup_algebraic_cache-Tuple{Any, Any}","page":"Element API reference","text":"setup_algebraic_cache(integrator, dh) -> cache\n\nThe cache serving every item algebraic_items declares — the analogue of one element cache per SubDofHandler serving all of its cells. Items are positioned per sweep, and the kernel reads which item it stands on from args.item (AlgebraicItem).\n\nThere is deliberately no silent fallback: declaring items without this method is a loud setup error, not algebraic rows that quietly assemble nothing.\n\n\n\n\n\n","title":"FerriteOperators.setup_algebraic_cache"},{"category":"method","location":"element-api/#FerriteOperators.setup_element_cache-Tuple{Any, Any}","page":"Element API reference","text":"setup_element_cache(integrator, sdh)\n\nSetup the element cache on a given subdofhandler. There is deliberately no silent no-op fallback: a missing method is a loud setup error, not an operator that assembles nothing.\n\nThe INTEGRATOR decides the precision its cache evaluates in; the engine passes no scalar type. Read it off the quadrature collection the integrator carries and spell it into the values object — CellValues(qr, ip, ip_geo) is Float64 whatever the rule says:\n\nqr = getquadraturerule(integrator.qrc, sdh)\nT  = element_value_type(integrator.qrc)\nMyCache(CellValues(T, qr, ip, ip_geo))\n\n\n\n\n\n","title":"FerriteOperators.setup_element_cache"},{"category":"method","location":"element-api/#FerriteOperators.setup_facet_item_cache-Tuple{Any, Any}","page":"Element API reference","text":"setup_facet_item_cache(integrator, sdh) -> AbstractSurfaceElementCache\n\nThe AbstractSurfaceElementCache serving the facets facet_items declares for sdh — one per subdomain, the surface counterpart of setup_element_cache.\n\nThere is deliberately no silent fallback: declaring facet items without this method is a loud setup error, not a boundary term that quietly assembles nothing.\n\n\n\n\n\n","title":"FerriteOperators.setup_facet_item_cache"},{"category":"method","location":"element-api/#FerriteOperators.with_action_storage-Tuple{Any, Any, Any}","page":"Element API reference","text":"with_action_storage(cache, storage::StorageElection, sdh) -> cache\n\nThe cache a matrix-free operator's storage election (MatrixFreeAction) asks for. Two kinds of method meet here, and dispatch keeps them apart:\n\nThe ELEMENT's, specialized on ITS cache type and the member it keeps something for — a Stored() method allocating the element's per-quadrature-point store. An element that keeps nothing writes none and serves Stored() and Recompute() identically.\nThe FRAMEWORK's, specialized on ElementAssembly and no cache type, which wraps ANY bilinear cache in an ElementAssemblyCache. It is strictly more specific in the storage argument than an element's Recompute() method and strictly less specific in the cache argument than an element's Stored() one, so the two never collide.\n\nwarning: Experimental surface\nThis hook exists for the matrix-free form's storage election and may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.with_action_storage"},{"category":"method","location":"element-api/#FerriteOperators.with_assembly_form-Tuple{Any, Any, Any}","page":"Element API reference","text":"with_assembly_form(cache, form, sdh) -> cache\n\nThe cache setup_element_cache built, carrying the element-side half of the operator FORM's elections. The identity for every form that elects nothing of the element; MatrixFreeAction resolves its storage election through with_action_storage here, before the engine builds the workspaces and device layouts.\n\nwarning: Experimental surface\nThis hook exists for the matrix-free form's storage election and may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.with_assembly_form"},{"category":"constant","location":"element-api/#FerriteOperators.DEBUG","page":"Element API reference","text":"FerriteOperators.DEBUG\n\nCompile-time flag selecting the exhaustive form of setup-time checks whose production form samples. Loaded from the use_debug preference and therefore constant-folded: production carries neither check nor branch. Mirrors Ferrite.DEBUG but is set independently of it.\n\n\n\n\n\n","title":"FerriteOperators.DEBUG"},{"category":"method","location":"element-api/#FerriteOperators.debug_mode-Tuple{}","page":"Element API reference","text":"FerriteOperators.debug_mode(; enable = true)\n\nTurn the FerriteOperators.DEBUG preference on or off. Takes effect after a Julia session restart, since the flag is baked in at precompilation.\n\n\n\n\n\n","title":"FerriteOperators.debug_mode"},{"category":"method","location":"element-api/#FerriteOperators.geometric_subdomain_interpolation-Tuple{SubDofHandler}","page":"Element API reference","text":"geometric_subdomain_interpolation(sdh::SubDofHandler) -> VectorizedInterpolation\n\nThe geometric interpolation of sdh's cells, vectorized to the grid's spatial dimension — the ip_geo a CellValues/FacetValues constructor takes. Read off the subdomain's FIRST cell: a SubDofHandler is uniform in cell type by construction.\n\n\n\n\n\n","title":"FerriteOperators.geometric_subdomain_interpolation"},{"category":"method","location":"element-api/#FerriteOperators.get_first_cell-Tuple{SubDofHandler}","page":"Element API reference","text":"get_first_cell(sdh::SubDofHandler) -> AbstractCell\n\nThe first cell of sdh's cellset. A SubDofHandler is uniform in cell type, so this is the cell a setup hook reads a subdomain's geometry from.\n\n\n\n\n\n","title":"FerriteOperators.get_first_cell"},{"category":"section","location":"engine-api/#Assembly-engine-API-reference","page":"Assembly engine API reference","text":"","title":"Assembly engine API reference"},{"category":"type","location":"engine-api/#FerriteOperators.AssemblyTask","page":"Assembly engine API reference","text":"AssemblyTask(kind, inner_assembler, states, p, ctx)\n\nThe single per-cell assembly sweep shared by all operators. kind selects what is computed (and thereby which element kernel is called), states is the NamedTuple of global slot sources (empty for state-independent kinds), p the user parameters, ctx the time-integration context (or nothing).\n\n\n\n\n\n","title":"FerriteOperators.AssemblyTask"},{"category":"type","location":"engine-api/#FerriteOperators.FunctionalFamily","page":"Assembly engine API reference","text":"NoFamily\nFunctionalFamily\n\nThe per-worker driver-shape routing. NoFamily marks a sweep that scatters its result through an assembler (every request-carrying kind); FunctionalFamily selects the VALUE-RETURNING driver instead: its kernel returns the item's contribution and the sweep reduces the returned values, so it needs no per-worker state at all.\n\nA sensitivity kind's OUTPUT buffers (SensitivityBuffers) are engine/workspace-owned unconditionally for a nonlinear operator (see needs_ad_decoration) rather than gated by a third family — the ForwardDiff seeding machinery lives on the resolved element cache (ADElementCache) instead of a family-gated workspace member.\n\n\n\n\n\n","title":"FerriteOperators.FunctionalFamily"},{"category":"type","location":"engine-api/#FerriteOperators.FunctionalKind","page":"Assembly engine API reference","text":"FunctionalKind{tag}\nFunctionalKind(tag::Symbol)\n\nNames a functional (reduction) query: a global scalar or Tensors tensor integrated from per-item contributions (energy, dissipation, a surface flux, …). Element caches implement evaluate_cell_functional, evaluate_facet_functional or evaluate_algebraic_functional dispatching on the tag; solvers evaluate through evaluate_functional.\n\n\n\n\n\n","title":"FerriteOperators.FunctionalKind"},{"category":"type","location":"engine-api/#FerriteOperators.JacobianKind","page":"Assembly engine API reference","text":"JacobianKind{slot, C <: CorrectionMode}\nJacobianKind{slot}()\nJacobianKind()\n\nAssembly of the Jacobian ∂F/∂slot into the operator's matrix. slot names the state slot differentiated against; JacobianKind() is JacobianKind{:u, Consistent}(), the Newton path. Every other slot is a component of a multi-slot linearization (JacobianKind{:du}() for the DAE mass block, JacobianKind{:v}()/JacobianKind{:a}() for structural dynamics); the chain-rule weights that fold components into a Newton matrix are the solver's, not the framework's. slot = :q is the block a Schur-complement consumer or the generic corrector combination wants — see condense_internal!.\n\nC is the CorrectionMode; JacobianKind{slot}() defaults it to Consistent, so FrozenQ must be spelled: JacobianKind{slot, FrozenQ}().\n\nThe differentiated slot must carry a plain vector source — AffineRate slots are reconstructed at gather time and frozen under AD, so the entry point rejects them.\n\nElements serve the kind analytically (assemble_cell! on JacobianRequest{slot, C}, declared through provides_analytic) or through ForwardDiff seeding of the named slot buffer, which computes exactly the FrozenQ partial — correct as-is for C = FrozenQ, and admissible for C = Consistent only when the element has no condensed internal state to miss a correction for.\n\n\n\n\n\n","title":"FerriteOperators.JacobianKind"},{"category":"type","location":"engine-api/#FerriteOperators.JacobianResidualKind","page":"Assembly engine API reference","text":"JacobianResidualKind{C <: CorrectionMode}\nJacobianResidualKind()\n\nState-dependent J(u) and r(u), fused (the Newton hot path). C is the CorrectionMode of the Jacobian half; JacobianResidualKind() defaults it to Consistent — see JacobianKind.\n\n\n\n\n\n","title":"FerriteOperators.JacobianResidualKind"},{"category":"type","location":"engine-api/#FerriteOperators.MatrixFreeActionKind","page":"Assembly engine API reference","text":"MatrixFreeActionKind()\n\nThe ACTION y = A·u of the operator a bilinear form induces — the sweep a MatrixFreeAction operator's mul! runs. It reaches apply_element_action!, never a request, there being no element matrix for one to point at.\n\nIts state gather writes exactly ndofs_per_cell entries, unlike the ResidualKind gather that resizes, which is what lets the sweep run on a device whose per-worker slot buffer is a view into a shared batch.\n\nwarning: Experimental surface\nThis kind may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.MatrixFreeActionKind"},{"category":"type","location":"engine-api/#FerriteOperators.NoFamily","page":"Assembly engine API reference","text":"NoFamily\nFunctionalFamily\n\nThe per-worker driver-shape routing. NoFamily marks a sweep that scatters its result through an assembler (every request-carrying kind); FunctionalFamily selects the VALUE-RETURNING driver instead: its kernel returns the item's contribution and the sweep reduces the returned values, so it needs no per-worker state at all.\n\nA sensitivity kind's OUTPUT buffers (SensitivityBuffers) are engine/workspace-owned unconditionally for a nonlinear operator (see needs_ad_decoration) rather than gated by a third family — the ForwardDiff seeding machinery lives on the resolved element cache (ADElementCache) instead of a family-gated workspace member.\n\n\n\n\n\n","title":"FerriteOperators.NoFamily"},{"category":"type","location":"engine-api/#FerriteOperators.ParameterJacobianKind","page":"Assembly engine API reference","text":"ParameterJacobianKind()\n\nAssembly of ∂F/∂θ into a dense residual_size × nθ target, θ being the flat view parameter_vector defines. Issued by update_parameter_jacobian!; served by an analytic ParameterJacobianRequest kernel or by ForwardDiff over the residual kernel. The seed dimension arrives with p, so the sweep builds its differentiation configuration per call.\n\n\n\n\n\n","title":"FerriteOperators.ParameterJacobianKind"},{"category":"type","location":"engine-api/#FerriteOperators.ParameterVJPKind","page":"Assembly engine API reference","text":"ParameterVJPKind(λ)\n\nThe matrix-free pullback (∂F/∂θ)ᵀλ — the adjoint gradient, without ever materializing ∂F/∂θ. λ is the global adjoint vector, gathered per item. Issued by parameter_vjp!; the kernel is ParameterVJPRequest.\n\n\n\n\n\n","title":"FerriteOperators.ParameterVJPKind"},{"category":"type","location":"engine-api/#FerriteOperators.QuadratureDataKind","page":"Assembly engine API reference","text":"QuadratureDataKind()\n\nThe PARTIAL-assembly fill: every cell's element cache writes the per-quadrature-point factors its action will read (fill_quadrature_data!). It reaches no request and no assembler, cells writing disjoint slices of a store the cache owns, and runs at setup and on every update_operator! — so a factor depending on a parameter or on the time is as fresh as the last such call and no fresher.\n\nwarning: Experimental surface\nThis kind may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.QuadratureDataKind"},{"category":"type","location":"engine-api/#FerriteOperators.ResidualKind","page":"Assembly engine API reference","text":"ResidualKind()\n\nAssembly of the state-dependent residual r(u) into the operator's vector. The kernel it reaches is ResidualRequest — the one kernel every element must implement, and the one every AD fallback differentiates.\n\n\n\n\n\n","title":"FerriteOperators.ResidualKind"},{"category":"type","location":"engine-api/#FerriteOperators.StateJVPKind","page":"Assembly engine API reference","text":"StateJVPKind(v)\n\nThe matrix-free action (∂F/∂u)·v — one directional-Dual sweep per item under the AD fallback, no matrix anywhere. Issued by state_jvp!; the kernel is StateJVPRequest.\n\n\n\n\n\n","title":"FerriteOperators.StateJVPKind"},{"category":"type","location":"engine-api/#FerriteOperators.StateVJPKind","page":"Assembly engine API reference","text":"StateVJPKind(λ)\n\nThe matrix-free pullback (∂F/∂u)ᵀλ — the action adjoint time stepping applies, computed per item as the gradient of λₑ·rₑ under the AD fallback. Issued by state_vjp!; the kernel is StateVJPRequest.\n\n\n\n\n\n","title":"FerriteOperators.StateVJPKind"},{"category":"type","location":"engine-api/#FerriteOperators.TimeSensitivityKind","page":"Assembly engine API reference","text":"TimeSensitivityKind()\n\nAssembly of ∂F/∂t at evaluation_time(ctx). Time reaches elements through the context alone, so this kind seeds through that channel: the AD sweep hands the kernel a Dual-timed context, and FiniteDifferenceSensitivity evaluates the primal residual at perturbed context times instead. A kernel reading its time from args.p gets a silently zero result. Issued by time_sensitivity!.\n\n\n\n\n\n","title":"FerriteOperators.TimeSensitivityKind"},{"category":"type","location":"engine-api/#FerriteOperators.WeightedJacobianKind","page":"Assembly engine API reference","text":"WeightedJacobianKind(weights::NamedTuple)\nWeightedJacobianKind{slots}(weights)\n\nAssembly of the weighted Jacobian W = Σₛ wₛ ∂F/∂s in ONE sweep, over the slots weights names and at frozen values of every other slot — the matrix a scheme actually solves with (W = M/(γΔt) + K for SDIRK/backward Euler, M/(βΔt²) + γ/(βΔt) C + K for Newmark). The slot set is the type parameter slots; the weights are runtime payload and eltype-generic.\n\nWeights are REQUEST payload: the kernel reads them from WeightedJacobianRequest and the composed fallback folds the same NamedTuple with combine!, so the two routes cannot disagree about the scheme's scalars. Solvers issue the kind through assemble_weighted_jacobian!, which selects the route.\n\nC is the CorrectionMode of every participating slot; WeightedJacobianKind(weights) defaults it to Consistent, and WeightedJacobianKind{slots, FrozenQ}(weights) spells the partial.\n\nAn element opts into the fused route with\n\nprovides_analytic(::Type{<:MyCache}, ::WeightedJacobianKind) = true\nassemble_cell!(req::WeightedJacobianRequest, cache::MyCache, args) = # reads req.weights\n\nWithout it the sweep derives W from the residual kernel by seeding every participating slot with its weight-scaled Duals — real weights only, since the element matrix and the Dual machinery are real. Complex weights (transformed Radau) go through the composed route.\n\nnote: AffineRate participation\nThe AD route freezes AffineRate slots at gather time, so a reconstructed slot cannot participate and the sweep rejects one — the same rule as JacobianKind. An ANALYTIC weighted kernel is exempt: it forms the combination internally, which is what a multilevel-Newton element with a rate-coupled local problem needs (its condensed tangent −(∂L/∂q)⁻¹(∂L/∂ε + slope·∂L/∂ε̇) carries the slope inside the local inverse and cannot be recovered by weighting separated partials).\n\n\n\n\n\n","title":"FerriteOperators.WeightedJacobianKind"},{"category":"method","location":"engine-api/#FerriteOperators._item_family-Tuple{Any}","page":"Assembly engine API reference","text":"_item_family(ws) -> family tag\n_family_reduction_sweep(kind, task, ws) -> value\n\nThe two halves an item family contributes to the derived reduction routing: the tag reduction_families names it by, and the driver body a declaring kind runs on it. One method each per family, beside that family's other drivers; nothing marks a workspace no reduction traverses.\n\n\n\n\n\n","title":"FerriteOperators._item_family"},{"category":"method","location":"engine-api/#FerriteOperators.assembles_matrix-Tuple{Any}","page":"Assembly engine API reference","text":"assembles_matrix(kind) -> Bool\nassembles_vector(kind) -> Bool\ndepends_on_unknowns(kind) -> Bool\n\nWhat a sweep of kind does with the workspace: which of ws.Ke/ws.re it zeroes and scatters, and whether it gathers the state slots. The driver bodies consult these instead of testing kind membership, so a downstream kind joins the built-in driver by declaring them.\n\nThe defaults read the built-in family unions, so the answers are compile-time constants and the branches they guard are eliminated. An overload must return a literal for the same reason:\n\nFerriteOperators.assembles_matrix(::MyKind) = true\n\nThere is exactly one root method each, so an overload is always strictly more specific and cannot create an ambiguity.\n\n\n\n\n\n","title":"FerriteOperators.assembles_matrix"},{"category":"method","location":"engine-api/#FerriteOperators.assembles_vector-Tuple{Any}","page":"Assembly engine API reference","text":"assembles_matrix(kind) -> Bool\nassembles_vector(kind) -> Bool\ndepends_on_unknowns(kind) -> Bool\n\nWhat a sweep of kind does with the workspace: which of ws.Ke/ws.re it zeroes and scatters, and whether it gathers the state slots. The driver bodies consult these instead of testing kind membership, so a downstream kind joins the built-in driver by declaring them.\n\nThe defaults read the built-in family unions, so the answers are compile-time constants and the branches they guard are eliminated. An overload must return a literal for the same reason:\n\nFerriteOperators.assembles_matrix(::MyKind) = true\n\nThere is exactly one root method each, so an overload is always strictly more specific and cannot create an ambiguity.\n\n\n\n\n\n","title":"FerriteOperators.assembles_vector"},{"category":"method","location":"engine-api/#FerriteOperators.depends_on_unknowns-Tuple{Any}","page":"Assembly engine API reference","text":"assembles_matrix(kind) -> Bool\nassembles_vector(kind) -> Bool\ndepends_on_unknowns(kind) -> Bool\n\nWhat a sweep of kind does with the workspace: which of ws.Ke/ws.re it zeroes and scatters, and whether it gathers the state slots. The driver bodies consult these instead of testing kind membership, so a downstream kind joins the built-in driver by declaring them.\n\nThe defaults read the built-in family unions, so the answers are compile-time constants and the branches they guard are eliminated. An overload must return a literal for the same reason:\n\nFerriteOperators.assembles_matrix(::MyKind) = true\n\nThere is exactly one root method each, so an overload is always strictly more specific and cannot create an ambiguity.\n\n\n\n\n\n","title":"FerriteOperators.depends_on_unknowns"},{"category":"function","location":"engine-api/#FerriteOperators.evaluate_functional","page":"Assembly engine API reference","text":"evaluate_functional(op, kind, states, p, ctx = nothing)\nevaluate_functional(op, kind, u::AbstractVector, p)\n\nEvaluate the functional named by kind over the operator's domain: the sum of the per-item contributions its kernels return (a Number or a Tensors tensor). kind is a FunctionalKind or any downstream kind declaring reduction_families; anything else is rejected by run_reduction. Contributions fold per worker in item order and the per-worker partials reduce in a fixed order — results are deterministic for a fixed worker count. Nothing is written into the operator, so an operator declaring no functional kind serves one just as well.\n\nCell items contribute through evaluate_cell_functional, facet items through evaluate_facet_functional and algebraic items through evaluate_algebraic_functional.\n\nTwo failure modes are kept apart. STRUCTURAL emptiness — no items in the operator's partitions, or no subdomain whose element cache can contribute — is a misconfiguration, raised as an ArgumentError before any cell runs. A sweep that runs and whose kernels all answer nothing is the DATA-DEPENDENT case: an empty sum, which a kind declaring functional_value_type answers with zero(T) and an undeclared kind cannot answer at all (there is no T to take the identity of) and reports as an ArgumentError.\n\n\n\n\n\n","title":"FerriteOperators.evaluate_functional"},{"category":"method","location":"engine-api/#FerriteOperators.execute_single_task!-Tuple{FerriteOperators.AssemblyTask, FerriteOperators.AssemblyWorkspace}","page":"Assembly engine API reference","text":"execute_single_task!(task, ws)\n\nOne item's work: route task's kind to its driver body (execute_kind!) on the workspace ws is positioned on. This is where the per-item debug timer sits.\n\nA DEVICE kernel calls execute_kind! directly instead: @timeit_debug expands to a try/finally whether or not the timings are enabled, and GPUCompiler rejects the exception frame.\n\n\n\n\n\n","title":"FerriteOperators.execute_single_task!"},{"category":"method","location":"engine-api/#FerriteOperators.functional_cell_sweep-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"functional_cell_sweep(kind, task, ws) -> value\n\nThe built-in VALUE-RETURNING driver body, reusable by a downstream kind's own execute_kind!. Reinitializes the element's values, gathers the state slots without writing anything back, and returns what evaluate_cell_functional gives for the cell — nothing for a cell that contributes nothing.\n\nUnlike primal_cell_sweep! and sensitivity_cell_sweep! it writes no result into the workspace and scatters nothing; the sweep reduces what it returns (run_reduction).\n\n\n\n\n\n","title":"FerriteOperators.functional_cell_sweep"},{"category":"method","location":"engine-api/#FerriteOperators.functional_value_type-Tuple{Any}","page":"Assembly engine API reference","text":"functional_value_type(kind) -> Type\n\nThe type a value-returning sweep of kind reduces to, or Nothing when the kind does not declare one. Queried on the INSTANCE, because every consumer is a running sweep holding the kind it was issued with.\n\nDeclaring it types the worker's fold from the start: the accumulator is seeded with zero(T) instead of with the first contributing item, so the partials are a concretely typed array and a kernel returning something else fails loudly instead of widening the reduction. REQUIRED under a parallel device, whose per-worker partials are allocated before the batch runs; the sequential fold can still infer the type from the first contribution.\n\nFerriteOperators.functional_value_type(::FunctionalKind{:energy}) = Float64\nFerriteOperators.functional_value_type(::FunctionalKind{:gradient_volume}) = Vec{2, Float64}\n\nThere is exactly one root method, so an overload is always strictly more specific and cannot create an ambiguity. Return a literal — the fold's accumulator type folds out of it.\n\n\n\n\n\n","title":"FerriteOperators.functional_value_type"},{"category":"method","location":"engine-api/#FerriteOperators.item_dofs-Tuple{Any}","page":"Assembly engine API reference","text":"item_dofs(ws) -> AbstractVector{Int}\n\nThe global dof indices of the current item's local system: iterator_dofs where the item's family declares no global dofs (global_dofs for cells, facet_item_global_dofs for facet items), and the augmented [iterator_dofs(cell); global dofs] vector the workspace carries otherwise. Every gather of a sweep addresses through this, so the augmented tail reaches the slot buffers and the adjoint payloads. On an AlgebraicWorkspace it is the item's own dof vector, that family having no cell dofs to start from.\n\n\n\n\n\n","title":"FerriteOperators.item_dofs"},{"category":"method","location":"engine-api/#FerriteOperators.matrix_free_cell_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"matrix_free_cell_sweep!(kind, task, ws)\n\nThe matrix-free ACTION driver body: gather the trial state into the workspace's :u slot buffer, evaluate yₑ = Kₑ·uₑ through apply_element_action! into ws.re, and scatter it. No element matrix is formed and none is stored.\n\nIt differs from primal_cell_sweep! in one way, and that is what makes it run on a device: the gather is FIXED-WIDTH instead of load_slots!'s resize-then-broadcast, which a per-worker view into a shared device batch cannot serve. Where the cache names that width as a compile-time constant (element_local_length) the gather targets an immutable static vector, which is also the dof window the scatter uses, so the window is read off the item ONCE. Without a static extent the scatter re-derives it through scatter_address.\n\n@inline and @timeit_debug-free for the same reasons primal_cell_sweep! is.\n\n\n\n\n\n","title":"FerriteOperators.matrix_free_cell_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.primal_cell_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"primal_cell_sweep!(kind, task, ws)\n\nThe built-in primal driver body, reusable by a downstream kind's own execute_kind!. Zeroes the buffers assembles_matrix / assembles_vector name, reinitializes the element's values, queries the cell parameters, runs the cell kernel — gathering the state slots iff depends_on_unknowns — and scatters through scatter_local!.\n\nBoundary terms are their own item family (facet_items) and take no part here: a cell sweep runs the volumetric kernel and nothing else.\n\nThe kernel it calls is cell_kernel!(kind, …), whose generic method issues the kind's request analytically; the built-in kinds with an AD fallback specialize it. It writes nothing back: condense_internal! is the only writer of q, so a primal sweep is a pure evaluation at whatever q is stored.\n\nThe body carries no @timeit_debug frame — it has to compile for a GPU, and GPUCompiler rejects the try/finally the macro expands to. The per-item timer sits at execute_single_task! instead.\n\nIt is @inline for the same reason matrix_free_cell_sweep! is: a device kernel that CALLS it instead of containing it passes ws — a struct of array views — through per-thread local memory instead of registers.\n\n\n\n\n\n","title":"FerriteOperators.primal_cell_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.quadrature_data_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"quadrature_data_sweep!(kind, task, ws)\n\nThe PARTIAL-assembly fill driver body: position the element's values, query its parameters, and let it write its own per-quadrature-point store (fill_quadrature_data!). Nothing is gathered or scattered and no workspace buffer is touched, so this sweep needs no assembler and runs under either scheduling policy.\n\n\n\n\n\n","title":"FerriteOperators.quadrature_data_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.reduction_families-Tuple{Type}","page":"Assembly engine API reference","text":"reduction_families(::Type{K}) -> Tuple of family tags\n\nThe item families a reduction of kind K integrates over, spelled with the tags :cells, :facets and :algebraic. This is the ONE declaration a downstream reduction kind writes:\n\nFerriteOperators.reduction_families(::Type{<:MyFunctional}) = (:cells,)\n\nThe engine derives the rest from it — sweep_family (a declaring kind is value-returning), the execute_kind! routing (a declared family runs that family's reduction driver body, an undeclared one contributes nothing to the sweep) and the structural half of the reduction precondition (a family the kind does not name cannot contribute, so its subdomains are neither required nor traversed).\n\nThe empty default means \"not a reduction\": every request-carrying kind, whose sweeps fill the workspace buffers and scatter through an assembler.\n\nThe per-family hooks stay the underlying reality and an explicit method still wins, being strictly more specific than the derived route — which is how a kind whose family bodies are NOT the built-in reduction drivers (CondensationKind) declares its families here and keeps its own execute_kind! methods.\n\nQueried on the TYPE, like sweep_family. Return a literal: the routing and the precondition then fold to constants.\n\n\n\n\n\n","title":"FerriteOperators.reduction_families"},{"category":"function","location":"engine-api/#FerriteOperators.request_type","page":"Assembly engine API reference","text":"request_type(kind) -> Type\nmaterialize_request(kind, ws) -> AbstractAssemblyRequest\n\nThe single kind → request association. request_type is the pure form used wherever a kernel method is looked up (the setup-time trait ↔ kernel checks); materialize_request is the executing form, binding the workspace buffers a sweep of kind accumulates into. Drivers — cell, facet, patch — and the validation tables all go through these two, so a new kind enters the framework by adding one pair of methods here.\n\nBuffers are zeroed by the driver before the request is materialized, not here.\n\n\n\n\n\n","title":"FerriteOperators.request_type"},{"category":"method","location":"engine-api/#FerriteOperators.run_reduction-Tuple{Any, Any, NamedTuple, Any, Any}","page":"Assembly engine API reference","text":"run_reduction(kind, op, states::NamedTuple, p, ctx) -> value\n\nThe value-returning counterpart of run_sweep!: the same per-sweep validation, but the sweep hands its result back as a value instead of scattering it, and no workspace state is read or written. nothing means no item contributed.\n\nWhich shape a kind runs in is sweep_family: only a FunctionalFamily kind is value-returning, so this is the entry point a downstream scalar or tensor kind reaches once it declares reduction_families — which is also what routes it to the reduction driver body of every family it names (functional_cell_sweep is the cell family's).\n\n\n\n\n\n","title":"FerriteOperators.run_reduction"},{"category":"method","location":"engine-api/#FerriteOperators.scatter_address-Tuple{Any}","page":"Assembly engine API reference","text":"scatter_address(ws)\n\nWhat a scatter of the current item addresses. Without a global-dof declaration for the item's family it is iterator_scatter_address's answer for the item's iterator, which every assembler in the package takes and reads celldofs from by default. With them the local system spans dofs no cell owns, so the augmented dof vector is the only address that describes it. An algebraic item is addressed by its dof vector, there being no cell to name it by.\n\n\n\n\n\n","title":"FerriteOperators.scatter_address"},{"category":"function","location":"engine-api/#FerriteOperators.scatter_local!","page":"Assembly engine API reference","text":"scatter_local!(kind, assembler, ws, address = scatter_address(ws))\n\nHand the local buffers a sweep of kind filled to the assembler. Which buffers those are is assembles_matrix/assembles_vector, so the three routes are selected at compile time and a downstream kind is scattered by the same body. The item is addressed through scatter_address by default; a caller that already holds that address (matrix_free_cell_sweep!, off its own gather) passes it instead.\n\n\n\n\n\n","title":"FerriteOperators.scatter_local!"},{"category":"method","location":"engine-api/#FerriteOperators.scatter_request!-Tuple{ParameterJacobianRequest, Any, Any}","page":"Assembly engine API reference","text":"scatter_request!(req, assembler, address)\n\nHand a sensitivity request's payload to the assembler — the sensitivity counterpart of scatter_local!, dispatching on the request type instead of the kind since the destination buffer IS the request's own field. address is what scatter_address resolved for the item.\n\n\n\n\n\n","title":"FerriteOperators.scatter_request!"},{"category":"method","location":"engine-api/#FerriteOperators.sensitivity_cell_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"sensitivity_cell_sweep!(kind, task, ws)\n\nThe built-in sensitivity driver body, reusable by a downstream kind's own execute_kind!. Gathers the trial state, queries the cell parameters, binds the request through materialize_request(kind, ws, task), issues it against the resolved element cache, and scatters through scatter_request!. Nothing is written back into u.\n\n\n\n\n\n","title":"FerriteOperators.sensitivity_cell_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.sensitivity_kernel!-NTuple{4, Any}","page":"Assembly engine API reference","text":"sensitivity_kernel!(kind, task, ws, args)\n\nBind kind's request over ws.sensitivity, issue it against the resolved element cache, and scatter the result — the entry point a downstream sensitivity-family kind reaches (via sensitivity_cell_sweep!) with nothing beyond a materialize_request(::MyKind, ws, task) / scatter_request! pair.\n\n\n\n\n\n","title":"FerriteOperators.sensitivity_kernel!"},{"category":"method","location":"engine-api/#FerriteOperators.sweep_family-Union{Tuple{Type{K}}, Tuple{K}} where K","page":"Assembly engine API reference","text":"sweep_family(::Type{K}) -> family singleton\n\nThe per-worker family a sweep of kind K reads. Queried on the TYPE, because declarations carry kind types (normalized to their UnionAll base) while sweeps carry instances — one overload point serves both. FunctionalFamily() routes the kind to the value-returning driver (run_reduction); every other kind is NoFamily(). The default derives the answer from reduction_families and folds to a constant.\n\n\n\n\n\n","title":"FerriteOperators.sweep_family"},{"category":"type","location":"engine-api/#FerriteOperators.AlgebraicItemFamily","page":"Assembly engine API reference","text":"AlgebraicItemFamily()\n\nRegistration marker for the items algebraic_items declares — a dof set and nothing else — served by a single SubdomainCache over the whole handler.\n\nwarning: Experimental surface\nThe registration seam may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.AlgebraicItemFamily"},{"category":"type","location":"engine-api/#FerriteOperators.CellFamily","page":"Assembly engine API reference","text":"CellFamily()\n\nThe cell item family's registration marker: the cells of every subdomain, positioned by assembly_iterator and enumerated by item_provider. Registered by every operator.\n\nA family marker is a singleton and subtypes nothing — it only selects a setup_family_caches method, so a downstream family declares its own struct and needs no supertype from this package.\n\nwarning: Experimental surface\nThe registration seam may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.CellFamily"},{"category":"type","location":"engine-api/#FerriteOperators.FacetItemFamily","page":"Assembly engine API reference","text":"FacetItemFamily()\n\nRegistration marker for the boundary items facet_items declares, one SubdomainCache per subdomain that declares any.\n\nwarning: Experimental surface\nThe registration seam may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.FacetItemFamily"},{"category":"method","location":"engine-api/#FerriteOperators.item_families-Tuple{Any, Any}","page":"Assembly engine API reference","text":"item_families(integrator, dh) -> Tuple\n\nThe item families integrator's operator carries over dh, in TRAVERSAL ORDER. The tuple's order IS the order of engine.subdomain_caches, which a reduction's determinism rests on.\n\nThe default is CellFamily always, FacetItemFamily where some subdomain declares facet_items, AlgebraicItemFamily where the handler declares algebraic_items. An overload REPLACES that tuple:\n\nFerriteOperators.item_families(::MyIntegrator, dh) = (CellFamily(), MyFamily())\n\nThe composite and multi-domain wrappers do NOT forward this hook, though they do forward facet_items and algebraic_items: a wrapper's own answer is the operator's, and a sub-integrator's registration is not seen.\n\nwarning: Experimental surface\nThe registration seam may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.item_families"},{"category":"function","location":"engine-api/#FerriteOperators.setup_family_caches","page":"Assembly engine API reference","text":"setup_family_caches(family, strategy, integrator, dh, shared) -> SubdomainCaches\n\nOne registered family's SubdomainCaches, built once at setup_engine. The ONLY route a family's caches enter the engine by: the three shipped families answer it like any downstream one.\n\nA method builds, per subdomain it serves, the three things a SubdomainCache holds: the family's domain descriptor, the device instances of the workspace a sweep positions, and the partition its item set implies (compute_partition). An empty vector declines the operator.\n\nshared is what setup_engine resolved before any family ran:\n\nfield what it carries\nslots the declared state slot names the per-worker buffers are sized for\nneeds_sensitivity whether the workspaces carry SensitivityBuffers\nivh the resolved InternalVariableHandler\nad_backend the ADElementCache backend, nothing where wrapping is opted out\ndeclared_kinds the request kinds whose setup-time validation runs at setup\nelement_caches the resolved, decorated CELL element caches, one per subdomain\nglobal_dof_sets each subdomain's validated global_dofs declaration\nfacet_item_global_dof_sets each subdomain's validated facet_item_global_dofs declaration\nalgebraic_domain resolve_algebraic_domain's (cache, items), or nothing\n\nThe shipped methods live in operators/setup.jl (CellFamily), core/facet-task.jl and core/algebraic-task.jl. foreach_patch and setup_transfer_operator deliberately do NOT register; devdocs/design.md states why.\n\nwarning: Experimental surface\nThe registration seam may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.setup_family_caches"},{"category":"type","location":"engine-api/#FerriteOperators.FacetItem","page":"Assembly engine API reference","text":"FacetItem(cellid, local_facets)\n\nOne work item of the facet family: the owning cell and the local indices of its declared facets, ascending. Facets of one cell are never split across items — that keeps facets sharing the owning cell's dofs on one worker, and makes a facet item's local system owning-cell-shaped.\n\n\n\n\n\n","title":"FerriteOperators.FacetItem"},{"category":"type","location":"engine-api/#FerriteOperators.FacetItemWorkspace","page":"Assembly engine API reference","text":"FacetItemWorkspace\n\nPer-worker workspace of the facet item family: AssemblyWorkspace's buffers with the surface cache in the element slot and the item declaration added. A facet local system is OWNING-CELL-shaped, so Ke/re/the slot buffers are sized like the cell family's — padded by this family's own facet_item_global_dofs declaration, which lets a facet term couple to a dof no cell owns (the tying shape) without augmenting the subdomain's cell sweep.\n\nFerrite.reinit!(ws, index) positions it on an item: the geometry cache is reinitialized on the owning cell and the augmented dof vector's head refreshed from it, so item_dofs/scatter_address and every slot gather resolve as on the cell route.\n\ncurrent is a Ref{Int} for the same reason AlgebraicWorkspace carries one.\n\n\n\n\n\n","title":"FerriteOperators.FacetItemWorkspace"},{"category":"type","location":"engine-api/#FerriteOperators.FacetItems","page":"Assembly engine API reference","text":"FacetItems(sdh, items)\n\nWork-item provider of the facet family: the resolved FacetItems of one SubDofHandler, addressed by index.\n\nBoth partitions are DERIVED from the owning cells. SequentialScheduling hands out one chunk and lets the atomic scatter resolve the dofs neighbouring cells' facets share (dof_scatter_needs_atomic). ColoredScheduling colors the OWNING CELLS with Ferrite's cell coloring restricted to that set: items of one color share no dofs precisely because their owning cells do not, so the policy's barrier promise holds for facet items as it does for cells.\n\n\n\n\n\n","title":"FerriteOperators.FacetItems"},{"category":"method","location":"engine-api/#FerriteOperators.assert_facet_item_route-Tuple{Any, Any}","page":"Assembly engine API reference","text":"assert_facet_item_route(kind, cache)\n\nAssert that cache can serve kind on the facet-item route. The generic method passes: every other kind reaches its own assemble_facet! method, or a MethodError naming it.\n\nWeightedJacobianKind is the one kind that could take more than one, and takes exactly one — the FUSED assemble_facet!(::WeightedJacobianRequest, …) kernel. Nothing composes per-slot facet Jacobians behind the kernel's back, so a cache without the fused kernel is rejected naming that kernel.\n\nChecked at setup for every DECLARED kind (validate_facet_item_cache) and once per item in primal_facet_item_sweep! for the kind actually swept, which is the declare-to-check model the kernel probes follow.\n\n\n\n\n\n","title":"FerriteOperators.assert_facet_item_route"},{"category":"method","location":"engine-api/#FerriteOperators.create_facet_item_workspace-Union{Tuple{N}, NTuple{4, Any}, Tuple{Any, Any, Any, Any, NTuple{N, Symbol}}} where N","page":"Assembly engine API reference","text":"create_facet_item_workspace(cache, items, sdh, ivh, slots; needs_sensitivity = true, global_dofs = ())\n\nCreate a single FacetItemWorkspace. Buffer sizing is create_assembly_workspace's: the same allocate_element_* hooks on the surface cache, padded by the facet_item_global_dofs declaration.\n\n\n\n\n\n","title":"FerriteOperators.create_facet_item_workspace"},{"category":"method","location":"engine-api/#FerriteOperators.current_facets-Tuple{FerriteOperators.FacetItemWorkspace}","page":"Assembly engine API reference","text":"The local facet indices of the FacetItem ws is positioned on.\n\n\n\n\n\n","title":"FerriteOperators.current_facets"},{"category":"method","location":"engine-api/#FerriteOperators.facet_item_walk!-NTuple{4, Any}","page":"Assembly engine API reference","text":"facet_item_walk!(req, task, ws, statesₑ)\n\nThe facet walk of one item: req over the item's declared facets, in declaration order, with the facet parameters queried SEPARATELY per facet. The DECLARED set is the membership statement — a cache states its facets by declaring them, never by re-deriving them per kernel call.\n\nNo reinit_values! call: a facet kernel reinitializes its own FacetValues for the local facet index it was handed.\n\n\n\n\n\n","title":"FerriteOperators.facet_item_walk!"},{"category":"method","location":"engine-api/#FerriteOperators.functional_facet_item_sweep-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"functional_facet_item_sweep(kind, task, ws) -> value\n\nThe functional_cell_sweep counterpart of the facet item family: gathers the state slots without writing anything back and folds what evaluate_facet_functional returns over the item's declared facets, in declaration order. The item contributes nothing when every one of its facets does, which is what keeps the untyped fold's type scan going.\n\nThe per-facet parameter query and the absent reinit_values! call are facet_item_walk!'s; only the destination differs — a returned value instead of the request buffers.\n\n\n\n\n\n","title":"FerriteOperators.functional_facet_item_sweep"},{"category":"method","location":"engine-api/#FerriteOperators.primal_facet_item_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"primal_facet_item_sweep!(kind, task, ws)\n\nThe primal driver body of the facet item family: primal_cell_sweep! with the volumetric kernel taken out and the facet walk restricted to the item's own facets. Scatters ONCE per item through scatter_local! — one scatter for all of a cell's declared facets, which is what the item shape buys.\n\nassert_facet_item_route runs first, so a kind this cache cannot serve on this route fails naming the kernel to write instead of as a MethodError inside the walk.\n\n\n\n\n\n","title":"FerriteOperators.primal_facet_item_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.resolve_facet_items-Tuple{Int64, SubDofHandler, Any}","page":"Assembly engine API reference","text":"resolve_facet_items(index, sdh, declared) -> Vector{FacetItem}\n\nThe facet_items declaration of subdomain index grouped into work items: the declared facets sorted, then collected per owning cell. Sorting is what makes the item order — and with it the coloring, the scatter order and the floating-point summation — reproducible from a Set{FacetIndex}, whose own iteration order is not.\n\nThrows unless every facet's cell lies in sdh.cellset (a facet whose cell this subdomain does not own has no local system here), its local index exists on that cell, and no facet is declared twice.\n\n\n\n\n\n","title":"FerriteOperators.resolve_facet_items"},{"category":"method","location":"engine-api/#FerriteOperators.sensitivity_facet_item_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"sensitivity_facet_item_sweep!(kind, task, ws)\n\nThe sensitivity_cell_sweep! counterpart of the facet item family: a facet-item term DOES enter the sensitivity sweeps. The request is bound over SensitivityBuffers and scattered through scatter_request!; nothing is written back into u.\n\nAnalytic only — facet kernels have no automatic-differentiation fallback in any sweep. Declaring the kind makes validate_facet_item_cache demand the kernel at setup instead of letting the sweep reach a missing method.\n\n\n\n\n\n","title":"FerriteOperators.sensitivity_facet_item_sweep!"},{"category":"function","location":"engine-api/#FerriteOperators.validate_facet_item_cache","page":"Assembly engine API reference","text":"validate_facet_item_cache(cache, declared_requests = ())\n\nSetup-time consistency check for facet item caches, the validate_element_cache counterpart. The mandatory ResidualRequest facet kernel must exist, and every kind provides_analytic claims must have a matching assemble_facet! method.\n\nA DECLARED sensitivity kind is checked harder — the kernel must exist, not merely be trait-consistent — because facet contributions have no automatic-differentiation fallback, unlike a volumetric cache whose ADElementCache decoration would serve it from the residual. Every declared kind additionally passes assert_facet_item_route, the route-level check the sweep repeats per item.\n\nThe probes run against unwrap, the type an author would have written the kernel on: a decorator's forwarding methods answer hasmethod for everyone and would make every probe pass vacuously. A composite cache recurses into its inners from there, for the same reason: its blanket fan-out method answers every probe.\n\n\n\n\n\n","title":"FerriteOperators.validate_facet_item_cache"},{"category":"type","location":"engine-api/#FerriteOperators.AlgebraicItems","page":"Assembly engine API reference","text":"AlgebraicItems(items)\n\nWork-item provider of the algebraic family: the declared dof vectors, addressed by index. Items of one provider generally SHARE dofs (several 0D rows on one lumped unknown), and that is what fixes both partitions. SequentialScheduling hands out one chunk and lets the atomic scatter resolve the collisions — the route a global_dofs declaration takes too. ColoredScheduling promises that no two items of a barrier share a dof, which for an unknown sharing pattern leaves one item per barrier.\n\n\n\n\n\n","title":"FerriteOperators.AlgebraicItems"},{"category":"type","location":"engine-api/#FerriteOperators.AlgebraicWorkspace","page":"Assembly engine API reference","text":"AlgebraicWorkspace\n\nPer-worker workspace of the algebraic item family: the item-sized local system (Ke, re, one slot buffer per declared slot), the algebraic cache, the SensitivityBuffers the sensitivity entry points need, and the item declaration this worker resolves indices against. Ferrite.reinit!(ws, index) positions it on an item.\n\nEvery buffer is FIXED-size: one provider's items are uniformly sized (validated at setup), so no sweep ever resizes anything here.\n\ncurrent is a Ref{Int} for the same reason PatchAssemblyWorkspace carries one. ivh mirrors AssemblyWorkspace's field of the same name: the shared InternalVariableHandler an :q slot's InternalSource gather resolves the current item's range against.\n\n\n\n\n\n","title":"FerriteOperators.AlgebraicWorkspace"},{"category":"method","location":"engine-api/#FerriteOperators.condensation_algebraic_sweep!-Tuple{CondensationKind, Any, Any}","page":"Assembly engine API reference","text":"condensation_algebraic_sweep!(kind::CondensationKind, task, ws) -> CondensationReport\n\nThe condensation_cell_sweep! counterpart of the algebraic family: hands the gathered slots to condense_algebraic! and copies the item-local q buffer it filled into the :q slot's global vector over the item's internal_variable_range — the item block of the [ū | q_cells | q_items] tail. Gated on has_internal_state above.\n\n\n\n\n\n","title":"FerriteOperators.condensation_algebraic_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.create_algebraic_workspace-Union{Tuple{T}, Tuple{N}, Tuple{Any, Any, NTuple{N, Symbol}, Type{T}, Any}} where {N, T}","page":"Assembly engine API reference","text":"create_algebraic_workspace(cache, items, slots, ::Type{T}, ivh; needs_sensitivity = true)\n\nCreate a single AlgebraicWorkspace whose buffers are sized by the common dof count of items — an item's local system spans its own dofs and nothing else, so there is no padding to apply.\n\nneeds_sensitivity selects whether SensitivityBuffers is built, the same STRUCTURAL decision the cell workspace makes (see needs_ad_decoration).\n\n\n\n\n\n","title":"FerriteOperators.create_algebraic_workspace"},{"category":"method","location":"engine-api/#FerriteOperators.current_item-Tuple{FerriteOperators.AlgebraicWorkspace}","page":"Assembly engine API reference","text":"The AlgebraicItem ws is positioned on, set by Ferrite.reinit!(ws, index).\n\n\n\n\n\n","title":"FerriteOperators.current_item"},{"category":"method","location":"engine-api/#FerriteOperators.decorate_algebraic_cache-Union{Tuple{T}, Tuple{Any, Int64, Any, Type{T}}} where T","page":"Assembly engine API reference","text":"decorate_algebraic_cache(cache, ndofs, ad_backend, ::Type{T})\n\nThe decorate_element_cache counterpart for the algebraic family: resolve cache into the form the driver calls unconditionally, with ADElementCache sized by the item's dof count rather than by a SubDofHandler. ad_backend === nothing opts out of the AD step.\n\n\n\n\n\n","title":"FerriteOperators.decorate_algebraic_cache"},{"category":"method","location":"engine-api/#FerriteOperators.functional_algebraic_sweep-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"functional_algebraic_sweep(kind, task, ws) -> value\n\nThe functional_cell_sweep counterpart of the algebraic family: gathers the state slots without writing anything back and returns what evaluate_algebraic_functional gives for the item (nothing by default).\n\n\n\n\n\n","title":"FerriteOperators.functional_algebraic_sweep"},{"category":"method","location":"engine-api/#FerriteOperators.internal_jacobian_algebraic_sweep!-Union{Tuple{C}, Tuple{JacobianKind{:q, C}, Any, Any}} where C","page":"Assembly engine API reference","text":"internal_jacobian_algebraic_sweep!(kind::JacobianKind{:q}, task, ws)\n\nThe internal_jacobian_cell_sweep! counterpart of the algebraic family: the item's ∂F/∂q block, its rows the item's own dofs and its columns the item's internal_variable_range. A stateless algebraic cache owns no internal dofs, so its range is empty and it never reaches a kernel.\n\n\n\n\n\n","title":"FerriteOperators.internal_jacobian_algebraic_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.primal_algebraic_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"primal_algebraic_sweep!(kind, task, ws)\n\nThe primal driver body of the algebraic family: primal_cell_sweep! with the geometry taken out. No reinit_values! (an item carries no values objects) and no facet walk (it carries no facets); the buffer zeroing, the parameter query, the depends_on_unknowns-gated slot gather and the scatter_local! scatter are the cell family's.\n\n\n\n\n\n","title":"FerriteOperators.primal_algebraic_sweep!"},{"category":"method","location":"engine-api/#FerriteOperators.resolve_algebraic_domain-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"resolve_algebraic_domain(integrator, dh, declared_kinds) -> (cache, items) or `nothing`\n\nThe algebraic_items declaration resolved and validated once, before setup_internal_variable_handler or any decoration: nothing where the integrator declares no items, otherwise the raw cache setup_algebraic_cache built and the resolved item dof vectors — what setup_internal_variable_handler needs to size a condensed algebraic cache's item block, and what the AlgebraicItemFamily's setup_family_caches method needs to finish the domain. It rides shared.algebraic_domain from there.\n\n\n\n\n\n","title":"FerriteOperators.resolve_algebraic_domain"},{"category":"method","location":"engine-api/#FerriteOperators.resolve_algebraic_items-Tuple{Any, Int64}","page":"Assembly engine API reference","text":"resolve_algebraic_items(declared, ndofs_total) -> Vector{Vector{Int}}\n\nThe algebraic_items declaration resolved once, before any cache exists: uniform item size (the fixed-size local buffers depend on it), in-bounds dofs, and no dof repeated WITHIN an item, which would receive the same contribution twice. Dofs shared BETWEEN items are the normal case, and are what the partition derivation accounts for.\n\n\n\n\n\n","title":"FerriteOperators.resolve_algebraic_items"},{"category":"method","location":"engine-api/#FerriteOperators.sensitivity_algebraic_sweep!-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"sensitivity_algebraic_sweep!(kind, task, ws)\n\nThe sensitivity_cell_sweep! counterpart of the algebraic family: the request is bound over SensitivityBuffers, issued against the resolved cache and scattered through scatter_request!. Nothing is written back into u.\n\n\n\n\n\n","title":"FerriteOperators.sensitivity_algebraic_sweep!"},{"category":"function","location":"engine-api/#FerriteOperators.validate_algebraic_cache","page":"Assembly engine API reference","text":"validate_algebraic_cache(cache, declared_requests = ())\n\nSetup-time consistency check for algebraic caches, the validate_element_cache counterpart: the mandatory ResidualRequest kernel must exist, every kind provides_analytic claims must have a matching assemble_algebraic! method, and — when the cache declares has_internal_state — every primal kind requires_admissibility_check names must be served analytically or declared internal_state_insensitive, exactly like a condensed CELL cache. Those two are the only escapes here: an algebraic item has no cellid to key a corrector store by, so there is no generic AD Consistent bootstrap (see condense_algebraic!) and never ADElementCache's condensed_corrector combination.\n\nRuns on the RAW cache, before decorate_algebraic_cache: the two subjects the AbstractElementCacheDecorator convention distinguishes coincide here, since the generic routes a decorator would add are CellArgs-shaped and this family has none. The kernel probe still takes the unwrap fixpoint, so a hand-decorated cache is probed on its author-written method set.\n\n\n\n\n\n","title":"FerriteOperators.validate_algebraic_cache"},{"category":"type","location":"engine-api/#FerriteOperators.AbstractAssemblyForm","page":"Assembly engine API reference","text":"Which representation of the operator is produced — the MFEM assembly level. Orthogonal to how the work is scheduled and to the device it runs on. FullAssembly and MatrixFreeAction are the members; the axis and the form keyword of AssemblyStrategy are the extension point a further assembly level is added at.\n\n\n\n\n\n","title":"FerriteOperators.AbstractAssemblyForm"},{"category":"type","location":"engine-api/#FerriteOperators.AbstractAssemblyStrategy","page":"Assembly engine API reference","text":"AbstractAssemblyStrategy\n\nDispatch anchor for an operator's assembly strategy; AssemblyStrategy is the sole concrete composition of the form/scheduling/device axes below.\n\n\n\n\n\n","title":"FerriteOperators.AbstractAssemblyStrategy"},{"category":"type","location":"engine-api/#FerriteOperators.AbstractSchedulingPolicy","page":"Assembly engine API reference","text":"How parallel work is made safe — the second strategy axis. See SequentialScheduling and ColoredScheduling.\n\n\n\n\n\n","title":"FerriteOperators.AbstractSchedulingPolicy"},{"category":"type","location":"engine-api/#FerriteOperators.AbstractWorkspace","page":"Assembly engine API reference","text":"AbstractWorkspace\n\nSupertype of the per-worker workspaces a device backend allocates and manages. Every concrete workspace must implement:\n\nFerrite.reinit!(ws, item) — position it on the item its family is indexed by (a cell id, a patch index, an algebraic item index)\nduplicate_for_device(device::AbstractCPUDevice, ws) — an independent copy for a parallel worker\n\n\n\n\n\n","title":"FerriteOperators.AbstractWorkspace"},{"category":"type","location":"engine-api/#FerriteOperators.AssemblyStrategy","page":"Assembly engine API reference","text":"AssemblyStrategy(form, scheduling, device)\n\nThe composition of three orthogonal axes: the operator form (AbstractAssemblyForm — what is produced), the scheduling policy (AbstractSchedulingPolicy — how parallel work is made safe), and the device. The named constructors below build the common compositions.\n\n\n\n\n\n","title":"FerriteOperators.AssemblyStrategy"},{"category":"method","location":"engine-api/#FerriteOperators.AssemblyStrategy-Tuple{FerriteOperators.AbstractDevice}","page":"Assembly engine API reference","text":"AssemblyStrategy(device; form = FullAssembly(), scheduling = SequentialScheduling())\n\nKeyword convenience over the 3-arg constructor: the form/scheduling/device axes are the model, and the defaults here cover the common composition (FullAssembly under SequentialScheduling, i.e. one chunk — the only composition that admits the global-dof declarations (global_dofs, facet_item_global_dofs) and the algebraic item family). Pass scheduling = ColoredScheduling() for a parallel device without atomic support.\n\n\n\n\n\n","title":"FerriteOperators.AssemblyStrategy"},{"category":"type","location":"engine-api/#FerriteOperators.AssemblyWorkspace","page":"Assembly engine API reference","text":"AssemblyWorkspace\n\nPer-worker workspace for square operator assembly (bilinear, nonlinear, linear): element-local buffers and caches reused across cells, plus the SensitivityBuffers a nonlinear operator's sensitivity entry points need.\n\nIMMUTABLE: every field is bound at construction, a sweep only filling the buffers they point at. That is what lets the workspace cross a device boundary — a scalar or tensor evaluation RETURNS its value instead of parking it in a slot here (see evaluate_functional).\n\nCore fields:\n\nKe, re, cell, ivh: element matrix, element residual, Ferrite.CellCache, internal variable handler\nslot_buffers: NamedTuple of element-local state buffers, one per declared slot\nelement: the volumetric element cache (AbstractVolumetricElementCache)\nsensitivity: SensitivityBuffers, or nothing for a family that never issues a sensitivity kind (bilinear, linear)\ndofs: augmented dof vector [celldofs(cell); the declared global dofs] (global_dofs), or nothing where the integrator declares none. The tail is written once at construction, the head refreshed by Ferrite.reinit!; the nothing type is what makes the un-augmented path return celldofs(ws.cell) directly, with neither copy nor run-time branch.\n\n\n\n\n\n","title":"FerriteOperators.AssemblyWorkspace"},{"category":"type","location":"engine-api/#FerriteOperators.BlockedOperatorSpecification","page":"Assembly engine API reference","text":"BlockedOperatorSpecification(block_sizes, matrix_type; algebraic_couplings = (),\n                             constraint_handler = nothing, sparsity_entries = nothing)\n\nThe operator's global matrix as a BlockMatrix over the row/column split block_sizes, allocated from a BlockSparsityPattern and the same three declarations as StandardOperatorSpecification. matrix_type is REQUIRED: this package depends on neither BlockArrays nor SparseMatricesCSR, so the user loads them and names the type (BlockMatrix{Float64, Matrix{SparseMatrixCSR{1, Float64, Int}}}).\n\nThe residual stays a plain Vector — Ferrite's BlockAssembler takes a non-blocked f. A LINEAR operator holds no matrix, so a blocked specification on one is rejected at setup.\n\n\n\n\n\n","title":"FerriteOperators.BlockedOperatorSpecification"},{"category":"type","location":"engine-api/#FerriteOperators.CellItems","page":"Assembly engine API reference","text":"CellItems(sdh)\n\nThe default work-item provider: the cells of one SubDofHandler. Item providers are what compute_partition consumes — every other item family brings its own provider type (FacetItems, AlgebraicItems, PatchItems).\n\n\n\n\n\n","title":"FerriteOperators.CellItems"},{"category":"type","location":"engine-api/#FerriteOperators.ColoredScheduling","page":"Assembly engine API reference","text":"ColoredScheduling(alg = ColoringAlgorithm.WorkStream)\n\nRace freedom by coloring: the partition is a sequence of colors, no two items of one color sharing a dof, so the scatter needs no atomics. Required for eltypes without atomic support, and what makes a run reproducible element-by-element.\n\n\n\n\n\n","title":"FerriteOperators.ColoredScheduling"},{"category":"type","location":"engine-api/#FerriteOperators.CorrectorElection","page":"Assembly engine API reference","text":"StorageElection\nStored <: StorageElection\nRecompute <: StorageElection\nElementAssembly <: StorageElection\n\nWHAT a sweep keeps between evaluations — a construction-time election trading memory against flops. Two consumers: the storage field of MatrixFreeAction, where the three members are MFEM's PARTIAL, NONE and ELEMENT assembly levels, and corrector_election.\n\nRecompute — keep nothing.\nStored — keep the per-quadrature-point quantity.\nElementAssembly — keep the dense element MATRICES. A matrix-free-action election only; a consumer that does not implement it says so (corrector_election_error).\n\nCorrectorElection is an alias for this supertype.\n\nwarning: Experimental surface\nThis election family may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.CorrectorElection"},{"category":"type","location":"engine-api/#FerriteOperators.ElementAssembly","page":"Assembly engine API reference","text":"ElementAssembly()\n\nKeep the dense element MATRICES — MFEM's ELEMENT level, the third member of MatrixFreeAction's storage ladder. Every mul! is a gather, a dense yₑ = Kₑ·uₑ and a scatter; no quadrature point is visited.\n\nThe matrices are formed at setup_operator and refilled by update_operator!, through the element's own element-matrix kernel where it declares one (provides_analytic for JacobianKind{:u}) and through ndofs_per_cell applications of apply_element_action! to the unit vectors otherwise. An element serving neither is refused at setup.\n\nIt costs ndofs_per_cell² scalars per cell, so it is the LOW-order election. WorkerPerElement or LanesPerElement, but never CooperativeElement, a dense product having no lattice to split.\n\nBytes/cell against the same form's assembled matrix, on a tensor-product hexahedral mesh with one Float32 scalar field (p = polynomial order); the packed column is SymmetricElementMatrix's election (element_matrix_symmetry):\n\np ElementAssembly (ndofs_per_cell² · 4) packed (ndofs_per_cell·(ndofs_per_cell+1)/2 · 4) assembled (global CSR, amortized)\n1 256 144 220\n2 2916 1512 4157\n3 16384 8320 27318\n\nndofs_per_cell² overtakes the assembled matrix's per-cell share between p = 1 and p = 2 and keeps widening, so this is the cheapest MATRIX-FREE storage at p = 1–2 and Stored/Recompute take over above that. The fill is paid at setup_operator and on every update_operator!, never on a bare mul!.\n\nAn element whose bilinear form is symmetric may additionally elect SymmetricElementMatrix (element_matrix_symmetry) — the packed column — at the price of a per-worker ndofs_per_cell² scratch buffer at FILL time only.\n\n\n\n\n\n","title":"FerriteOperators.ElementAssembly"},{"category":"type","location":"engine-api/#FerriteOperators.FullAssembly","page":"Assembly engine API reference","text":"FULL level: assemble into a global sparse matrix / global vector.\n\n\n\n\n\n","title":"FerriteOperators.FullAssembly"},{"category":"type","location":"engine-api/#FerriteOperators.GeneralElementMatrix","page":"Assembly engine API reference","text":"GeneralElementMatrix()\n\nThe default element_matrix_symmetry election: no symmetry claim, Kₑ stored dense.\n\n\n\n\n\n","title":"FerriteOperators.GeneralElementMatrix"},{"category":"type","location":"engine-api/#FerriteOperators.MatrixFreeAction","page":"Assembly engine API reference","text":"MatrixFreeAction(; element_mapping = WorkerPerElement(), storage = Stored())\n\nELEMENT/PARTIAL/NONE level: the operator stores no GLOBAL matrix and evaluates its action y = A·u element by element on every mul! (MatrixFreeFerriteOperator). setup_operator returns that operator for an AbstractBilinearIntegrator whose caches serve the elected storage level.\n\nelement_mapping selects how one element's action maps onto the device's workers (AbstractElementMapping) — the same element definition under each. It is resolved onto the device at setup (with_element_mapping), so a mapping the device has no kernel for, or a cache that does not serve it, is a setup error.\n\nstorage is WHAT the operator keeps between actions (StorageElection), and separates the three MFEM levels this form spans:\n\nStored (the default) is PARTIAL. Each element precomputes its per-quadrature-point factors into its own store (fill_quadrature_data!) and every action is then contractions and reads.\nRecompute is NONE. Nothing is kept and every action re-derives the geometry at the quadrature point that consumes it — the election for a mesh whose factors do not fit, or a coefficient that changes faster than a store can be refilled.\nElementAssembly is ELEMENT. The dense element matrices are kept and every action is a gather, a dense product and a scatter — no quadrature point is visited. ndofs_per_cell² scalars per cell, so it is the LOW-order election, and WorkerPerElement or LanesPerElement.\n\nThe first two are the element's own storage and reach its cache through with_action_storage; a cache that keeps nothing serves both identically. The third is the framework's (ElementAssemblyCache) and serves any bilinear cache. Both are filled at setup and refilled by update_operator!.\n\nwarning: Experimental surface\nThe matrix-free form, its operator type and the element entry points it calls may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.MatrixFreeAction"},{"category":"type","location":"engine-api/#FerriteOperators.Recompute","page":"Assembly engine API reference","text":"Recompute()\n\nKeep nothing and re-derive at every point of use: a matrix-free element's geometry at the quadrature point that consumes it (MFEM's NONE level), or a condensed element's corrector from the item's current (u, q) (corrector_election). Recomputation is EXACT, not approximate.\n\n\n\n\n\n","title":"FerriteOperators.Recompute"},{"category":"type","location":"engine-api/#FerriteOperators.SensitivityBuffers","page":"Assembly engine API reference","text":"SensitivityBuffers\n\nPer-worker OUTPUT buffers the five sensitivity requests (ParameterJacobianRequest, ParameterVJPRequest, TimeSensitivityRequest, StateJVPRequest, StateVJPRequest) accumulate into and the engine scatters, plus the rectangular ∂F/∂q block (update_internal_jacobian!), also a local block whose column space is not the field space. The outputs half of the AD decorator's buffer split — ADElementCache holds the seeds and configs.\n\nElement-sized members (λₑ, vₑ, Jvₑ, gu, gₜ) are eager; the rectangular ones are (re)allocated once their column count is known — θ/Bₑ/gθ by parameter_sweep_buffers!, Kqₑ by internal_sweep_buffers!.\n\n\n\n\n\n","title":"FerriteOperators.SensitivityBuffers"},{"category":"type","location":"engine-api/#FerriteOperators.SequentialScheduling","page":"Assembly engine API reference","text":"SequentialScheduling()\n\nRace freedom by atomic scatter: every worker assembles into the same global target, collisions resolved atomically. Requires an eltype with atomic support.\n\n\n\n\n\n","title":"FerriteOperators.SequentialScheduling"},{"category":"type","location":"engine-api/#FerriteOperators.StandardOperatorSpecification","page":"Assembly engine API reference","text":"StandardOperatorSpecification(; algebraic_couplings = (), constraint_handler = nothing,\n                                matrix_type = nothing, sparsity_entries = nothing)\n\nThe operator's global matrix as a monolithic sparse matrix over the pattern create_system_matrix builds from three declarations BlockedOperatorSpecification shares (the zero-argument form is FullAssembly's default):\n\nalgebraic_couplings — Ferrite coupling descriptors (CellCoupling, FacetCoupling, AlgebraicCoupling) for the entries a global_dofs or facet_item_global_dofs declaration couples into, never inferred from the dof declaration. A missing descriptor surfaces as Ferrite's missing-sparsity-entry error on the first assembly.\nconstraint_handler — sparsity room for the constraint entries (add_constraint_entries!). Applying the constraints stays the caller's, through Ferrite's apply!/apply_assemble!.\nsparsity_entries — a callable f(sp, dh) run on the freshly built pattern after the entries above, for the coupling an ITEM FAMILY introduces and the DofHandler's cell pattern does not carry: an item spanning two cells (an interface/DG traversal, item_provider) scatters a block indexed by the dofs of BOTH. nothing (the default) adds none. The framework never infers them — which dofs an item couples is the provider's own adjacency — the same doctrine global_dofs states for its tail:\nsparsity_entries = (sp, dh) -> Ferrite.add_interface_entries!(\n    sp, dh, nothing; topology = ExclusiveTopology(get_grid(dh)))\nIt is re-run for every matrix allocated under this specification (the operator's own, and the ones allocate_components and StageBlockOperator build beside it), so it must be a pure entry-adding function and not a pre-built pattern to hand out.\nmatrix_type — the concrete matrix type to allocate, nothing (the default) meaning SparseMatrixCSC{value_type(device), index_type(device)}. Naming one is what a DEVICE matrix takes: this package depends on no GPU vendor package, so the user loads it and names the type (StandardOperatorSpecification(; matrix_type = CuSparseMatrixCSC{Float32, Int32})), exactly as BlockedOperatorSpecification does for its block storage. The pattern stays this package's. Its element type must be the device's value_type, and Ferrite must have a start_assemble method for it — both checked at setup.\n\n\n\n\n\n","title":"FerriteOperators.StandardOperatorSpecification"},{"category":"type","location":"engine-api/#FerriteOperators.StorageElection","page":"Assembly engine API reference","text":"StorageElection\nStored <: StorageElection\nRecompute <: StorageElection\nElementAssembly <: StorageElection\n\nWHAT a sweep keeps between evaluations — a construction-time election trading memory against flops. Two consumers: the storage field of MatrixFreeAction, where the three members are MFEM's PARTIAL, NONE and ELEMENT assembly levels, and corrector_election.\n\nRecompute — keep nothing.\nStored — keep the per-quadrature-point quantity.\nElementAssembly — keep the dense element MATRICES. A matrix-free-action election only; a consumer that does not implement it says so (corrector_election_error).\n\nCorrectorElection is an alias for this supertype.\n\nwarning: Experimental surface\nThis election family may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.StorageElection"},{"category":"type","location":"engine-api/#FerriteOperators.Stored","page":"Assembly engine API reference","text":"Stored()\n\nKeep the per-quadrature-point quantity: a matrix-free element's geometric factors (fill_quadrature_data! — MFEM's PARTIAL level), or a condensed element's stored corrector (corrector_election).\n\n\n\n\n\n","title":"FerriteOperators.Stored"},{"category":"type","location":"engine-api/#FerriteOperators.SymmetricElementMatrix","page":"Assembly engine API reference","text":"SymmetricElementMatrix()\n\nDeclares Kₑ = Kₑᵀ for every cell of the subdomain — see element_matrix_symmetry for the trust contract and the hazard of a wrong declaration.\n\n\n\n\n\n","title":"FerriteOperators.SymmetricElementMatrix"},{"category":"method","location":"engine-api/#FerriteOperators.compute_partition-Tuple{AssemblyStrategy, SubDofHandler}","page":"Assembly engine API reference","text":"compute_partition(strategy, provider)\ncompute_partition(scheduling, provider)\n\nThe work partition for a scheduling policy and an item provider (item_provider): an iterable of iterables, the outer level synchronization barriers (e.g. colors), the inner level work items that may run concurrently (cell ids for CellItems, item indices for every other family).\n\nPartition obligations. A partition is SAFE GIVEN A VALID PARTITION, never thread-safe. What makes a parallel sweep race-free is a promise about the provider's own item adjacency, which the framework cannot check.\n\nUnder ColoredScheduling the provider promises that no two items of one inner chunk share a scatter dof. That promise is what lets the scatter run without atomics (dof_scatter_needs_atomic), so a chunk that breaks it is a silent race, not an error.\nUnder SequentialScheduling the provider promises nothing: the atomic scatter resolves the collisions.\n\nThe three shipped providers each argue the promise in their own terms: FacetItems colors the OWNING CELLS; AlgebraicItems has an unknown sharing pattern and puts one item per barrier; PatchItems REFUSES to color, not carrying the adjacency the promise would rest on.\n\n\n\n\n\n","title":"FerriteOperators.compute_partition"},{"category":"method","location":"engine-api/#FerriteOperators.create_assembly_workspace-Union{Tuple{N}, Tuple{Any, Any, Any}, Tuple{Any, Any, Any, NTuple{N, Symbol}}} where N","page":"Assembly engine API reference","text":"create_assembly_workspace(element, sdh, ivh, slots; needs_sensitivity = true,\n                          global_dofs = (), iterator = assembly_iterator(nothing, element, sdh))\n\nCreate one AssemblyWorkspace with freshly allocated element-local buffers, one state buffer per declared slot name and sized to ndofs_per_cell; a slot gathered through InternalSource (a condensed element's q) is resized to the cell's internal-dof range on every gather instead, that count being unrelated to the field dof count and free to vary per cell.\n\nneeds_sensitivity selects whether SensitivityBuffers is built — STRUCTURAL, decided by the integrator family (needs_ad_decoration).\n\nglobal_dofs is the subdomain's global_dofs declaration: every element-local buffer is padded by its length, and the workspace carries the augmented dof vector the sweep's gathers and scatters address.\n\niterator is what positions the workspace on an item and what rides args.cell, assembly_iterator's answer for this subdomain.\n\nThe buffers carry whatever scalar the allocate_element_* hooks return — the ELEMENT's precision (element_value_type), which need not be the device's.\n\n\n\n\n\n","title":"FerriteOperators.create_assembly_workspace"},{"category":"method","location":"engine-api/#FerriteOperators.default_strategy-Tuple{}","page":"Assembly engine API reference","text":"default_strategy()\n\nThe recommended shared-memory AssemblyStrategy for whatever is currently loaded: AssemblyStrategy over a PolyesterDevice once Polyester.jl is loaded (using Polyester activates FerriteOperatorsPolyesterExt, which is what gives PolyesterDevice its execute_on_device!/reduce_on_device methods — without the extension the struct exists but has no execution route), and over a SequentialCPUDevice otherwise. A setup-time call: the extension check is a runtime lookup, not a compile-time constant, so it reflects whatever is loaded when it runs.\n\nLoad Polyester to get the parallel default. What this returns MAY CHANGE between minor versions as better defaults become available — a caller that needs a strategy that does not move under it should construct one explicitly instead.\n\n\n\n\n\n","title":"FerriteOperators.default_strategy"},{"category":"method","location":"engine-api/#FerriteOperators.device_worker_view-Tuple{FerriteOperators.AssemblyWorkspace, Any}","page":"Assembly engine API reference","text":"device_worker_view(ws::AssemblyWorkspace, worker)\n\nWorker worker's slice of the batched workspace a GPU device's setup_device_instances built. The internal-variable handler is shared read-only; everything else slices.\n\n\n\n\n\n","title":"FerriteOperators.device_worker_view"},{"category":"method","location":"engine-api/#FerriteOperators.element_matrix_symmetry-Tuple{Any}","page":"Assembly engine API reference","text":"element_matrix_symmetry(cache) -> GeneralElementMatrix()\n                                -> SymmetricElementMatrix()\n\nWhether cache's bilinear form is symmetric. Consumed by ElementAssemblyCache ONLY, read once off the wrapped cache at construction, so the action has no runtime branch.\n\nGeneralElementMatrix() (the default) makes no claim and Kₑ is stored dense. SymmetricElementMatrix is TRUSTED UNCHECKED: a wrong declaration silently reads only the upper triangle, SYMMETRIZING the assembled operator rather than erroring. Declare it only where the FORM is provably symmetric for every value the cache's coefficients can take; where the coefficient is not symmetric BY TYPE, check its value at construction (as the FerriteOperatorsTensorProduct diffusion cache does).\n\nwarning: Experimental surface\nThis election and the two singletons below may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.element_matrix_symmetry"},{"category":"method","location":"engine-api/#FerriteOperators.internal_sweep_buffers!-Tuple{FerriteOperators.SensitivityBuffers, Int64}","page":"Assembly engine API reference","text":"internal_sweep_buffers!(s::SensitivityBuffers, nq) -> SensitivityBuffers\n\nSize the ∂F/∂q block (Kqₑ) for an item owning nq condensed internal dofs, reallocating only when that count changed on this worker. The count is per ITEM, not per subdomain, so this runs per item rather than per sweep (update_internal_jacobian!).\n\n\n\n\n\n","title":"FerriteOperators.internal_sweep_buffers!"},{"category":"method","location":"engine-api/#FerriteOperators.item_provider-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"item_provider(kind, element_cache, sdh) -> provider\n\nWHAT the items of a sweep of kind over element_cache on subdomain sdh ARE. The second half of the iteration seam, beside assembly_iterator, which says how to POSITION on one of them. Resolved ONCE per (sweep kind, element cache, subdomain) at setup_operator; its answer is what compute_partition consumes, and the partition is what a sweep walks.\n\nThe default is CellItems(sdh) — the cells of the subdomain.\n\nTwo seams and not one because the iterator and the item set vary INDEPENDENTLY: the facet family runs a custom provider (FacetItems) over the STOCK cell iterator, the matrix-free action a custom iterator over the STOCK provider.\n\nThe hazard. Overloading one seam and not the other is not an error: the other answers with its default, and an interface iterator left with the default provider is positioned on CELL ids — succeeding, and assembling the wrong operator. No framework check sees this; the item COUNT a sweep visits does.\n\nA provider carries its family's whole partition safety argument — see compute_partition.\n\n\n\n\n\n","title":"FerriteOperators.item_provider"},{"category":"method","location":"engine-api/#FerriteOperators.n_workers-Tuple{SequentialCPUDevice, Any}","page":"Assembly engine API reference","text":"n_workers(device, partition) -> Int\n\nNumber of parallel workers for this device and partition — the size of the per-worker device cache setup_device_instances builds.\n\nA PolyesterDevice can occupy at most Threads.nthreads() of them at once, and min_items_per_worker is the smallest share a worker is given, so the count is the smaller of the thread count and the number of such shares the largest barrier of partition holds. Workspaces are therefore per WORKER, not per share: a worker walks the items it was given with the one workspace it owns.\n\nA KernelAbstractionsDevice sizes them from its launch policy (launch_geometry) over the largest barrier, the kernel indexing the per-worker caches unchecked. Under CooperativeElement the worker IS the workgroup and the count is the largest barrier itself. LanesPerElement stages nothing per LANE, so this method serves it unchanged.\n\n\n\n\n\n","title":"FerriteOperators.n_workers"},{"category":"method","location":"engine-api/#FerriteOperators.operator_specification-Tuple{FullAssembly}","page":"Assembly engine API reference","text":"operator_specification(form) -> spec or `nothing`\n\nThe global-storage declaration a form carries, nothing for a form that allocates no global array (MatrixFreeAction). The setup-time device walls read the specification through this, so a storage-free form skips the storage checks instead of naming a field it does not have.\n\n\n\n\n\n","title":"FerriteOperators.operator_specification"},{"category":"method","location":"engine-api/#FerriteOperators.parameter_sweep_buffers!-Tuple{FerriteOperators.SensitivityBuffers, Int64}","page":"Assembly engine API reference","text":"parameter_sweep_buffers!(s::SensitivityBuffers, nθ) -> SensitivityBuffers\n\nSize the parameter-sweep members (θ, Bₑ, gθ) for the nθ flat parameters arriving with p, reallocating only when nθ changed on this worker.\n\n\n\n\n\n","title":"FerriteOperators.parameter_sweep_buffers!"},{"category":"type","location":"engine-api/#FerriteOperators.AbstractCPUDevice","page":"Assembly engine API reference","text":"AbstractCPUDevice{ValueType, IndexType} <: AbstractDevice{ValueType, IndexType}\n\nDispatch anchor for CPU devices (SequentialCPUDevice, PolyesterDevice): duplicate_for_device's per-worker copies are plain Julia objects, as opposed to a GPU device's struct-of-arrays layout.\n\n\n\n\n\n","title":"FerriteOperators.AbstractCPUDevice"},{"category":"type","location":"engine-api/#FerriteOperators.AbstractElementMapping","page":"Assembly engine API reference","text":"AbstractElementMapping\n\nHow ONE item's work maps onto a device's workers. The election lives on the operator's FORM (MatrixFreeAction) and is realized on the device through with_element_mapping, because the seams that change shape with it — n_workers, setup_device_instances, execute_on_device! — all read the device.\n\nWorkerPerElement is the only one an assembling form takes.\n\nwarning: Experimental surface\nThis mapping family may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.AbstractElementMapping"},{"category":"type","location":"engine-api/#FerriteOperators.AbstractGPUDevice","page":"Assembly engine API reference","text":"AbstractGPUDevice{ValueType, IndexType} <: AbstractDevice{ValueType, IndexType}\n\nDispatch anchor for GPU devices: a downstream device implementation subtypes this to pick up the GPU-shaped setup_device_instances/ duplicate_for_device contract instead of the CPU one.\n\n\n\n\n\n","title":"FerriteOperators.AbstractGPUDevice"},{"category":"type","location":"engine-api/#FerriteOperators.CooperativeElement","page":"Assembly engine API reference","text":"CooperativeElement()\n\nOne WORKGROUP owns one item: the group's workers split the element's lattice between them, stage the element state and the intermediates in group-local memory and synchronize between contraction stages. Needs group-local memory and barriers, so it is a KernelAbstractionsDevice mapping only, and an element serves it only where it implements the cooperative kernel entries (cooperative_stage!).\n\n\n\n\n\n","title":"FerriteOperators.CooperativeElement"},{"category":"type","location":"engine-api/#FerriteOperators.KernelAbstractionsDevice","page":"Assembly engine API reference","text":"KernelAbstractionsDevice(backend; value_type = Float64, index_type = Int,\n                         items_per_worker = 2, max_workgroup_size = 64)\n\nAssembly on a KernelAbstractions.jl backend — CUDABackend() for CUDA, KA.CPU() to run the same kernels on the host. Load KernelAbstractions.jl and Adapt.jl to activate this device; the vendor package (and the device matrix type it names, see StandardOperatorSpecification) is the caller's, this package depends on none.\n\nvalue_type/index_type are the scalar and matrix-index types of the GLOBAL system — Float32/Int32 is the usual GPU election. The precision the element caches evaluate in is the integrator's, elected through its quadrature collection (element_value_type), and the two are free to differ.\n\nitems_per_worker and max_workgroup_size are the LAUNCH POLICY, and n_workers derives the per-worker cache size from them, so a sweep launches exactly the geometry setup allocated for. A barrier of n items runs ceil(n / items_per_worker) workers in groups of at most max_workgroup_size, so a SMALLER group size means MORE groups over the same workers, spreading a colour across the device's multiprocessors. items_per_worker trades the per-worker scratch (element buffers, geometry cache and values objects, all sized by the worker count) against that parallelism.\n\nelement_mapping is resolved from the operator's form (with_element_mapping) and is not a constructor argument: a FullAssembly operator is always WorkerPerElement, and a MatrixFreeAction one elects between that and CooperativeElement.\n\nAn ASSEMBLING sweep requires ColoredScheduling and covers CELL items only. What is rejected at setup, each with a message naming the limitation: SequentialScheduling under FullAssembly, facet items, algebraic items, patch and transfer operators, condensed internal state, nonlinear (AD-decorated) integrators, a BlockedOperatorSpecification, constraints declared on the operator specification, global_dofs declarations, and a matrix type Ferrite has no assembler for. Value-returning sweeps (functionals, quadrature evaluation) are rejected when they run.\n\n\n\n\n\n","title":"FerriteOperators.KernelAbstractionsDevice"},{"category":"type","location":"engine-api/#FerriteOperators.LanesPerElement","page":"Assembly engine API reference","text":"LanesPerElement(; lanes = nothing)\n\nOne element's OUTPUT ROWS split across a block of lanes: lane l of a block of nlanes owns rows l:nlanes:ndofs_per_cell of yₑ, reads all of uₑ through the item's dof window, keeps its row's accumulator in a REGISTER and scatters that row itself. The rows are independent by construction, so this mapping needs neither group-local memory nor a barrier — it is a grid-stride kernel over (element, lane) pairs, where CooperativeElement splits an element's LATTICE and needs the barriers that go with it. A KernelAbstractionsDevice mapping only; a CPU device sweeps one item per worker from gather to scatter.\n\nlanes is a LAUNCH POLICY, not element math: nothing takes the block from the element's own extent (element_local_length) capped by the device's max_workgroup_size, and an explicit count overrides that. A block shorter than the row count gives each lane several rows, which is the same kernel. The workgroup hosts max_workgroup_size ÷ nlanes ELEMENTS, so a small element leaves no lane of a group idle.\n\nIt serves the ElementAssembly storage level alone, through element_action_row: the two per-quadrature-point levels re-derive an element's values objects per cell into per-worker state that the lanes of one element would race on. That restriction, and a cache serving neither element_action_row nor a compile-time element_local_length, are setup errors naming the alternative.\n\nIts scatter addresses one dof per owned row, read off the item's dof window (iterator_dofs) directly — iterator_scatter_address is not consulted, so an item family whose scatter address differs from that window is outside this mapping. That one is NOT checked at setup: the two seams agree for every item family this package ships.\n\nwarning: Experimental surface\nThis mapping and the element entry it calls may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.LanesPerElement"},{"category":"type","location":"engine-api/#FerriteOperators.PolyesterDevice","page":"Assembly engine API reference","text":"PolyesterDevice(; min_items_per_worker = 32)\n\nThreaded algorithms via Polyester.jl. Load Polyester.jl to activate this device.\n\nmin_items_per_worker is the smallest share of a barrier's items a worker is given: a barrier of n items runs on min(Threads.nthreads(), cld(n, min_items_per_worker)) workers, so a LOWER value means MORE parallelism on a small barrier. Keyword-only — the number is a threshold, not a worker count.\n\n\n\n\n\n","title":"FerriteOperators.PolyesterDevice"},{"category":"type","location":"engine-api/#FerriteOperators.SequentialCPUDevice","page":"Assembly engine API reference","text":"SequentialCPUDevice()\n\nSequential algorithms on CPU.\n\n\n\n\n\n","title":"FerriteOperators.SequentialCPUDevice"},{"category":"type","location":"engine-api/#FerriteOperators.WorkerPerElement","page":"Assembly engine API reference","text":"WorkerPerElement()\n\nOne worker owns one item from gather to scatter — the classical mapping, and the only one an assembling form takes.\n\n\n\n\n\n","title":"FerriteOperators.WorkerPerElement"},{"category":"method","location":"engine-api/#FerriteOperators.adapt_partition-Tuple{FerriteOperators.AbstractDevice, Any}","page":"Assembly engine API reference","text":"adapt_partition(device, partition)\n\nThe partition as the device's kernels consume it. The identity on a CPU device; a GPU device moves each barrier's item list into device memory here, once at setup, so a sweep transfers nothing.\n\n\n\n\n\n","title":"FerriteOperators.adapt_partition"},{"category":"method","location":"engine-api/#FerriteOperators.adapt_shared-Tuple{FerriteOperators.AbstractDevice, Any}","page":"Assembly engine API reference","text":"adapt_shared(device, x) -> x\n\nRead-only data every worker reads UNCHANGED: the identity on a CPU device, moved into device memory once at setup on a GPU one. The counterpart of setup_device_instances for what is not per worker — an element cache's quadrature-data store, a coefficient table — so a cache's device layout names no backend.\n\nwarning: Experimental surface\nThis hook may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.adapt_shared"},{"category":"method","location":"engine-api/#FerriteOperators.device_subdomain_handler-Tuple{Nothing, Any}","page":"Assembly engine API reference","text":"device_subdomain_handler(device_handler, subdomain_index)\n\nSubdomain subdomain_index's entry of what setup_device_handler returned, nothing where there is no device handler.\n\n\n\n\n\n","title":"FerriteOperators.device_subdomain_handler"},{"category":"function","location":"engine-api/#FerriteOperators.device_worker_view","page":"Assembly engine API reference","text":"device_worker_view(batched, worker)\n\nWorker worker's slice of a struct-of-arrays object setup_device_instances built, called INSIDE the assembly kernel.\n\nThere is deliberately NO generic fallback, so a forgotten field surfaces as a MethodError rather than as silently aliased scratch. A cache author writes one method per batched cache:\n\nsetup_device_instances(dev::AbstractGPUDevice, c::MyCache, n) =\n    MyCache(c.D, setup_device_instances(dev, c.cellvalues, n))\ndevice_worker_view(c::MyCache, w) = MyCache(c.D, device_worker_view(c.cellvalues, w))\n\n\n\n\n\n","title":"FerriteOperators.device_worker_view"},{"category":"function","location":"engine-api/#FerriteOperators.duplicate_for_device","page":"Assembly engine API reference","text":"duplicate_for_device(device, x)\n\nx's per-worker counterpart for device: an independent copy of mutable per-worker scratch, the same object shared read-only where sharing is safe, or (GPU) a device-resident layout.\n\nThe generic fallback shares an isbits x and throws a MethodError for anything else, so a type owning mutable state needs a method and a missing one surfaces at setup rather than as an aliasing bug at assembly time.\n\n\n\n\n\n","title":"FerriteOperators.duplicate_for_device"},{"category":"method","location":"engine-api/#FerriteOperators.execute_on_device!-Tuple{Any, FerriteOperators.AbstractDevice, Any, Any}","page":"Assembly engine API reference","text":"execute_on_device!(task, device, workspaces, items)\n\nExecute a task on a device. workspaces holds the per-worker scratch; items is a nested list of task-specific indices whose inner lists are guaranteed to be executable in parallel.\n\n\n\n\n\n","title":"FerriteOperators.execute_on_device!"},{"category":"method","location":"engine-api/#FerriteOperators.fold_items-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"fold_items(task, ws, items, acc = initial_partial(task.kind)) -> value\n\nOne worker's partial: run task over items in order on ws and sum what the per-item kernels return into acc, a nothing return contributing nothing. Pass the previous partial back in to continue the fold over the next barrier.\n\nWith a declared functional_value_type the fold returns T; without one, Union{Nothing, T}.\n\n\n\n\n\n","title":"FerriteOperators.fold_items"},{"category":"method","location":"engine-api/#FerriteOperators.lane_launch_geometry-Tuple{KernelAbstractionsDevice, Integer, Integer}","page":"Assembly engine API reference","text":"lane_launch_geometry(device, nlanes, n_items) -> (workgroup_size, n_workgroups, n_slots)\n\nThe launch geometry of one barrier under LanesPerElement: n_slots ELEMENT slots, each a block of nlanes consecutive lanes, packed max_workgroup_size ÷ nlanes blocks to a workgroup so a short block leaves no lane of a group idle.\n\nn_slots is launch_geometry's own worker count for the same barrier — the lane block stages nothing per lane, so n_workers sizes the caches unchanged. Whole blocks are what a workgroup holds, so the last group may carry slots beyond n_slots; the kernel drops those.\n\n\n\n\n\n","title":"FerriteOperators.lane_launch_geometry"},{"category":"method","location":"engine-api/#FerriteOperators.launch_geometry-Tuple{KernelAbstractionsDevice, Integer}","page":"Assembly engine API reference","text":"launch_geometry(device::KernelAbstractionsDevice, n_items) -> (workgroup_size, n_workgroups)\n\nThe launch geometry of one barrier of n_items items: items_per_worker items per worker, at most max_workgroup_size workers per group. workgroup_size * n_workgroups is the worker count of THAT barrier, and it is monotone in n_items, so it never exceeds the n_workers the caches were sized for.\n\n\n\n\n\n","title":"FerriteOperators.launch_geometry"},{"category":"method","location":"engine-api/#FerriteOperators.reduce_on_device-Tuple{Any, FerriteOperators.AbstractDevice, Any, Any}","page":"Assembly engine API reference","text":"reduce_on_device(task, device, workspaces, items) -> value\n\nThe VALUE-RETURNING counterpart of execute_on_device!, for tasks whose per-item kernel returns its contribution instead of scattering it. Values fold per worker in item order through fold_items and the partials reduce in worker order, so the result is deterministic for a fixed worker count. nothing means nothing contributed.\n\n\n\n\n\n","title":"FerriteOperators.reduce_on_device"},{"category":"method","location":"engine-api/#FerriteOperators.setup_device_handler-Tuple{FerriteOperators.AbstractDevice, Any}","page":"Assembly engine API reference","text":"setup_device_handler(device, dh) -> handler\n\nThe device-resident counterpart of dh a device's caches are built against, resolved once per engine and split per subdomain by device_subdomain_handler. nothing for a CPU device, which reads the host handler directly.\n\n\n\n\n\n","title":"FerriteOperators.setup_device_handler"},{"category":"method","location":"engine-api/#FerriteOperators.setup_device_instances-Tuple{FerriteOperators.AbstractDevice, Any, Any}","page":"Assembly engine API reference","text":"setup_device_instances(device, object, n_instances)\nsetup_device_instances(device, object, n_instances, iterator)\n\nDevice scratch: object duplicated for n_instances parallel workers. SequentialCPUDevice returns the 1-element tuple (object,), a threaded CPU device a Vector of independent duplicate_for_device copies, and a GPU device a struct-of-arrays variant whose per-worker slice is device_worker_view.\n\nThe 4-argument form is what the engine calls on a subdomain's WORKSPACE. iterator must be that subdomain's DEVICE-resident item iterator: the workspace the host allocated carries a HOST geometry cache, which adapt returns unchanged and no error reports. It defaults to the 3-argument form.\n\n\n\n\n\n","title":"FerriteOperators.setup_device_instances"},{"category":"method","location":"engine-api/#FerriteOperators.value_type-Union{Tuple{FerriteOperators.AbstractDevice{ValueType}}, Tuple{ValueType}} where ValueType","page":"Assembly engine API reference","text":"value_type(device) -> Type\n\nThe scalar element type of the GLOBAL system device assembles into (the ValueType type parameter of its AbstractDevice): the system matrix and vector it allocates. The ELEMENT-local scalar is the integrator's own election (element_value_type) and need not match — the scatter converts.\n\n\n\n\n\n","title":"FerriteOperators.value_type"},{"category":"method","location":"engine-api/#FerriteOperators.with_element_mapping-Tuple{FerriteOperators.AbstractDevice, WorkerPerElement}","page":"Assembly engine API reference","text":"with_element_mapping(device, mapping) -> device\n\nThe device that executes mapping (AbstractElementMapping), which a matrix-free operator resolves once at setup_operator.\n\nWorkerPerElement is every device's own execution and returns it unchanged. A mapping a device has no kernel for is rejected here, at setup, rather than as a missing method inside the first sweep.\n\n\n\n\n\n","title":"FerriteOperators.with_element_mapping"},{"category":"type","location":"engine-api/#FerriteOperators.NestedGridCellCache","page":"Assembly engine API reference","text":"NestedGridCellCache\n\nCache for iterating fine cells, e.g. to assemble a transfer operator between a fine and a coarse grid where every fine cell is a child of exactly one coarse cell. The caller supplies the mesh-hierarchy data:\n\nfine2coarse :: Vector{Int} – maps fine_cell_id → coarse_cell_id.\nchild_ref_coords :: Vector{Vector{Vec{dim,T}}} – for each fine cell the reference coordinates of that cell's nodes inside the parent (coarse) reference element.\n\nAccessors: cellid (the fine cell), get_fine_coordinates/get_coarse_coordinates, getrowdofs (fine dofs, the rows of the resulting matrix), getcolumndofs (coarse dofs, the columns) and get_child_ref_coords.\n\n\n\n\n\n","title":"FerriteOperators.NestedGridCellCache"},{"category":"type","location":"engine-api/#FerriteOperators.NestedGridCellIterator","page":"Assembly engine API reference","text":"NestedGridCellIterator(dh_fine, dh_coarse, fine2coarse, child_ref_coords [, cellset])\n\nIterator over the fine cells of two hierarchically nested grids, e.g. for assembling a transfer operator between them. dh_fine/dh_coarse are the DofHandlers of the two grids, fine2coarse and child_ref_coords are as in NestedGridCellCache, and cellset restricts the iteration (default: all fine cells).\n\nwarning: Warning\nStateful – do not collect or broadcast over this iterator.\n\n\n\n\n\n","title":"FerriteOperators.NestedGridCellIterator"},{"category":"type","location":"engine-api/#FerriteOperators.SameGridCellCache","page":"Assembly engine API reference","text":"SameGridCellCache\n\nCache for iterating the cells of a shared grid, e.g. to assemble a transfer (prolongation / restriction) operator between two DofHandlers on the same grid and the same set of cells (polynomial p-multigrid). The geometry is shared; each DofHandler provides its own dof ids.\n\nAccessors: cellid, getnodes, getcoordinates, plus getrowdofs (from dh_row, typically the fine/test space) and getcolumndofs (from dh_col, the coarse/trial space).\n\n\n\n\n\n","title":"FerriteOperators.SameGridCellCache"},{"category":"type","location":"engine-api/#FerriteOperators.SameGridCellIterator","page":"Assembly engine API reference","text":"SameGridCellIterator(dh_row, dh_col [, cellset])\nSameGridCellIterator(sdh_row::SubDofHandler, sdh_col::SubDofHandler)\n\nIterates the cells of a shared grid, reinitialising and returning a SameGridCellCache per step. dh_row (row / test / fine space) and dh_col (column / trial / coarse space) must live on the same grid object; from a pair of SubDofHandlers the iteration is restricted to their common cellset.\n\nwarning: Warning\nStateful – do not collect or broadcast over this iterator.\n\n\n\n\n\n","title":"FerriteOperators.SameGridCellIterator"},{"category":"method","location":"engine-api/#FerriteOperators.assembly_iterator-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"assembly_iterator(kind, element_cache, sdh)\n\nEXPERIMENTAL. What a sweep of kind positions on one item of the HOST SubDofHandler sdh, and what rides args.cell while the element kernels run. Resolved ONCE per (sweep kind, element cache, subdomain) at setup_operator; a sweep only calls position_item on it. The default is Ferrite's CellCache over sdh; device_assembly_iterator is the device shape.\n\nThe positioned value must answer three accessors and no more: Ferrite.cellid(it), iterator_dofs and iterator_handler. Everything else a shipped kernel reaches for — Ferrite.getcoordinates, Ferrite.getnodes, Ferrite.reinit!(cv, it), … — is CONVENTIONAL between the iterator and the elements written for it; the framework never calls them. Which members a sweep REFRESHES is item_update_flags's separate per-kind declaration.\n\nA declaration narrows the CACHE argument, and may narrow the kind on top of it, but must never narrow ONLY the kind — the same rule governs item_provider, item_update_flags and reinit_values!; devdocs/design.md states why. A kind-level default goes on default_assembly_iterator instead.\n\n\n\n\n\n","title":"FerriteOperators.assembly_iterator"},{"category":"method","location":"engine-api/#FerriteOperators.decorate_device_iterator-Tuple{Any, Any}","page":"Assembly engine API reference","text":"decorate_device_iterator(it, sdh) -> it\n\nThe setup-time fact only the HOST SubDofHandler sdh can cheaply produce, folded into the device iterator it — device_assembly_iterator's default body, keyed on the ITERATOR's type. The default returns it unchanged.\n\nwarning: Experimental surface\nInternal to the device iteration seam; it may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.decorate_device_iterator"},{"category":"method","location":"engine-api/#FerriteOperators.default_assembly_iterator-Tuple{Any, Any}","page":"Assembly engine API reference","text":"default_assembly_iterator(kind, sdh)\n\nWhat a sweep of kind positions on where the element cache declares nothing — assembly_iterator's default body, keyed on the kind and the handler alone, so it sits BELOW every cache declaration instead of tying with it. The default is Ferrite's CellCache over sdh.\n\nwarning: Experimental surface\nInternal to the iteration seam; it may change in a minor release.\n\n\n\n\n\n","title":"FerriteOperators.default_assembly_iterator"},{"category":"method","location":"engine-api/#FerriteOperators.device_assembly_iterator-NTuple{4, Any}","page":"Assembly engine API reference","text":"device_assembly_iterator(kind, element_cache, sdh, device_sdh)\n\nEXPERIMENTAL. The DEVICE shape of assembly_iterator: what a sweep of kind positions on one item of the device-resident device_sdh. It takes BOTH handlers, since a device iterator may need a setup-time fact only the HOST one can cheaply produce.\n\nThe default forwards to the host construction over the device handler, so a downstream iterator needing no host-only fact writes only the host method. One that does either overloads this directly (narrowing the CACHE, per assembly_iterator's spelling rule) or answers decorate_device_iterator on its own iterator type.\n\nwarning: Experimental surface\nThe device iterator layout is still moving; this seam's spelling may change in a minor release. A device-resident iterator also indexes fields of Ferrite's own DeviceSubDofHandler (cell_dofs, cell_dofs_offset) to answer iterator_dofs. Those fields are UNEXPORTED Ferrite internals: this seam is only as stable as they are.\n\n\n\n\n\n","title":"FerriteOperators.device_assembly_iterator"},{"category":"method","location":"engine-api/#FerriteOperators.get_child_ref_coords-Tuple{NestedGridCellCache}","page":"Assembly engine API reference","text":"get_child_ref_coords(tc::NestedGridCellCache)\n\nThe current fine cell's nodes in the reference frame of its parent coarse cell, where coarse-grid shape functions are evaluated at fine-grid quadrature points.\n\n\n\n\n\n","title":"FerriteOperators.get_child_ref_coords"},{"category":"method","location":"engine-api/#FerriteOperators.get_coarse_coordinates-Tuple{NestedGridCellCache}","page":"Assembly engine API reference","text":"get_fine_coordinates(tc) -> Vector{<:Vec}\nget_coarse_coordinates(tc) -> Vector{<:Vec}\n\nThe node coordinates of the current fine cell and of its parent coarse cell — the two geometries a nested transfer kernel maps between — both refreshed by Ferrite.reinit! on the NestedGridCellCache.\n\n\n\n\n\n","title":"FerriteOperators.get_coarse_coordinates"},{"category":"method","location":"engine-api/#FerriteOperators.get_fine_coordinates-Tuple{NestedGridCellCache}","page":"Assembly engine API reference","text":"get_fine_coordinates(tc) -> Vector{<:Vec}\nget_coarse_coordinates(tc) -> Vector{<:Vec}\n\nThe node coordinates of the current fine cell and of its parent coarse cell — the two geometries a nested transfer kernel maps between — both refreshed by Ferrite.reinit! on the NestedGridCellCache.\n\n\n\n\n\n","title":"FerriteOperators.get_fine_coordinates"},{"category":"method","location":"engine-api/#FerriteOperators.getcolumndofs-Tuple{SameGridCellCache}","page":"Assembly engine API reference","text":"getrowdofs(tc) -> Vector{Int}\ngetcolumndofs(tc) -> Vector{Int}\n\nThe dof vectors indexing the rectangular element matrix: rows from the test-space DofHandler, columns from the trial-space one. For a nested cache that is the fine cell's dofs and its parent coarse cell's.\n\n\n\n\n\n","title":"FerriteOperators.getcolumndofs"},{"category":"method","location":"engine-api/#FerriteOperators.getrowdofs-Tuple{SameGridCellCache}","page":"Assembly engine API reference","text":"getrowdofs(tc) -> Vector{Int}\ngetcolumndofs(tc) -> Vector{Int}\n\nThe dof vectors indexing the rectangular element matrix: rows from the test-space DofHandler, columns from the trial-space one. For a nested cache that is the fine cell's dofs and its parent coarse cell's.\n\n\n\n\n\n","title":"FerriteOperators.getrowdofs"},{"category":"method","location":"engine-api/#FerriteOperators.item_update_flags-Tuple{Any, Any}","page":"Assembly engine API reference","text":"item_update_flags(kind, element_cache) -> Ferrite.UpdateFlags\n\nWhat a sweep of kind over element_cache READS off the cache assembly_iterator positions, in Ferrite's UpdateFlags vocabulary. Every flag is set by default.\n\nIt governs STAGING only: a declaration that under-states what the kernels read is a stale read rather than an error, the same contract Ferrite's own UpdateFlags carries. Answer it with a literal, so the staging it guards folds away.\n\nFerriteOperators.item_update_flags(::MatrixFreeActionKind, ::MyCache) =\n    Ferrite.UpdateFlags(nodes = false, coords = false, dofs = true)\n\n\n\n\n\n","title":"FerriteOperators.item_update_flags"},{"category":"method","location":"engine-api/#FerriteOperators.iterator_dofs-Tuple{Any}","page":"Assembly engine API reference","text":"iterator_dofs(it) -> AbstractVector{<:Integer}\n\nREQUIRED accessor (one of three — see assembly_iterator): the global dof indices of the item it is positioned on. The default is Ferrite.celldofs(it).\n\nThis window doubles as the scatter address by default (iterator_scatter_address), and Ferrite's assembler requires it to be DUPLICATE FREE. An item spanning more than one cell repeats a dof wherever the cells share it — two face-neighbours of a CONTINUOUS space share the dofs on their common facet — so such a family needs a discontinuous space, or item sides that do not touch a shared facet.\n\n\n\n\n\n","title":"FerriteOperators.iterator_dofs"},{"category":"method","location":"engine-api/#FerriteOperators.iterator_handler-Tuple{Any}","page":"Assembly engine API reference","text":"iterator_handler(it) -> SubDofHandler-like\n\nREQUIRED accessor (one of three — see assembly_iterator): the SubDofHandler (or device counterpart) it was constructed over. The default reads the dh field Ferrite's own caches carry; framework code goes through this rather than the field, so an iterator need not carry one named dh.\n\n\n\n\n\n","title":"FerriteOperators.iterator_handler"},{"category":"method","location":"engine-api/#FerriteOperators.iterator_scatter_address-Tuple{Any}","page":"Assembly engine API reference","text":"iterator_scatter_address(it)\n\nWhat a scatter of the current item addresses, absent a family-level global-dof declaration (global_dofs) — see scatter_address, which reads this. The default is iterator_dofs.\n\nLeft untyped so a rectangular transfer item may answer with a two-index (rowdofs, coldofs) pair. That shape is EXPERIMENTAL and has no consumer in scatter_local!; the transfer family keeps its own driver.\n\nNOT CONSULTED by the matrix-free action wherever the element names a compile-time element_local_length: matrix_free_cell_sweep! and the LanesPerElement kernel both address through iterator_dofs directly. An item family whose scatter address DIFFERS from its dof window must therefore not name a compile-time extent.\n\n\n\n\n\n","title":"FerriteOperators.iterator_scatter_address"},{"category":"method","location":"engine-api/#FerriteOperators.position_item-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"position_item(ws, item, kind) -> ws\n\nPosition the workspace ws on item for a sweep of kind and return the POSITIONED workspace, which need not be ws itself: an iterator positioned by CONSTRUCTION hands back a new value, and only the returned one is positioned.\n\nThe default is Ferrite.reinit!(ws, item); AssemblyWorkspace overloads it, dispatching to position_iterator for the iterator's own half.\n\n\n\n\n\n","title":"FerriteOperators.position_item"},{"category":"method","location":"engine-api/#FerriteOperators.position_iterator-Tuple{Any, Any, Any}","page":"Assembly engine API reference","text":"position_iterator(it, item, flags::Ferrite.UpdateFlags) -> it\n\nThe ITERATOR's half of positioning an AssemblyWorkspace on item, with flags looked up from item_update_flags. Returns the positioned iterator: the default mutates it in place and returns it (Ferrite's reinit! contract), an iterator positioned by CONSTRUCTION returns a new value.\n\nflags are ADVISORY — an iterator that stages nothing ignores them.\n\n\n\n\n\n","title":"FerriteOperators.position_iterator"},{"category":"type","location":"engine-api/#FerriteOperators.QVector","page":"Assembly engine API reference","text":"QVector{T, VT, OT, NT} <: AbstractVector{T}\n\nFlat storage for quadrature-point data across all cells, with per-cell random access through get_range_for_cell.\n\ndata: flat storage (AbstractVector{T}) holding all quadrature values\noffsets: offsets[cellid] is the 1-based start index in data for cell cellid\nnpoints: quadrature points of cell cellid as npoints[cellid], or one count for every cell where that count is uniform\n\nBuilt by setup_qvector from a Ferrite.DofHandler or an assembled operator.\n\n\n\n\n\n","title":"FerriteOperators.QVector"},{"category":"method","location":"engine-api/#FerriteOperators.get_range_for_cell-Tuple{QVector, Integer}","page":"Assembly engine API reference","text":"get_range_for_cell(q::QVector, cellid::Integer)\n\nA mutable view into the slice of q that belongs to cell cellid, of the length q.npoints gives that cell.\n\n\n\n\n\n","title":"FerriteOperators.get_range_for_cell"},{"category":"method","location":"engine-api/#FerriteOperators.setup_qvector-Union{Tuple{T}, Tuple{Type{T}, Any}} where T","page":"Assembly engine API reference","text":"setup_qvector(::Type{T}, operator) -> QVector{T}\n\nBuild a QVector laid out by the quadrature structure of operator, the per-cell point count taken via getnquadpoints from the element caches in its subdomain caches.\n\n\n\n\n\n","title":"FerriteOperators.setup_qvector"},{"category":"method","location":"engine-api/#FerriteOperators.setup_qvector-Union{Tuple{T}, Tuple{Type{T}, Ferrite.AbstractDofHandler, Any}} where T","page":"Assembly engine API reference","text":"setup_qvector(::Type{T}, dh::AbstractDofHandler, qrc) -> QVector{T}\n\nBuild a QVector with element type T, laid out by qrc over the cells of dh: every SubDofHandler contributes getnquadpoints(getquadraturerule(qrc, sdh)) points per cell, and cells outside every subdomain zero.\n\n\n\n\n\n","title":"FerriteOperators.setup_qvector"},{"category":"method","location":"engine-api/#FerriteOperators.setup_qvector-Union{Tuple{T}, Tuple{Type{T}, SubDofHandler, Integer}} where T","page":"Assembly engine API reference","text":"setup_qvector(::Type{T}, sdh::SubDofHandler, npoints_per_cell::Integer) -> QVector{T}\n\nBuild a QVector carrying npoints_per_cell points on every cell of sdh and none on any other cell of the grid — the layout a PER-SUBDOMAIN store needs (a matrix-free element's partial-assembly quadrature data), where the (dh, qrc) form above sizes the data for every subdomain at once.\n\n\n\n\n\n","title":"FerriteOperators.setup_qvector"},{"category":"type","location":"engine-api/#FerriteOperators.QuadratureEvaluationKind","page":"Assembly engine API reference","text":"QuadratureEvaluationKind(f, q, set)\n\nEvaluate f(uₑ, qp, cell, element_cache, pₑ, ctx) at every quadrature point of every cell (optionally restricted to set), storing the returned values in the QVector q. q is shared across workers — different cells own disjoint slices, so no duplication is needed.\n\nctx is the sweep's context, the same channel the request-shaped kernels read their time from (evaluation_time); p stays configuration.\n\n\n\n\n\n","title":"FerriteOperators.QuadratureEvaluationKind"},{"category":"function","location":"engine-api/#FerriteOperators.evaluate_quadrature!","page":"Assembly engine API reference","text":"evaluate_quadrature!(q::QVector, op, u, p, f, [set = nothing]; ctx = nothing)\n\nEvaluate f(uₑ, qp, cell, element_cache, pₑ, ctx) at every quadrature point and store the returned values in q, using op's assembly engine. With set given, only its cells are evaluated and the remaining entries of q are left untouched.\n\nctx reaches the kernel unchanged, so a per-quadrature-point evaluation reads the time of a transient sweep exactly as an element kernel does — through evaluation_time — and nothing (the default) is the stationary evaluation.\n\n\n\n\n\n","title":"FerriteOperators.evaluate_quadrature!"},{"category":"method","location":"engine-api/#FerriteOperators.allocate_vector-Tuple{FerriteOperators.AbstractDevice, Any}","page":"Assembly engine API reference","text":"allocate_vector(device, dh)\n\nThe operator's global vector for device. A device whose vectors are a plain Vector answers through vector_type; a GPU device, whose allocator needs the backend OBJECT rather than a type, has its own method.\n\n\n\n\n\n","title":"FerriteOperators.allocate_vector"},{"category":"type","location":"engine-api/#FerriteOperators.FacetQuadratureRuleCollection","page":"Assembly engine API reference","text":"FacetQuadratureRuleCollection([::Type{T},] order::Int)\n\nA collection of facet quadrature rules across different cell types, evaluated in the scalar type T (Float64 where it is not named) — the facet counterpart of QuadratureRuleCollection.\n\n\n\n\n\n","title":"FerriteOperators.FacetQuadratureRuleCollection"},{"category":"type","location":"engine-api/#FerriteOperators.QuadratureRuleCollection","page":"Assembly engine API reference","text":"QuadratureRuleCollection([::Type{T},] order::Int)\n\nA collection of quadrature rules across different cell types, evaluated in the scalar type T (Float64 where it is not named).\n\nT is the INTEGRATOR's election of the precision its elements evaluate in: the rules getquadraturerule hands out carry it, and an element cache reads it back through element_value_type. It is independent of the value_type the device accumulates the global system in.\n\n\n\n\n\n","title":"FerriteOperators.QuadratureRuleCollection"},{"category":"method","location":"engine-api/#FerriteOperators.getquadraturerule-Union{Tuple{ref_shape}, Tuple{T}, Tuple{order}, Tuple{QuadratureRuleCollection{order, T}, Ferrite.AbstractCell{ref_shape}}} where {order, T, ref_shape}","page":"Assembly engine API reference","text":"getquadraturerule(qrc, cell::AbstractCell)\ngetquadraturerule(qrc, sdh::SubDofHandler)\n\nThe collection's rule for a reference shape: the cell's own, or — the form setup_element_cache uses — the shape of the subdomain's first cell, shared by every cell of a SubDofHandler. Defined for QuadratureRuleCollection and FacetQuadratureRuleCollection.\n\nThe rule carries the collection's scalar type, so the precision an integrator elected travels with it.\n\n\n\n\n\n","title":"FerriteOperators.getquadraturerule"},{"category":"type","location":"engine-api/#FerriteOperators.DeviceInternalVariableHandler","page":"Assembly engine API reference","text":"DeviceInternalVariableHandler()\n\nThe device-side stand-in for an InternalVariableHandler that lays out NO condensed block. InternalVariableHandler is mutable and so never isbits, which keeps it out of a GPU kernel; this singleton is, and answers the one question a sweep asks — every cell's and item's internal range is empty, exactly as the placeholder handler answers on the host.\n\nA handler that DOES lay out a block has no device counterpart: condensation is rejected at setup for a GPU device.\n\n\n\n\n\n","title":"FerriteOperators.DeviceInternalVariableHandler"},{"category":"type","location":"engine-api/#FerriteOperators.InternalVariableHandler","page":"Assembly engine API reference","text":"InternalVariableHandler(internal_variable_offsets, item_variable_offsets, base_offset, ndofs)\n\nLayout of the condensed internal-variable block appended to the solution vector, u = [ū; q_cells; q_items] — cell-owned internal state first, then the algebraic-item family's, both numbered into the SAME tail. A cell declares its internal dof count through get_number_of_internal_dofs_per_element, an algebraic item through get_number_of_internal_dofs_per_algebraic_item; the handler turns either into solution-vector ranges (internal_variable_offset, internal_variable_range), keyed by cellid for a cell and by an AlgebraicItem for an item. Two methods dispatching on the key's type, not one shared Int-keyed method: the two keys are unrelated small positive integers, so sharing a method would silently return the wrong range whenever a cellid and an item index coincide.\n\nInternal dofs stay out of the Ferrite.DofHandler deliberately: their count varies per quadrature point, per material, and per item, which a field-based numbering would have to pad.\n\n\n\n\n\n","title":"FerriteOperators.InternalVariableHandler"},{"category":"method","location":"engine-api/#FerriteOperators.has_internal_dof_block-Tuple{InternalVariableHandler}","page":"Assembly engine API reference","text":"Whether ivh lays out a condensed block at all — the placeholder does not.\n\n\n\n\n\n","title":"FerriteOperators.has_internal_dof_block"},{"category":"method","location":"engine-api/#FerriteOperators.internal_variable_offset-Tuple{InternalVariableHandler, Int64}","page":"Assembly engine API reference","text":"internal_variable_offset(ivh, cellid) -> Int\n\nIndex in the solution vector immediately before cell cellid's internal variables, absolute: the preceding field dofs are already included.\n\n\n\n\n\n","title":"FerriteOperators.internal_variable_offset"},{"category":"method","location":"engine-api/#FerriteOperators.internal_variable_range-Tuple{InternalVariableHandler, Int64}","page":"Assembly engine API reference","text":"internal_variable_range(ivh, cellid::Int) -> UnitRange{Int}\ninternal_variable_range(ivh, item::AlgebraicItem) -> UnitRange{Int}\n\nRange of a cell's (keyed by cellid) or an algebraic item's (keyed by AlgebraicItem, i.e. args.item) internal variables in the solution vector, absolute like internal_variable_offset. Item ranges sit AFTER the whole cell block, [ū | q_cells | q_items].\n\n\n\n\n\n","title":"FerriteOperators.internal_variable_range"},{"category":"type","location":"engine-api/#FerriteOperators.VTKQuadratureGrid","page":"Assembly engine API reference","text":"VTKQuadratureGrid{T, sdim}\n\nA VTK-compatible \"grid\" where every node is a quadrature point and every cell a single-node VTK_VERTEX — the mesh backing a VTKQuadratureFile.\n\nBuild with VTKQuadratureGrid(dh, qrc).\n\n\n\n\n\n","title":"FerriteOperators.VTKQuadratureGrid"},{"category":"method","location":"engine-api/#FerriteOperators.VTKQuadratureGrid-Tuple{Ferrite.AbstractDofHandler, Any}","page":"Assembly engine API reference","text":"VTKQuadratureGrid(dh::AbstractDofHandler, qrc) -> VTKQuadratureGrid\n\nBuild a quadrature-point \"grid\" from dh and qrc. Nodes are emitted in ascending cell-ID order, matching a QVector built with the same dh/qrc, so its flat data maps to VTK point data without reordering.\n\n\n\n\n\n","title":"FerriteOperators.VTKQuadratureGrid"},{"category":"type","location":"engine-api/#FerriteOperators.QuadratureDataQuery","page":"Assembly engine API reference","text":"QuadratureDataQuery{T, QV <: QVector{T}}\n\nA post-processing query buffer holding quadrature-point results.\n\nbuffer — QVector{T} storing the result at every quadrature point\nset    — cell IDs to evaluate; nothing means all cells\n\nBuild with prepare_quadrature_query, execute with process_query!.\n\n\n\n\n\n","title":"FerriteOperators.QuadratureDataQuery"},{"category":"type","location":"engine-api/#FerriteOperators.VTKQuadratureFile","page":"Assembly engine API reference","text":"VTKQuadratureFile\n\nA VTK file handler for quadrature-point data: Ferrite.VTKGridFile, but backed by a VTKQuadratureGrid. The do-block form closes the file:\n\nqgrid = VTKQuadratureGrid(dh, qrc)\nVTKQuadratureFile(\"output\", qgrid) do vtk\n    write_quadrature_data(vtk, σ, \"stress\")\nend\n\n\n\n\n\n","title":"FerriteOperators.VTKQuadratureFile"},{"category":"method","location":"engine-api/#FerriteOperators.prepare_quadrature_query-Union{Tuple{T}, Tuple{Type{T}, Any}} where T","page":"Assembly engine API reference","text":"prepare_quadrature_query(::Type{T}, op; set = nothing)\nprepare_quadrature_query(::Type{T}, prototype::QuadratureDataQuery)\n\nBuild a QuadratureDataQuery{T}.\n\nThe first form allocates a fresh QVector{T} whose layout matches op; set restricts evaluation to the given cell IDs. The second reuses the prototype's offset/npoints layout, for multiple queries over one mesh.\n\n\n\n\n\n","title":"FerriteOperators.prepare_quadrature_query"},{"category":"method","location":"engine-api/#FerriteOperators.process_query!-Tuple{QuadratureDataQuery, Vararg{Any, 4}}","page":"Assembly engine API reference","text":"process_query!(query::QuadratureDataQuery, op, u, p, f; ctx = nothing)\n\nEvaluate f(ue, qp, cell, element_cache, pe, ctx) at every quadrature point into query.buffer. A non-nothing query.set restricts evaluation to those cells; all others retain their current (typically zero) values. ctx is the sweep's context, passed through to evaluate_quadrature!.\n\n\n\n\n\n","title":"FerriteOperators.process_query!"},{"category":"method","location":"engine-api/#FerriteOperators.write_quadrature_data-Tuple{VTKQuadratureFile, QVector{var\"#s406\", VT} where {var\"#s406\"<:Real, VT<:AbstractVector{var\"#s406\"}}, Any}","page":"Assembly engine API reference","text":"write_quadrature_data(vtk::VTKQuadratureFile, q::QVector, name)\n\nWrite quadrature-point data from q to the VTK point-data field name; scalar (QVector{<:Real}) and vector (QVector{Vec{dim,T}}) data are supported.\n\n\n\n\n\n","title":"FerriteOperators.write_quadrature_data"},{"category":"method","location":"engine-api/#FerriteOperators.write_quadrature_data-Tuple{VTKQuadratureFile, QuadratureDataQuery, Any}","page":"Assembly engine API reference","text":"write_quadrature_data(vtk::VTKQuadratureFile, q::QuadratureDataQuery, name)\n\nWrite the buffer inside q to the VTK point-data field name.\n\n\n\n\n\n","title":"FerriteOperators.write_quadrature_data"},{"category":"section","location":"operators/#Operators-and-entry-points","page":"Operators and entry points","text":"strategy = AssemblyStrategy(SequentialCPUDevice())\nop = setup_operator(strategy, integrator, dh; slots = (:u, :uprev))\n\n# canonical states/ctx forms; u-vector conveniences exist for stationary use\nupdate_linearization!(op, residual, (u = u, uprev = uprev), p, TimeIntegrationContext(t, Δt, γ̃))\nevaluate!(op, residual, (u = u,), p, nothing)\nmul!(y, op.J, v)\n\nAn operator is a payload (matrix/vector) plus an AssemblyEngine plus its integrator. Every entry point funnels into one sweep over the engine's subdomain caches, so the difference between a residual, a Jacobian, and a sensitivity is the request kind, not a separate driver.\n\nevaluate! is deliberately polymorphic: it evaluates whatever the integrator encodes — a nonlinear residual, the action of the linear operator a bilinear form induces, a hand-fused scheme residual — and the name says exactly that much.\n\nWhich assembly entry point an operator takes follows its integrator family. A nonlinear operator assembles through update_linearization! — op.J alone, or op.J and the residual in one fused sweep — and evaluates through evaluate!. A bilinear or linear operator assembles its payload — op.A, op.b — through update_operator!(op, p, ctx), which carries no states because the form has none to depend on; a bilinear operator also answers evaluate!, the action A·u computed from the residual kernels without touching op.A.","title":"Operators and entry points"},{"category":"section","location":"operators/#Setup-time-declarations","page":"Operators and entry points","text":"slots and requests are the setup-time declarations setup_operator takes: slot names and request kinds, nothing else.\n\n# an operator for a solver that assembles weighted Jacobians and drives with\n# the residual, over two slots\nop = setup_operator(strategy, integrator, dh;\n                    slots = (:u, :du),\n                    requests = (WeightedJacobianKind, ResidualKind))\n\nNeither carries coefficients — γ, tableaus and weights are per-evaluation solver data — and neither is term-shaped; a term needing its own context or sink is its own sweep.\n\nslots sizes the buffers: the engine allocates one per-worker slot buffer per name, and a sweep whose states name a slot the operator never declared fails loudly instead of erroring per cell. Names are the whole declaration — a slot carries no declared type, and its source is chosen per call (see Slots and rate reconstruction).\n\nrequests moves checks forward: declaring a kind runs its trait ↔ kernel and internal-state admissibility checks at setup_operator instead of on first use — an inadmissible adjoint fails when the operator is built, not mid-solve. Kinds are normalized to their UnionAll base, so an instance or a payload-parameterized type (ParameterVJPKind(zeros(n))) declares the same kind as its bare name:\n\nop = setup_operator(strategy, integrator, dh;\n                    requests = (ParameterVJPKind, TimeSensitivityKind))\n\nWhich element caches carry ADElementCache decoration, and whether the workspace carries SensitivityBuffers at all, is decided separately and structurally by the integrator family (needs_ad_decoration) — a bilinear or linear operator carries no AD/sensitivity machinery whatever an element cache does or does not implement analytically, and whatever requests names. Declaring a kind builds no per-worker state of its own. The workspace is immutable — every field is bound at setup_operator, and a sweep works by filling the buffers those fields point at.\n\nDeclaring a FunctionalKind checks nothing, which follows from the shape of the kind rather than being an omission: a functional sweep's kernel returns the cell's contribution instead of filling a request (has_cell_request is false for it), so there is no trait ↔ kernel pairing to hold it against. What such a kind does declare is its reduction's value type, on the kind rather than at setup — FerriteOperators.functional_value_type, required under a parallel device and described with the kernel hook in Writing elements.\n\nDeclaring a kind stays a hint, never a capability restriction: undeclared kinds remain usable and simply run their checks at the call-time entry points instead of at setup.","title":"Setup-time declarations"},{"category":"section","location":"operators/#The-assembled-matrix","page":"Operators and entry points","text":"What the operator's global matrix looks like is the operator specification carried by FullAssembly. StandardOperatorSpecification — the default — gives a monolithic SparseMatrixCSC, and both specifications take the same two pattern declarations:\n\nalgebraic_couplings, Ferrite coupling descriptors (CellCoupling, FacetCoupling, AlgebraicCoupling) for the entries a global_dofs or facet_item_global_dofs declaration couples into — see Elements with global dofs — and for those an algebraic_items declaration needs, see Algebraic terms;\nconstraint_handler, which adds the entries Ferrite's CONSTRAINT condensation needs room to write (unrelated to the internal-variable condensation of Condensed elements). Sparsity only: applying the constraints to the assembled system stays with the caller, through Ferrite's apply!/apply_assemble!.\n\nnote: `algebraic_couplings` requires Ferrite's mesh-free algebraic variables\nSee the canonical capability note under Algebraic terms. FerriteOperators passes the keyword through only when something is declared, so an operator declaring no couplings — the default () — allocates its pattern exactly as before; declaring one on a Ferrite without the capability fails loudly at the Ferrite call.","title":"The assembled matrix"},{"category":"section","location":"operators/#Blocked-matrices-with-CSR-blocks","page":"Operators and entry points","text":"BlockedOperatorSpecification assembles into a BlockMatrix instead — the layout a fieldwise preconditioner or a Schur-complement consumer wants, and the way to give the blocks a storage format the monolithic path does not offer. The matrix type is the caller's: this package depends on neither BlockArrays nor SparseMatricesCSR, so the user loads them and names the type.\n\nusing BlockArrays, SparseMatricesCSR\n\nspec     = BlockedOperatorSpecification([nu, nalg],\n                                        BlockMatrix{Float64, Matrix{SparseMatrixCSR{1, Float64, Int}}};\n                                        algebraic_couplings = (coupling,))\nstrategy = AssemblyStrategy(FullAssembly(spec), SequentialScheduling(), SequentialCPUDevice())\nop       = setup_operator(strategy, integrator, dh)\n\nupdate_linearization!(op, r, u, p)   # the fused Newton path\n\nThe residual stays a plain Vector — create_system_vector is unchanged, and Ferrite's BlockAssembler takes a non-blocked f. The fused, matrix-only and residual-only sweeps all work, so a bilinear operator assembles into a blocked target just as a nonlinear one does. A linear operator holds no matrix at all, and a blocked specification on one is rejected at setup_operator rather than silently dropped.","title":"Blocked matrices with CSR blocks"},{"category":"section","location":"operators/#Slots-and-rate-reconstruction","page":"Operators and entry points","text":"Time discretization of the global unknowns is solver-owned: solvers pass slot values (reconstructed histories, rates) and contexts; elements never encode a scheme. The hand-derived first-order path — an element reading uprev and ctx and owning its discretization — is a supported opt-in pattern, expressed through the same slot interface as everything else.\n\nA rate-like slot can be reconstructed from the primary unknown instead of being materialized by the solver: an AffineRate source gives the slot the cell-local value slope · (u − anchor), e.g. update_linearization!(op, r, (u = u, du = AffineRate(1/Δt, uprev)), p, ctx) for backward Euler. The :u slot must precede the reconstructed one. Kernels read the reconstructed values through args.states.du and nothing else, so an element stays scheme-agnostic. The assembled Jacobian is ∂F/∂u at frozen slot values; the chain-rule term through the reconstruction is contributed by the solver's per-slot weights.","title":"Slots and rate reconstruction"},{"category":"section","location":"operators/#Components-and-stage-operators","page":"Operators and entry points","text":"A multi-slot linearization can be assembled one slot at a time and folded by the solver, so a scheme's matrix never needs its own kernel:\n\ncomps = allocate_components(op, (:Ju, :Jdu))          # one shared sparsity pattern\nassemble_slot_jacobian!(comps.Ju,  op, JacobianKind{:u}(),  states, p, ctx)\nassemble_slot_jacobian!(comps.Jdu, op, JacobianKind{:du}(), states, p, ctx)\ncombine!(W, comps, (Jdu = 1 / Δt, Ju = 1.0))          # backward Euler Newton matrix\n\nallocate_components hands out square system matrices that share one sparsity pattern (aliased colptr/rowval, private nzval), which makes combine! a pure values operation and apply_zero! safe on any member; structural mutation of a component breaks the bag and is not supported. Components are plain system matrices — every assembly entry point fills them. combine! is eltype-generic: real components with complex weights combine into a complex target from share_pattern(A, ComplexF64).\n\nThe differentiated slot must carry a plain vector source. An AffineRate slot is reconstructed at gather time and frozen under AD, so JacobianKind{:du}() against it is rejected — assemble the components against plain sources and let the reconstruction slope enter as a weight.\n\nA facet_items term must serve every slot the components are assembled for. The facet driver hands the sweep's JacobianRequest{:du} to the surface cache with no AD fallback, so a cache implementing only ResidualRequest and JacobianRequest{:u} raises a MethodError on the first item of a :du component sweep.\n\nFully implicit Runge-Kutta assembles s stage pairs and applies the s×s Newton block δᵢⱼ Jdu⁽ⁱ⁾ + Δt aᵢⱼ Ju⁽ⁱ⁾ without ever building it:\n\nsbop = StageBlockOperator(op, A, c, Δt)\nassemble_stages!(sbop, op, stage_states, p, ctxs)     # 2s sweeps, one per stage and slot\nmul!(y, sbop, x)                                      # x, y stage-stacked, length s·n\n\nThe transformed (simplified-Newton) variant needs no stage-block machinery: diagonalized Radau uses stage-independent Jacobians, i.e. a single (Ju, Jdu) bag plus one complex combine!(W_λ, comps, (Jdu = 1.0, Ju = Δt*λ)) per eigenvalue of A⁻¹.","title":"Components and stage operators"},{"category":"section","location":"operators/#Weighted-Jacobians","page":"Operators and entry points","text":"The fold itself is an entry point, so a scheme's matrix is one call:\n\nW = share_pattern(op.J)\nassemble_weighted_jacobian!(W, op, (u = 1.0, du = 1/(γ*Δt)), states, p, ctx)\n\nassemble_weighted_jacobian! assembles W = Σₛ wₛ ∂F/∂s over the slots the weights NamedTuple names, at frozen values of every other slot. The weights are request payload: a fused element kernel reads them from WeightedJacobianRequest and the composed fallback folds the same NamedTuple with combine!, so the two routes cannot disagree about the scheme's scalars.\n\nWhich route runs is a capability of the element caches, not a caller choice. An element opts into the fused route by declaring the analytic kernel — this is where a hand-derived scheme matrix belongs, since it computes a combination that no single-slot Jacobian does:\n\nFerriteOperators.provides_analytic(::Type{<:MyCache}, ::WeightedJacobianKind) = true\nfunction FerriteOperators.assemble_cell!(req::WeightedJacobianRequest, cache::MyCache, args)\n    wu, wdu = req.weights.u, req.weights.du\n    # ... accumulate wu·∂F/∂u + wdu·∂F/∂du into req.K ...\nend\n\nWithout it the fused route seeds every participating slot in the residual kernel with its weight-scaled Duals — one sweep, one seed dimension. The composed route (per-slot sweeps into operator-held components, folded by combine!) runs for complex weights, since the element matrix and the Dual machinery are real, and wherever condensed internal state makes the AD-seeded route inadmissible; there each participating JacobianKind applies its own guards, so the weighted kind is servable exactly when every participating slot Jacobian is.","title":"Weighted Jacobians"},{"category":"section","location":"operators/#Boundary-terms-in-a-weighted-sweep","page":"Operators and entry points","text":"Facet kernels have no AD fallback, so a boundary term (facet_items) serves a weighted sweep through ONE route: its own assemble_facet!(::WeightedJacobianRequest, …) kernel, declared through provides_analytic. Nothing composes per-slot facet Jacobians behind the kernel's back. A cache without it is an ArgumentError naming that kernel — at setup_operator where the kind is declared, and on the first item swept otherwise.\n\nA CompositeFacetItemCache routes per inner, which is what puts a boundary spring (∂F/∂u only) next to a dashpot (∂F/∂v only) in one W: each inner accumulates its own fused contribution, the composite claiming neither slot.\n\nAn AffineRate slot may participate only through an analytic weighted kernel: reconstructed slots are frozen under AD, while a kernel forming the combination itself sees the slope through its weights. That exemption is what a multilevel-Newton element with a rate-coupled local problem needs — its condensed tangent carries the slope inside the local inverse, where post-hoc weighting of separated partial Jacobians cannot put it.","title":"Boundary terms in a weighted sweep"},{"category":"section","location":"operators/#Sensitivities","page":"Operators and entry points","text":"update_parameter_jacobian!(B, op, states, p, ctx)   # ∂F/∂θ, dense\nparameter_vjp!(g, op, λ, states, p, ctx)            # (∂F/∂θ)ᵀλ, matrix-free\nstate_jvp!(Jv, op, v, states, p, ctx)               # (∂F/∂u)·v, no matrix\nstate_vjp!(g, op, λ, states, p, ctx)                # (∂F/∂u)ᵀλ, the adjoint action\ntime_sensitivity!(g, op, states, p, ctx)            # ∂F/∂t at evaluation_time(ctx)\ntime_sensitivity!(g, op, states, p, ctx; method = FiniteDifferenceSensitivity())\n\nθ is the flat view defined by parameter_vector / rebuild_parameters; entries it does not expose are static and no sensitivity cost scales with them. Per cache, analytic sensitivity kernels win; otherwise the resolved cache — automatically wrapped in ADElementCache at setup_operator time — differentiates the residual kernel. The engine itself never forks between the two: it always calls assemble_cell! on the resolved cache, analytic-or-decorated. Sensitivity sweeps never write back into the caller's state.\n\nBoundary terms (facet_items) are their own traversal and enter ∂F/∂θ, ∂F/∂t and the matrix-free state products like any other item family — analytically only, since facet kernels have no AD fallback (see Facet items). Declaring the sensitivity kind makes setup demand that kernel loudly; a surface cache missing it is an ArgumentError at setup_operator, never a silently incomplete sensitivity.\n\n∂F/∂t seeds through the context channel: the AD sweep hands the kernel a context whose evaluation time is Dual-valued, and the finite-difference method evaluates the primal residual at contexts with perturbed times. An element therefore reads time as evaluation_time(args.ctx), and time_sensitivity! requires a context — passing nothing is an ArgumentError.\n\nAdmissibility with internal state: a sensitivity sweep is phase two of the two-stage protocol, so a condensed element's residual kernel is PURE (it reads the frozen q a prior condense_internal! wrote) and a plain AD fallback computes a genuine ∂F/∂·|_q partial rather than differentiating through an iteration — but the sensitivity kinds carry no CorrectionMode (they are always the total), so that partial is missing the ∂F/∂q · dq/d· correction. A cache with has_internal_state is therefore admissible for a sensitivity kind only if it (a) provides the analytic kernel (the correction, folded in — the payoff a corrector store unlocks: exact parameter/state sensitivities on a condensed element, generically, once the store exists), (b) declares internal_state_insensitive (asserting the local equations do not depend on the seeded quantity, so there is nothing to correct — then AD is exact), (c) declares local_conditions!, which lets the decorator derive dq/dθ and dq/dt from the element's own local equations and complete the total itself (parameter and time kinds, cell caches), or (d) for time sensitivities, the caller selects FiniteDifferenceSensitivity (primal evaluations on a protected copy, condensing at each — the total, but it bypasses analytic sensitivity kernels).\n\nTwo derivative mechanisms exist. Operator-level FiniteDifferenceSensitivity is the Dual-free route: it differences evaluate!, so every term enters — including a facet term whose cache carries no analytic sensitivity kernel — and no kernel ever sees a Dual. The ADElementCache decorator is the PER-CACHE route: analytic kernels win cache by cache, it is allocation-free per cell for the state and time sweeps, and it is volumetric only. There is no cache-level finite-difference decorator; the rationale records why.\n\n∂F/∂q, the block coupling the residual to a condensed element's internal state, is its own rectangular target rather than a slot Jacobian:\n\nKq = allocate_internal_jacobian(op)                  # residual_size(op) × ndofs(ivh)\nupdate_internal_jacobian!(Kq, op, states, p, ctx)\n\nState and time derivative sweeps (update_linearization! via AD, state_jvp!, state_vjp!, time_sensitivity!) run over per-worker preallocated buffers and ForwardDiff configurations and are allocation-free per cell. The parameter sweeps preallocate their output buffers and build their ForwardDiff configurations per call, since the seed dimension nθ arrives with p.","title":"Sensitivities"},{"category":"section","location":"operators/#Verifying-derivative-implementations","page":"Operators and entry points","text":"res = check_derivatives(op, states, p, ctx)\nres.passed                      # conjunction of all non-skipped checks\nres.checks.jacobian.err         # per-check relative error / skip reason\n\ncheck_derivatives cross-checks every derivative path — the assembled Jacobian, fused-vs-split residual, parameter Jacobian/VJP, state JVP/VJP, time sensitivity — against central finite differences of the operator's own residual, through the public entry points. A wrong analytic kernel fails its check against the FD referee; inadmissible or unsupported checks are skipped with the reason recorded. The parameter checks respect the differentiable/static split: only the entries exposed by parameter_vector are probed.\n\nThe FD referee evaluates the operator's full residual, boundary terms included, which is what makes it a referee for the analytic facet kernels a facet_items term's sensitivities rest on (see Sensitivities).\n\nThe time check runs only with a context and is recorded as a skip without one. Passing weights = (u = …, du = …) adds the two weighted-Jacobian checks: the fused Σₛ wₛ ∂F/∂s against per-slot finite differences, and fused-against-composed equality — the caller cannot tell which route an operator takes, so the two must be interchangeable.","title":"Verifying derivative implementations"},{"category":"section","location":"operators/#Quadrature-data","page":"Operators and entry points","text":"Per-quadrature-point evaluation runs through the same engine as assembly:\n\nq = setup_qvector(Float64, dh, qrc)\nevaluate_quadrature!(q, op, u, p, (uₑ, qp, cell, cache, pₑ, ctx) -> ...)\nevaluate_quadrature!(q, op, u, p, f; ctx = TimeIntegrationContext(t, Δt, γ̃))\n\nwith QVector as the flat storage, cell-set filtering, query/store hooks for element-owned layouts, and the VTK export layer (VTKQuadratureGrid, VTKQuadratureFile, write_quadrature_data) for visualization at quadrature points.\n\nThe per-quadrature-point kernel carries the sweep's context in its last slot, the same channel a request-shaped kernel reads its time from (evaluation_time); ctx = nothing — the default — is the stationary evaluation. p stays configuration, as everywhere else.","title":"Quadrature data"},{"category":"section","location":"operators/#Evaluating-without-assembling","page":"Operators and entry points","text":"An integrator whose term is never assembled — a functional integrand, a quadrature-point postprocessing kernel — takes setup_evaluation_operator instead:\n\nop = setup_evaluation_operator(strategy, integrator, dh)\nΦ  = evaluate_functional(op, MyFunctional(), (u = u,), p)\nevaluate_quadrature!(q, op, u, p, f)\n\nThe engine is the one setup_operator builds, with the same caches and the same setup-time validation; the difference is that no matrix and no vector are allocated, and the assembly entry points (update_operator!, update_linearization!, evaluate!) reject the call rather than assemble into a payload the term has no use for.","title":"Evaluating without assembling"},{"category":"section","location":"operators/#Transfer-operators","page":"Operators and entry points","text":"Rectangular transfer (prolongation/restriction) operators between two DofHandlers — same-grid (p-multigrid) and nested-grid (geometric multigrid) variants — have their own constructors (setup_transfer_operator, setup_nested_transfer_operator) and their own integrators (MassProlongatorIntegrator, NestedMassProlongatorIntegrator). They assemble sequentially on the CPU into a rectangular sparse matrix.\n\nwarning: Experimental surface\nThe transfer constructors and operator types may change in a minor release; the assembled matrix and its sparsity are not affected.","title":"Transfer operators"},{"category":"section","location":"devdocs/design/#The-layer-contract","page":"The layer contract","text":"This package owns two layers of the assembly pipeline — the term and the operator. Every design question about \"where does this belong\" is answered by which of the two owns the information, or by the finding that neither does and it belongs to the caller.\n\nowner owns in this package\nterm layer scheme-agnostic integrands over one work item element caches and their request-typed kernels\noperator layer evaluating a set of terms at (states, p, ctx) the engine, its workspaces, and the entry points\nthe caller — solver code composing evaluations into a discretization nothing: there is no scheme-facing API here. The solver holds the history vectors and tableau coefficients, fills the slots, supplies the weights, and constructs the context","title":"The layer contract"},{"category":"section","location":"devdocs/design/#The-term-layer","page":"The layer contract","text":"An element expresses an integrand. It reads slot values, a parameter view, and a per-sweep context — and nothing else. It does not know which time discretization produced the values in its slots, whether a Jacobian will be derived from it by automatic differentiation, or how many other terms share its cell.\n\nThe consequence that makes the layering worth having: one element serves every scheme. A residual kernel that reads args.states.u and args.states.du serves backward Euler, BDF-k, SDIRK stages, and a fully implicit Runge-Kutta stage row without a line of change, because each of those merely supplies different slot values.\n\nA hand-fused integrator — one that derives its own discretization from uprev and ctx, or that computes a scheme's combined matrix directly — puts solver-owned discretization inside an element cache by deliberate authorship. That is a legitimate choice (it is how a deliberately manual first-order discretization, or a multilevel-Newton element with a rate-coupled local problem, is written), and the framework serves it through the same request-typed kernels. The distinction is one of authorship, not of mechanism.","title":"The term layer"},{"category":"section","location":"devdocs/design/#The-operator-layer","page":"The layer contract","text":"An operator evaluates its term set at (states, p, ctx) and scatters the result. It owns the item loop, the per-worker workspaces, the geometry cache reinitialization, the slot gathers, the request materialization, and the choice between an analytic kernel and its derivative fallback.\n\nIt owns no time discretization and no scheme coefficients. states arrive as whatever the solver assembled them to be; ctx carries the scalars of this evaluation; weights are per-request payload.\n\nevaluate! is deliberately polymorphic. It evaluates whatever the integrator encodes — a nonlinear residual, the action of the linear operator a bilinear form induces (its element matrix acting on the element vector), or a hand-fused scheme residual — and the name says exactly that much and no more.","title":"The operator layer"},{"category":"section","location":"devdocs/design/#The-caller's-side-of-the-contract","page":"The layer contract","text":"The solver owns the discretization, and it owns it outside this package: nothing here declares, registers or hosts a scheme. The solver holds the history vectors and tableau coefficients, decides what each slot contains for this evaluation, supplies the chain-rule weights that fold per-slot Jacobians into the matrix it solves with, and constructs the context.\n\nWhere a scheme needs several evaluation times — generalized-α evaluating stiffness at tₙ₊₁₋αf and inertia at tₙ₊₁₋αm — the solver splits the problem into term-subset operators and runs one sweep per term at its own context, combining the results as components. One monolithic kernel cannot host two evaluation times, and the context is not the place to smuggle a second one: this is a modeling requirement on the operator split, not a gap in the context channel.","title":"The caller's side of the contract"},{"category":"section","location":"devdocs/design/#The-channel-decision-table","page":"The layer contract","text":"Every piece of information a kernel might want has one channel. The decision is made by the shape of the information, not by who produced it.\n\nshape channel notes\ndof-shaped, one value per dof a slot (args.states.<name>) histories, rates, adjoint directions, stochastic realizations — anything the operator can gather with a dof map. Slots may be plain vectors, AffineRate reconstructions, or — for a condensed element's internal state q — InternalSource restrictions (see Condensed elements).\npoint-shaped, one value per quadrature point quadrature storage and the query seams QVector for stored per-QP data; query_cell_parameters / query_facet_parameters for element-owned gathers, including parameter fields.\na scalar of this sweep args.ctx t, Δt, γ̃ in TimeIntegrationContext. A scheme with richer per-sweep scalars passes its own context type; framework code touches contexts only through evaluation_time, with_time and stage_scaling.\nconfiguration, constant across the sweep args.p material parameters and the user's bag. Never time, never history. p stays opaque: a solver-side wrapper is unwrapped by the cache's own query_cell_parameters.\nper-worker mutable working memory element cache fields duplicated — not aliased — per worker by duplicate_for_device, see storage classes for elements with local problems.\na scheme scalar attached to a slot request payload rides on the request instead of the args bundle — that is what WeightedJacobianKind does with its weights.\n\nTwo consequences worth stating explicitly.\n\nTime reaches elements through ctx, and only through ctx. If t could hide inside p, every wrapper would need an unwrapping convention, and the framework itself must see t to seed the ∂F/∂t sweep. A kernel that reads its time from args.p gets a silently zero time sensitivity.\n\nγ̃ is not a rate slope. It is the normalized local stage interval of the element-local internal-variable problem, fixed by q = q_ref + γ̃·g(·, q). Under backward Euler γ̃ and a rate slope happen to be reciprocals, which makes 1/γ̃ accidentally right there and wrong everywhere else. Rate slopes belong to the slot that carries the reconstruction.","title":"The channel decision table"},{"category":"section","location":"devdocs/design/#Extension-points","page":"The layer contract","text":"The framework is extended by adding methods to a small number of generic functions rather than by subclassing drivers.\n\nNew request kinds — a kind is defined entirely outside this package. The built-in kind families are trait defaults, not a closed world: what a sweep does with the workspace is a set of overloadable predicates, so a downstream kind reuses the built-in driver bodies rather than reimplementing them.\n\nThe complete recipe:\n\nstruct MyKind end\nstruct MyRequest{M <: AbstractMatrix} <: FerriteOperators.AbstractAssemblyRequest\n    K::M\nend\n\n# 1. The kind → request association (the pure form and the executing form).\nFerriteOperators.request_type(::MyKind) = MyRequest\nFerriteOperators.materialize_request(::MyKind, ws) = MyRequest(ws.Ke)\n\n# 2. What the sweep does with the workspace. Return literals: these guard the\n#    driver's branches and must fold away.\nFerriteOperators.assembles_matrix(::MyKind) = true\n# assembles_vector / depends_on_unknowns default to `false` for a new kind.\n\n# 3. The driver, per ITEM FAMILY. Annotate the workspace: `execute_kind!` is\n#    looked up as `(kind, task, workspace)`, so an unannotated method also\n#    catches the facet and algebraic workspaces, which a cell body cannot drive.\nFerriteOperators.execute_kind!(kind::MyKind, task, ws::FerriteOperators.AssemblyWorkspace) =\n    FerriteOperators.primal_cell_sweep!(kind, task, ws)\n\nThe annotation is not optional on an operator carrying facet or algebraic items. execute_single_task! dispatches on the workspace, and the built-in kinds get away with unannotated cell methods only because the facet and algebraic methods are more specific on the kind argument (execute_kind!(::PrimalKind, task, ws::FacetItemWorkspace)). A downstream kind is outside those unions, so it has no such method to lose to: an unannotated execute_kind!(::MyKind, task, ws) is the only candidate for every family and hands a FacetItemWorkspace to primal_cell_sweep!. Give the kind one method per family it must serve, and an explicit = nothing for each family it deliberately does not — which is exactly how the built-in kinds declare that facet items carry no condensed internal state.\n\nA kind whose sweep needs per-worker scratch beyond ws.Ke/ws.re reads SensitivityBuffers through ws.sensitivity — structurally present whenever the operator's integrator needs_ad_decoration (any AbstractNonlinearIntegrator) and nothing otherwise, no family declaration required. materialize_request(::MyKind, ws, task) (the 3-arg form) is where a sensitivity-shaped kind binds it; see the five built-in sensitivity kinds for the pattern.\n\nA kind computing a global scalar or tensor rather than something scattered declares the item families it integrates over instead, which routes it to the value-returning driver: the per-item kernel returns its contribution and the sweep folds the returned values, so there is no request type, no assembler, and no workspace state at either end.\n\nThat one declaration stands in for the per-family spelling above. reduction_families answers execute_kind! for every family (the named ones run that family's reduction driver body, the rest contribute nothing), sweep_family (a declaring kind is value-returning) and the structural precondition that decides which subdomains a reduction requires and traverses.\n\nstruct MyFunctionalKind end\nFerriteOperators.reduction_families(::Type{<:MyFunctionalKind}) = (:cells,)\nFerriteOperators.has_cell_request(::Type{<:MyFunctionalKind}) = false\n\n# The type the reduction accumulates in. Optional on a sequential device, where\n# the first contributing item fixes it; REQUIRED on a parallel one, whose\n# per-worker partials are allocated before the batch runs.\nFerriteOperators.functional_value_type(::MyFunctionalKind) = Float64\n\n# the kernel hook the provided body calls\nfunction FerriteOperators.evaluate_cell_functional(::MyFunctionalKind, cache::MyCache, args)\n    # ... return this cell's contribution, or `nothing` for none ...\nend\n\nvalue = FerriteOperators.run_reduction(MyFunctionalKind(), op, states, p, ctx)\n\nDeclaring the value type seeds every worker's fold with zero(T), so a worker that sees no contribution hands back the reduction's additive identity rather than a \"nothing yet\" marker, and a kernel returning some other type is an ArgumentError naming the declaration instead of a silently widened accumulator.\n\nA reduction whose family bodies are NOT the provided ones keeps its own execute_kind! methods — they are strictly more specific than the derived route — and still declares reduction_families for the structural half. CondensationKind is that case: value-returning over cells and algebraic items, with a write-back its driver bodies do and no provided body has.\n\nFunctionalKind is exactly this with a tag, and evaluate_functional is its entry point.\n\nElements then serve it like any built-in kind — provides_analytic(::Type{<:MyCache}, ::MyKind) = true plus an assemble_cell!(req::MyRequest, cache::MyCache, args) method — and the operator issues it through assemble_into!(MyKind(), (A,), op, states, p, ctx). Declaring it (setup_operator(...; requests = (MyKind,))) runs its setup-time trait ↔ kernel validation.\n\nThirteen provided bodies exist, across the four workspace types:\n\nitem family registration workspace provided bodies\ncells CellFamily AssemblyWorkspace primal_cell_sweep! (buffer zeroing, values reinit, slot gather, cell kernel, scatter — no write-back: condense_internal! is the only writer of q); sensitivity_cell_sweep! (trial gather, no write-back, dispatch to sensitivity_kernel!); functional_cell_sweep (slot gather, no write-back, RETURN what the kernel hook gives); condensation_cell_sweep! (slot gather, dispatch to condense_cell!, RETURN the CondensationReport AND write the trial q back — the one combination the others don't have); internal_jacobian_cell_sweep! (the rectangular ∂F/∂q block)\nfacet items FacetItemFamily FacetItemWorkspace primal_facet_item_sweep!; sensitivity_facet_item_sweep!; functional_facet_item_sweep (slot gather, no write-back, fold what the facet hook gives over the item's declared facets). Condensation, JacobianKind{:q} and quadrature evaluation are explicit nothing methods — the family has no body for them\nalgebraic items AlgebraicItemFamily AlgebraicWorkspace primal_algebraic_sweep!; sensitivity_algebraic_sweep!; functional_algebraic_sweep; condensation_algebraic_sweep!; internal_jacobian_algebraic_sweep!\npatches — (see setup_family_caches) PatchAssemblyWorkspace the PatchCallbackKind body, reached through foreach_patch rather than the operator entry points; the per-patch assembly itself is assemble_patch_target!, called by the callback\n\nA kind riding primal_cell_sweep! without its own cell_kernel! method gets the plain analytic route.\n\nDeclarations carry kind types, normalized to their UnionAll base, while sweeps carry instances. Two hooks bridge that for validation: validation_instance supplies the placeholder instance traits are queried on — a kind whose payload is a type parameter must overload it, since the default K() cannot construct one — and has_cell_request is false for a kind reaching the element through a hook other than assemble_cell!. requires_admissibility_check opts a kind into the internal-state admissibility rule at setup. That rule reads FerriteOperators.serves_kind — \"does the RESOLVED cache answer this kind, by kernel or by a decorator's generic route\" — and not provides_analytic, which is reserved for \"is there a hand-written kernel\" and forwards through the decorators unchanged.\n\nNew per-worker state — a workspace is a fixed core (geometry cache, slot buffers, Ke/re) plus SensitivityBuffers, present exactly when needs_ad_decoration says so — structural, by integrator family, not by declaration. The workspace itself is immutable, so a sweep fills buffers and never rebinds a field. There is no third, downstream-openable family: an element cache wanting its own per-worker scratch carries it as an ordinary cache field, duplicated per worker by its own duplicate_for_device (see storage classes for elements with local problems).\n\nNew AD backends — ADElementCache's backend field is the seam: ForwardDiffAD is the default, and a downstream extension implements its own buffer struct plus the same eight assemble_cell! methods for its own backend marker type, activated via setup_operator(...; ad_backend = MyBackend()).\n\nNew devices and scheduling — execute_on_device!, setup_device_instances and compute_partition are the three hooks a device or scheduling policy implements; the item loop and the workspaces are shared. A device whose per-worker state is a struct of arrays rather than an array of structs — KernelAbstractionsDevice is the shipped one — additionally implements device_worker_view (the in-kernel slice), n_workers, adapt_partition and, where its geometry cache needs a device-resident handler, setup_device_handler. It answers allocate_vector(device, dh) for the global vector, and allocate_operator_matrix for the global matrix whose type the operator specification names.\n\nA device that cannot serve an item family says so at setup through assert_device_supported rather than failing inside the first sweep; the GPU method there is the list of what the device kernel covers today.\n\nNew item iterators — assembly_iterator(kind, element_cache, sdh) decides what a sweep of kind positions on one item of the HOST SubDofHandler sdh, and what rides args.cell while the element kernels run; the default is Ferrite's CellCache. Three accessors are REQUIRED by the framework and no more:\n\nstruct MyIterator\n    # ...\nend\nFerrite.reinit!(it::MyIterator, item) = (# position in place; return it)\n\nFerrite.cellid(it::MyIterator)                    = ...  # a representative cell id\nFerriteOperators.iterator_dofs(it::MyIterator)    = ...  # the item's global dof indices\nFerriteOperators.iterator_handler(it::MyIterator) = ...  # the SubDofHandler it was built over\n\nFerriteOperators.assembly_iterator(kind, ::MyCache, sdh) = MyIterator(sdh)\n\nEverything else a shipped element kernel reaches for through args.cell — Ferrite.getcoordinates, Ferrite.getnodes, Ferrite.reinit!(cv, it), … — is CONVENTIONAL between the iterator and the elements written for it: the framework never calls them, so an iterator author implements whichever subset its own elements need.\n\nAn iterator positioned by CONSTRUCTION (an immutable value, as the device cursor is) rather than in place overloads position_iterator(it, item, flags) instead of Ferrite.reinit!, returning the new value; position_item — the only thing a sweep calls — carries whichever value came back into the workspace, so both positioning styles compose with the rest of the engine unchanged. item_update_flags(kind, element_cache) lets an iterator that stages some of what it carries answer, per (kind, cache) pair, which members a positioning refreshes; an iterator that stages nothing ignores it, and an under-declaring pair reads a stale buffer rather than erroring, the same contract Ferrite's own UpdateFlags carries.\n\nThe DEVICE shape is a fourth hook, device_assembly_iterator(kind, element_cache, sdh, device_sdh), whose default forwards to assembly_iterator over the device handler — a downstream iterator needing no host-only setup fact writes only the host method; one that does either overloads this instead (narrowing the CACHE, per the rule below) or answers decorate_device_iterator on its own iterator type, which is where the matrix-free action's uniform-dof-stride check lives. reinit_values!'s setup-time admissibility probe is validated against the subdomain's RESOLVED iterator type, so a cache author who annotates it against a custom iterator still passes.\n\nOnce constructed, a device iterator reaches the workspace through a FIFTH hook: the batching setup_device_instances(device, it, n) moves whatever it STAGES onto the device, and device_worker_view(it, worker) is the in-kernel slice. The workspace's iterator slot routes through a private, ext-level _batch_iterator(device, it, n) whose GENERIC default is setup_device_instances(device, it, n) itself, so a downstream device iterator writes only that 3-arg hook and needs no ext-private method. Ferrite.CellCache and the KA extension's DeviceCellCursor are the two shipped iterators with a more specific answer — Ferrite's own struct-of-arrays batching, and the cooperative mapping's positioned-by-construction fork.\n\nNew item SETS — an iterator says how to POSITION on an item; it does not say what the items ARE. That is the second seam, item_provider(kind, element_cache, sdh), whose answer compute_partition turns into the barriers and chunks a sweep walks. The default is CellItems(sdh).\n\nThe two vary independently, which is why they are two seams: the facet family runs a custom provider over the stock cell iterator, and the matrix-free action a custom iterator over the stock provider. A family that is BOTH — a two-sided interface traversal whose item is a pair of cells and whose local system is indexed by both cells' dofs — is these six methods and nothing else:\n\nFerriteOperators.assembly_iterator(kind, ::MyCache, sdh) = MyIterator(sdh, …)\nFerriteOperators.item_provider(kind, ::MyCache, sdh)     = MyItems(sdh, …)\n\nFerrite.reinit!(it::MyIterator, item::Int) = …   # position it; `position_iterator`'s default calls this\nFerriteOperators.compute_partition(::SequentialScheduling, p::MyItems) = (collect(eachindex(…)),)\nFerriteOperators.compute_partition(::ColoredScheduling,    p::MyItems) = …  # see below\nFerriteOperators.duplicate_for_device(::AbstractCPUDevice, it::MyIterator) = MyIterator(…)\n\nplus the three required accessors above. Ferrite.reinit! is what positions the iterator on an item — position_iterator's default is exactly that call, and an iterator positioned by CONSTRUCTION instead (a device cursor) overloads position_iterator and writes no reinit! at all.\n\nBoth seams narrow the CACHE argument, and that is a rule. A declaration may narrow the sweep kind on top of it; it must never narrow ONLY the kind. The decorators forward these seams with methods that narrow the cache and leave the kind open (AbstractElementCacheDecorator), so a kind-narrow/cache-open method ties with those forwards over every decorated cache and Julia reports the call ambiguous. The rule covers item_update_flags and reinit_values! for the same reason. A KIND-level default — the matrix-free action's device cursor — goes on default_assembly_iterator instead, which sits BELOW every cache declaration, so a cache that names its own iterator keeps it under every kind.\n\nOverloading one seam and forgetting the other is not an error and not a MethodError: the other answers with its default, and an interface iterator left with CellItems is positioned on CELL ids.\n\nHalf of that hazard is checked and half is not. A method written against a signature the engine does not call — the wrong sweep kind, the wrong handler type, the wrong arity — is rejected at setup by assert_iteration_signatures, which takes the ELEMENT CACHE as its subject: a hook with any method narrowing that argument to a type this subdomain's cache conforms to must have one the engine's own call resolves to. A method that is simply ABSENT has no drift to detect and stays invisible. Assert the item COUNT a sweep visits; no framework check can see that one.\n\nA provider carries its family's whole partition safety argument, and the wording the package uses is exact: a partition is safe given a valid partition, never thread-safe. Under ColoredScheduling the provider promises that no two items of one inner chunk share a SCATTER DOF — that promise, and nothing else, is what makes the scatter race-free without atomics. Under SequentialScheduling it promises nothing and the atomic scatter resolves the collisions. The three shipped providers each argue it in their own terms (FacetItems colors the owning cells, AlgebraicItems puts one item per barrier, PatchItems refuses to color at all).\n\nTwo structural consequences of a local system spanning more than one cell:\n\nThe dof window iterator_dofs returns is the scatter's address, and Ferrite's assembler requires it to be DUPLICATE FREE. Two cells of a continuous space share the dofs on their common facet, so a two-sided item over one repeats them; the family's space is discontinuous, or the item's sides do not touch.\nThe entries that window addresses are not in the DofHandler's cell pattern, and the framework never infers them. Declare them through the operator specification's sparsity_entries (StandardOperatorSpecification) — the same doctrine global_dofs states for its tail.\n\nNew item FAMILIES — the two seams above narrow what the CELL family positions on and enumerates, which is all a family riding the cell route needs. Registering a family of its OWN is the third seam, and it is what an operator carrying more than one traversal at a time needs: item_families(integrator, dh) names the families the engine carries, in TRAVERSAL order, and setup_family_caches(family, strategy, integrator, dh, shared) builds one family's SubdomainCaches.\n\nstruct MyFamily end\n\nFerriteOperators.item_families(::MyIntegrator, dh) = (CellFamily(), MyFamily())\n\nFerriteOperators.setup_family_caches(::MyFamily, strategy, integrator, dh, shared) =\n    # one SubdomainCache per subdomain this family serves; `()` declines\n\nThe tuple REPLACES the default, which derives CellFamily() always, FacetItemFamily where a subdomain declares facet_items and AlgebraicItemFamily where the handler declares algebraic_items. So an integrator returning only its own marker carries only that family — the cells are not swept — and one returning (CellFamily(), MyFamily()) carries both, cells first. That order is the order of engine.subdomain_caches, which the reduction determinism contract rests on.\n\nshared carries what setup_engine resolved before any family ran, and every field is available to every family; setup_family_caches tabulates them.\n\nNo ENGINE-REGISTERED family has a privileged setup path. The three shipped families are three setup_family_caches methods and nothing else — the cell one in operators/setup.jl, the facet-item one in core/facet-task.jl, the algebraic one in core/algebraic-task.jl — reached by the same dispatch a downstream marker reaches, and setup_engine iterates item_families rather than calling any of them by name.\n\nTwo families deliberately do not register, for reasons that are properties of those families rather than of this seam. foreach_patch is sequential-CPU-only because the callback's collectors are the CALLER's, so this package cannot duplicate them per worker, and PatchAssemblyWorkspace positions through a Ref resolved against its provider. setup_transfer_operator restricts to sequential full assembly by design and assembles a RECTANGULAR matrix through a driver of its own. Both adopt the iteration seams above and keep their own entry points.\n\nTwo scope limits. The composite and multi-domain wrappers forward facet_items and algebraic_items, so a wrapped sub-integrator's BUILT-IN families are carried through the default above — but they do not forward item_families: a wrapper's own answer is the operator's. And a family whose subdomain caches are not cell-shaped builds them from names this package does not export; a downstream family with cell-shaped caches answers with the CellFamily method instead.\n\nwarning: Experimental surface\nitem_families, setup_family_caches and the three family markers are experimental and may change in a minor release, as the whole iteration protocol's registration half.\n\nNew assembly levels — a form member decides what setup_operator returns and, through operator_specification, whether the global-storage walls apply to it at all. MatrixFreeAction is the second member: it allocates nothing, and its operator's mul! rides the ordinary sweep (run_sweep! → execute_on_subdomains! → execute_on_device!) under its own kind, whose driver body (matrix_free_cell_sweep!) differs from primal_cell_sweep! only in gathering fixed-width.\n\nA form choice a DEVICE has to realize — the element mapping — resolves onto the device at setup through with_element_mapping, because n_workers, setup_device_instances and execute_on_device! receive the device and never the form. A choice the ELEMENT has to realize — the storage election separating the ELEMENT, PARTIAL and NONE levels — resolves onto the caches through with_assembly_form for the mirrored reason: setup_element_cache receives the subdomain and never the form. Its two per-quadrature-point members reach the element's own hook (with_action_storage); the ELEMENT member is element-AGNOSTIC and wraps whatever cache the integrator built in an ElementAssemblyCache, which is why a cache with no matrix-free kernel serves it. What either keeps is filled by a sweep of its own kind (QuadratureDataKind), which scatters nothing and carries no assembler.","title":"Extension points"},{"category":"section","location":"example-elements/#Example-elements","page":"Example elements","text":"","title":"Example elements"},{"category":"section","location":"example-elements/#Nesting-the-two-stage-protocol","page":"Example elements","text":"SimpleNestedHomogenization is the two-stage protocol nested inside itself: a macroscopic bar whose stress at every quadrature point is the homogenized response of a MICRO finite element problem, itself a condensed element (SimpleRelaxingBar) with its own per-quadrature-point internal variable. The two-stage protocol is the contract at both levels, and the nesting is FerriteOperators inside FerriteOperators: the micro problem is an ordinary operator built by setup_operator and driven by condense_internal!/update_linearization!, held as an element-cache field.\n\nWhere each phase runs. Phase one (condense_cell!) runs the micro Newton per macro quadrature point. Every one of its iterations is itself a phase one followed by a phase two — condense_internal! solves the micro material's internal variables at the current micro state, then update_linearization! evaluates the micro residual and CONDENSED micro tangent there. Phase two of the macro element is a pure evaluation: the residual re-evaluates the micro residual at the stored micro state (a sweep, no solve), and the Jacobian reads the homogenized tangent phase one stored.\n\nThe storage classes, one field per class (storage classes):\n\nclass field lifetime\nimmutable problem structure micro — micro grid, DofHandler, driven/free dof split, the micro measure built once in setup_element_cache, ALIASED across workers\nper-worker solve workspace workspace — the worker's OWN micro operator and its state/history/residual buffers rebuilt by duplicate_for_device, never shared\nper-item state tangents — an ItemStates store of each quadrature point's (frozen, consistent) homogenized tangent aliased, item slots disjoint per worker, dropped by rollback_state!\n\nThe micro operator belongs to the second class and not the first: it owns a matrix, element caches and corrector stores that a sweep writes, so sharing one across workers is the race an aliased cache must never introduce.\n\nWhere the micro state lives. The whole micro state [micro ū; micro q] of every macro quadrature point rides the macro [ū; q] tail — the macro element's get_number_of_internal_dofs_per_element returns nqp × (micro ū + micro q) per cell — so trial write-back, rollback_state! and commit_state! carry the micro states with no second mechanism. The micro Newton starts from the COMMITTED micro state rather than the current trial tail, which is what keeps phase one a function of (ū, qprev) alone: warm starting from the tail would make the converged q depend on how many times the caller condensed on the way there.\n\nThe composition of implicit function theorems. The micro tangent K that the micro Newton steps with already has the micro internal variables eliminated (the micro element's own Consistent correction). Eliminating the micro equilibrium on top of it is the second implicit function theorem, and it is the Schur complement of that same K onto the driven dofs:\n\nfracmathrmdbarsigmamathrmdbarvarepsilon\n  = frac1Omega_micro x^d cdot\n    left(K_dd - K_df K_ff^-1 K_fdright) x^d \n\nThe CorrectionMode composes with it: the FrozenQ partial of the macro element is the same contraction over the micro problem's own FROZEN tangent — no micro equilibrium, no micro internal response — so the macro mode selects which micro tangent the contraction runs over. The CondensationReport composes the same way: a micro condensation that did not converge makes the macro-local problem unconverged, so a failure at either level surfaces in the report the macro condense_internal! returns.\n\nThe boundary. A nested element is ANALYTIC-PROVIDER territory. No generic or AD route reaches through two levels: the decorator's generic Consistent combination (condensed_corrector) and the local_conditions! route both differentiate ONE element kernel, and a kernel that runs a nested operator's sweeps is not eltype-generic — it hands Float64 buffers to another operator. Their q seeds do FIT a nested element — they are sized from the element's declared internal-dof count, nqp × (micro ū + micro q) here — so what does not reach through is the differentiation, not the buffer shape. SimpleNestedHomogenization therefore declares provides_analytic for the Jacobian-shaped kinds it serves and nothing else, and the sweep's parameter bag is not passed down: parameter and time sensitivities through two levels need the same implicit function theorem composition once more, one level deeper.","title":"Nesting the two-stage protocol"},{"category":"section","location":"example-elements/#Reference","page":"Example elements","text":"","title":"Reference"},{"category":"module","location":"example-elements/#FerriteOperatorsExampleElements.FerriteOperatorsExampleElements","page":"Example elements","text":"Example elements for FerriteOperators.\n\nMinimal, readable implementations of the element contract — one per feature it exposes: a bilinear form (SimpleBilinearDiffusionIntegrator, SimpleBilinearMassIntegrator), a linear form (SimpleLinearIntegrator), a nonlinear element with analytic tangent (SimpleHyperelasticityIntegrator), a condensed element with per-quadrature-point internal state (SimpleCondensedLinearViscoelasticity), one whose local problem is nonlinear and communicates with the outer solver (SimpleCondensedPowerLawRelaxation), and a NESTED pair whose local problem is itself a condensed finite element problem (SimpleRelaxingBar inside SimpleNestedHomogenization).\n\nThe two bilinear forms' matrix-free, sum-factorized counterparts (SumFactorizedDiffusionIntegrator, SumFactorizedMassIntegrator) live in FerriteOperatorsTensorProduct as that package's own reference consumers.\n\nMeant to be read, copied and used as test fixtures: not tuned for production use, and carrying no stability guarantee beyond the contract they demonstrate.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.FerriteOperatorsExampleElements"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.InexactLocalSolveContext","page":"Example elements","text":"InexactLocalSolveContext(inner, local_tolerance)\n\nDecorates inner with the local residual tolerance the outer solver asks element-local problems to solve to this sweep (the inexact-Newton forcing term, e.g. ηₖ‖F(uₖ)‖), consumed once per trial point at condense_internal! time rather than on every sweep that solves. Elements read it through local_solve_tolerance; framework context handling (evaluation_time, with_time) passes through to inner.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.InexactLocalSolveContext"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.LocalNewtonSettings","page":"Example elements","text":"LocalNewtonSettings(; max_iterations = 25, tolerance = 1.0e-12)\n\nStatic configuration of an element-local Newton solver: the iteration budget and the tightest absolute local residual tolerance the element ever solves to — a solver may only LOOSEN it, per local_solve_tolerance. Read per quadrature point by SimpleCondensedPowerLawRelaxation and SimpleRelaxingBar, and per micro Newton by SimpleNestedHomogenization. Exceeding the budget is reported through CondensationReport.converged, not thrown.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.LocalNewtonSettings"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.MaxwellParameters","page":"Example elements","text":"Material parameters of the standard linear solid used by SimpleCondensedLinearViscoelasticity: spring moduli E₀, E₁, shear modulus μ, dashpot viscosity η₁ and Poisson's ratio ν.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.MaxwellParameters"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.MicroBarProblem","page":"Example elements","text":"MicroBarProblem\n\nImmutable problem structure of one SimpleNestedHomogenization quadrature point's micro problem — mesh, handler, the driven/free dof split and the micro domain's measure. Built once in setup_element_cache from what the integrator carries and deliberately ALIASED across workers; nothing here is written during a sweep.\n\nThe driven dofs are the bar's two ends, loaded affinely by the macroscopic strain (u_d = ε̄ x_d), which is what makes the reaction σ̄ = Σ_d r_d x_d / |Ω_micro| the work-conjugate homogenized stress.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.MicroBarProblem"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.MicroBarWorkspace","page":"Example elements","text":"MicroBarWorkspace\n\nPer-worker mutable solve workspace of SimpleNestedHomogenization: the worker's OWN micro operator — a full FerriteOperators operator over the micro problem, built by duplicate_for_device so no two workers share its matrix, element caches or corrector stores — and the micro state, history and residual buffers its Newton runs on.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.MicroBarWorkspace"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.NortonRelaxationParameters","page":"Example elements","text":"Material parameters of the power-law relaxation model used by SimpleCondensedPowerLawRelaxation: diffusivity κ, exchange modulus α between field and internal state, dashpot viscosity η and Norton exponent n (n = 1 degenerates to a linear dashpot).\n\nGeneric over T and promoting so that rebuild_parameters can return a DUAL-valued bag from a seeded θ, as every generic parameter path needs; a monomorphic field type would reject it.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.NortonRelaxationParameters"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.RelaxingBarParameters","page":"Example elements","text":"Material parameters of the relaxing bar used by SimpleRelaxingBar and, as the micro material, by SimpleNestedHomogenization: equilibrium modulus E₀, relaxing-branch modulus E₁, dashpot viscosity η and Norton exponent n (n = 1 degenerates to a linear dashpot).\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.RelaxingBarParameters"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleBilinearDiffusionElementCache","page":"Example elements","text":"The cache associated with SimpleBilinearDiffusionIntegrator to assemble element diffusion matrices.\n\ncellvalues is left unconstrained because a GPU device's batched cache holds the struct-of-arrays container over n_workers CellValues, not a CellValues.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleBilinearDiffusionElementCache"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleBilinearDiffusionIntegrator","page":"Example elements","text":"SimpleBilinearDiffusionIntegrator\n\nRepresents the integrand of the bilinear form a(uv) = int nabla v(x) cdot D nabla u(x) dx for a given diffusion value D and uv from the same function space.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleBilinearDiffusionIntegrator"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleBilinearMassElementCache","page":"Example elements","text":"The cache associated with SimpleBilinearMassIntegrator to assemble element mass matrices.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleBilinearMassElementCache"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleBilinearMassIntegrator","page":"Example elements","text":"SimpleBilinearMassIntegrator\n\nRepresents the integrand of the bilinear form a(uv) = int v(x) cdot rho u(x) dx for a given density rho and uv from the same function space.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleBilinearMassIntegrator"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleCondensedLinearViscoelasticity","page":"Example elements","text":"SimpleCondensedLinearViscoelasticity(material_parameters, qrc, displacement_name, viscosity_name;\n                                      corrector = Stored())\n\nLinear viscoelasticity (standard linear solid) with the viscous strain εᵛ as a condensed per-quadrature-point internal variable. The element owns the LOCAL stage problem for εᵛ (scaled by stage_scaling(ctx)), a direct solve per quadrature point in condense_cell! — condense_internal! must run before any evaluation sweep.\n\ncorrector elects where the Mandel factorization A of the local operator, the Consistent kernel's input, comes from (CorrectorElection): Stored() retains it per quadrature point, Recompute() re-forms it in the kernel from the same ℂ and γ̃ — a 6×6 matrix per quadrature point against one Mandel assembly per Jacobian sweep. Only the recomputing path reads stage_scaling(args.ctx), so only it requires a context.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleCondensedLinearViscoelasticity"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleCondensedLinearViscoelasticityCache","page":"Example elements","text":"The cache associated with SimpleCondensedLinearViscoelasticity. Under a Stored election correctors holds the per-quadrature-point Mandel factorization A that condense_cell! wrote and the Consistent kernel reads; under Recompute it is nothing and the kernel re-forms A. Declares has_internal_state.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleCondensedLinearViscoelasticityCache"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleCondensedPowerLawRelaxation","page":"Example elements","text":"SimpleCondensedPowerLawRelaxation(material_parameters, qrc, field_name, internal_name;\n                                   local_solver = LocalNewtonSettings(),\n                                   corrector = Stored())\n\nDiffusion of a scalar field u exchanging with a condensed per-quadrature-point internal state q across a power-law (Norton) dashpot:\n\nr(u q) = int_Omega kappa nabla u cdot nabla delta u\n        + alpha (u - q) delta u  mathrmdOmega \nqquad\ndot q = fracsigma sigma^n-1eta\nquad sigma = alpha(u - q) \n\nThe element owns the LOCAL stage problem q = q_prev + γ̃ · q̇(u, q), nonlinear for n ≠ 1, solved per quadrature point by a Newton iteration started at q_prev inside condense_cell! — condense_internal! must run before any evaluation sweep. The Consistent kernels read the implicit-function-theorem slopes dq/du and dq/dθ, so the element is admissible under the internal-state rule.\n\nIt talks to the outer solver both ways: inward the requested local tolerance (local_solve_tolerance, clamped from below by the element's own LocalNewtonSettings), outward the CondensationReport.\n\ncorrector elects where those slopes come from (CorrectorElection): Stored() keeps them per quadrature point, Recompute() re-derives them from the converged (u, q) in the kernel that needs them. Both produce the same numbers.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleCondensedPowerLawRelaxation"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleCondensedPowerLawRelaxationCache","page":"Example elements","text":"The cache associated with SimpleCondensedPowerLawRelaxation. Under a Stored election it holds the per-quadrature-point slopes the Consistent kernels read — correctors (dq/du) and param_correctors (dq/dθ, θ = (α, η, n)) — and nothing in both fields under Recompute, which re-derives them in the kernel. weight, the solver's :u chain-rule scalar of the last condensation, is O(1) per cache and kept under either election: a Stored() slope has it folded in, the recomputing path reads it back. Declares has_internal_state.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleCondensedPowerLawRelaxationCache"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleHyperelasticityElementCache","page":"Example elements","text":"The cache associated with SimpleHyperelasticityIntegrator. It serves the residual, the Jacobian and the fused Jacobian-residual analytically, all from Tensors derivatives of the energy.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleHyperelasticityElementCache"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleHyperelasticityIntegrator","page":"Example elements","text":"SimpleHyperelasticityIntegrator{EnergyType}\n\nHyperelasticity with strain energy density psi(F), F = I + nabla u.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleHyperelasticityIntegrator"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleLinearElementCache","page":"Example elements","text":"The cache associated with SimpleLinearIntegrator to assemble element \"constant\" vectors.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleLinearElementCache"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleLinearIntegrator","page":"Example elements","text":"SimpleLinearIntegrator\n\nRepresents the integrand of the linear form b(v) = int f v(x) dx for a given constant f and v from the test function space.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleLinearIntegrator"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleNestedHomogenization","page":"Example elements","text":"SimpleNestedHomogenization(micro, micro_elements, qrc, field_name, internal_name;\n                           local_solver = LocalNewtonSettings())\n\nA macroscopic bar whose stress at every quadrature point is the homogenized response of a MICRO finite element problem that itself carries condensed internal variables — the two-stage protocol nested inside itself:\n\nr(bar u bar q) = int_barOmega barsigma deltabarvarepsilon mathrmdx\nqquad\nbarsigma = frac1Omega_micro sum_d r_d(x) x_d \n\nwhere x solves the micro problem of micro (a SimpleRelaxingBar on micro_elements elements) driven by u_d = ε̄ x_d on its two ends. Phase one (condense_cell!) runs that micro Newton — whose every iteration condenses the micro problem's own internal variables first — and forms the homogenized tangent by the implicit function theorem on the converged micro tangent, i.e. its Schur complement onto the driven dofs. Phase two is a pure evaluation: the residual reads the stored micro state, the Jacobian the stored homogenized tangent.\n\nThe whole micro state [micro ū; micro q] of every macro quadrature point rides the macro [ū; q] vector, so trial write-back, rollback_state! and commit_state! carry it with no second mechanism. local_solver budgets the MICRO NEWTON; the micro material's own local solver budgets the level below it, and a failure at either level folds into this element's CondensationReport.\n\nAnalytic-only, for both CorrectionModes: no generic or AD route reaches through two levels — a kernel that runs another operator's sweeps is not eltype-generic — and the sweep's parameter bag is not passed down, the micro problem's configuration being the micro integrator's own.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleNestedHomogenization"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleNestedHomogenizationCache","page":"Example elements","text":"The cache associated with SimpleNestedHomogenization, carrying one field per storage class: micro is immutable problem structure (aliased), workspace the per-worker micro operator and its buffers (duplicated), and tangents the per-item ItemStates store holding each quadrature point's (frozen, consistent) homogenized tangent pair (aliased; item slots are disjoint per worker). Declares has_internal_state.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleNestedHomogenizationCache"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleRelaxingBar","page":"Example elements","text":"SimpleRelaxingBar(material_parameters, qrc, field_name, internal_name;\n                  moduli = Float64[], local_solver = LocalNewtonSettings())\n\nA one-dimensional bar with the viscous strain q as a condensed per-quadrature-point internal variable:\n\nr(u q) = int_Omega sigma(varepsilon q) deltavarepsilon  mathrmdx\nqquad\nsigma = c(E_0 varepsilon + E_1 (varepsilon - q))\nqquad\nq = q_prev + tildegamma fracsigma_v sigma_v^n-1eta\nquad sigma_v = cE_1 (varepsilon - q)\n\nwith ε = ∂u/∂x and c the cell's entry of moduli (1 for every cell where moduli is empty — the homogeneous bar). The local q problem is nonlinear for n ≠ 1 and solved by a Newton iteration per quadrature point in condense_cell!; condense_internal! must run before any evaluation sweep. Consistent reads the stored implicit-function-theorem slope dq/dε, FrozenQ drops it.\n\nThis is the MICRO element of SimpleNestedHomogenization, and — on a macroscopic mesh, with moduli empty — the single-material law that element's homogenized response reduces to.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleRelaxingBar"},{"category":"type","location":"example-elements/#FerriteOperatorsExampleElements.SimpleRelaxingBarCache","page":"Example elements","text":"The cache associated with SimpleRelaxingBar. correctors holds the per-quadrature-point slope dq/dε that condense_cell! wrote and the Consistent kernel reads; moduli is immutable problem structure, aliased across workers. Declares has_internal_state.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.SimpleRelaxingBarCache"},{"category":"method","location":"example-elements/#FerriteOperators.assemble_cell!-Tuple{JacobianRequest{:q}, FerriteOperatorsExampleElements.SimpleCondensedPowerLawRelaxationCache, CellArgs}","page":"Example elements","text":"assemble_cell!(req::JacobianRequest{:q}, cache::SimpleCondensedPowerLawRelaxationCache, args) -> K\n\n∂F/∂q, the LOCAL ndofs × nqp block a Schur-complement consumer or a generic corrector combination wants. A pure function of (u, q) — no store needed, and Consistent and FrozenQ coincide because q is the seed itself. K is sized by the caller: q's dofs are internal to the cell, so there is no global scatter target and never a global sweep for this kind.\n\n\n\n\n\n","title":"FerriteOperators.assemble_cell!"},{"category":"method","location":"example-elements/#FerriteOperators.condense_cell!-Tuple{FerriteOperatorsExampleElements.SimpleNestedHomogenizationCache, CellArgs, NamedTuple}","page":"Example elements","text":"condense_cell!(cache::SimpleNestedHomogenizationCache, args, weights) -> CondensationReport\ncondense_cell!(cache::SimpleNestedHomogenizationCache, args, ::Nothing) -> CondensationReport\n\nRun every quadrature point's micro Newton to convergence, write the converged micro state into the macro [ū; q] tail and store the homogenized tangents. The residual-only election skips the two tangent sweeps and stores nothing; the micro states it writes are bit-identical, the election governing only what is formed after the solve.\n\nThe report is this level's: solves counts the macro quadrature points, iterations the micro Newton iterations, worst_residual the largest micro equilibrium residual at exit. A micro condensation that did not converge makes the macro-local problem unconverged, so a failure at either level surfaces here.\n\n\n\n\n\n","title":"FerriteOperators.condense_cell!"},{"category":"method","location":"example-elements/#FerriteOperators.condense_cell!-Union{Tuple{NQP}, Tuple{FerriteOperatorsExampleElements.SimpleCondensedLinearViscoelasticityCache{NQP}, CellArgs, NamedTuple}} where NQP","page":"Example elements","text":"condense_cell!(cache::SimpleCondensedLinearViscoelasticityCache, args, weights) -> CondensationReport\n\nSolve the local viscous-strain problem at every quadrature point — a direct linear solve, always converged with zero inner iterations — write the trial εᵛ and retain the Mandel factorization A.\n\n\n\n\n\n","title":"FerriteOperators.condense_cell!"},{"category":"method","location":"example-elements/#FerriteOperators.condense_cell!-Union{Tuple{NQP}, Tuple{FerriteOperatorsExampleElements.SimpleCondensedPowerLawRelaxationCache{NQP}, CellArgs, NamedTuple}} where NQP","page":"Example elements","text":"condense_cell!(cache::SimpleCondensedPowerLawRelaxationCache, args, weights) -> CondensationReport\n\nSolve the local Norton-dashpot problem at every quadrature point and write the trial q. A Stored election stores the slopes dq/du and dq/dθ alongside; Recompute does not, and the kernels re-derive them from the pair this hook converged.\n\n\n\n\n\n","title":"FerriteOperators.condense_cell!"},{"category":"method","location":"example-elements/#FerriteOperators.condense_cell!-Union{Tuple{NQP}, Tuple{FerriteOperatorsExampleElements.SimpleCondensedPowerLawRelaxationCache{NQP}, CellArgs, Nothing}} where NQP","page":"Example elements","text":"condense_cell!(cache::SimpleCondensedPowerLawRelaxationCache, args, ::Nothing) -> CondensationReport\n\nThe residual-only election: the same local solves writing the same trial q, with neither slope formed. condense_internal! has already dropped the stores, so the Consistent kernels refuse until a weighted condensation refills them.\n\n\n\n\n\n","title":"FerriteOperators.condense_cell!"},{"category":"method","location":"example-elements/#FerriteOperators.condense_cell!-Union{Tuple{NQP}, Tuple{FerriteOperatorsExampleElements.SimpleRelaxingBarCache{NQP}, CellArgs, NamedTuple}} where NQP","page":"Example elements","text":"condense_cell!(cache::SimpleRelaxingBarCache, args, weights) -> CondensationReport\n\nSolve the local viscous-strain problem at every quadrature point, write the trial q and store the slope dq/dε the Consistent kernel reads.\n\n\n\n\n\n","title":"FerriteOperators.condense_cell!"},{"category":"method","location":"example-elements/#FerriteOperatorsExampleElements.local_solve_tolerance-Tuple{Any}","page":"Example elements","text":"local_solve_tolerance(ctx) -> tolerance or `nothing`\n\nThe local residual tolerance the outer solver requests for this sweep. nothing, the default for every context type, leaves the element at its own LocalNewtonSettings tolerance — which is also the floor: a tighter request is ignored.\n\n\n\n\n\n","title":"FerriteOperatorsExampleElements.local_solve_tolerance"},{"category":"section","location":"#FerriteOperators","page":"Home","text":"A SciML compatible high performance parallel assembly system for Ferrite.jl.\n\nnote: Note\nFor an assembly framework in Ferrite.jl style we refer users for now to FerriteAssembly.jl.\n\nwarning: Warning\nThis package is under heavy development. Expect regular breaking changes for now. If you are interested in joining development, then either comment an issue or reach out via julialang.zulipchat.com, via mail or via julialang.slack.com. Alternatively open a discussion if you have something specific in mind.\n\nnote: Note\nIf you are interested in using this package, then I am also happy to to get some constructive feedback, especially if things don't work out in the current design. This can be done via julialang.slack.com, julialang.zulipchat.com or via mail.","title":"FerriteOperators"},{"category":"section","location":"#What-this-package-is","page":"Home","text":"FerriteOperators sits between Ferrite modeling code and solver code. Its design follows the fundamental finite-element operator decomposition popularized by MFEM and libCEED: element restriction, basis evaluation, pointwise physics, and global scatter are separate concerns, and how much of the operator is materialized (full sparse matrix, stored element matrices, matrix-free action) is a strategy axis, not a property of the physics.\n\nElements express scheme-agnostic integrands. Operators evaluate a set of them at a given state, parameter bag, and per-sweep context. Solvers own the time discretization and compose operator evaluations into a scheme. The layer contract states that division of labour precisely.","title":"What this package is"},{"category":"section","location":"#Quickstart","page":"Home","text":"using FerriteOperators\n\nstrategy = AssemblyStrategy(SequentialCPUDevice())\nop = setup_operator(strategy, MyIntegrator(qrc, :u), dh; slots = (:u, :uprev))\n\nr = zeros(ndofs(dh))\nupdate_linearization!(op, r, (u = u, uprev = uprev), p, TimeIntegrationContext(t, Δt, γ̃))\nΔu = op.J \\ r\n\nAn element supplies one mandatory residual kernel; the assembled Jacobian, the fused Newton path, and every sensitivity follow from it by ForwardDiff unless analytic kernels are declared.\n\nfunction FerriteOperators.assemble_cell!(req::ResidualRequest, cache::MyCache, args)\n    (; cv) = cache\n    uₑ = args.states.u\n    for qp in 1:getnquadpoints(cv)\n        dΩ = getdetJdV(cv, qp)\n        # ... accumulate into req.r ...\n    end\nend","title":"Quickstart"},{"category":"section","location":"#The-assembly-strategy","page":"Home","text":"Which machinery an operator is built on is one composite choice, and the three axes are orthogonal: the operator form (AbstractAssemblyForm — the MFEM assembly level), the scheduling policy (SequentialScheduling / ColoredScheduling — how parallel work is made race-safe), and the device (sequential CPU, threaded via Polyester, GPU via KernelAbstractions.jl). AssemblyStrategy(device; form, scheduling) is the keyword convenience constructor for the common compositions: AssemblyStrategy(device) and AssemblyStrategy(device; scheduling = ColoredScheduling()).\n\nPolyesterDevice lives behind a package extension: using Polyester activates it, and without that load the type exists with no execution route. default_strategy resolves that at call time — the Polyester device where the extension is loaded, SequentialCPUDevice otherwise.","title":"The assembly strategy"},{"category":"section","location":"#GPU-assembly","page":"Home","text":"KernelAbstractionsDevice assembles bilinear and linear forms on a KernelAbstractions.jl backend. This package depends on no GPU vendor package: the backend object and the device matrix type both come from the caller.\n\nusing CUDA, Adapt, KernelAbstractions   # `using CUDA` loads all of these\nimport CUDA: CUSPARSE.CuSparseMatrixCSC\n\ndevice   = KernelAbstractionsDevice(CUDABackend(); value_type = Float32, index_type = Int32)\nspec     = StandardOperatorSpecification(; matrix_type = CuSparseMatrixCSC{Float32, Int32})\nstrategy = AssemblyStrategy(FullAssembly(spec), ColoredScheduling(), device)\n\nintegrator = MyIntegrator(QuadratureRuleCollection(Float32, 2), :u)  # element precision\nop = setup_operator(strategy, integrator, dh)   # op.A lives on the device\nupdate_operator!(op, p)\n\nvalue_type governs the GLOBAL system alone; the element caches' precision is the INTEGRATOR's, elected through its quadrature collection (see Evaluation precision). The two may differ — the scatter converts. Build the grid with Float32 coordinates too: a Float64 grid assembles correctly, but its coordinates are what the geometry mapping computes in. The vector and the assembled matrix both stay on the device; a sweep transfers nothing.\n\nWhat the device covers is CELL items under ColoredScheduling, which is REQUIRED: Ferrite's device matrix assembler accumulates without atomics. Everything outside that slice — other schedulings, other item families, condensed state, nonlinear integrators, blocked or constrained specifications, and so on — is rejected at setup with a message naming the limitation; KernelAbstractionsDevice lists them.\n\nAn element cache reaches the device by declaring which of its fields are batched per worker and which are shared, through setup_device_instances and device_worker_view:\n\nsetup_device_instances(dev::AbstractGPUDevice, c::MyCache, n) =\n    MyCache(c.D, setup_device_instances(dev, c.cellvalues, n))\ndevice_worker_view(c::MyCache, w) = MyCache(c.D, device_worker_view(c.cellvalues, w))\n\nThe cache's field type parameters have to admit the batched layout: on the device a cellvalues::CV field holds a struct-of-arrays container over n workers, not a CellValues.","title":"GPU assembly"},{"category":"section","location":"#Assembly-levels","page":"Home","text":"FullAssembly assembles the global matrix and vector and serves every operator family — the FULL level, and the default.\n\nMatrixFreeAction covers the other three: the operator stores no GLOBAL matrix and every mul! evaluates y = A·u element by element. setup_operator returns a MatrixFreeFerriteOperator for a bilinear integrator whose caches serve the elected storage level — apply_element_action! for the two per-quadrature-point levels — and a setup error naming the method otherwise.\n\nstrategy = AssemblyStrategy(SequentialCPUDevice(); form = MatrixFreeAction())\nop = setup_operator(strategy, SumFactorizedDiffusionIntegrator(2.5, qrc, :u), dh)\nmul!(y, op, u)          # no matrix anywhere\n\nThe form carries a second choice, how ONE element's work maps onto the device's workers (AbstractElementMapping):\n\nWorkerPerElement — one worker owns one element from gather to scatter. Runs on every device.\nCooperativeElement — one WORKGROUP owns one element, its workers splitting the element's lattice between them with the state and every intermediate in group-local memory. A KernelAbstractionsDevice mapping only, and served by caches that implement the cooperative pipeline (cooperative_stage!).\nLanesPerElement — one BLOCK OF LANES owns one element, each lane owning rows of yₑ in registers and scattering them itself (element_action_row). No group-local memory and no barrier. A KernelAbstractionsDevice mapping only, and the ElementAssembly storage level only.\n\nform = MatrixFreeAction(; element_mapping = CooperativeElement())\nop   = setup_operator(AssemblyStrategy(form, SequentialScheduling(), device), integrator, dh)\n\nThe election is realized on the device at setup (with_element_mapping). The TERM never names the mapping: one element definition, chosen between on the strategy side.\n\nIt carries a third choice, storage, which is WHAT the operator keeps between actions (StorageElection) — MFEM's other three assembly levels:\n\nform = MatrixFreeAction(; storage = ElementAssembly())   # `Stored()` is the default\n\nRecompute is NONE: nothing is kept and the geometry is re-derived at the quadrature point that consumes it.\nStored is PARTIAL: the element precomputes its per-quadrature-point factors (fill_quadrature_data!) and every action is contractions and reads.\nElementAssembly is ELEMENT: the dense element matrices are kept and every action is a gather, a dense yₑ = Kₑ·uₑ and a scatter. It costs ndofs_per_cell² scalars per cell, so it is the LOW-order election, and it runs WorkerPerElement or LanesPerElement — the two mappings of a dense product, whole or by rows.\n\nElementAssembly is the cheapest matrix-free storage at p = 1–2; ndofs_per_cell² overtakes the assembled matrix's per-cell share above that, which is where Stored/Recompute take over. ElementAssembly tabulates the bytes and states the fill cost.\n\nAn element whose form is symmetric may additionally elect SymmetricElementMatrix (element_matrix_symmetry), which packs Kₑ's upper triangle only. Bytes are not the whole election: the byte win is unconditional, but under WorkerPerElement the packed read costs ACTION time above p = 1 (measured on an RTX 2080: −20% at p = 1, +96% at p = 2, +43% at p = 3, the packed index map folding to literals at ndofs_per_cell = 8 and not above).\n\nBoth stored levels are filled at setup and refilled by update_operator!, whose freshness contract is the one an assembled operator has: a factor that depends on p or on the context time is as fresh as the last such call.\n\nThe first two are the element's own storage and reach its cache through with_action_storage at setup — an element that keeps nothing serves both identically. The third is the framework's (ElementAssemblyCache) and serves ANY bilinear cache: an element with an ordinary element-matrix kernel and no matrix-free kernel at all runs the ELEMENT level, and a matrix-free one has its matrices filled from its own action.\n\nWriting such an element is AbstractTensorProductElementCache plus a POINTWISE MAP (tensor_product_pointwise) — the D block of the operator decomposition; the 1D operators, the lattice permutations, the contractions and both mapping pipelines are the core's. Two reference elements ship over that one core: a diffusion action (a tensor on the reference gradient) and a mass action (a scalar on the interpolated value).\n\nwarning: Experimental surface\nThe matrix-free form, MatrixFreeFerriteOperator, the tensor-product core and the element entry points they call may change in a minor release.\n\nAll operator entry points funnel into one task body executed by a shared device loop:\n\nfor chunk in partition\n    parfor item in chunk\n        reinit!(workspace, item)                # geometry cache\n        reinit_values!(cache, cell, kind)       # element values, once per sweep\n        execute_single_task!(task, workspace)\n    end\nend\n\nThe layer contract has the layer table that names who owns what along that path — requests, engines and workspaces included.","title":"Assembly levels"},{"category":"section","location":"#Where-to-read-on","page":"Home","text":"Writing elements — request-typed kernels, the cell/facet argument bundle, values reinitialization, parameter queries, analytic opt-ins, condensed elements, functionals.\nOperators and entry points — setup and its declarations, the assembly entry points, slots and rate reconstruction, sensitivities, weighted Jacobians, component bags and stage operators, derivative verification, quadrature data, transfer operators.\nPatch items — multi-cell work items with patch-local scatter (experimental).\n\nAPI reference:\n\nElement API reference — the contracts an element cache implements, and the request types its kernels take.\nProvided integrators and caches — composition, multi-domain routing, the AD decorator, the transfer prolongators.\nExample elements — the worked implementations in FerriteOperatorsExampleElements.\nOperator API reference — the operator types and every assembly, sensitivity and condensation entry point.\nAssembly engine API reference — kinds, drivers, strategies, devices, workspaces and the quadrature layer.\n\nDeveloper documentation:\n\nThe layer contract — the term and operator layers, their ownership boundaries and what the calling solver owns instead, the channel decision table, and the framework's extension points.\nDesign rationale — why the design is the way it is: the decisions, the alternatives that were rejected, and what they cost.","title":"Where to read on"}]
}
