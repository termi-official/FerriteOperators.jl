```@meta
CurrentModule = FerriteOperators
```

# Item family API reference

The additive item families — work items that are not cells. Their element-facing
declarations are described in [Writing elements](@ref); the engine's core
sweeps, strategies and devices are in the
[Assembly engine API reference](@ref).

```@autodocs
Modules = [FerriteOperators]
Pages = [
    "core/facet-task.jl",
    "core/algebraic-task.jl",
]
```
